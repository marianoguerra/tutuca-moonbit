// GENERATED by `tutuca gen-views` from loader_bar.html — DO NOT EDIT.
//
// The views of LoaderBar, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing loader_bar_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn loader_bar_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "span",
      [@anode.attr("class", "loader-bar flex items-center gap-2")],
      [
        @anode.hide(
          App(name="empty?", args=[Field("samples")]),
          @anode.h("span", [@anode.attr("class", "join")], [
            @anode.each(
              0,
              Field("samples"),
              @anode.h(
                "button",
                [
                  @anode.attr("class", "btn btn-xs join-item load-sample"),
                  @anode.eid(0),
                ],
                [@anode.dyn_text(BindMember(name="value", prop="label"))],
              ),
            ),
          ]),
        ),
        @anode.text(" "),
        @anode.show(
          Field("showUrl"),
          @anode.h("span", [@anode.attr("class", "join")], [
            @anode.h(
              "input",
              [
                @anode.attr(
                  "class", "input input-xs input-bordered join-item bar-url",
                ),
                @anode.attr("placeholder", "…/name.tutuca.tar.gz"),
                @anode.dyn_attr("value", Field("url")),
                @anode.eid(1),
              ],
              [],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-xs join-item load-url"),
                @anode.eid(2),
              ],
              [@anode.text("load")],
            ),
          ]),
        ),
        @anode.text(" "),
        @anode.h(
          "span",
          [@anode.attr("class", "text-xs opacity-60 bar-status")],
          [@anode.dyn_text(Field("status"))],
        ),
      ],
    ),
    [
      [
        @anode.on("click", HandlerName(name="loadSample", ns=Receive), args=[
          BindMember(name="value", prop="url"),
        ]),
      ],
      [
        @anode.on("input", HandlerName(name="typeUrl", ns=Receive), args=[
          EventPath(["value"]),
        ]),
        @anode.on("keydown", HandlerName(name="loadUrl", ns=Receive), modifiers=[
          "send",
        ]),
      ],
      [@anode.on("click", HandlerName(name="loadUrl", ns=Receive))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn loader_bar_views() -> Map[String, @anode.View] {
  { "main": loader_bar_main_view_ir() }
}

///|
/// The LoaderBar component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `LoaderBarState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn loader_bar_component(
  name? : String = "LoaderBar",
  views? : Map[String, @anode.View] = loader_bar_views(),
  init? : LoaderBarState = LoaderBarState::zero(),
  common_style? : String = loader_bar_common_style,
  global_style? : String = loader_bar_global_style,
  update? : (LoaderBarState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    LoaderBarState,
  ],
  swap? : (LoaderBarInput) -> ((
    LoaderBarState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([LoadSample, LoadUrl, TypeUrl] : Array[LoaderBarInput]),
          LoaderBarInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
