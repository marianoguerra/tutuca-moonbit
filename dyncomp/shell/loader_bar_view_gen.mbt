// GENERATED by `tutuca gen-views` from loader_bar.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views loader_bar.html --name LoaderBar --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// One bundle a page offers to load by URL: what to call it, and where it
/// is. A HOST supplies these; nothing here knows any bundle's name.
pub(all) struct LoaderBarSample {
  label : String
  url : String
} derive(Eq, @debug.Debug)

///|
/// Where a bundle comes from: a button, a URL box, or a file dropped on the
/// page. Every wasm-gc page that hosts bundles needs this and none of them
/// need a different one, which is why it stopped being two.
/// 
/// It sits INSIDE a page's own bar rather than around it, because the whole
/// viewport belongs to the page. What it cannot decide is what a load MEANS
/// — the universal host counts it against a session being restored, the
/// storybook rebuilds its story set — so it reports through `notify` and
/// bubbles `catalog-changed`, and the page decides.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct LoaderBarState {
  status : String
  /// What is in the URL box. Kept as state rather than read off the DOM, so
  /// pressing Enter and pressing the button are one handler.
  url : String
  /// Whether to draw the URL box at all. A page that only offers its own
  /// bundles has no use for it.
  showUrl : Bool
  /// The bundles to offer, as buttons. Empty is the ordinary case and draws
  /// nothing: a first-time visitor should not meet a row of buttons naming
  /// archives they have never heard of.
  samples : Array[LoaderBarSample]
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..LoaderBarState::zero(), label: "Counter" }
pub fn LoaderBarState::zero() -> LoaderBarState {
  { status: "", url: "", showUrl: false, samples: [] }
}

///|
/// The `Sample` record as a `@tutuca.Value`.
fn loader_bar_sample_to_value(x : LoaderBarSample) -> @tutuca.Value {
  Map({ "label": Str(x.label), "url": Str(x.url) })
}

///|
/// The `Sample` record back out. None on a shape mismatch.
fn loader_bar_sample_of_value(v : @tutuca.Value) -> LoaderBarSample? {
  guard v is Map(m) else { return None }
  guard m.get("label") is Some(Str(f_label)) else { return None }
  guard m.get("url") is Some(Str(f_url)) else { return None }
  Some({ label: f_label, url: f_url })
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for LoaderBarState with fn encode(s) {
  {
    "status": Str(s.status),
    "url": Str(s.url),
    "showUrl": Bool(s.showUrl),
    "samples": (
      List(s.samples.map(x => loader_bar_sample_to_value(x))) : @tutuca.Value),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for LoaderBarState with fn decode(f) {
  guard f.get("status") is Some(Str(v_status)) else { return None }
  guard f.get("url") is Some(Str(v_url)) else { return None }
  guard f.get("showUrl") is Some(Bool(v_showUrl)) else { return None }
  guard f.get("samples") is Some(t1_samples) else { return None }
  guard t1_samples is List(u1_samples) else { return None }
  let v_samples : Array[LoaderBarSample] = []
  for e1_samples in u1_samples {
    guard loader_bar_sample_of_value(e1_samples) is Some(x1_samples) else {
      return None
    }
    v_samples.push(x1_samples)
  }
  Some({ status: v_status, url: v_url, showUrl: v_showUrl, samples: v_samples })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum LoaderBarReceive {
  /// Files the page forwarded — dropped anywhere on it, or on a cell that
  /// handed back what was not its.
  FilesDropped(@tutuca.Value)
  /// The bridge's own answers, routed here because this is the path that
  /// asked for the load.
  DyncompLoaded(String)
  DyncompError(String)
  LoadSample(@tutuca.Value)
  LoadUrl
  TypeUrl(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn LoaderBarReceive::from_dispatch(
  d : @component.Dispatch,
) -> LoaderBarReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("filesDropped", [a0]) => FilesDropped(a0)
      ("dyncompLoaded", [Str(a0)]) => DyncompLoaded(a0)
      ("dyncompError", [Str(a0)]) => DyncompError(a0)
      ("loadSample", [a0]) => LoadSample(a0)
      ("loadUrl", []) => LoadUrl
      ("typeUrl", [Str(a0)]) => TypeUrl(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn LoaderBarReceive::to_dispatch(
  self : LoaderBarReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    FilesDropped(a0) => ("filesDropped", [a0])
    DyncompLoaded(a0) => ("dyncompLoaded", [Str(a0)])
    DyncompError(a0) => ("dyncompError", [Str(a0)])
    LoadSample(a0) => ("loadSample", [a0])
    LoadUrl => ("loadUrl", [])
    TypeUrl(a0) => ("typeUrl", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn LoaderBarReceive::send(
  self : LoaderBarReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The Intent arm of @component.Dispatch, decoded.
///
/// Declared as `variant intent` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum LoaderBarIntent {
  /// The catalog changed. Whatever is above re-reads it — a bar showing
  /// search results, or a gallery showing every configuration — rather than
  /// telling a person to ask again.
  CatalogChanged(String)
  /// A intent the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Intent dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn LoaderBarIntent::from_dispatch(
  d : @component.Dispatch,
) -> LoaderBarIntent? {
  guard d is Intent(name, args) else { return None }
  Some(
    match (name, args) {
      ("catalogChanged", [Str(a0)]) => CatalogChanged(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn LoaderBarIntent::to_dispatch(
  self : LoaderBarIntent,
) -> (String, Array[@tutuca.Value]) {
  match self {
    CatalogChanged(a0) => ("catalogChanged", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Dispatch this intent along `opts.route` — the default `dyn lex` when
/// nothing says otherwise. The `dyn` leg starts at this component's
/// PARENT, so a component never re-enters its own handler.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn LoaderBarIntent::dispatch(
  self : LoaderBarIntent,
  ctx : &@tutuca.Ctx,
  opts? : @tutuca.IntentOpts = @tutuca.IntentOpts::new(),
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.intent(name, args, opts)
}

///|
/// The file-level style — pass as `common_style~`.
pub let loader_bar_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let loader_bar_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum LoaderBarInput {
  LoadSample
  LoadUrl
  TypeUrl
} derive(Eq, @debug.Debug)

///|
pub fn LoaderBarInput::name(self : LoaderBarInput) -> String {
  match self {
    LoadSample => "loadSample"
    LoadUrl => "loadUrl"
    TypeUrl => "typeUrl"
  }
}

///|
pub fn LoaderBarInput::parse(s : String) -> LoaderBarInput? {
  match s {
    "loadSample" => Some(LoadSample)
    "loadUrl" => Some(LoadUrl)
    "typeUrl" => Some(TypeUrl)
    _ => None
  }
}

///|
/// The VIEW-named half of @component.Dispatch's `Receive` arm, decoded.
/// Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to loader_bar.html breaks every `match` over this type
/// until it is handled.
pub(all) enum LoaderBarMsg {
  /// `@on.click="loadSample …"`
  LoadSample(@tutuca.Value)
  /// `@on.keydown, @on.click="loadUrl …"`
  LoadUrl
  /// `@on.input="typeUrl …"`
  TypeUrl(String)
  /// A dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for an `Intent` — a routed name is raised by component code and
/// never written in a view, so the views cannot type it. A `Receive` the
/// views do not name decodes to `Unknown`, which is what the sibling
/// `<T>Receive` (the schema's half of the same bucket) is for.
pub fn LoaderBarMsg::from_dispatch(d : @component.Dispatch) -> LoaderBarMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("loadSample", [a0]) => LoadSample(a0)
      ("loadUrl", []) => LoadUrl
      ("typeUrl", [Str(a0)]) => TypeUrl(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for LoaderBarState with fn schema() {
  @component.SchemaInfo::new(
    name="LoaderBar",
    fingerprint="e8383c4cbc8a1b3a",
    fields=[
      @component.FieldInfo::new("status", TyText),
      @component.FieldInfo::new("url", TyText),
      @component.FieldInfo::new("showUrl", TyBool),
      @component.FieldInfo::new("samples", TyList(TyRecord("Sample"))),
    ],
    view_handlers=["loadSample", "loadUrl", "typeUrl"],
    receives=["filesDropped", "dyncompLoaded", "dyncompError"],
    intents=["catalogChanged"],
    view_names=["main"],
  )
}
