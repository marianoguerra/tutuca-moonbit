// GENERATED by `tutuca gen` from schema_viewer.tutu — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen schema_viewer.tutu --name SchemaViewer --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the SchemaViewer component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SchemaViewerState {
  value : @tutuca.Value
  raw : @tutuca.Value
  raw_schema : @tutuca.Value
  show_raw : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..SchemaViewerState::zero(), label: "Counter" }
pub fn SchemaViewerState::zero() -> SchemaViewerState {
  { value: Null, raw: Null, raw_schema: Null, show_raw: false, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SchemaViewerState with fn encode(s) {
  {
    "value": s.value,
    "raw": s.raw,
    "raw_schema": s.raw_schema,
    "show_raw": Bool(s.show_raw),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SchemaViewerState with fn decode(f) {
  let v_value = f.get("value").unwrap_or(Null)
  let v_raw = f.get("raw").unwrap_or(Null)
  let v_raw_schema = f.get("raw_schema").unwrap_or(Null)
  guard f.get("show_raw") is Some(Bool(v_show_raw)) else { return None }
  Some({
    value: v_value,
    raw: v_raw,
    raw_schema: v_raw_schema,
    show_raw: v_show_raw,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let schema_viewer_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let schema_viewer_global_style : String = ""

///|
/// Every `$name` the views call, plus the block's own `pred` / `compute` names — the bucket `compute~` keys.
///
/// Absent, because the script block answers them: raw_pane_has_something_in_it.
/// The block's own handlers are merged into this bucket, so an arm for one of
/// these could never run.
pub(all) enum SchemaViewerCompute {
  ToggleLabel
} derive(Eq, @debug.Debug)

///|
pub fn SchemaViewerCompute::name(self : SchemaViewerCompute) -> String {
  match self {
    ToggleLabel => "toggle_label"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SchemaViewerState with fn schema() {
  @component.SchemaInfo::new(
    name="SchemaViewer",
    fingerprint="4988cf73d754b9f9",
    fields=[
      @component.FieldInfo::new("value", TyAny),
      @component.FieldInfo::new("raw", TyAny),
      @component.FieldInfo::new("raw_schema", TyAny),
      @component.FieldInfo::new("show_raw", TyBool),
    ],
    methods=["raw_pane_has_something_in_it", "toggle_label"],
    invariants=["raw_pane_has_something_in_it"],
    view_names=["main"],
  )
}
