// GENERATED by `tutuca gen` from schema_viewer.tutu — DO NOT EDIT.
//
// The views of SchemaViewer, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing schema_viewer_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `spec:` section, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let schema_viewer_spec_source : String =
  #|state SchemaViewer {
  #|  value: Any
  #|  raw: Any
  #|  raw_schema: Any
  #|  show_raw: Bool
  #|  invariant raw_pane_has_something_in_it
  #|    format 'the raw pane is showing and there is nothing behind it' { (.show_raw implies (not (null? .raw))) }
  #|}

///|
/// The compiled `main` view.
fn schema_viewer_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "inline-flex flex-col items-start gap-1 schema-viewer",
        ),
      ],
      [
        @anode.h(
          "button",
          [
            @anode.attr("type", "button"),
            @anode.attr("class", "btn btn-xs btn-ghost self-end raw-toggle"),
            @anode.eid(0),
          ],
          [@anode.dyn_text(@tutuca.Expr::method_("toggle_label"), id=0)],
        ),
        @anode.text(" "),
        @anode.hide(
          @tutuca.Expr::field("show_raw"),
          @anode.render(1, @tutuca.Expr::field("value")),
          id=2,
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::field("show_raw"),
          @anode.render(3, @tutuca.Expr::field("raw")),
          id=4,
        ),
      ],
    ),
    [
      [
        @anode.on(
          "click",
          @anode.const_null(),
          action=@anode.set_property_action(
            "show_raw",
            @tutuca.Expr::app("not", [@tutuca.Expr::field("show_raw")]),
          ),
        ),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn schema_viewer_views() -> Map[String, @anode.View] {
  { "main": schema_viewer_main_view_ir() }
}

///|
/// `invariant raw_pane_has_something_in_it` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn schema_viewer_compute_raw_pane_has_something_in_it(
  s : SchemaViewerState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      !s.show_raw ||
      !(match @tutuca.builtin("null?") {
        Some(b) => (b.apply)([s.raw]).is_truthy()
        None => false
      }),
    ) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn schema_viewer_inline_compute() -> Map[
  String,
  (SchemaViewerState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  {
    "raw_pane_has_something_in_it": schema_viewer_compute_raw_pane_has_something_in_it,
  }
}

///|
/// The SchemaViewer component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SchemaViewerState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn schema_viewer_component(
  name? : String = "SchemaViewer",
  views? : Map[String, @anode.View] = schema_viewer_views(),
  initial? : SchemaViewerState = SchemaViewerState::zero(),
  common_style? : String = schema_viewer_common_style,
  global_style? : String = schema_viewer_global_style,
  update? : (SchemaViewerState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SchemaViewerState,
  ],
  compute? : (SchemaViewerCompute) -> ((
    SchemaViewerState,
    Array[@tutuca.Value],
    &@tutuca.Stack,
  ) -> @tutuca.Value)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute={
      let bucket = match compute {
        Some(f) =>
          @component.build_bucket(
            ([ToggleLabel] : Array[SchemaViewerCompute]),
            SchemaViewerCompute::name,
            f,
          )
        None => Map([])
      }
      for k, h in schema_viewer_inline_compute() {
        bucket[k] = h
      }
      bucket
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=schema_viewer_spec_source,
  )
}
