// GENERATED by `tutuca gen` from json.tutu — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen json.tutu --name Json --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the JsonNull component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct JsonNullState {} derive(Default)

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..JsonNullState::zero(), label: "Counter" }
pub fn JsonNullState::zero() -> JsonNullState {
  Default::default()
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonNullState with fn encode(_s) {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonNullState with fn decode(_f) {
  Some(Default::default())
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_null_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_null_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonNullState with fn schema() {
  @component.SchemaInfo::new(name="JsonNull", fingerprint="83efae9bb2c436be", view_names=[
    "main",
  ])
}

///|
/// The state of the JsonBoolean component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct JsonBooleanState {
  value : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..JsonBooleanState::zero(), label: "Counter" }
pub fn JsonBooleanState::zero() -> JsonBooleanState {
  { value: false, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonBooleanState with fn encode(s) {
  { "value": Bool(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonBooleanState with fn decode(f) {
  guard f.get("value") is Some(Bool(v_value)) else { return None }
  Some({ value: v_value, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_boolean_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_boolean_global_style : String = ""

///|
/// Every callable the views name, plus the file's own `pred` / `compute` names — the bucket `compute~` keys.
pub(all) enum JsonBooleanCompute {
  CssClass
  Text
} derive(Eq, @debug.Debug)

///|
pub fn JsonBooleanCompute::name(self : JsonBooleanCompute) -> String {
  match self {
    CssClass => "css_class"
    Text => "text"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonBooleanState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonBoolean",
    fingerprint="2363bc044eed83a9",
    fields=[@component.FieldInfo::new("value", TyBool)],
    methods=["css_class", "text"],
    view_names=["main", "_margauiClasses"],
  )
}

///|
/// The state of the JsonNumber component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct JsonNumberState {
  value : Double
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..JsonNumberState::zero(), label: "Counter" }
pub fn JsonNumberState::zero() -> JsonNumberState {
  { value: 0.0, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonNumberState with fn encode(s) {
  { "value": Num(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonNumberState with fn decode(f) {
  guard f.get("value") is Some(Num(v_value)) else { return None }
  Some({ value: v_value, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_number_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_number_global_style : String = ""

///|
/// Every callable the views name, plus the file's own `pred` / `compute` names — the bucket `compute~` keys.
pub(all) enum JsonNumberCompute {
  Text
} derive(Eq, @debug.Debug)

///|
pub fn JsonNumberCompute::name(self : JsonNumberCompute) -> String {
  match self {
    Text => "text"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonNumberState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonNumber",
    fingerprint="2f0640ab8fb7d267",
    fields=[@component.FieldInfo::new("value", TyFloat)],
    methods=["text"],
    view_names=["main"],
  )
}

///|
/// The state of the JsonString component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct JsonStringState {
  value : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..JsonStringState::zero(), label: "Counter" }
pub fn JsonStringState::zero() -> JsonStringState {
  { value: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonStringState with fn encode(s) {
  { "value": Str(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonStringState with fn decode(f) {
  guard f.get("value") is Some(Str(v_value)) else { return None }
  Some({ value: v_value, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_string_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_string_global_style : String = ""

///|
/// Every callable the views name, plus the file's own `pred` / `compute` names — the bucket `compute~` keys.
pub(all) enum JsonStringCompute {
  Text
} derive(Eq, @debug.Debug)

///|
pub fn JsonStringCompute::name(self : JsonStringCompute) -> String {
  match self {
    Text => "text"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonStringState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonString",
    fingerprint="7e15bdab8f977d4f",
    fields=[@component.FieldInfo::new("value", TyText)],
    methods=["text"],
    view_names=["main"],
  )
}

///|
/// The state of the ValueFn component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct ValueFnState {} derive(Default)

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..ValueFnState::zero(), label: "Counter" }
pub fn ValueFnState::zero() -> ValueFnState {
  Default::default()
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ValueFnState with fn encode(_s) {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ValueFnState with fn decode(_f) {
  Some(Default::default())
}

///|
/// The file-level style — pass as `common_style~`.
pub let value_fn_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let value_fn_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ValueFnState with fn schema() {
  @component.SchemaInfo::new(name="ValueFn", fingerprint="044e4f337f794208", view_names=[
    "main",
  ])
}

///|
/// The state of the ValueObj component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct ValueObjState {
  text : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..ValueObjState::zero(), label: "Counter" }
pub fn ValueObjState::zero() -> ValueObjState {
  { text: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ValueObjState with fn encode(s) {
  { "text": Str(s.text) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ValueObjState with fn decode(f) {
  guard f.get("text") is Some(Str(v_text)) else { return None }
  Some({ text: v_text, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let value_obj_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let value_obj_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ValueObjState with fn schema() {
  @component.SchemaInfo::new(
    name="ValueObj",
    fingerprint="9301803b31b11322",
    fields=[@component.FieldInfo::new("text", TyText)],
    view_names=["main"],
  )
}

///|
/// The state of the JsonArray component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct JsonArrayState {
  items : Array[@tutuca.Value]
  is_expanded : Bool
  items_per_page : Int
  current_page : Int
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..JsonArrayState::zero(), label: "Counter" }
pub fn JsonArrayState::zero() -> JsonArrayState {
  { items: [], is_expanded: false, items_per_page: 0, current_page: 0, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonArrayState with fn encode(s) {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "is_expanded": Bool(s.is_expanded),
    "items_per_page": Num(s.items_per_page.to_double()),
    "current_page": Num(s.current_page.to_double()),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonArrayState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("is_expanded") is Some(Bool(v_is_expanded)) else { return None }
  guard f.get("items_per_page") is Some(Num(t1_items_per_page)) else {
    return None
  }
  let v_items_per_page = t1_items_per_page.to_int()
  guard f.get("current_page") is Some(Num(t1_current_page)) else { return None }
  let v_current_page = t1_current_page.to_int()
  Some({
    items: v_items,
    is_expanded: v_is_expanded,
    items_per_page: v_items_per_page,
    current_page: v_current_page,
  })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Every addressed name: the `message` the `spec:` section declares,
/// plus every bare `~on_<event>` handler the views write. One bucket holds
/// them
/// however they were sent, so one type covers it — where the two overlap
/// the schema's declared payload wins, and a view whose call sites
/// disagree with it is a build error.
pub(all) enum JsonArrayMsg {
  NextPage
  PrevPage
  /// A message the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn JsonArrayMsg::from_dispatch(d : @component.Dispatch) -> JsonArrayMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("next_page", []) => NextPage
      ("prev_page", []) => PrevPage
      _ => {
        match name {
          "next_page" => @tutuca.bad_payload(name, "", args)
          "prev_page" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn JsonArrayMsg::to_dispatch(
  self : JsonArrayMsg,
) -> (String, Array[@tutuca.Value]) {
  match self {
    NextPage => ("next_page", [])
    PrevPage => ("prev_page", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn JsonArrayMsg::send(self : JsonArrayMsg, ctx : &@tutuca.Ctx) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_array_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_array_global_style : String = ""

///|
/// Every callable the views name, plus the file's own `pred` / `compute` names — the bucket `compute~` keys.
///
/// Absent, because the `logic:` section answers them: page_exists.
/// The block's own handlers are merged into this bucket, so an arm for one of
/// these could never run.
pub(all) enum JsonArrayCompute {
  ArrowText
  CannotNextPage
  CannotPrevPage
  CountText
  IsItemsEmpty
  PageIndicatorText
  ShowPagination
  TypeText
} derive(Eq, @debug.Debug)

///|
pub fn JsonArrayCompute::name(self : JsonArrayCompute) -> String {
  match self {
    ArrowText => "arrow_text"
    CannotNextPage => "cannot_next_page"
    CannotPrevPage => "cannot_prev_page"
    CountText => "count_text"
    IsItemsEmpty => "is_items_empty"
    PageIndicatorText => "page_indicator_text"
    ShowPagination => "show_pagination"
    TypeText => "type_text"
  }
}

///|
/// Every `@loop-with` handler the views name.
pub(all) enum JsonArrayLoopWith {
  GetPageRange
} derive(Eq, @debug.Debug)

///|
pub fn JsonArrayLoopWith::name(self : JsonArrayLoopWith) -> String {
  match self {
    GetPageRange => "get_page_range"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonArrayState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonArray",
    fingerprint="6e270d753e52b905",
    fields=[
      @component.FieldInfo::new("items", TyList(TyComp(None))),
      @component.FieldInfo::new("is_expanded", TyBool),
      @component.FieldInfo::new("items_per_page", TyInt(width=32, signed=true)),
      @component.FieldInfo::new("current_page", TyInt(width=32, signed=true)),
    ],
    view_handlers=["next_page", "prev_page"],
    methods=[
      "arrow_text", "cannot_next_page", "cannot_prev_page", "count_text", "is_items_empty",
      "page_exists", "page_indicator_text", "show_pagination", "type_text",
    ],
    invariants=["page_exists"],
    domains=[
      { field: "items_per_page", domain: DBetween(lo=DLit(1), hi=DLit(500)), },
      { field: "current_page", domain: DCompare(op=DGe, rhs=DLit(0)), },
    ],
    view_names=["main"],
  )
}

///|
/// The state of the JsonObject component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct JsonObjectState {
  items : Array[@tutuca.Value]
  is_expanded : Bool
  items_per_page : Int
  current_page : Int
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..JsonObjectState::zero(), label: "Counter" }
pub fn JsonObjectState::zero() -> JsonObjectState {
  { items: [], is_expanded: false, items_per_page: 0, current_page: 0, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonObjectState with fn encode(s) {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "is_expanded": Bool(s.is_expanded),
    "items_per_page": Num(s.items_per_page.to_double()),
    "current_page": Num(s.current_page.to_double()),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonObjectState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("is_expanded") is Some(Bool(v_is_expanded)) else { return None }
  guard f.get("items_per_page") is Some(Num(t1_items_per_page)) else {
    return None
  }
  let v_items_per_page = t1_items_per_page.to_int()
  guard f.get("current_page") is Some(Num(t1_current_page)) else { return None }
  let v_current_page = t1_current_page.to_int()
  Some({
    items: v_items,
    is_expanded: v_is_expanded,
    items_per_page: v_items_per_page,
    current_page: v_current_page,
  })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Every addressed name: the `message` the `spec:` section declares,
/// plus every bare `~on_<event>` handler the views write. One bucket holds
/// them
/// however they were sent, so one type covers it — where the two overlap
/// the schema's declared payload wins, and a view whose call sites
/// disagree with it is a build error.
pub(all) enum JsonObjectMsg {
  NextPage
  PrevPage
  /// A message the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn JsonObjectMsg::from_dispatch(d : @component.Dispatch) -> JsonObjectMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("next_page", []) => NextPage
      ("prev_page", []) => PrevPage
      _ => {
        match name {
          "next_page" => @tutuca.bad_payload(name, "", args)
          "prev_page" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn JsonObjectMsg::to_dispatch(
  self : JsonObjectMsg,
) -> (String, Array[@tutuca.Value]) {
  match self {
    NextPage => ("next_page", [])
    PrevPage => ("prev_page", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn JsonObjectMsg::send(self : JsonObjectMsg, ctx : &@tutuca.Ctx) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_object_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_object_global_style : String = ""

///|
/// Every callable the views name, plus the file's own `pred` / `compute` names — the bucket `compute~` keys.
///
/// Absent, because the `logic:` section answers them: page_exists.
/// The block's own handlers are merged into this bucket, so an arm for one of
/// these could never run.
pub(all) enum JsonObjectCompute {
  ArrowText
  CannotNextPage
  CannotPrevPage
  CountText
  IsItemsEmpty
  PageIndicatorText
  ShowPagination
  TypeText
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectCompute::name(self : JsonObjectCompute) -> String {
  match self {
    ArrowText => "arrow_text"
    CannotNextPage => "cannot_next_page"
    CannotPrevPage => "cannot_prev_page"
    CountText => "count_text"
    IsItemsEmpty => "is_items_empty"
    PageIndicatorText => "page_indicator_text"
    ShowPagination => "show_pagination"
    TypeText => "type_text"
  }
}

///|
/// Every `@loop-with` handler the views name.
pub(all) enum JsonObjectLoopWith {
  GetPageRange
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectLoopWith::name(self : JsonObjectLoopWith) -> String {
  match self {
    GetPageRange => "get_page_range"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonObjectState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonObject",
    fingerprint="69aa0a2061660b08",
    fields=[
      @component.FieldInfo::new("items", TyList(TyComp(Some("JsonProperty")))),
      @component.FieldInfo::new("is_expanded", TyBool),
      @component.FieldInfo::new("items_per_page", TyInt(width=32, signed=true)),
      @component.FieldInfo::new("current_page", TyInt(width=32, signed=true)),
    ],
    view_handlers=["next_page", "prev_page"],
    methods=[
      "arrow_text", "cannot_next_page", "cannot_prev_page", "count_text", "is_items_empty",
      "page_exists", "page_indicator_text", "show_pagination", "type_text",
    ],
    invariants=["page_exists"],
    domains=[
      { field: "items_per_page", domain: DBetween(lo=DLit(1), hi=DLit(500)), },
      { field: "current_page", domain: DCompare(op=DGe, rhs=DLit(0)), },
    ],
    view_names=["main"],
  )
}

///|
/// The state of the JsonProperty component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct JsonPropertyState {
  key : String
  child : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..JsonPropertyState::zero(), label: "Counter" }
pub fn JsonPropertyState::zero() -> JsonPropertyState {
  { key: "", child: Null, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonPropertyState with fn encode(s) {
  { "key": Str(s.key), "child": s.child }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonPropertyState with fn decode(f) {
  guard f.get("key") is Some(Str(v_key)) else { return None }
  let v_child = f.get("child").unwrap_or(Null)
  Some({ key: v_key, child: v_child, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_property_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_property_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonPropertyState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonProperty",
    fingerprint="8607f94eaf0170c7",
    fields=[
      @component.FieldInfo::new("key", TyText),
      @component.FieldInfo::new("child", TyComp(None)),
    ],
    view_names=["main"],
  )
}

///|
/// The state of the DataInspector component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct DataInspectorState {
  value : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..DataInspectorState::zero(), label: "Counter" }
pub fn DataInspectorState::zero() -> DataInspectorState {
  { value: Null, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for DataInspectorState with fn encode(s) {
  { "value": s.value }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for DataInspectorState with fn decode(f) {
  let v_value = f.get("value").unwrap_or(Null)
  Some({ value: v_value, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let data_inspector_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let data_inspector_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for DataInspectorState with fn schema() {
  @component.SchemaInfo::new(
    name="DataInspector",
    fingerprint="3b4e7089042c78af",
    fields=[@component.FieldInfo::new("value", TyComp(None))],
    view_names=["main"],
  )
}
