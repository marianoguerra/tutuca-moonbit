// GENERATED by `tutuca gen` from schema.html — DO NOT EDIT.
//
// The views of SchemaProperty, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing schema_property_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let schema_property_spec_source : String =
  #|  state SchemaProperty {
  #|    key : String
  #|    child : Any
  #|    required : Bool
  #|
  #|    where key is nonempty
  #|  }
  #|
  #|  state SchemaBranch {
  #|    label : String
  #|    child : Any
  #|    labelClass : String
  #|
  #|    where label is nonempty
  #|  }

///|
/// The compiled `main` view.
fn schema_property_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "contents")], [
      @anode.h(
        "span",
        [
          @anode.attr(
            "class", "font-mono text-sm flex items-center gap-1 leading-tight",
          ),
        ],
        [
          @anode.h(
            "span",
            [@anode.attr("class", "text-base-content/60 schema-key")],
            [@anode.dyn_text(@tutuca.Expr::field("key"))],
          ),
          @anode.text(" "),
          @anode.show(
            @tutuca.Expr::field("required"),
            @anode.h(
              "span",
              [
                @anode.attr("class", "text-error font-bold"),
                @anode.attr("title", "required"),
              ],
              [@anode.text("*")],
            ),
          ),
          @anode.text(" "),
          @anode.h("span", [@anode.attr("class", "text-base-content/30")], [
            @anode.text(":"),
          ]),
        ],
      ),
      @anode.text(" "),
      @anode.render(0, @tutuca.Expr::field("child")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn schema_property_views() -> Map[String, @anode.View] {
  { "main": schema_property_main_view_ir() }
}

///|
/// The SchemaProperty component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SchemaPropertyState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn schema_property_component(
  name? : String = "SchemaProperty",
  views? : Map[String, @anode.View] = schema_property_views(),
  initial? : SchemaPropertyState = SchemaPropertyState::zero(),
  common_style? : String = schema_property_common_style,
  global_style? : String = schema_property_global_style,
  update? : (SchemaPropertyState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SchemaPropertyState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=schema_property_spec_source,
  )
}

///|
/// The compiled `main` view.
fn schema_branch_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "contents")], [
      @anode.h(
        "span",
        [@anode.attr("class", "flex items-center gap-1 leading-tight")],
        [
          @anode.h(
            "span",
            [@anode.dyn_attr("class", @tutuca.Expr::method_("labelCssClass"))],
            [@anode.dyn_text(@tutuca.Expr::field("label"))],
          ),
          @anode.text(" "),
          @anode.h("span", [@anode.attr("class", "text-base-content/30")], [
            @anode.text(":"),
          ]),
        ],
      ),
      @anode.text(" "),
      @anode.render(0, @tutuca.Expr::field("child")),
    ]),
    [],
  )
}

///|
/// The compiled `_margauiClasses` view.
fn schema_branch_margaui_classes_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "_margauiClasses",
    @anode.h(
      "p",
      [@anode.attr("class", "text-accent text-secondary text-info text-error")],
      [],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn schema_branch_views() -> Map[String, @anode.View] {
  {
    "main": schema_branch_main_view_ir(),
    "_margauiClasses": schema_branch_margaui_classes_view_ir(),
  }
}

///|
/// The SchemaBranch component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SchemaBranchState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn schema_branch_component(
  name? : String = "SchemaBranch",
  views? : Map[String, @anode.View] = schema_branch_views(),
  initial? : SchemaBranchState = SchemaBranchState::zero(),
  common_style? : String = schema_branch_common_style,
  global_style? : String = schema_branch_global_style,
  update? : (SchemaBranchState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SchemaBranchState,
  ],
  compute? : (SchemaBranchCompute) -> ((
    SchemaBranchState,
    Array[@tutuca.Value],
    &@tutuca.Stack,
  ) -> @tutuca.Value)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) =>
        @component.build_bucket(
          ([LabelCssClass] : Array[SchemaBranchCompute]),
          SchemaBranchCompute::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=schema_property_spec_source,
  )
}
