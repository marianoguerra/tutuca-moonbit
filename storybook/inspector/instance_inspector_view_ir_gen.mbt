// GENERATED by `tutuca gen-views` from instance_inspector.html — DO NOT EDIT.
//
// The views of InstanceFields, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing instance_fields_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let instance_fields_spec_source : String =
  #|  state InstanceFields {
  #|    items : Array[Any]
  #|    isExpanded : Bool
  #|    itemsPerPage : Int
  #|    currentPage : Int
  #|    typeName : String
  #|  }
  #|
  #|  state InstanceInspector {
  #|    value : Any
  #|  }
  #|
  #|  state InstanceExplorer {
  #|    activeTab : String
  #|    instanceView : Any
  #|    componentView : Any
  #|    hasComponent : Bool
  #|  }

///|
/// The compiled `main` view.
fn instance_fields_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "span",
      [@anode.attr("class", "font-mono text-sm leading-tight inline-block")],
      [
        @anode.h(
          "span",
          [@anode.attr("class", "inline-flex items-center gap-2")],
          [
            @anode.h(
              "button",
              [
                @anode.attr("type", "button"),
                @anode.attr(
                  "class", "cursor-pointer text-base-content/70 hover:text-base-content inline-flex items-center gap-1",
                ),
                @anode.dyn_attr("disabled", Method("isItemsEmpty")),
                @anode.eid(0),
              ],
              [
                @anode.h("span", [], [@anode.dyn_text(Method("arrowText"))]),
                @anode.text(" "),
                @anode.h(
                  "span",
                  [@anode.attr("class", "font-semibold instance-fields")],
                  [@anode.dyn_text(Method("typeText"))],
                ),
                @anode.text(" "),
                @anode.h("span", [], [@anode.dyn_text(Method("countText"))]),
              ],
            ),
            @anode.text(" "),
            @anode.show(
              Method("showPagination"),
              @anode.h("div", [@anode.attr("class", "join")], [
                @anode.h(
                  "button",
                  [
                    @anode.attr("type", "button"),
                    @anode.attr("class", "join-item btn btn-xs pg-prev"),
                    @anode.dyn_attr("disabled", Method("cannotPrevPage")),
                    @anode.eid(1),
                  ],
                  [@anode.text("«")],
                ),
                @anode.text(" "),
                @anode.h(
                  "span",
                  [
                    @anode.attr(
                      "class", "join-item badge font-mono text-xs pg-label",
                    ),
                  ],
                  [@anode.dyn_text(Method("pageIndicatorText"))],
                ),
                @anode.text(" "),
                @anode.h(
                  "button",
                  [
                    @anode.attr("type", "button"),
                    @anode.attr("class", "join-item btn btn-xs pg-next"),
                    @anode.dyn_attr("disabled", Method("cannotNextPage")),
                    @anode.eid(2),
                  ],
                  [@anode.text("»")],
                ),
              ]),
            ),
          ],
        ),
        @anode.text(" "),
        @anode.show(
          Field("isExpanded"),
          @anode.h(
            "div",
            [
              @anode.attr(
                "class", "ml-1 flex flex-col gap-0.5 border-l border-base-content/15 pl-2 mt-0.5",
              ),
            ],
            [
              @anode.each(
                1,
                Field("items"),
                @anode.render_it(0),
                loop_with_val=HandlerName(name="getPageRange", ns=Alter),
                from_render_each=true,
              ),
            ],
          ),
        ),
      ],
    ),
    [
      [@anode.on("click", HandlerName(name="toggleIsExpanded", ns=Receive))],
      [@anode.on("click", HandlerName(name="prevPage", ns=Receive))],
      [@anode.on("click", HandlerName(name="nextPage", ns=Receive))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn instance_fields_views() -> Map[String, @anode.View] {
  { "main": instance_fields_main_view_ir() }
}

///|
/// The InstanceFields component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `InstanceFieldsState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn instance_fields_component(
  name? : String = "InstanceFields",
  views? : Map[String, @anode.View] = instance_fields_views(),
  init? : InstanceFieldsState = InstanceFieldsState::zero(),
  common_style? : String = instance_fields_common_style,
  global_style? : String = instance_fields_global_style,
  update? : (InstanceFieldsState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    InstanceFieldsState,
  ],
  compute? : (InstanceFieldsMethod) -> ((
    InstanceFieldsState,
    Array[@tutuca.Value],
    &@tutuca.Stack,
  ) -> @tutuca.Value)?,
  swap? : (InstanceFieldsInput) -> ((
    InstanceFieldsState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  loop_with? : (InstanceFieldsLoopWith) -> ((
    InstanceFieldsState,
    @tutuca.Value,
    @component.LoopCtx,
  ) -> @component.LoopWith)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) =>
        @component.build_bucket(
          (
            [
              ArrowText,
              CannotNextPage,
              CannotPrevPage,
              CountText,
              IsItemsEmpty,
              PageIndicatorText,
              ShowPagination,
              TypeText,
            ] : Array[InstanceFieldsMethod]),
          InstanceFieldsMethod::name,
          f,
        )
      None => Map([])
    },
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([NextPage, PrevPage, ToggleIsExpanded] : Array[InstanceFieldsInput]),
          InstanceFieldsInput::name,
          f,
        )
      None => Map([])
    },
    loop_with=match loop_with {
      Some(f) =>
        @component.build_bucket(
          ([GetPageRange] : Array[InstanceFieldsLoopWith]),
          InstanceFieldsLoopWith::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=instance_fields_spec_source,
  )
}

///|
/// The compiled `main` view.
fn instance_inspector_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("span", [@anode.attr("class", "contents")], [
      @anode.render(0, Field("value")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn instance_inspector_views() -> Map[String, @anode.View] {
  { "main": instance_inspector_main_view_ir() }
}

///|
/// The InstanceInspector component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `InstanceInspectorState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn instance_inspector_component(
  name? : String = "InstanceInspector",
  views? : Map[String, @anode.View] = instance_inspector_views(),
  init? : InstanceInspectorState = InstanceInspectorState::zero(),
  common_style? : String = instance_inspector_common_style,
  global_style? : String = instance_inspector_global_style,
  update? : (InstanceInspectorState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    InstanceInspectorState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=instance_fields_spec_source,
  )
}

///|
/// The compiled `main` view.
fn instance_explorer_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "font-mono text-sm leading-snug flex flex-col gap-3 instance-explorer",
        ),
      ],
      [
        @anode.h(
          "div",
          [@anode.attr("role", "tablist"), @anode.attr("class", "tabs")],
          [
            @anode.h(
              "a",
              [
                @anode.attr("role", "tab"),
                @anode.if_attr(
                  "class",
                  App(name="is", args=[
                    Field("activeTab"),
                    @anode.const_str("instance"),
                  ]),
                  then_val=@anode.const_str("tab tab-active tab-instance"),
                  else_val=@anode.const_str("tab tab-instance"),
                ),
                @anode.eid(0),
              ],
              [@anode.text("\n      Instance\n    ")],
            ),
            @anode.text(" "),
            @anode.show(
              Field("hasComponent"),
              @anode.h(
                "a",
                [
                  @anode.attr("role", "tab"),
                  @anode.if_attr(
                    "class",
                    App(name="is", args=[
                      Field("activeTab"),
                      @anode.const_str("component"),
                    ]),
                    then_val=@anode.const_str("tab tab-active tab-component"),
                    else_val=@anode.const_str("tab tab-component"),
                  ),
                  @anode.eid(1),
                ],
                [@anode.text("\n      Component\n    ")],
              ),
            ),
          ],
        ),
        @anode.text(" "),
        @anode.show(
          App(name="is", args=[Field("activeTab"), @anode.const_str("instance")]),
          @anode.h("div", [], [@anode.render(0, Field("instanceView"))]),
        ),
        @anode.text(" "),
        @anode.show(
          App(name="is", args=[
            Field("activeTab"),
            @anode.const_str("component"),
          ]),
          @anode.h("div", [], [
            @anode.show(
              Field("hasComponent"),
              @anode.h("div", [], [@anode.render(1, Field("componentView"))]),
            ),
            @anode.text(" "),
            @anode.hide(
              Field("hasComponent"),
              @anode.h(
                "div",
                [@anode.attr("class", "text-base-content/50 text-xs")],
                [
                  @anode.text(
                    "\n      Pass the Component descriptor to inspect its definition.\n    ",
                  ),
                ],
              ),
            ),
          ]),
        ),
      ],
    ),
    [
      [
        @anode.on("click", HandlerName(name="setActiveTab", ns=Receive), args=[
          @anode.const_str("instance"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="setActiveTab", ns=Receive), args=[
          @anode.const_str("component"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn instance_explorer_views() -> Map[String, @anode.View] {
  { "main": instance_explorer_main_view_ir() }
}

///|
/// The InstanceExplorer component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `InstanceExplorerState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn instance_explorer_component(
  name? : String = "InstanceExplorer",
  views? : Map[String, @anode.View] = instance_explorer_views(),
  init? : InstanceExplorerState = InstanceExplorerState::zero(),
  common_style? : String = instance_explorer_common_style,
  global_style? : String = instance_explorer_global_style,
  update? : (InstanceExplorerState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    InstanceExplorerState,
  ],
  swap? : (InstanceExplorerInput) -> ((
    InstanceExplorerState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetActiveTab] : Array[InstanceExplorerInput]),
          InstanceExplorerInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=instance_fields_spec_source,
  )
}
