// GENERATED by `tutuca gen` from trace_view.tutu — DO NOT EDIT.
//
// The views of TraceRow, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing trace_row_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `spec:` section, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let trace_row_spec_source : String =
  #|state TraceRow {
  #|  seq: Int
  #|  turn: Int
  #|  walk: Int
  #|  has_walk: Bool
  #|  verb: String
  #|  verb_class: String
  #|  name: String
  #|  place: String
  #|  args: String
  #|  input: Bool
  #|  row_class: String
  #|  depth: Int
  #|  current: Bool
  #|  after: Instance?
  #|  has_after: Bool
  #|  open: Bool
  #|  invariant has_walk_tracks_walk
  #|    format $'hasWalk says {.has_walk} for walk {.walk}' { (.has_walk is (.walk is not 0)) }
  #|  where verb is one of ["send", "intent", "offered", "answer", "refused"]
  #|  where seq >= 0
  #|  where turn >= 0
  #|  where walk >= 0
  #|  where depth >= 0
  #|}
  #|handle TraceRow {
  #|  message {
  #|    toggle
  #|  }
  #|}
  #|state TraceLog {
  #|  rows: Array[Instance[TraceRow]]
  #|  has_rows: Bool
  #|  inputs_only: Bool
  #|  turn: Int
  #|  empty_hint: String
  #|  invariant has_rows_tracks_rows
  #|    format $'hasRows says {.has_rows} for {(len .rows)} rows' { (.has_rows is ((len .rows) > 0)) }
  #|  where turn >= 0
  #|}
  #|handle TraceLog {
  #|  message {
  #|    set_turn(Int),
  #|    show_all_turns
  #|  }
  #|}
  #|state TraceHeader {
  #|  component: String
  #|  module: String
  #|  example: String
  #|  fingerprint: String
  #|  events: Int
  #|  inputs: Int
  #|  turns: Int
  #|  relocated_from: String
  #|  is_relocated: Bool
  #|  compacted: Bool
  #|  invariant inputs_are_events
  #|    format $'{.inputs} inputs out of {.events} events' { (.inputs <= .events) }
  #|  invariant turns_are_events
  #|    format $'{.turns} turns out of {.events} events' { (.turns <= .events) }
  #|  invariant is_relocated_tracks_from
  #|    format 'isRelocated and relocatedFrom disagree' { (.is_relocated is ((len .relocated_from) > 0)) }
  #|  where events >= 0
  #|  where inputs >= 0
  #|  where turns >= 0
  #|}
  #|state TracePlace {
  #|  label: String
  #|  display: String
  #|  component: String
  #|  events: Int
  #|  depth: Int
  #|  selected: Bool
  #|  row_class: String
  #|  mountable: Bool
  #|  where events >= 0
  #|  where depth >= 0
  #|}
  #|handle TracePlace {
  #|  message {
  #|    pick_place(String)
  #|  }
  #|}
  #|state TracePlaces {
  #|  places: Array[Instance[TracePlace]]
  #|  has_places: Bool
  #|  selected: String
  #|  heading: String
  #|  empty_hint: String
  #|  invariant has_places_tracks_places
  #|    format $'hasPlaces says {.has_places} for {(len .places)} places' { (.has_places is ((len .places) > 0)) }
  #|}
  #|state RecorderBar {
  #|  recording: Bool
  #|  scope: String
  #|  scope_label: String
  #|  all_class: String
  #|  section_class: String
  #|  example_class: String
  #|  events: Int
  #|  turns: Int
  #|  has_trace: Bool
  #|  picking: Bool
  #|  invariant turns_are_events
  #|    format $'{.turns} turns out of {.events} events' { (.turns <= .events) }
  #|  where scope is one of ["all", "section", "example"]
  #|  where events >= 0
  #|  where turns >= 0
  #|}
  #|handle RecorderBar {
  #|  message {
  #|    set_scope(String),
  #|    open_picker,
  #|    close_picker,
  #|    start_recording,
  #|    stop_recording,
  #|    clear_recording
  #|  }
  #|}
  #|state TracePanel {
  #|  bar: Instance[RecorderBar]
  #|  header: Instance[TraceHeader]
  #|  places: Instance[TracePlaces]
  #|  log: Instance[TraceLog]
  #|  record_tab_class: String
  #|  trace_tab_class: String
  #|  select_tab_class: String
  #|  select_label: String
  #|  tab: String
  #|  has_trace: Bool
  #|  selecting: Bool
  #|  show_trace: Bool
  #|  show_replay: Bool
  #|  show_import: Bool
  #|  at: Int
  #|  total: Int
  #|  notice: String
  #|  has_notice: Bool
  #|  draft: String
  #|  can_replay: Bool
  #|  replaying: Bool
  #|  replay_tab_class: String
  #|  invariant has_notice_tracks_notice
  #|    format $'hasNotice says {.has_notice} for a notice of {(len .notice)}' { (.has_notice is ((len .notice) > 0)) }
  #|  invariant can_replay_needs_a_trace
  #|    format 'the replay controls are live without a trace behind them' { (.can_replay implies .has_trace) }
  #|  invariant replaying_needs_a_replay
  #|    format 'a rerun is playing that was never mounted' { (.replaying implies .can_replay) }
  #|  invariant at_within_total
  #|    format $'parked at {.at} of {.total}' { (.at <= .total) }
  #|  invariant show_trace_needs_a_trace
  #|    format 'the trace pane is showing without a trace behind it' { (.show_trace implies .has_trace) }
  #|  invariant show_replay_needs_a_trace
  #|    format 'the replay pane is showing without a trace behind it' { (.show_replay implies .has_trace) }
  #|  where tab is one of ["record", "trace", "pick", "replay"]
  #|  where at >= 0
  #|  where total >= 0
  #|}
  #|handle TracePanel {
  #|  message {
  #|    set_tab(String),
  #|    set_at(Int),
  #|    toggle_selecting,
  #|    export_trace,
  #|    import_draft,
  #|    cut_to_place,
  #|    replay_step,
  #|    replay_run,
  #|    replay_reset
  #|  }
  #|}

///|
/// The compiled `main` view.
fn trace_row_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.dyn_attr("class", @tutuca.Expr::field("row_class"))],
      [
        @anode.h(
          "div",
          [
            @anode.attr(
              "class", "row-head flex items-center gap-2 flex-wrap cursor-pointer",
            ),
            @anode.eid(0),
          ],
          [
            @anode.h(
              "span",
              [
                @anode.attr(
                  "class", "badge badge-xs tabular-nums opacity-50 row-seq",
                ),
              ],
              [@anode.dyn_text(@tutuca.Expr::field("seq"), id=0)],
            ),
            @anode.text(" "),
            @anode.h(
              "span",
              [
                @anode.attr(
                  "title", "What this was: a message, an intent, an offer to the host, the host's answer, or a refusal",
                ),
                @anode.dyn_attr("class", @tutuca.Expr::field("verb_class")),
              ],
              [@anode.dyn_text(@tutuca.Expr::field("verb"), id=1)],
            ),
            @anode.text(" "),
            @anode.h(
              "span",
              [@anode.attr("class", "font-mono font-semibold row-name")],
              [@anode.dyn_text(@tutuca.Expr::field("name"), id=2)],
            ),
            @anode.text(" "),
            @anode.h(
              "span",
              [
                @anode.attr("title", "Where it landed"),
                @anode.attr("class", "text-xs opacity-40 font-mono row-place"),
              ],
              [@anode.dyn_text(@tutuca.Expr::field("place"), id=3)],
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::field("input"),
              @anode.h(
                "span",
                [
                  @anode.attr(
                    "title", "A rerun feeds this one; everything else it produces for itself",
                  ),
                  @anode.attr(
                    "class", "badge badge-xs badge-primary badge-outline row-input",
                  ),
                ],
                [@anode.text("input")],
              ),
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::field("has_walk"),
              @anode.h(
                "span",
                [
                  @anode.attr("title", "Every hop of one intent shares this"),
                  @anode.attr("class", "badge badge-xs badge-ghost row-walk"),
                ],
                [
                  @anode.dyn_text(@anode.const_str("walk "), id=4),
                  @anode.dyn_text(@tutuca.Expr::field("walk"), id=5),
                ],
              ),
            ),
            @anode.text(" "),
            @anode.h(
              "span",
              [
                @anode.attr(
                  "class", "ml-auto text-xs opacity-40 tabular-nums row-turn",
                ),
              ],
              [
                @anode.dyn_text(@anode.const_str("turn "), id=6),
                @anode.dyn_text(@tutuca.Expr::field("turn"), id=7),
              ],
            ),
          ],
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::field("open"),
          @anode.h(
            "div",
            [
              @anode.attr(
                "class", "pl-6 flex flex-col gap-1 font-mono text-xs row-detail",
              ),
            ],
            [
              @anode.show(
                @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("args")]),
                @anode.h("div", [@anode.attr("class", "opacity-60 row-args")], [
                  @anode.dyn_text(@anode.const_str("args "), id=8),
                  @anode.dyn_text(@tutuca.Expr::field("args"), id=9),
                ]),
              ),
              @anode.text(" "),
              @anode.show(
                @tutuca.Expr::field("has_after"),
                @anode.h("div", [], [
                  @anode.h("div", [@anode.attr("class", "opacity-50")], [
                    @anode.text("after"),
                  ]),
                  @anode.text(" "),
                  @anode.render(10, @tutuca.Expr::field("after")),
                ]),
              ),
              @anode.text(" "),
              @anode.hide(
                @tutuca.Expr::field("has_after"),
                @anode.h(
                  "div",
                  [@anode.attr("class", "opacity-40 row-noafter")],
                  [@anode.text("no state was kept for this one")],
                ),
              ),
            ],
          ),
        ),
      ],
    ),
    [[@anode.on("click", @tutuca.Expr::name("toggle"))]],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn trace_row_views() -> Map[String, @anode.View] {
  { "main": trace_row_main_view_ir() }
}

///|
/// The dispatches the file's `logic:` section answers.
///
/// Composed AHEAD of the author's `update~`, which stays optional and is
/// consulted only for a name this does not answer. GENERATED — edit the
/// block, not this.
fn trace_row_inline_update(
  s : TraceRowState,
  msg : @component.Dispatch,
  _ctx : &@tutuca.Ctx,
) -> @component.Update[TraceRowState] {
  match msg {
    Receive("toggle", _args) => {
      let s = { ..s, open: !s.open, }
      guard (Bool(s.has_walk) : @tutuca.Value) ==
        (
          Bool(
            !((Num(s.walk.to_double()) : @tutuca.Value) ==
            (Num(0.0) : @tutuca.Value)),
          ) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="toggle",
          pred="has_walk_tracks_walk",
          sentence="hasWalk says " +
            (Bool(s.has_walk) : @tutuca.Value).to_display_string() +
            " for walk " +
            (Num(s.walk.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "verb",
              domain: DOneOf([
                Str("send"),
                Str("intent"),
                Str("offered"),
                Str("answer"),
                Str("refused"),
              ]),
            },
            { field: "seq", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "turn", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "walk", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "depth", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="toggle",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      Next(s)
    }
    _ => Unhandled
  }
}

///|
/// `invariant has_walk_tracks_walk` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_row_compute_has_walk_tracks_walk(
  s : TraceRowState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      (Bool(s.has_walk) : @tutuca.Value) ==
      (
        Bool(
          !((Num(s.walk.to_double()) : @tutuca.Value) ==
          (Num(0.0) : @tutuca.Value)),
        ) : @tutuca.Value),
    ) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn trace_row_inline_compute() -> Map[
  String,
  (TraceRowState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  { "has_walk_tracks_walk": trace_row_compute_has_walk_tracks_walk }
}

///|
/// The TraceRow component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `TraceRowState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn trace_row_component(
  name? : String = "TraceRow",
  views? : Map[String, @anode.View] = trace_row_views(),
  initial? : TraceRowState = TraceRowState::zero(),
  common_style? : String = trace_row_common_style,
  global_style? : String = trace_row_global_style,
  update? : (TraceRowState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    TraceRowState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update=(s, msg, ctx) => {
      match trace_row_inline_update(s, msg, ctx) {
        Unhandled =>
          match update {
            Some(u) => u(s, msg, ctx)
            None => Unhandled
          }
        answered => answered
      }
    },
    compute=trace_row_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=trace_row_spec_source,
  )
}

///|
/// The compiled `main` view.
fn trace_log_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "trace-log flex flex-col gap-1 max-h-[50vh] overflow-y-auto p-2",
        ),
      ],
      [
        @anode.hide(
          @tutuca.Expr::field("has_rows"),
          @anode.h(
            "div",
            [@anode.attr("class", "text-xs opacity-40 empty-hint")],
            [@anode.dyn_text(@tutuca.Expr::field("empty_hint"), id=0)],
          ),
        ),
        @anode.text(" "),
        @anode.each(
          2,
          @tutuca.Expr::field("rows"),
          @anode.render_it(1),
          from_render_each=true,
        ),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn trace_log_views() -> Map[String, @anode.View] {
  { "main": trace_log_main_view_ir() }
}

///|
/// The dispatches the file's `logic:` section answers.
///
/// Composed AHEAD of the author's `update~`, which stays optional and is
/// consulted only for a name this does not answer. GENERATED — edit the
/// block, not this.
fn trace_log_inline_update(
  s : TraceLogState,
  msg : @component.Dispatch,
  _ctx : &@tutuca.Ctx,
) -> @component.Update[TraceLogState] {
  match msg {
    Receive("set_turn", args) => {
      guard args.get(0) is Some(Num(t_)) else { return Unchanged }
      let s = { ..s, turn: t_.to_int(), }
      guard (Bool(s.has_rows) : @tutuca.Value) ==
        (Bool(s.rows.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="set_turn",
          pred="has_rows_tracks_rows",
          sentence="hasRows says " +
            (Bool(s.has_rows) : @tutuca.Value).to_display_string() +
            " for " +
            (Num(s.rows.length().to_double()) : @tutuca.Value).to_display_string() +
            " rows",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [{ field: "turn", domain: DCompare(op=DGe, rhs=DLit(0)), }],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="set_turn",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      Next(s)
    }
    Receive("show_all_turns", _args) => {
      let s = { ..s, turn: 0, }
      guard (Bool(s.has_rows) : @tutuca.Value) ==
        (Bool(s.rows.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="show_all_turns",
          pred="has_rows_tracks_rows",
          sentence="hasRows says " +
            (Bool(s.has_rows) : @tutuca.Value).to_display_string() +
            " for " +
            (Num(s.rows.length().to_double()) : @tutuca.Value).to_display_string() +
            " rows",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [{ field: "turn", domain: DCompare(op=DGe, rhs=DLit(0)), }],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="show_all_turns",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      Next(s)
    }
    _ => Unhandled
  }
}

///|
/// `invariant has_rows_tracks_rows` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_log_compute_has_rows_tracks_rows(
  s : TraceLogState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      (Bool(s.has_rows) : @tutuca.Value) ==
      (Bool(s.rows.length().to_double() > 0.0) : @tutuca.Value),
    ) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn trace_log_inline_compute() -> Map[
  String,
  (TraceLogState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  { "has_rows_tracks_rows": trace_log_compute_has_rows_tracks_rows }
}

///|
/// The TraceLog component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `TraceLogState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn trace_log_component(
  name? : String = "TraceLog",
  views? : Map[String, @anode.View] = trace_log_views(),
  initial? : TraceLogState = TraceLogState::zero(),
  common_style? : String = trace_log_common_style,
  global_style? : String = trace_log_global_style,
  update? : (TraceLogState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    TraceLogState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update=(s, msg, ctx) => {
      match trace_log_inline_update(s, msg, ctx) {
        Unhandled =>
          match update {
            Some(u) => u(s, msg, ctx)
            None => Unhandled
          }
        answered => answered
      }
    },
    compute=trace_log_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=trace_row_spec_source,
  )
}

///|
/// The compiled `main` view.
fn trace_header_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "trace-header flex flex-col gap-1 p-2 text-sm")],
      [
        @anode.h(
          "div",
          [@anode.attr("class", "flex items-center gap-2 flex-wrap")],
          [
            @anode.h(
              "span",
              [
                @anode.attr(
                  "class", "badge badge-sm badge-primary header-component",
                ),
              ],
              [@anode.dyn_text(@tutuca.Expr::field("component"), id=0)],
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("module")]),
              @anode.h(
                "span",
                [
                  @anode.attr(
                    "class", "text-xs opacity-50 font-mono header-module",
                  ),
                ],
                [@anode.dyn_text(@tutuca.Expr::field("module"), id=1)],
              ),
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("example")]),
              @anode.h(
                "span",
                [@anode.attr("class", "text-xs opacity-50 header-example")],
                [@anode.dyn_text(@tutuca.Expr::field("example"), id=2)],
              ),
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::field("is_relocated"),
              @anode.h(
                "span",
                [
                  @anode.attr(
                    "title", "This trace was cut down to a subtree; every path in it was rewritten",
                  ),
                  @anode.attr(
                    "class", "badge badge-xs badge-warning header-relocated",
                  ),
                ],
                [
                  @anode.dyn_text(@anode.const_str("relocated from "), id=3),
                  @anode.dyn_text(@tutuca.Expr::field("relocated_from"), id=4),
                ],
              ),
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::field("compacted"),
              @anode.h(
                "span",
                [
                  @anode.attr(
                    "title", "The derived events were dropped: this trace can no longer be relocated or scripted",
                  ),
                  @anode.attr(
                    "class", "badge badge-xs badge-ghost header-compacted",
                  ),
                ],
                [@anode.text("compacted")],
              ),
            ),
          ],
        ),
        @anode.h(
          "div",
          [@anode.attr("class", "flex gap-3 text-xs opacity-60 tabular-nums")],
          [
            @anode.h("span", [@anode.attr("class", "header-events")], [
              @anode.dyn_text(@tutuca.Expr::field("events"), id=5),
              @anode.dyn_text(@anode.const_str(" events"), id=6),
            ]),
            @anode.text(" "),
            @anode.h("span", [@anode.attr("class", "header-inputs")], [
              @anode.dyn_text(@tutuca.Expr::field("inputs"), id=7),
              @anode.dyn_text(@anode.const_str(" fed on a rerun"), id=8),
            ]),
            @anode.text(" "),
            @anode.h("span", [@anode.attr("class", "header-turns")], [
              @anode.dyn_text(@tutuca.Expr::field("turns"), id=9),
              @anode.dyn_text(@anode.const_str(" turns"), id=10),
            ]),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("fingerprint")]),
              @anode.h(
                "span",
                [@anode.attr("class", "opacity-40 font-mono header-fp")],
                [@anode.dyn_text(@tutuca.Expr::field("fingerprint"), id=11)],
              ),
            ),
          ],
        ),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn trace_header_views() -> Map[String, @anode.View] {
  { "main": trace_header_main_view_ir() }
}

///|
/// `invariant inputs_are_events` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_header_compute_inputs_are_events(
  s : TraceHeaderState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(s.inputs.to_double() <= s.events.to_double()) : @tutuca.Value)
}

///|
/// `invariant turns_are_events` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_header_compute_turns_are_events(
  s : TraceHeaderState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(s.turns.to_double() <= s.events.to_double()) : @tutuca.Value)
}

///|
/// `invariant is_relocated_tracks_from` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_header_compute_is_relocated_tracks_from(
  s : TraceHeaderState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      (Bool(s.is_relocated) : @tutuca.Value) ==
      (Bool(s.relocated_from.length().to_double() > 0.0) : @tutuca.Value),
    ) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn trace_header_inline_compute() -> Map[
  String,
  (TraceHeaderState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  {
    "inputs_are_events": trace_header_compute_inputs_are_events,
    "turns_are_events": trace_header_compute_turns_are_events,
    "is_relocated_tracks_from": trace_header_compute_is_relocated_tracks_from,
  }
}

///|
/// The TraceHeader component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `TraceHeaderState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn trace_header_component(
  name? : String = "TraceHeader",
  views? : Map[String, @anode.View] = trace_header_views(),
  initial? : TraceHeaderState = TraceHeaderState::zero(),
  common_style? : String = trace_header_common_style,
  global_style? : String = trace_header_global_style,
  update? : (TraceHeaderState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    TraceHeaderState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute=trace_header_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=trace_row_spec_source,
  )
}

///|
/// The compiled `main` view.
fn trace_place_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "button",
      [
        @anode.dyn_attr("class", @tutuca.Expr::field("row_class")),
        @anode.eid(0),
      ],
      [
        @anode.h("span", [@anode.attr("class", "font-mono place-label")], [
          @anode.dyn_text(@tutuca.Expr::field("display"), id=0),
        ]),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("component")]),
          @anode.h(
            "span",
            [@anode.attr("class", "badge badge-xs badge-ghost place-component")],
            [@anode.dyn_text(@tutuca.Expr::field("component"), id=1)],
          ),
        ),
        @anode.text(" "),
        @anode.hide(
          @tutuca.Expr::field("mountable"),
          @anode.h(
            "span",
            [
              @anode.attr(
                "title", "Cutting here would not yield a component, so the trace could not be mounted",
              ),
              @anode.attr(
                "class", "badge badge-xs badge-warning place-unmountable",
              ),
            ],
            [@anode.text("not a component")],
          ),
        ),
        @anode.text(" "),
        @anode.h(
          "span",
          [
            @anode.attr(
              "class", "ml-auto text-xs opacity-40 tabular-nums place-count",
            ),
          ],
          [@anode.dyn_text(@tutuca.Expr::field("events"), id=2)],
        ),
      ],
    ),
    [
      [
        @anode.on("click", @tutuca.Expr::name("pick_place"), args=[
          @tutuca.Expr::field("label"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn trace_place_views() -> Map[String, @anode.View] {
  { "main": trace_place_main_view_ir() }
}

///|
/// The dispatches the file's `logic:` section answers.
///
/// Composed AHEAD of the author's `update~`, which stays optional and is
/// consulted only for a name this does not answer. GENERATED — edit the
/// block, not this.
fn trace_place_inline_update(
  s : TracePlaceState,
  msg : @component.Dispatch,
  ctx : &@tutuca.Ctx,
) -> @component.Update[TracePlaceState] {
  match msg {
    Receive("pick_place", args) => {
      guard args.get(0) is Some(Str(p_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_pick",
          [(Str(p_) : @tutuca.Value)],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            { field: "events", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "depth", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="pick_place",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    _ => Unhandled
  }
}

///|
/// The TracePlace component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `TracePlaceState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn trace_place_component(
  name? : String = "TracePlace",
  views? : Map[String, @anode.View] = trace_place_views(),
  initial? : TracePlaceState = TracePlaceState::zero(),
  common_style? : String = trace_place_common_style,
  global_style? : String = trace_place_global_style,
  update? : (TracePlaceState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    TracePlaceState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update=(s, msg, ctx) => {
      match trace_place_inline_update(s, msg, ctx) {
        Unhandled =>
          match update {
            Some(u) => u(s, msg, ctx)
            None => Unhandled
          }
        answered => answered
      }
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=trace_row_spec_source,
  )
}

///|
/// The compiled `main` view.
fn trace_places_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "trace-places flex flex-col gap-1 p-2")],
      [
        @anode.h(
          "div",
          [@anode.attr("class", "text-xs opacity-50 places-heading")],
          [@anode.dyn_text(@tutuca.Expr::field("heading"), id=0)],
        ),
        @anode.text(" "),
        @anode.hide(
          @tutuca.Expr::field("has_places"),
          @anode.h(
            "div",
            [@anode.attr("class", "text-xs opacity-40 empty-hint")],
            [@anode.dyn_text(@tutuca.Expr::field("empty_hint"), id=1)],
          ),
        ),
        @anode.text(" "),
        @anode.each(
          3,
          @tutuca.Expr::field("places"),
          @anode.render_it(2),
          from_render_each=true,
        ),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn trace_places_views() -> Map[String, @anode.View] {
  { "main": trace_places_main_view_ir() }
}

///|
/// `invariant has_places_tracks_places` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_places_compute_has_places_tracks_places(
  s : TracePlacesState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      (Bool(s.has_places) : @tutuca.Value) ==
      (Bool(s.places.length().to_double() > 0.0) : @tutuca.Value),
    ) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn trace_places_inline_compute() -> Map[
  String,
  (TracePlacesState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  { "has_places_tracks_places": trace_places_compute_has_places_tracks_places }
}

///|
/// The TracePlaces component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `TracePlacesState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn trace_places_component(
  name? : String = "TracePlaces",
  views? : Map[String, @anode.View] = trace_places_views(),
  initial? : TracePlacesState = TracePlacesState::zero(),
  common_style? : String = trace_places_common_style,
  global_style? : String = trace_places_global_style,
  update? : (TracePlacesState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    TracePlacesState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute=trace_places_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=trace_row_spec_source,
  )
}

///|
/// The compiled `main` view.
fn recorder_bar_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-1")], [
      @anode.h(
        "div",
        [
          @anode.attr(
            "class", "recorder-bar flex items-center gap-2 p-2 flex-wrap",
          ),
        ],
        [
          @anode.show(
            @tutuca.Expr::field("recording"),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-sm btn-error rec-stop"),
                @anode.eid(0),
              ],
              [@anode.text(" Stop recording ")],
            ),
          ),
          @anode.text(" "),
          @anode.hide(
            @tutuca.Expr::field("recording"),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-sm btn-primary rec-start"),
                @anode.eid(1),
              ],
              [@anode.text(" Record ")],
            ),
          ),
          @anode.text(" "),
          @anode.h(
            "span",
            [@anode.attr("class", "badge badge-sm badge-ghost rec-scope")],
            [@anode.dyn_text(@tutuca.Expr::field("scope_label"), id=0)],
          ),
          @anode.text(" "),
          @anode.show(
            @tutuca.Expr::field("recording"),
            @anode.h(
              "span",
              [
                @anode.attr(
                  "class", "text-xs opacity-60 tabular-nums rec-count",
                ),
              ],
              [
                @anode.dyn_text(@tutuca.Expr::field("events"), id=1),
                @anode.dyn_text(@anode.const_str(" events · "), id=2),
                @anode.dyn_text(@tutuca.Expr::field("turns"), id=3),
                @anode.dyn_text(@anode.const_str(" turns"), id=4),
              ],
            ),
          ),
          @anode.text(" "),
          @anode.show(
            @tutuca.Expr::field("has_trace"),
            @anode.h("hide", [], [
              @anode.h(
                "button",
                [
                  @anode.attr("class", "btn btn-sm btn-ghost rec-clear"),
                  @anode.eid(2),
                ],
                [@anode.text(" Discard ")],
              ),
            ]),
          ),
        ],
      ),
      @anode.text(" "),
      @anode.comment(
        " Inline rather than a view of its own: what a recording covers has to be asked in the place the Record button is, and a second template holding the same three buttons is a second place for them to drift. ",
      ),
      @anode.text(" "),
      @anode.show(
        @tutuca.Expr::field("picking"),
        @anode.h(
          "div",
          [@anode.attr("class", "recorder-picker flex flex-col gap-2 p-2")],
          [
            @anode.h("div", [@anode.attr("class", "text-sm")], [
              @anode.text("What should this recording cover?"),
            ]),
            @anode.h("div", [@anode.attr("class", "flex gap-2 flex-wrap")], [
              @anode.h(
                "button",
                [
                  @anode.dyn_attr("class", @tutuca.Expr::field("all_class")),
                  @anode.eid(3),
                ],
                [@anode.text("The whole storybook")],
              ),
              @anode.text(" "),
              @anode.h(
                "button",
                [
                  @anode.dyn_attr("class", @tutuca.Expr::field("section_class")),
                  @anode.eid(4),
                ],
                [@anode.text("The active section")],
              ),
              @anode.text(" "),
              @anode.h(
                "button",
                [
                  @anode.dyn_attr("class", @tutuca.Expr::field("example_class")),
                  @anode.eid(5),
                ],
                [@anode.text("This example only")],
              ),
            ]),
            @anode.h("div", [@anode.attr("class", "text-xs opacity-50")], [
              @anode.text(
                " A narrower scope is not a filter applied afterwards — it is what gets mounted with a recorder on it, so nothing else is ever captured. You can still cut a recording down to one component when you stop. ",
              ),
            ]),
            @anode.h("div", [@anode.attr("class", "flex gap-2")], [
              @anode.h(
                "button",
                [
                  @anode.attr("class", "btn btn-sm btn-primary picker-go"),
                  @anode.eid(6),
                ],
                [@anode.text("Start")],
              ),
              @anode.text(" "),
              @anode.h(
                "button",
                [
                  @anode.attr("class", "btn btn-sm btn-ghost picker-cancel"),
                  @anode.eid(7),
                ],
                [@anode.text("Cancel")],
              ),
            ]),
          ],
        ),
      ),
    ]),
    [
      [@anode.on("click", @tutuca.Expr::name("stop_recording"))],
      [@anode.on("click", @tutuca.Expr::name("open_picker"))],
      [@anode.on("click", @tutuca.Expr::name("clear_recording"))],
      [
        @anode.on("click", @tutuca.Expr::name("set_scope"), args=[
          @anode.const_str("all"),
        ]),
      ],
      [
        @anode.on("click", @tutuca.Expr::name("set_scope"), args=[
          @anode.const_str("section"),
        ]),
      ],
      [
        @anode.on("click", @tutuca.Expr::name("set_scope"), args=[
          @anode.const_str("example"),
        ]),
      ],
      [@anode.on("click", @tutuca.Expr::name("start_recording"))],
      [@anode.on("click", @tutuca.Expr::name("close_picker"))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn recorder_bar_views() -> Map[String, @anode.View] {
  { "main": recorder_bar_main_view_ir() }
}

///|
/// The dispatches the file's `logic:` section answers.
///
/// Composed AHEAD of the author's `update~`, which stays optional and is
/// consulted only for a name this does not answer. GENERATED — edit the
/// block, not this.
fn recorder_bar_inline_update(
  s : RecorderBarState,
  msg : @component.Dispatch,
  ctx : &@tutuca.Ctx,
) -> @component.Update[RecorderBarState] {
  match msg {
    Receive("set_scope", args) => {
      guard args.get(0) is Some(Str(s_)) else { return Unchanged }
      let s = { ..s, scope: s_, }
      let s = {
        ..s,
        scope_label: if (Str(s_) : @tutuca.Value) ==
          (Str("section") : @tutuca.Value) {
          "the active section"
        } else if (Str(s_) : @tutuca.Value) == (Str("example") : @tutuca.Value) {
          "this example"
        } else {
          "the whole storybook"
        },
      }
      let s = {
        ..s,
        all_class: if (Str(s_) : @tutuca.Value) == (Str("all") : @tutuca.Value) {
          "btn btn-sm pick-all btn-primary"
        } else {
          "btn btn-sm pick-all btn-outline"
        },
      }
      let s = {
        ..s,
        section_class: if (Str(s_) : @tutuca.Value) ==
          (Str("section") : @tutuca.Value) {
          "btn btn-sm pick-section btn-primary"
        } else {
          "btn btn-sm pick-section btn-outline"
        },
      }
      let s = {
        ..s,
        example_class: if (Str(s_) : @tutuca.Value) ==
          (Str("example") : @tutuca.Value) {
          "btn btn-sm pick-example btn-primary"
        } else {
          "btn btn-sm pick-example btn-outline"
        },
      }
      guard s.turns.to_double() <= s.events.to_double() else {
        @tutuca.invariant_failed(
          handler="set_scope",
          pred="turns_are_events",
          sentence=(Num(s.turns.to_double()) : @tutuca.Value).to_display_string() +
            " turns out of " +
            (Num(s.events.to_double()) : @tutuca.Value).to_display_string() +
            " events",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "scope",
              domain: DOneOf([Str("all"), Str("section"), Str("example")]),
            },
            { field: "events", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "turns", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="set_scope",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      Next(s)
    }
    Receive("open_picker", _args) => {
      let s = { ..s, picking: true, }
      guard s.turns.to_double() <= s.events.to_double() else {
        @tutuca.invariant_failed(
          handler="open_picker",
          pred="turns_are_events",
          sentence=(Num(s.turns.to_double()) : @tutuca.Value).to_display_string() +
            " turns out of " +
            (Num(s.events.to_double()) : @tutuca.Value).to_display_string() +
            " events",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "scope",
              domain: DOneOf([Str("all"), Str("section"), Str("example")]),
            },
            { field: "events", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "turns", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="open_picker",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      Next(s)
    }
    Receive("close_picker", _args) => {
      let s = { ..s, picking: false, }
      guard s.turns.to_double() <= s.events.to_double() else {
        @tutuca.invariant_failed(
          handler="close_picker",
          pred="turns_are_events",
          sentence=(Num(s.turns.to_double()) : @tutuca.Value).to_display_string() +
            " turns out of " +
            (Num(s.events.to_double()) : @tutuca.Value).to_display_string() +
            " events",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "scope",
              domain: DOneOf([Str("all"), Str("section"), Str("example")]),
            },
            { field: "events", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "turns", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="close_picker",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      Next(s)
    }
    Receive("start_recording", _args) => {
      let effects : Array[() -> Unit] = []
      let s = { ..s, picking: false, }
      effects.push(() => {
        ctx.intent(
          "trace_start",
          [(Str(s.scope) : @tutuca.Value)],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.turns.to_double() <= s.events.to_double() else {
        @tutuca.invariant_failed(
          handler="start_recording",
          pred="turns_are_events",
          sentence=(Num(s.turns.to_double()) : @tutuca.Value).to_display_string() +
            " turns out of " +
            (Num(s.events.to_double()) : @tutuca.Value).to_display_string() +
            " events",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "scope",
              domain: DOneOf([Str("all"), Str("section"), Str("example")]),
            },
            { field: "events", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "turns", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="start_recording",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("stop_recording", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_stop",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.turns.to_double() <= s.events.to_double() else {
        @tutuca.invariant_failed(
          handler="stop_recording",
          pred="turns_are_events",
          sentence=(Num(s.turns.to_double()) : @tutuca.Value).to_display_string() +
            " turns out of " +
            (Num(s.events.to_double()) : @tutuca.Value).to_display_string() +
            " events",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "scope",
              domain: DOneOf([Str("all"), Str("section"), Str("example")]),
            },
            { field: "events", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "turns", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="stop_recording",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("clear_recording", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_discard",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.turns.to_double() <= s.events.to_double() else {
        @tutuca.invariant_failed(
          handler="clear_recording",
          pred="turns_are_events",
          sentence=(Num(s.turns.to_double()) : @tutuca.Value).to_display_string() +
            " turns out of " +
            (Num(s.events.to_double()) : @tutuca.Value).to_display_string() +
            " events",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "scope",
              domain: DOneOf([Str("all"), Str("section"), Str("example")]),
            },
            { field: "events", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "turns", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="clear_recording",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    _ => Unhandled
  }
}

///|
/// `invariant turns_are_events` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn recorder_bar_compute_turns_are_events(
  s : RecorderBarState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(s.turns.to_double() <= s.events.to_double()) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn recorder_bar_inline_compute() -> Map[
  String,
  (RecorderBarState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  { "turns_are_events": recorder_bar_compute_turns_are_events }
}

///|
/// The RecorderBar component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `RecorderBarState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn recorder_bar_component(
  name? : String = "RecorderBar",
  views? : Map[String, @anode.View] = recorder_bar_views(),
  initial? : RecorderBarState = RecorderBarState::zero(),
  common_style? : String = recorder_bar_common_style,
  global_style? : String = recorder_bar_global_style,
  update? : (RecorderBarState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    RecorderBarState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update=(s, msg, ctx) => {
      match recorder_bar_inline_update(s, msg, ctx) {
        Unhandled =>
          match update {
            Some(u) => u(s, msg, ctx)
            None => Unhandled
          }
        answered => answered
      }
    },
    compute=recorder_bar_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=trace_row_spec_source,
  )
}

///|
/// The compiled `main` view.
fn trace_panel_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "trace-panel flex flex-col gap-2")], [
      @anode.h(
        "div",
        [
          @anode.attr(
            "class", "flex items-center gap-2 border-b border-base-content/10 px-2",
          ),
        ],
        [
          @anode.h(
            "button",
            [
              @anode.dyn_attr("class", @tutuca.Expr::field("record_tab_class")),
              @anode.eid(0),
            ],
            [@anode.text("Record")],
          ),
          @anode.text(" "),
          @anode.show(
            @tutuca.Expr::field("has_trace"),
            @anode.h(
              "button",
              [
                @anode.dyn_attr("class", @tutuca.Expr::field("trace_tab_class")),
                @anode.eid(1),
              ],
              [@anode.text("Trace")],
            ),
          ),
          @anode.text(" "),
          @anode.show(
            @tutuca.Expr::field("has_trace"),
            @anode.h(
              "button",
              [
                @anode.dyn_attr(
                  "class",
                  @tutuca.Expr::field("replay_tab_class"),
                ),
                @anode.eid(2),
              ],
              [@anode.text("Replay")],
            ),
          ),
          @anode.text(" "),
          @anode.show(
            @tutuca.Expr::field("has_trace"),
            @anode.h(
              "button",
              [
                @anode.dyn_attr(
                  "class",
                  @tutuca.Expr::field("select_tab_class"),
                ),
                @anode.eid(3),
              ],
              [@anode.dyn_text(@tutuca.Expr::field("select_label"), id=0)],
            ),
          ),
          @anode.text(" "),
          @anode.show(
            @tutuca.Expr::field("has_trace"),
            @anode.h(
              "span",
              [
                @anode.attr(
                  "class", "ml-auto text-xs opacity-40 tabular-nums panel-pos",
                ),
              ],
              [
                @anode.dyn_text(@tutuca.Expr::field("at"), id=1),
                @anode.dyn_text(@anode.const_str(" / "), id=2),
                @anode.dyn_text(@tutuca.Expr::field("total"), id=3),
              ],
            ),
          ),
        ],
      ),
      @anode.text(" "),
      @anode.comment(
        " Above every pane, because it is about the thing the reader just did and not about the pane they happen to be looking at. A parse error on import belongs where they can see it without changing tabs. ",
      ),
      @anode.text(" "),
      @anode.show(
        @tutuca.Expr::field("has_notice"),
        @anode.h(
          "div",
          [
            @anode.attr(
              "class", "trace-notice alert alert-warning text-xs mx-2 py-1",
            ),
          ],
          [@anode.dyn_text(@tutuca.Expr::field("notice"), id=4)],
        ),
      ),
      @anode.text(" "),
      @anode.render(5, @tutuca.Expr::field("bar")),
      @anode.text(" "),
      @anode.show(
        @tutuca.Expr::field("show_trace"),
        @anode.h(
          "div",
          [@anode.attr("class", "panel-trace flex flex-col gap-2")],
          [
            @anode.h(
              "div",
              [@anode.attr("class", "flex items-center gap-2 px-2 flex-wrap")],
              [
                @anode.h(
                  "button",
                  [
                    @anode.attr("class", "btn btn-sm btn-ghost trace-export"),
                    @anode.eid(4),
                  ],
                  [@anode.text(" Download ")],
                ),
                @anode.text(" "),
                @anode.comment(
                  " Enabled by a pick, and the pick is a separate press for the reason the host's comment gives: choosing a place is the thing a person does several times before they mean it. ",
                ),
                @anode.text(" "),
                @anode.h(
                  "button",
                  [
                    @anode.attr("class", "btn btn-sm btn-ghost trace-cut"),
                    @anode.eid(5),
                  ],
                  [@anode.text(" Cut to the picked place ")],
                ),
              ],
            ),
            @anode.text(" "),
            @anode.render(6, @tutuca.Expr::field("header")),
            @anode.text(" "),
            @anode.render(7, @tutuca.Expr::field("places")),
            @anode.text(" "),
            @anode.render(8, @tutuca.Expr::field("log")),
          ],
        ),
      ),
      @anode.text(" "),
      @anode.comment(
        " The rerun. It drives the STORY, not a preview of one: the gallery is a single transactor with every story mounted under it, so the events go into the instance already on screen and the reader watches it move. ",
      ),
      @anode.text(" "),
      @anode.show(
        @tutuca.Expr::field("show_replay"),
        @anode.h(
          "div",
          [@anode.attr("class", "panel-replay flex flex-col gap-2 p-2")],
          [
            @anode.show(
              @tutuca.Expr::field("can_replay"),
              @anode.h("div", [@anode.attr("class", "flex gap-2 flex-wrap")], [
                @anode.h(
                  "button",
                  [
                    @anode.attr("class", "btn btn-sm btn-primary replay-step"),
                    @anode.eid(6),
                  ],
                  [@anode.text("Step")],
                ),
                @anode.text(" "),
                @anode.h(
                  "button",
                  [@anode.attr("class", "btn btn-sm replay-run"), @anode.eid(7)],
                  [@anode.text("Run")],
                ),
                @anode.text(" "),
                @anode.h(
                  "button",
                  [
                    @anode.attr("class", "btn btn-sm btn-ghost replay-reset"),
                    @anode.eid(8),
                  ],
                  [@anode.text("Reset")],
                ),
                @anode.text(" "),
                @anode.h(
                  "span",
                  [
                    @anode.attr(
                      "class", "text-xs opacity-60 tabular-nums replay-pos self-center",
                    ),
                  ],
                  [
                    @anode.dyn_text(@tutuca.Expr::field("at"), id=9),
                    @anode.dyn_text(@anode.const_str(" of "), id=10),
                    @anode.dyn_text(@tutuca.Expr::field("total"), id=11),
                  ],
                ),
              ]),
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::field("can_replay"),
              @anode.h("div", [@anode.attr("class", "text-xs opacity-50")], [
                @anode.text(
                  " The story above is the one being driven. A step feeds one whole turn — what the recording drained together — so the app is never left mid-cascade in a state it was never in. ",
                ),
              ]),
            ),
          ],
        ),
      ),
      @anode.text(" "),
      @anode.comment(
        " Import lives on the empty pane as well as the full one: the reader who has no recording is exactly the reader who wants to load somebody else's. ",
      ),
      @anode.text(" "),
      @anode.show(
        @tutuca.Expr::field("show_import"),
        @anode.h(
          "div",
          [@anode.attr("class", "panel-import flex flex-col gap-2 p-2")],
          [
            @anode.h("div", [@anode.attr("class", "text-xs opacity-50")], [
              @anode.text("Paste a .trace.jsonl to load it."),
            ]),
            @anode.text(" "),
            @anode.h(
              "textarea",
              [
                @anode.attr("class", "textarea textarea-sm trace-draft"),
                @anode.attr("rows", "3"),
                @anode.dyn_attr("value", @tutuca.Expr::field("draft")),
                @anode.eid(9),
              ],
              [],
            ),
            @anode.text(" "),
            @anode.h("div", [], [
              @anode.h(
                "button",
                [
                  @anode.attr("class", "btn btn-sm trace-import"),
                  @anode.eid(10),
                ],
                [@anode.text("Load")],
              ),
            ]),
          ],
        ),
      ),
      @anode.text(" "),
      @anode.hide(
        @tutuca.Expr::field("has_trace"),
        @anode.h(
          "div",
          [@anode.attr("class", "panel-empty flex flex-col gap-2 px-2 pb-2")],
          [
            @anode.h("div", [@anode.attr("class", "text-xs opacity-40")], [
              @anode.text(
                " Nothing recorded yet. Press Record, use the component, then stop — or paste a recording somebody else made. ",
              ),
            ]),
            @anode.text(" "),
            @anode.h(
              "textarea",
              [
                @anode.attr("class", "textarea textarea-sm trace-draft-empty"),
                @anode.attr("rows", "3"),
                @anode.dyn_attr("value", @tutuca.Expr::field("draft")),
                @anode.eid(11),
              ],
              [],
            ),
            @anode.text(" "),
            @anode.h("div", [], [
              @anode.h(
                "button",
                [
                  @anode.attr("class", "btn btn-sm trace-import-empty"),
                  @anode.eid(12),
                ],
                [@anode.text("Load")],
              ),
            ]),
          ],
        ),
      ),
    ]),
    [
      [
        @anode.on("click", @tutuca.Expr::name("set_tab"), args=[
          @anode.const_str("record"),
        ]),
      ],
      [
        @anode.on("click", @tutuca.Expr::name("set_tab"), args=[
          @anode.const_str("trace"),
        ]),
      ],
      [
        @anode.on("click", @tutuca.Expr::name("set_tab"), args=[
          @anode.const_str("replay"),
        ]),
      ],
      [@anode.on("click", @tutuca.Expr::name("toggle_selecting"))],
      [@anode.on("click", @tutuca.Expr::name("export_trace"))],
      [@anode.on("click", @tutuca.Expr::name("cut_to_place"))],
      [@anode.on("click", @tutuca.Expr::name("replay_step"))],
      [@anode.on("click", @tutuca.Expr::name("replay_run"))],
      [@anode.on("click", @tutuca.Expr::name("replay_reset"))],
      [
        @anode.on(
          "input",
          @anode.const_null(),
          binding=@anode.BindString,
          action=@anode.set_property_action(
            "draft",
            @tutuca.Expr::event_path(["value"]),
          ),
        ),
      ],
      [@anode.on("click", @tutuca.Expr::name("import_draft"))],
      [
        @anode.on(
          "input",
          @anode.const_null(),
          binding=@anode.BindString,
          action=@anode.set_property_action(
            "draft",
            @tutuca.Expr::event_path(["value"]),
          ),
        ),
      ],
      [@anode.on("click", @tutuca.Expr::name("import_draft"))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn trace_panel_views() -> Map[String, @anode.View] {
  { "main": trace_panel_main_view_ir() }
}

///|
/// The dispatches the file's `logic:` section answers.
///
/// Composed AHEAD of the author's `update~`, which stays optional and is
/// consulted only for a name this does not answer. GENERATED — edit the
/// block, not this.
fn trace_panel_inline_update(
  s : TracePanelState,
  msg : @component.Dispatch,
  ctx : &@tutuca.Ctx,
) -> @component.Update[TracePanelState] {
  match msg {
    Receive("toggle_selecting", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_select",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.at.to_double() <= s.total.to_double() else {
        @tutuca.invariant_failed(
          handler="toggle_selecting",
          pred="at_within_total",
          sentence="parked at " +
            (Num(s.at.to_double()) : @tutuca.Value).to_display_string() +
            " of " +
            (Num(s.total.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.can_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="toggle_selecting",
          pred="can_replay_needs_a_trace",
          sentence="the replay controls are live without a trace behind them",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard (Bool(s.has_notice) : @tutuca.Value) ==
        (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="toggle_selecting",
          pred="has_notice_tracks_notice",
          sentence="hasNotice says " +
            (Bool(s.has_notice) : @tutuca.Value).to_display_string() +
            " for a notice of " +
            (Num(s.notice.length().to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.replaying || s.can_replay else {
        @tutuca.invariant_failed(
          handler="toggle_selecting",
          pred="replaying_needs_a_replay",
          sentence="a rerun is playing that was never mounted",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_trace || s.has_trace else {
        @tutuca.invariant_failed(
          handler="toggle_selecting",
          pred="show_trace_needs_a_trace",
          sentence="the trace pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="toggle_selecting",
          pred="show_replay_needs_a_trace",
          sentence="the replay pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "tab",
              domain: DOneOf([
                Str("record"),
                Str("trace"),
                Str("pick"),
                Str("replay"),
              ]),
            },
            { field: "at", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "total", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="toggle_selecting",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("set_tab", args) => {
      guard args.get(0) is Some(Str(t_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      let s = { ..s, tab: t_, }
      effects.push(() => {
        ctx.intent(
          "trace_set_tab",
          [(Str(t_) : @tutuca.Value)],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.at.to_double() <= s.total.to_double() else {
        @tutuca.invariant_failed(
          handler="set_tab",
          pred="at_within_total",
          sentence="parked at " +
            (Num(s.at.to_double()) : @tutuca.Value).to_display_string() +
            " of " +
            (Num(s.total.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.can_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="set_tab",
          pred="can_replay_needs_a_trace",
          sentence="the replay controls are live without a trace behind them",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard (Bool(s.has_notice) : @tutuca.Value) ==
        (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="set_tab",
          pred="has_notice_tracks_notice",
          sentence="hasNotice says " +
            (Bool(s.has_notice) : @tutuca.Value).to_display_string() +
            " for a notice of " +
            (Num(s.notice.length().to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.replaying || s.can_replay else {
        @tutuca.invariant_failed(
          handler="set_tab",
          pred="replaying_needs_a_replay",
          sentence="a rerun is playing that was never mounted",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_trace || s.has_trace else {
        @tutuca.invariant_failed(
          handler="set_tab",
          pred="show_trace_needs_a_trace",
          sentence="the trace pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="set_tab",
          pred="show_replay_needs_a_trace",
          sentence="the replay pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "tab",
              domain: DOneOf([
                Str("record"),
                Str("trace"),
                Str("pick"),
                Str("replay"),
              ]),
            },
            { field: "at", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "total", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="set_tab",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("export_trace", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_export",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.at.to_double() <= s.total.to_double() else {
        @tutuca.invariant_failed(
          handler="export_trace",
          pred="at_within_total",
          sentence="parked at " +
            (Num(s.at.to_double()) : @tutuca.Value).to_display_string() +
            " of " +
            (Num(s.total.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.can_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="export_trace",
          pred="can_replay_needs_a_trace",
          sentence="the replay controls are live without a trace behind them",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard (Bool(s.has_notice) : @tutuca.Value) ==
        (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="export_trace",
          pred="has_notice_tracks_notice",
          sentence="hasNotice says " +
            (Bool(s.has_notice) : @tutuca.Value).to_display_string() +
            " for a notice of " +
            (Num(s.notice.length().to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.replaying || s.can_replay else {
        @tutuca.invariant_failed(
          handler="export_trace",
          pred="replaying_needs_a_replay",
          sentence="a rerun is playing that was never mounted",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_trace || s.has_trace else {
        @tutuca.invariant_failed(
          handler="export_trace",
          pred="show_trace_needs_a_trace",
          sentence="the trace pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="export_trace",
          pred="show_replay_needs_a_trace",
          sentence="the replay pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "tab",
              domain: DOneOf([
                Str("record"),
                Str("trace"),
                Str("pick"),
                Str("replay"),
              ]),
            },
            { field: "at", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "total", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="export_trace",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("import_draft", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_import",
          [(Str(s.draft) : @tutuca.Value)],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.at.to_double() <= s.total.to_double() else {
        @tutuca.invariant_failed(
          handler="import_draft",
          pred="at_within_total",
          sentence="parked at " +
            (Num(s.at.to_double()) : @tutuca.Value).to_display_string() +
            " of " +
            (Num(s.total.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.can_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="import_draft",
          pred="can_replay_needs_a_trace",
          sentence="the replay controls are live without a trace behind them",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard (Bool(s.has_notice) : @tutuca.Value) ==
        (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="import_draft",
          pred="has_notice_tracks_notice",
          sentence="hasNotice says " +
            (Bool(s.has_notice) : @tutuca.Value).to_display_string() +
            " for a notice of " +
            (Num(s.notice.length().to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.replaying || s.can_replay else {
        @tutuca.invariant_failed(
          handler="import_draft",
          pred="replaying_needs_a_replay",
          sentence="a rerun is playing that was never mounted",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_trace || s.has_trace else {
        @tutuca.invariant_failed(
          handler="import_draft",
          pred="show_trace_needs_a_trace",
          sentence="the trace pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="import_draft",
          pred="show_replay_needs_a_trace",
          sentence="the replay pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "tab",
              domain: DOneOf([
                Str("record"),
                Str("trace"),
                Str("pick"),
                Str("replay"),
              ]),
            },
            { field: "at", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "total", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="import_draft",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("cut_to_place", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_relocate",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.at.to_double() <= s.total.to_double() else {
        @tutuca.invariant_failed(
          handler="cut_to_place",
          pred="at_within_total",
          sentence="parked at " +
            (Num(s.at.to_double()) : @tutuca.Value).to_display_string() +
            " of " +
            (Num(s.total.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.can_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="cut_to_place",
          pred="can_replay_needs_a_trace",
          sentence="the replay controls are live without a trace behind them",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard (Bool(s.has_notice) : @tutuca.Value) ==
        (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="cut_to_place",
          pred="has_notice_tracks_notice",
          sentence="hasNotice says " +
            (Bool(s.has_notice) : @tutuca.Value).to_display_string() +
            " for a notice of " +
            (Num(s.notice.length().to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.replaying || s.can_replay else {
        @tutuca.invariant_failed(
          handler="cut_to_place",
          pred="replaying_needs_a_replay",
          sentence="a rerun is playing that was never mounted",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_trace || s.has_trace else {
        @tutuca.invariant_failed(
          handler="cut_to_place",
          pred="show_trace_needs_a_trace",
          sentence="the trace pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="cut_to_place",
          pred="show_replay_needs_a_trace",
          sentence="the replay pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "tab",
              domain: DOneOf([
                Str("record"),
                Str("trace"),
                Str("pick"),
                Str("replay"),
              ]),
            },
            { field: "at", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "total", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="cut_to_place",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("replay_step", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_replay_step",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.at.to_double() <= s.total.to_double() else {
        @tutuca.invariant_failed(
          handler="replay_step",
          pred="at_within_total",
          sentence="parked at " +
            (Num(s.at.to_double()) : @tutuca.Value).to_display_string() +
            " of " +
            (Num(s.total.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.can_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_step",
          pred="can_replay_needs_a_trace",
          sentence="the replay controls are live without a trace behind them",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard (Bool(s.has_notice) : @tutuca.Value) ==
        (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="replay_step",
          pred="has_notice_tracks_notice",
          sentence="hasNotice says " +
            (Bool(s.has_notice) : @tutuca.Value).to_display_string() +
            " for a notice of " +
            (Num(s.notice.length().to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.replaying || s.can_replay else {
        @tutuca.invariant_failed(
          handler="replay_step",
          pred="replaying_needs_a_replay",
          sentence="a rerun is playing that was never mounted",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_trace || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_step",
          pred="show_trace_needs_a_trace",
          sentence="the trace pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_step",
          pred="show_replay_needs_a_trace",
          sentence="the replay pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "tab",
              domain: DOneOf([
                Str("record"),
                Str("trace"),
                Str("pick"),
                Str("replay"),
              ]),
            },
            { field: "at", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "total", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="replay_step",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("replay_run", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_replay_run",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.at.to_double() <= s.total.to_double() else {
        @tutuca.invariant_failed(
          handler="replay_run",
          pred="at_within_total",
          sentence="parked at " +
            (Num(s.at.to_double()) : @tutuca.Value).to_display_string() +
            " of " +
            (Num(s.total.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.can_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_run",
          pred="can_replay_needs_a_trace",
          sentence="the replay controls are live without a trace behind them",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard (Bool(s.has_notice) : @tutuca.Value) ==
        (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="replay_run",
          pred="has_notice_tracks_notice",
          sentence="hasNotice says " +
            (Bool(s.has_notice) : @tutuca.Value).to_display_string() +
            " for a notice of " +
            (Num(s.notice.length().to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.replaying || s.can_replay else {
        @tutuca.invariant_failed(
          handler="replay_run",
          pred="replaying_needs_a_replay",
          sentence="a rerun is playing that was never mounted",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_trace || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_run",
          pred="show_trace_needs_a_trace",
          sentence="the trace pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_run",
          pred="show_replay_needs_a_trace",
          sentence="the replay pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "tab",
              domain: DOneOf([
                Str("record"),
                Str("trace"),
                Str("pick"),
                Str("replay"),
              ]),
            },
            { field: "at", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "total", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="replay_run",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("replay_reset", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "trace_replay_reset",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      guard s.at.to_double() <= s.total.to_double() else {
        @tutuca.invariant_failed(
          handler="replay_reset",
          pred="at_within_total",
          sentence="parked at " +
            (Num(s.at.to_double()) : @tutuca.Value).to_display_string() +
            " of " +
            (Num(s.total.to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.can_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_reset",
          pred="can_replay_needs_a_trace",
          sentence="the replay controls are live without a trace behind them",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard (Bool(s.has_notice) : @tutuca.Value) ==
        (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="replay_reset",
          pred="has_notice_tracks_notice",
          sentence="hasNotice says " +
            (Bool(s.has_notice) : @tutuca.Value).to_display_string() +
            " for a notice of " +
            (Num(s.notice.length().to_double()) : @tutuca.Value).to_display_string(),
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.replaying || s.can_replay else {
        @tutuca.invariant_failed(
          handler="replay_reset",
          pred="replaying_needs_a_replay",
          sentence="a rerun is playing that was never mounted",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_trace || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_reset",
          pred="show_trace_needs_a_trace",
          sentence="the trace pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      guard !s.show_replay || s.has_trace else {
        @tutuca.invariant_failed(
          handler="replay_reset",
          pred="show_replay_needs_a_trace",
          sentence="the replay pane is showing without a trace behind it",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let domain_fields = @component.Fields::encode(s)
      match
        @tutuca.first_broken_domain(
          [
            {
              field: "tab",
              domain: DOneOf([
                Str("record"),
                Str("trace"),
                Str("pick"),
                Str("replay"),
              ]),
            },
            { field: "at", domain: DCompare(op=DGe, rhs=DLit(0)), },
            { field: "total", domain: DCompare(op=DGe, rhs=DLit(0)), },
          ],
          domain_fields,
        ) {
        Some(domain_broke) => {
          @tutuca.domain_failed(
            handler="replay_reset",
            field=domain_broke.field,
            sentence=@tutuca.broken_domain_sentence(domain_broke, domain_fields),
            state=(Map(domain_fields) : @tutuca.Value),
          )
          return Unchanged
        }
        None => ()
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    _ => Unhandled
  }
}

///|
/// `invariant has_notice_tracks_notice` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_panel_compute_has_notice_tracks_notice(
  s : TracePanelState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      (Bool(s.has_notice) : @tutuca.Value) ==
      (Bool(s.notice.length().to_double() > 0.0) : @tutuca.Value),
    ) : @tutuca.Value)
}

///|
/// `invariant can_replay_needs_a_trace` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_panel_compute_can_replay_needs_a_trace(
  s : TracePanelState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(!s.can_replay || s.has_trace) : @tutuca.Value)
}

///|
/// `invariant replaying_needs_a_replay` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_panel_compute_replaying_needs_a_replay(
  s : TracePanelState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(!s.replaying || s.can_replay) : @tutuca.Value)
}

///|
/// `invariant at_within_total` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_panel_compute_at_within_total(
  s : TracePanelState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(s.at.to_double() <= s.total.to_double()) : @tutuca.Value)
}

///|
/// `invariant show_trace_needs_a_trace` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_panel_compute_show_trace_needs_a_trace(
  s : TracePanelState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(!s.show_trace || s.has_trace) : @tutuca.Value)
}

///|
/// `invariant show_replay_needs_a_trace` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn trace_panel_compute_show_replay_needs_a_trace(
  s : TracePanelState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(!s.show_replay || s.has_trace) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn trace_panel_inline_compute() -> Map[
  String,
  (TracePanelState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  {
    "has_notice_tracks_notice": trace_panel_compute_has_notice_tracks_notice,
    "can_replay_needs_a_trace": trace_panel_compute_can_replay_needs_a_trace,
    "replaying_needs_a_replay": trace_panel_compute_replaying_needs_a_replay,
    "at_within_total": trace_panel_compute_at_within_total,
    "show_trace_needs_a_trace": trace_panel_compute_show_trace_needs_a_trace,
    "show_replay_needs_a_trace": trace_panel_compute_show_replay_needs_a_trace,
  }
}

///|
/// The TracePanel component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `TracePanelState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn trace_panel_component(
  name? : String = "TracePanel",
  views? : Map[String, @anode.View] = trace_panel_views(),
  initial? : TracePanelState = TracePanelState::zero(),
  common_style? : String = trace_panel_common_style,
  global_style? : String = trace_panel_global_style,
  update? : (TracePanelState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    TracePanelState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update=(s, msg, ctx) => {
      match trace_panel_inline_update(s, msg, ctx) {
        Unhandled =>
          match update {
            Some(u) => u(s, msg, ctx)
            None => Unhandled
          }
        answered => answered
      }
    },
    compute=trace_panel_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=trace_row_spec_source,
  )
}
