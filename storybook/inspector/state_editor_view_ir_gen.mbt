// GENERATED by `tutuca gen` from state_editor.html — DO NOT EDIT.
//
// The views of StateEditorRow, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing state_editor_row_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let state_editor_row_spec_source : String =
  #|  state StateEditorRow {
  #|    name : String
  #|    type : String
  #|    kind : String
  #|    text : String
  #|    num : Double
  #|    flag : Bool
  #|    readonly : String
  #|    editable : Bool
  #|
  #|    where kind is one of ["text", "num", "bool", "readonly"]
  #|
  #|    where name is nonempty
  #|    where type is nonempty
  #|
  #|    invariant editableTracksKind
  #|      format $'a {.kind} row says editable is {.editable}' {
  #|        .editable is (.kind is not 'readonly')
  #|      }
  #|  }
  #|
  #|  state StateEditor {
  #|    target : Instance
  #|    rows : Array[StateEditorRow]
  #|    name : String
  #|  }

///|
/// The compiled `main` view.
fn state_editor_row_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "label",
      [
        @anode.attr(
          "class", "flex items-center gap-2 leading-tight state-editor-row",
        ),
      ],
      [
        @anode.h(
          "span",
          [
            @anode.attr(
              "class", "font-mono text-xs w-32 shrink-0 text-right opacity-70 row-name",
            ),
          ],
          [@anode.dyn_text(@tutuca.Expr::field("name"))],
        ),
        @anode.text(" "),
        @anode.h(
          "span",
          [
            @anode.attr(
              "class", "font-mono text-[0.65rem] w-24 shrink-0 opacity-40 row-type",
            ),
          ],
          [@anode.dyn_text(@tutuca.Expr::field("type"))],
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("is", [
            @tutuca.Expr::field("kind"),
            @anode.const_str("bool"),
          ]),
          @anode.h(
            "input",
            [
              @anode.attr("type", "checkbox"),
              @anode.attr("class", "checkbox checkbox-xs row-bool"),
              @anode.dyn_attr("checked", @tutuca.Expr::field("flag")),
              @anode.eid(0),
            ],
            [],
          ),
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("is", [
            @tutuca.Expr::field("kind"),
            @anode.const_str("num"),
          ]),
          @anode.h(
            "input",
            [
              @anode.attr("type", "number"),
              @anode.attr("class", "input input-xs input-bordered w-40 row-num"),
              @anode.dyn_attr("value", @tutuca.Expr::field("num")),
              @anode.eid(1),
            ],
            [],
          ),
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("is", [
            @tutuca.Expr::field("kind"),
            @anode.const_str("text"),
          ]),
          @anode.h(
            "input",
            [
              @anode.attr("type", "text"),
              @anode.attr(
                "class", "input input-xs input-bordered w-64 row-text",
              ),
              @anode.dyn_attr("value", @tutuca.Expr::field("text")),
              @anode.eid(2),
            ],
            [],
          ),
        ),
        @anode.text(" "),
        @anode.hide(
          @tutuca.Expr::field("editable"),
          @anode.h(
            "span",
            [@anode.attr("class", "font-mono text-xs opacity-50 row-readonly")],
            [@anode.dyn_text(@tutuca.Expr::field("readonly"))],
          ),
        ),
      ],
    ),
    [
      [
        @anode.on("input", @tutuca.Expr::name("edit"), args=[
          @tutuca.Expr::event_path(["value"]),
        ]),
      ],
      [
        @anode.on("input", @tutuca.Expr::name("edit"), args=[
          @tutuca.Expr::event_path(["valueAsFloat"]),
        ]),
      ],
      [
        @anode.on("input", @tutuca.Expr::name("edit"), args=[
          @tutuca.Expr::event_path(["value"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn state_editor_row_views() -> Map[String, @anode.View] {
  { "main": state_editor_row_main_view_ir() }
}

///|
/// `invariant editableTracksKind` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn state_editor_row_compute_editableTracksKind(
  s : StateEditorRowState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      (Bool(s.editable) : @tutuca.Value) ==
      (
        Bool(
          !((Str(s.kind) : @tutuca.Value) == (Str("readonly") : @tutuca.Value)),
        ) : @tutuca.Value),
    ) : @tutuca.Value)
}

///|
/// The `compute` names the `<script type="tutuca/script">` block answers.
/// GENERATED — edit the block, not this.
fn state_editor_row_inline_compute() -> Map[
  String,
  (StateEditorRowState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  { "editableTracksKind": state_editor_row_compute_editableTracksKind }
}

///|
/// The StateEditorRow component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `StateEditorRowState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn state_editor_row_component(
  name? : String = "StateEditorRow",
  views? : Map[String, @anode.View] = state_editor_row_views(),
  initial? : StateEditorRowState = StateEditorRowState::zero(),
  common_style? : String = state_editor_row_common_style,
  global_style? : String = state_editor_row_global_style,
  update? : (StateEditorRowState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    StateEditorRowState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute=state_editor_row_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=state_editor_row_spec_source,
  )
}

///|
/// The compiled `main` view.
fn state_editor_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "flex flex-col gap-1 state-editor")],
      [
        @anode.h(
          "div",
          [@anode.attr("class", "font-mono text-sm font-semibold editor-name")],
          [@anode.dyn_text(@tutuca.Expr::field("name"))],
        ),
        @anode.text(" "),
        @anode.each(
          1,
          @tutuca.Expr::field("rows"),
          @anode.render_it(0),
          from_render_each=true,
        ),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn state_editor_views() -> Map[String, @anode.View] {
  { "main": state_editor_main_view_ir() }
}

///|
/// The StateEditor component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `StateEditorState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn state_editor_component(
  name? : String = "StateEditor",
  views? : Map[String, @anode.View] = state_editor_views(),
  initial? : StateEditorState = StateEditorState::zero(),
  common_style? : String = state_editor_common_style,
  global_style? : String = state_editor_global_style,
  update? : (StateEditorState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    StateEditorState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=state_editor_row_spec_source,
  )
}
