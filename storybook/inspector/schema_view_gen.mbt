// GENERATED by `tutuca gen-views` from schema.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views schema.html --name Schema --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the SchemaProperty component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SchemaPropertyState {
  key : String
  child : @tutuca.Value
  required : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SchemaPropertyState::zero(), label: "Counter" }
pub fn SchemaPropertyState::zero() -> SchemaPropertyState {
  { key: "", child: Null, required: false, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SchemaPropertyState with fn encode(s) {
  { "key": Str(s.key), "child": s.child, "required": Bool(s.required) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SchemaPropertyState with fn decode(f) {
  guard f.get("key") is Some(Str(v_key)) else { return None }
  let v_child = f.get("child").unwrap_or(Null)
  guard f.get("required") is Some(Bool(v_required)) else { return None }
  Some({ key: v_key, child: v_child, required: v_required, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let schema_property_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let schema_property_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SchemaPropertyState with fn schema() {
  @component.SchemaInfo::new(
    name="SchemaProperty",
    fingerprint="9b8d046632f59f15",
    fields=[
      @component.FieldInfo::new("key", TyText),
      @component.FieldInfo::new("child", TyAny),
      @component.FieldInfo::new("required", TyBool),
    ],
    view_names=["main"],
  )
}

///|
/// The state of the SchemaBranch component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SchemaBranchState {
  label : String
  child : @tutuca.Value
  labelClass : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SchemaBranchState::zero(), label: "Counter" }
pub fn SchemaBranchState::zero() -> SchemaBranchState {
  { label: "", child: Null, labelClass: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SchemaBranchState with fn encode(s) {
  { "label": Str(s.label), "child": s.child, "labelClass": Str(s.labelClass) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SchemaBranchState with fn decode(f) {
  guard f.get("label") is Some(Str(v_label)) else { return None }
  let v_child = f.get("child").unwrap_or(Null)
  guard f.get("labelClass") is Some(Str(v_labelClass)) else { return None }
  Some({ label: v_label, child: v_child, labelClass: v_labelClass, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let schema_branch_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let schema_branch_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum SchemaBranchMethod {
  LabelCssClass
} derive(Eq, @debug.Debug)

///|
pub fn SchemaBranchMethod::name(self : SchemaBranchMethod) -> String {
  match self {
    LabelCssClass => "labelCssClass"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SchemaBranchState with fn schema() {
  @component.SchemaInfo::new(
    name="SchemaBranch",
    fingerprint="45bbfcec4d3d900d",
    fields=[
      @component.FieldInfo::new("label", TyText),
      @component.FieldInfo::new("child", TyAny),
      @component.FieldInfo::new("labelClass", TyText),
    ],
    methods=["labelCssClass"],
    view_names=["main", "_margauiClasses"],
  )
}
