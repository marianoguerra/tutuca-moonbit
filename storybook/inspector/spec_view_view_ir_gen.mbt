// GENERATED by `tutuca gen` from spec_view.tutu — DO NOT EDIT.
//
// The views of SpecField, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing spec_field_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `spec:` section, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let spec_field_spec_source : String =
  #|spec:
  #|  SpecField:
  #|    field name :: String
  #|
  #|    field type_name :: String
  #|
  #|    field attrs :: String
  #|    field doc :: String
  #|
  #|  SpecMsg:
  #|    field name :: String
  #|
  #|    field payload :: String
  #|    field doc :: String
  #|
  #|  SpecRule:
  #|    field kind :: String
  #|    field kind_class :: String
  #|    field name :: String
  #|
  #|    field source :: String
  #|
  #|    field format :: String
  #|    field doc :: String
  #|
  #|    where it.kind is_one_of ["pred", "invariant"]
  #|
  #|    where it.name non_empty
  #|
  #|    where it.source non_empty
  #|
  #|  SpecDoc:
  #|    field text :: String
  #|
  #|  SpecViewer:
  #|    field comp_name :: String
  #|    field sections :: List.of(Instance)
  #|    field has_spec :: Bool
  #|
  #|    field hint :: String
  #|
  #|    field note :: String
  #|
  #|    invariant hint_explains_the_empty_view:
  #|      ~format: "there is no declaration to show, and the view does not say why"
  #|      (!it.has_spec implies (it.hint.length() > 0))
  #|
  #|    intent toggle_all_sections(Bool)
  #|
  #|  SpecSource:
  #|    field source :: String
  #|    field has_source :: Bool
  #|    field hint :: String
  #|    field note :: String
  #|
  #|    invariant has_source_tracks_source:
  #|      ~format: "hasSource and the source disagree"
  #|      (it.has_source == (it.source.length() > 0))
  #|
  #|    invariant hint_explains_the_empty_source:
  #|      ~format: "there is no block to show, and the view does not say why"
  #|      (!it.has_source implies (it.hint.length() > 0))

///|
/// The compiled `main` view.
fn spec_field_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "flex flex-col gap-0.5 leading-tight spec-field")],
      [
        @anode.h(
          "div",
          [@anode.attr("class", "flex items-center gap-2 flex-wrap")],
          [
            @anode.h(
              "span",
              [
                @anode.attr(
                  "class", "text-base-content/70 font-mono text-sm spec-field-name",
                ),
              ],
              [@anode.dyn_text(@tutuca.Expr::field("name"), id=0)],
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("type_name")]),
              @anode.h("span", [@anode.attr("class", "text-base-content/30")], [
                @anode.text(":"),
              ]),
            ),
            @anode.text(" "),
            @anode.h(
              "span",
              [
                @anode.attr(
                  "class", "text-base-content/50 font-mono text-xs spec-field-type",
                ),
              ],
              [@anode.dyn_text(@tutuca.Expr::field("type_name"), id=1)],
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("attrs")]),
              @anode.h(
                "span",
                [
                  @anode.attr(
                    "class", "badge badge-xs badge-ghost font-mono spec-field-attrs",
                  ),
                ],
                [@anode.dyn_text(@tutuca.Expr::field("attrs"), id=2)],
              ),
            ),
          ],
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("doc")]),
          @anode.h(
            "div",
            [@anode.attr("class", "text-xs opacity-50 pl-2 spec-doc")],
            [@anode.dyn_text(@tutuca.Expr::field("doc"), id=3)],
          ),
        ),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn spec_field_views() -> Map[String, @anode.View] {
  { "main": spec_field_main_view_ir() }
}

///|
/// The SpecField component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SpecFieldState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn spec_field_component(
  name? : String = "SpecField",
  views? : Map[String, @anode.View] = spec_field_views(),
  initial? : SpecFieldState = SpecFieldState::zero(),
  common_style? : String = spec_field_common_style,
  global_style? : String = spec_field_global_style,
  update? : (SpecFieldState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SpecFieldState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=spec_field_spec_source,
  )
}

///|
/// The compiled `main` view.
fn spec_msg_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "flex flex-col gap-0.5 leading-tight spec-msg")],
      [
        @anode.h(
          "div",
          [@anode.attr("class", "flex items-center gap-2 flex-wrap")],
          [
            @anode.h(
              "span",
              [@anode.attr("class", "font-mono text-sm spec-msg-name")],
              [@anode.dyn_text(@tutuca.Expr::field("name"), id=0)],
            ),
            @anode.text(" "),
            @anode.show(
              @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("payload")]),
              @anode.h(
                "span",
                [
                  @anode.attr(
                    "class", "text-base-content/50 font-mono text-xs spec-msg-payload",
                  ),
                ],
                [@anode.dyn_text(@tutuca.Expr::field("payload"), id=1)],
              ),
            ),
          ],
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("doc")]),
          @anode.h(
            "div",
            [@anode.attr("class", "text-xs opacity-50 pl-2 spec-doc")],
            [@anode.dyn_text(@tutuca.Expr::field("doc"), id=2)],
          ),
        ),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn spec_msg_views() -> Map[String, @anode.View] {
  { "main": spec_msg_main_view_ir() }
}

///|
/// The SpecMsg component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SpecMsgState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn spec_msg_component(
  name? : String = "SpecMsg",
  views? : Map[String, @anode.View] = spec_msg_views(),
  initial? : SpecMsgState = SpecMsgState::zero(),
  common_style? : String = spec_msg_common_style,
  global_style? : String = spec_msg_global_style,
  update? : (SpecMsgState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SpecMsgState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=spec_field_spec_source,
  )
}

///|
/// The compiled `main` view.
fn spec_rule_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "flex flex-col gap-0.5 leading-tight spec-rule")],
      [
        @anode.h(
          "div",
          [@anode.attr("class", "flex items-center gap-2 flex-wrap")],
          [
            @anode.h(
              "span",
              [@anode.dyn_attr("class", @tutuca.Expr::field("kind_class"))],
              [@anode.dyn_text(@tutuca.Expr::field("kind"), id=0)],
            ),
            @anode.text(" "),
            @anode.h(
              "span",
              [
                @anode.attr(
                  "class", "font-mono text-sm font-semibold spec-rule-name",
                ),
              ],
              [@anode.dyn_text(@tutuca.Expr::field("name"), id=1)],
            ),
          ],
        ),
        @anode.text(" "),
        @anode.h(
          "code",
          [
            @anode.attr(
              "class", "text-xs font-mono opacity-70 pl-2 break-all spec-rule-source",
            ),
          ],
          [@anode.dyn_text(@tutuca.Expr::field("source"), id=2)],
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("format")]),
          @anode.h(
            "div",
            [@anode.attr("class", "text-xs opacity-50 pl-2 spec-rule-format")],
            [@anode.dyn_text(@tutuca.Expr::field("format"), id=3)],
          ),
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("doc")]),
          @anode.h(
            "div",
            [@anode.attr("class", "text-xs opacity-50 pl-2 spec-doc")],
            [@anode.dyn_text(@tutuca.Expr::field("doc"), id=4)],
          ),
        ),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn spec_rule_views() -> Map[String, @anode.View] {
  { "main": spec_rule_main_view_ir() }
}

///|
/// The SpecRule component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SpecRuleState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn spec_rule_component(
  name? : String = "SpecRule",
  views? : Map[String, @anode.View] = spec_rule_views(),
  initial? : SpecRuleState = SpecRuleState::zero(),
  common_style? : String = spec_rule_common_style,
  global_style? : String = spec_rule_global_style,
  update? : (SpecRuleState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SpecRuleState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=spec_field_spec_source,
  )
}

///|
/// The compiled `main` view.
fn spec_doc_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "text-xs opacity-60 italic whitespace-pre-wrap spec-doc-block",
        ),
      ],
      [@anode.dyn_text(@tutuca.Expr::field("text"), id=0)],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn spec_doc_views() -> Map[String, @anode.View] {
  { "main": spec_doc_main_view_ir() }
}

///|
/// The SpecDoc component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SpecDocState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn spec_doc_component(
  name? : String = "SpecDoc",
  views? : Map[String, @anode.View] = spec_doc_views(),
  initial? : SpecDocState = SpecDocState::zero(),
  common_style? : String = spec_doc_common_style,
  global_style? : String = spec_doc_global_style,
  update? : (SpecDocState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SpecDocState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=spec_field_spec_source,
  )
}

///|
/// The compiled `main` view.
fn spec_viewer_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "font-mono text-sm leading-tight flex flex-col gap-1 spec-viewer",
        ),
      ],
      [
        @anode.hide(
          @tutuca.Expr::field("has_spec"),
          @anode.h(
            "div",
            [@anode.attr("class", "text-xs opacity-50 spec-hint")],
            [@anode.dyn_text(@tutuca.Expr::field("hint"), id=0)],
          ),
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("note")]),
          @anode.h(
            "div",
            [@anode.attr("class", "text-xs opacity-40 italic spec-note")],
            [@anode.dyn_text(@tutuca.Expr::field("note"), id=1)],
          ),
        ),
        @anode.text(" "),
        @anode.each(
          3,
          @tutuca.Expr::field("sections"),
          @anode.render_it(2),
          from_render_each=true,
        ),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn spec_viewer_views() -> Map[String, @anode.View] {
  { "main": spec_viewer_main_view_ir() }
}

///|
/// `invariant hint_explains_the_empty_view` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn spec_viewer_compute_hint_explains_the_empty_view(
  s : SpecViewerState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(!(!s.has_spec) || s.hint.length().to_double() > 0.0) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn spec_viewer_inline_compute() -> Map[
  String,
  (SpecViewerState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  {
    "hint_explains_the_empty_view": spec_viewer_compute_hint_explains_the_empty_view,
  }
}

///|
/// The SpecViewer component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SpecViewerState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn spec_viewer_component(
  name? : String = "SpecViewer",
  views? : Map[String, @anode.View] = spec_viewer_views(),
  initial? : SpecViewerState = SpecViewerState::zero(),
  common_style? : String = spec_viewer_common_style,
  global_style? : String = spec_viewer_global_style,
  update? : (SpecViewerState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SpecViewerState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute=spec_viewer_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=spec_field_spec_source,
  )
}

///|
/// The compiled `main` view.
fn spec_source_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-1 spec-source")], [
      @anode.hide(
        @tutuca.Expr::field("has_source"),
        @anode.h(
          "div",
          [@anode.attr("class", "text-xs opacity-50 spec-source-hint")],
          [@anode.dyn_text(@tutuca.Expr::field("hint"), id=0)],
        ),
      ),
      @anode.text(" "),
      @anode.show(
        @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("note")]),
        @anode.h(
          "div",
          [@anode.attr("class", "text-xs opacity-40 italic spec-note")],
          [@anode.dyn_text(@tutuca.Expr::field("note"), id=1)],
        ),
      ),
      @anode.text(" "),
      @anode.show(
        @tutuca.Expr::field("has_source"),
        @anode.h(
          "pre",
          [
            @anode.attr(
              "class", "text-xs bg-base-200 rounded p-2 overflow-x-auto whitespace-pre-wrap",
            ),
          ],
          [
            @anode.h("code", [@anode.attr("class", "spec-source-text")], [
              @anode.dyn_text(@tutuca.Expr::field("source"), id=2),
            ]),
          ],
        ),
      ),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn spec_source_views() -> Map[String, @anode.View] {
  { "main": spec_source_main_view_ir() }
}

///|
/// `invariant has_source_tracks_source` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn spec_source_compute_has_source_tracks_source(
  s : SpecSourceState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      (Bool(s.has_source) : @tutuca.Value) ==
      (Bool(s.source.length().to_double() > 0.0) : @tutuca.Value),
    ) : @tutuca.Value)
}

///|
/// `invariant hint_explains_the_empty_source` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn spec_source_compute_hint_explains_the_empty_source(
  s : SpecSourceState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(!(!s.has_source) || s.hint.length().to_double() > 0.0) : @tutuca.Value)
}

///|
/// The `compute` names the file's `logic:` section answers.
/// GENERATED — edit the block, not this.
fn spec_source_inline_compute() -> Map[
  String,
  (SpecSourceState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  {
    "has_source_tracks_source": spec_source_compute_has_source_tracks_source,
    "hint_explains_the_empty_source": spec_source_compute_hint_explains_the_empty_source,
  }
}

///|
/// The SpecSource component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SpecSourceState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn spec_source_component(
  name? : String = "SpecSource",
  views? : Map[String, @anode.View] = spec_source_views(),
  initial? : SpecSourceState = SpecSourceState::zero(),
  common_style? : String = spec_source_common_style,
  global_style? : String = spec_source_global_style,
  update? : (SpecSourceState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SpecSourceState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute=spec_source_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=spec_field_spec_source,
  )
}
