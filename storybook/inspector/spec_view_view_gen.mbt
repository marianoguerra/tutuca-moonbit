// GENERATED by `tutuca gen` from spec_view.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen spec_view.html --name SpecView --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The declaration a component was built from, as something to READ.
/// 
/// Every row here is a projection of a `@statedef.StateDef` — the block
/// re-parsed out of `Component::spec` — and none of it is recomputed from
/// the schema beside it. The schema is the block's lossy projection: a rule
/// reaches it as a bare name and a message as a name with no payload, and
/// those two are exactly what a reader of a declaration came for.
/// One declared field: `name : type`, with the doc its author wrote.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SpecFieldState {
  name : String
  /// The author's own WIT spelling — `list<card>`, not `list`.
  typeName : String
  /// `@attr` / `@attr=value` on the field, on one line. Empty when it has
  /// none, which is the common case.
  attrs : String
  doc : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..SpecFieldState::zero(), label: "Counter" }
pub fn SpecFieldState::zero() -> SpecFieldState {
  { name: "", typeName: "", attrs: "", doc: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SpecFieldState with fn encode(s) {
  {
    "name": Str(s.name),
    "typeName": Str(s.typeName),
    "attrs": Str(s.attrs),
    "doc": Str(s.doc),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SpecFieldState with fn decode(f) {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("typeName") is Some(Str(v_typeName)) else { return None }
  guard f.get("attrs") is Some(Str(v_attrs)) else { return None }
  guard f.get("doc") is Some(Str(v_doc)) else { return None }
  Some({ name: v_name, typeName: v_typeName, attrs: v_attrs, doc: v_doc, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let spec_field_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let spec_field_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SpecFieldState with fn schema() {
  @component.SchemaInfo::new(
    name="SpecField",
    fingerprint="f92c4757b9557d96",
    fields=[
      @component.FieldInfo::new("name", TyText),
      @component.FieldInfo::new("typeName", TyText),
      @component.FieldInfo::new("attrs", TyText),
      @component.FieldInfo::new("doc", TyText),
    ],
    view_names=["main"],
  )
}

///|
/// One declared message: `name(Int, String)`.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SpecMsgState {
  /// The name a dispatch is addressed to.
  name : String
  /// Its payload types in parens, or empty for a message that carries none.
  payload : String
  doc : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..SpecMsgState::zero(), label: "Counter" }
pub fn SpecMsgState::zero() -> SpecMsgState {
  { name: "", payload: "", doc: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SpecMsgState with fn encode(s) {
  { "name": Str(s.name), "payload": Str(s.payload), "doc": Str(s.doc) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SpecMsgState with fn decode(f) {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("payload") is Some(Str(v_payload)) else { return None }
  guard f.get("doc") is Some(Str(v_doc)) else { return None }
  Some({ name: v_name, payload: v_payload, doc: v_doc, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let spec_msg_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let spec_msg_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SpecMsgState with fn schema() {
  @component.SchemaInfo::new(
    name="SpecMsg",
    fingerprint="22e4964563ea52b8",
    fields=[
      @component.FieldInfo::new("name", TyText),
      @component.FieldInfo::new("payload", TyText),
      @component.FieldInfo::new("doc", TyText),
    ],
    view_names=["main"],
  )
}

///|
/// One `pred` or `invariant`, with the expression as written.
/// 
/// The expression is carried rather than summarised because it is the whole
/// oracle: the Fuzz tab holds the component to exactly these words, so a
/// reader deciding whether a green run means anything has to see them.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SpecRuleState {
  /// `pred` or `invariant`.
  kind : String
  kindClass : String
  name : String
  /// The rule expression, as written.
  source : String
  /// The sentence its author wrote for the moment it fails.
  format : String
  doc : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..SpecRuleState::zero(), label: "Counter" }
pub fn SpecRuleState::zero() -> SpecRuleState {
  { kind: "", kindClass: "", name: "", source: "", format: "", doc: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SpecRuleState with fn encode(s) {
  {
    "kind": Str(s.kind),
    "kindClass": Str(s.kindClass),
    "name": Str(s.name),
    "source": Str(s.source),
    "format": Str(s.format),
    "doc": Str(s.doc),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SpecRuleState with fn decode(f) {
  guard f.get("kind") is Some(Str(v_kind)) else { return None }
  guard f.get("kindClass") is Some(Str(v_kindClass)) else { return None }
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("source") is Some(Str(v_source)) else { return None }
  guard f.get("format") is Some(Str(v_format)) else { return None }
  guard f.get("doc") is Some(Str(v_doc)) else { return None }
  Some({
    kind: v_kind,
    kindClass: v_kindClass,
    name: v_name,
    source: v_source,
    format: v_format,
    doc: v_doc,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let spec_rule_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let spec_rule_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SpecRuleState with fn schema() {
  @component.SchemaInfo::new(
    name="SpecRule",
    fingerprint="b48a7422685f8141",
    fields=[
      @component.FieldInfo::new("kind", TyText),
      @component.FieldInfo::new("kindClass", TyText),
      @component.FieldInfo::new("name", TyText),
      @component.FieldInfo::new("source", TyText),
      @component.FieldInfo::new("format", TyText),
      @component.FieldInfo::new("doc", TyText),
    ],
    domains=[
      { field: "kind", domain: DOneOf([Str("pred"), Str("invariant")]), },
      { field: "name", domain: DNonEmpty, },
      { field: "source", domain: DNonEmpty, },
    ],
    view_names=["main"],
  )
}

///|
/// A doc comment standing on its own — a component's or a named type's.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SpecDocState {
  text : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..SpecDocState::zero(), label: "Counter" }
pub fn SpecDocState::zero() -> SpecDocState {
  { text: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SpecDocState with fn encode(s) {
  { "text": Str(s.text) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SpecDocState with fn decode(f) {
  guard f.get("text") is Some(Str(v_text)) else { return None }
  Some({ text: v_text, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let spec_doc_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let spec_doc_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SpecDocState with fn schema() {
  @component.SchemaInfo::new(
    name="SpecDoc",
    fingerprint="72b98501ca63c2ed",
    fields=[@component.FieldInfo::new("text", TyText)],
    view_names=["main"],
  )
}

///|
/// The whole declaration, one collapsible section per component the block
/// declares. A block is FILE-level, so a file declaring three components
/// carries all three arms in every one of its wrappers; the story's own
/// component is the section that starts open.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SpecViewerState {
  compName : String
  sections : Array[@tutuca.Value]
  hasSpec : Bool
  /// Why there is nothing to show, when there is nothing to show.
  hint : String
  /// A caveat about the block itself — that `gen` shipped it without
  /// its prose, say. Empty for a block that reached the runtime whole.
  note : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..SpecViewerState::zero(), label: "Counter" }
pub fn SpecViewerState::zero() -> SpecViewerState {
  { compName: "", sections: [], hasSpec: false, hint: "", note: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SpecViewerState with fn encode(s) {
  {
    "compName": Str(s.compName),
    "sections": (List(s.sections.map(x => x)) : @tutuca.Value),
    "hasSpec": Bool(s.hasSpec),
    "hint": Str(s.hint),
    "note": Str(s.note),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SpecViewerState with fn decode(f) {
  guard f.get("compName") is Some(Str(v_compName)) else { return None }
  guard f.get("sections") is Some(t1_sections) else { return None }
  guard t1_sections is List(u1_sections) else { return None }
  let v_sections : Array[@tutuca.Value] = []
  for e1_sections in u1_sections {
    let x1_sections = e1_sections
    v_sections.push(x1_sections)
  }
  guard f.get("hasSpec") is Some(Bool(v_hasSpec)) else { return None }
  guard f.get("hint") is Some(Str(v_hint)) else { return None }
  guard f.get("note") is Some(Str(v_note)) else { return None }
  Some({
    compName: v_compName,
    sections: v_sections,
    hasSpec: v_hasSpec,
    hint: v_hint,
    note: v_note,
  })
}

///|
/// The Intent arm of @component.Dispatch, decoded.
///
/// Declared as `variant intent` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum SpecViewerIntent {
  ToggleAllSections(Bool)
  /// A intent the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Intent dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn SpecViewerIntent::from_dispatch(
  d : @component.Dispatch,
) -> SpecViewerIntent? {
  guard d is Intent(name, args) else { return None }
  Some(
    match (name, args) {
      ("toggleAllSections", [Bool(a0)]) => ToggleAllSections(a0)
      _ => {
        match name {
          "toggleAllSections" => @tutuca.bad_payload(name, "Bool", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn SpecViewerIntent::to_dispatch(
  self : SpecViewerIntent,
) -> (String, Array[@tutuca.Value]) {
  match self {
    ToggleAllSections(a0) => ("toggleAllSections", [Bool(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Dispatch this intent along `opts.route` — the default `dyn lex` when
/// nothing says otherwise. The `dyn` leg starts at this component's
/// PARENT, so a component never re-enters its own handler.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn SpecViewerIntent::dispatch(
  self : SpecViewerIntent,
  ctx : &@tutuca.Ctx,
  opts? : @tutuca.IntentOpts = @tutuca.IntentOpts::new(),
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.intent(name, args, opts)
}

///|
/// The file-level style — pass as `common_style~`.
pub let spec_viewer_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let spec_viewer_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SpecViewerState with fn schema() {
  @component.SchemaInfo::new(
    name="SpecViewer",
    fingerprint="c567153c22cae97d",
    fields=[
      @component.FieldInfo::new("compName", TyText),
      @component.FieldInfo::new("sections", TyList(TyComp(None))),
      @component.FieldInfo::new("hasSpec", TyBool),
      @component.FieldInfo::new("hint", TyText),
      @component.FieldInfo::new("note", TyText),
    ],
    intents=["toggleAllSections"],
    methods=["hintExplainsTheEmptyView"],
    invariants=["hintExplainsTheEmptyView"],
    view_names=["main"],
  )
}

///|
/// The same block, verbatim.
/// 
/// Beside the parsed view rather than instead of it: the parse is what the
/// runtime reads, and the source is what the author edits. A reader
/// reconciling a surprising Fuzz verdict with the file open in their editor
/// wants the second one.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SpecSourceState {
  source : String
  hasSource : Bool
  hint : String
  note : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..SpecSourceState::zero(), label: "Counter" }
pub fn SpecSourceState::zero() -> SpecSourceState {
  { source: "", hasSource: false, hint: "", note: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SpecSourceState with fn encode(s) {
  {
    "source": Str(s.source),
    "hasSource": Bool(s.hasSource),
    "hint": Str(s.hint),
    "note": Str(s.note),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SpecSourceState with fn decode(f) {
  guard f.get("source") is Some(Str(v_source)) else { return None }
  guard f.get("hasSource") is Some(Bool(v_hasSource)) else { return None }
  guard f.get("hint") is Some(Str(v_hint)) else { return None }
  guard f.get("note") is Some(Str(v_note)) else { return None }
  Some({ source: v_source, hasSource: v_hasSource, hint: v_hint, note: v_note, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let spec_source_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let spec_source_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SpecSourceState with fn schema() {
  @component.SchemaInfo::new(
    name="SpecSource",
    fingerprint="942cfaa112c9d1a7",
    fields=[
      @component.FieldInfo::new("source", TyText),
      @component.FieldInfo::new("hasSource", TyBool),
      @component.FieldInfo::new("hint", TyText),
      @component.FieldInfo::new("note", TyText),
    ],
    methods=["hasSourceTracksSource", "hintExplainsTheEmptySource"],
    invariants=["hasSourceTracksSource", "hintExplainsTheEmptySource"],
    view_names=["main"],
  )
}
