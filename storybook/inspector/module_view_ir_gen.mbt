// GENERATED by `tutuca gen-views` from module.html — DO NOT EDIT.
//
// The views of RichSample, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing rich_sample_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let rich_sample_spec_source : String =
  #|  state RichSample {
  #|    title : String
  #|    count : Int
  #|    tags : Array[String]
  #|    open : Bool
  #|
  #|    where count >= 0
  #|  }
  #|
  #|  handle RichSample {
  #|    message {
  #|    ping
  #|    onData
  #|
  #|    }
  #|  }
  #|
  #|  handle RichSample {
  #|    intent {
  #|    onChild
  #|
  #|    }
  #|  }
  #|
  #|  state SampleCard {
  #|    title : String
  #|    count : Int
  #|    tags : Array[String]
  #|    open : Bool
  #|
  #|    where count >= 0
  #|  }

///|
/// The compiled `main` view.
fn rich_sample_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.eid(0)], [
      @anode.h("span", [], [@anode.dyn_text(@tutuca.Expr::field("title"))]),
      @anode.text(" "),
      @anode.each(
        1,
        @tutuca.Expr::field("tags"),
        @anode.h("span", [], [
          @anode.dyn_text(@tutuca.Expr::bind("value"), id=0),
        ]),
        loop_with_val=@tutuca.Expr::name("rows"),
      ),
    ]),
    [[@anode.on("click", @tutuca.Expr::name("inc"))]],
  )
}

///|
/// The compiled `compact` view.
fn rich_sample_compact_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "compact",
    @anode.h("span", [@anode.eid(0)], [
      @anode.h("i", [], [@anode.dyn_text(@tutuca.Expr::field("title"))]),
      @anode.h("i", [], [@anode.dyn_text(@tutuca.Expr::method_("label"))]),
      @anode.h("i", [], [@anode.dyn_text(@tutuca.Expr::method_("double"))]),
    ]),
    [[@anode.on("click", @tutuca.Expr::name("reset"))]],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn rich_sample_views() -> Map[String, @anode.View] {
  {
    "main": rich_sample_main_view_ir(),
    "compact": rich_sample_compact_view_ir(),
  }
}

///|
/// The RichSample component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `RichSampleState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn rich_sample_component(
  name? : String = "RichSample",
  views? : Map[String, @anode.View] = rich_sample_views(),
  initial? : RichSampleState = RichSampleState::zero(),
  common_style? : String = rich_sample_common_style,
  global_style? : String = rich_sample_global_style,
  update? : (RichSampleState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    RichSampleState,
  ],
  compute? : (RichSampleCompute) -> ((
    RichSampleState,
    Array[@tutuca.Value],
    &@tutuca.Stack,
  ) -> @tutuca.Value)?,
  loop_with? : (RichSampleLoopWith) -> ((
    RichSampleState,
    @tutuca.Value,
    @component.LoopCtx,
  ) -> @component.LoopWith)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) =>
        @component.build_bucket(
          ([Double, Label] : Array[RichSampleCompute]),
          RichSampleCompute::name,
          f,
        )
      None => Map([])
    },
    loop_with=match loop_with {
      Some(f) =>
        @component.build_bucket(
          ([Rows] : Array[RichSampleLoopWith]),
          RichSampleLoopWith::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=rich_sample_spec_source,
  )
}

///|
/// The compiled `main` view.
fn sample_card_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "card"), @anode.eid(0)], [
      @anode.dyn_text(@tutuca.Expr::method_("label")),
    ]),
    [[@anode.on("click", @tutuca.Expr::name("bump"))]],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn sample_card_views() -> Map[String, @anode.View] {
  { "main": sample_card_main_view_ir() }
}

///|
/// The SampleCard component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SampleCardState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn sample_card_component(
  name? : String = "SampleCard",
  views? : Map[String, @anode.View] = sample_card_views(),
  initial? : SampleCardState = SampleCardState::zero(),
  common_style? : String = sample_card_common_style,
  global_style? : String = sample_card_global_style,
  update? : (SampleCardState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SampleCardState,
  ],
  compute? : (SampleCardCompute) -> ((
    SampleCardState,
    Array[@tutuca.Value],
    &@tutuca.Stack,
  ) -> @tutuca.Value)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) =>
        @component.build_bucket(
          ([Label] : Array[SampleCardCompute]),
          SampleCardCompute::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=rich_sample_spec_source,
  )
}
