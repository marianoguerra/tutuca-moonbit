// GENERATED by `tutuca gen-views` from personal_site.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views personal_site.html --name PersonalSite --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the AltUrl component.
///
/// Declared as `interface alt-url` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct AltUrlState {
  label : String
  url : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..AltUrlState::zero(), label: "Counter" }
pub fn AltUrlState::zero() -> AltUrlState {
  AltUrlState::{ label: "", url: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for AltUrlState with fn encode(s) {
  { "label": Str(s.label), "url": Str(s.url) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for AltUrlState with fn decode(f) {
  guard f.get("label") is Some(Str(v_label)) else { return None }
  guard f.get("url") is Some(Str(v_url)) else { return None }
  Some(AltUrlState::{ label: v_label, url: v_url })
}

///|
/// The file-level style — pass as `common_style~`.
pub let alt_url_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let alt_url_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for AltUrlState with fn schema() {
  @component.SchemaInfo::new(
    name="AltUrl",
    fingerprint="5894c82bae5b3921",
    fields=[
      @component.FieldInfo::new("label", TyText),
      @component.FieldInfo::new("url", TyText),
    ],
    view_names=["main"],
  )
}

///|
/// The state of the PsEntry component.
///
/// Declared as `interface ps-entry` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct PsEntryState {
  title : String
  description : String
  lang : String
  startYear : @tutuca.Value
  endYear : @tutuca.Value
  role : @tutuca.Value
  featured : Bool
  url : String
  altUrls : Array[@tutuca.Value]
  categories : Array[@tutuca.Value]
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..PsEntryState::zero(), label: "Counter" }
pub fn PsEntryState::zero() -> PsEntryState {
  PsEntryState::{
    title: "",
    description: "",
    lang: "",
    startYear: Null,
    endYear: Null,
    role: Null,
    featured: false,
    url: "",
    altUrls: [],
    categories: [],
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for PsEntryState with fn encode(s) {
  {
    "title": Str(s.title),
    "description": Str(s.description),
    "lang": Str(s.lang),
    "startYear": s.startYear,
    "endYear": s.endYear,
    "role": s.role,
    "featured": Bool(s.featured),
    "url": Str(s.url),
    "altUrls": (List(s.altUrls.map(x => x)) : @tutuca.Value),
    "categories": (List(s.categories.map(x => x)) : @tutuca.Value),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for PsEntryState with fn decode(f) {
  guard f.get("title") is Some(Str(v_title)) else { return None }
  guard f.get("description") is Some(Str(v_description)) else { return None }
  guard f.get("lang") is Some(Str(v_lang)) else { return None }
  let v_startYear = f.get("startYear").unwrap_or(Null)
  let v_endYear = f.get("endYear").unwrap_or(Null)
  let v_role = f.get("role").unwrap_or(Null)
  guard f.get("featured") is Some(Bool(v_featured)) else { return None }
  guard f.get("url") is Some(Str(v_url)) else { return None }
  guard f.get("altUrls") is Some(t1_altUrls) else { return None }
  guard t1_altUrls is List(u1_altUrls) else { return None }
  let v_altUrls : Array[@tutuca.Value] = []
  for e1_altUrls in u1_altUrls {
    let x1_altUrls = e1_altUrls
    v_altUrls.push(x1_altUrls)
  }
  guard f.get("categories") is Some(t1_categories) else { return None }
  guard t1_categories is List(u1_categories) else { return None }
  let v_categories : Array[@tutuca.Value] = []
  for e1_categories in u1_categories {
    let x1_categories = e1_categories
    v_categories.push(x1_categories)
  }
  Some(PsEntryState::{
    title: v_title,
    description: v_description,
    lang: v_lang,
    startYear: v_startYear,
    endYear: v_endYear,
    role: v_role,
    featured: v_featured,
    url: v_url,
    altUrls: v_altUrls,
    categories: v_categories,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let ps_entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let ps_entry_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum PsEntryMethod {
  IsSpanish
  LangFlag
  YearDisplay
} derive(Eq, @debug.Debug)

///|
pub fn PsEntryMethod::name(self : PsEntryMethod) -> String {
  match self {
    IsSpanish => "isSpanish"
    LangFlag => "langFlag"
    YearDisplay => "yearDisplay"
  }
}

///|
/// Every `@enrich-with` handler named INSIDE an `@each`.
pub(all) enum PsEntryEnrich {
  EnrichCategoryBadge
} derive(Eq, @debug.Debug)

///|
pub fn PsEntryEnrich::name(self : PsEntryEnrich) -> String {
  match self {
    EnrichCategoryBadge => "enrichCategoryBadge"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for PsEntryState with fn schema() {
  @component.SchemaInfo::new(
    name="PsEntry",
    fingerprint="dbbed9cebaa1412f",
    fields=[
      @component.FieldInfo::new("title", TyText),
      @component.FieldInfo::new("description", TyText),
      @component.FieldInfo::new("lang", TyText),
      @component.FieldInfo::new("startYear", TyAny),
      @component.FieldInfo::new("endYear", TyAny),
      @component.FieldInfo::new("role", TyAny),
      @component.FieldInfo::new("featured", TyBool),
      @component.FieldInfo::new("url", TyText),
      @component.FieldInfo::new("altUrls", TyList(TyAny)),
      @component.FieldInfo::new("categories", TyList(TyAny)),
    ],
    methods=["isSpanish", "langFlag", "yearDisplay"],
    view_names=["main"],
  )
}

///|
/// The state of the Root component.
///
/// Declared as `interface root` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct RootState {
  allEntries : Array[@tutuca.Value]
  allCategories : Array[String]
  allRoles : Array[String]
  selectedCategories : Map[String, Bool]
  selectedRoles : Map[String, Bool]
  featuredFirst : Bool
  sortByEnd : Bool
  sortAsc : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..RootState::zero(), label: "Counter" }
pub fn RootState::zero() -> RootState {
  RootState::{
    allEntries: [],
    allCategories: [],
    allRoles: [],
    selectedCategories: Map([]),
    selectedRoles: Map([]),
    featuredFirst: false,
    sortByEnd: false,
    sortAsc: false,
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for RootState with fn encode(s) {
  {
    "allEntries": (List(s.allEntries.map(x => x)) : @tutuca.Value),
    "allCategories": (List(s.allCategories.map(x => Str(x))) : @tutuca.Value),
    "allRoles": (List(s.allRoles.map(x => Str(x))) : @tutuca.Value),
    "selectedCategories": {
      let m : Map[String, @tutuca.Value] = Map([])
      for k, b in s.selectedCategories {
        m[k] = Bool(b)
      }
      (Map(m) : @tutuca.Value)
    },
    "selectedRoles": {
      let m : Map[String, @tutuca.Value] = Map([])
      for k, b in s.selectedRoles {
        m[k] = Bool(b)
      }
      (Map(m) : @tutuca.Value)
    },
    "featuredFirst": Bool(s.featuredFirst),
    "sortByEnd": Bool(s.sortByEnd),
    "sortAsc": Bool(s.sortAsc),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for RootState with fn decode(f) {
  guard f.get("allEntries") is Some(t1_allEntries) else { return None }
  guard t1_allEntries is List(u1_allEntries) else { return None }
  let v_allEntries : Array[@tutuca.Value] = []
  for e1_allEntries in u1_allEntries {
    let x1_allEntries = e1_allEntries
    v_allEntries.push(x1_allEntries)
  }
  guard f.get("allCategories") is Some(t1_allCategories) else { return None }
  guard t1_allCategories is List(u1_allCategories) else { return None }
  let v_allCategories : Array[String] = []
  for e1_allCategories in u1_allCategories {
    guard e1_allCategories is Str(x1_allCategories) else { return None }
    v_allCategories.push(x1_allCategories)
  }
  guard f.get("allRoles") is Some(t1_allRoles) else { return None }
  guard t1_allRoles is List(u1_allRoles) else { return None }
  let v_allRoles : Array[String] = []
  for e1_allRoles in u1_allRoles {
    guard e1_allRoles is Str(x1_allRoles) else { return None }
    v_allRoles.push(x1_allRoles)
  }
  guard f.get("selectedCategories") is Some(t1_selectedCategories) else {
    return None
  }
  guard t1_selectedCategories is Map(u1_selectedCategories) else { return None }
  let v_selectedCategories : Map[String, Bool] = Map([])
  for k, x in u1_selectedCategories {
    v_selectedCategories[k] = x is Bool(true)
  }
  guard f.get("selectedRoles") is Some(t1_selectedRoles) else { return None }
  guard t1_selectedRoles is Map(u1_selectedRoles) else { return None }
  let v_selectedRoles : Map[String, Bool] = Map([])
  for k, x in u1_selectedRoles {
    v_selectedRoles[k] = x is Bool(true)
  }
  guard f.get("featuredFirst") is Some(Bool(v_featuredFirst)) else {
    return None
  }
  guard f.get("sortByEnd") is Some(Bool(v_sortByEnd)) else { return None }
  guard f.get("sortAsc") is Some(Bool(v_sortAsc)) else { return None }
  Some(RootState::{
    allEntries: v_allEntries,
    allCategories: v_allCategories,
    allRoles: v_allRoles,
    selectedCategories: v_selectedCategories,
    selectedRoles: v_selectedRoles,
    featuredFirst: v_featuredFirst,
    sortByEnd: v_sortByEnd,
    sortAsc: v_sortAsc,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let root_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let root_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum RootInput {
  ClearFilters
  OnCategoryToggle
  OnRoleToggle
  OnToggleFeaturedFirst
  OnToggleSortAsc
  OnToggleSortByEnd
} derive(Eq, @debug.Debug)

///|
pub fn RootInput::name(self : RootInput) -> String {
  match self {
    ClearFilters => "clearFilters"
    OnCategoryToggle => "onCategoryToggle"
    OnRoleToggle => "onRoleToggle"
    OnToggleFeaturedFirst => "onToggleFeaturedFirst"
    OnToggleSortAsc => "onToggleSortAsc"
    OnToggleSortByEnd => "onToggleSortByEnd"
  }
}

///|
pub fn RootInput::parse(s : String) -> RootInput? {
  match s {
    "clearFilters" => Some(ClearFilters)
    "onCategoryToggle" => Some(OnCategoryToggle)
    "onRoleToggle" => Some(OnRoleToggle)
    "onToggleFeaturedFirst" => Some(OnToggleFeaturedFirst)
    "onToggleSortAsc" => Some(OnToggleSortAsc)
    "onToggleSortByEnd" => Some(OnToggleSortByEnd)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to personal_site.html breaks every `match` over this type
/// until it is handled.
pub(all) enum RootMsg {
  /// `@on.click="clearFilters …"`
  ClearFilters
  /// `@on.click="onCategoryToggle …"`
  OnCategoryToggle(@tutuca.Value, Bool)
  /// `@on.click="onRoleToggle …"`
  OnRoleToggle(@tutuca.Value, Bool)
  /// `@on.change="onToggleFeaturedFirst …"`
  OnToggleFeaturedFirst
  /// `@on.change="onToggleSortAsc …"`
  OnToggleSortAsc
  /// `@on.change="onToggleSortByEnd …"`
  OnToggleSortByEnd
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn RootMsg::from_dispatch(d : @component.Dispatch) -> RootMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("clearFilters", []) => ClearFilters
      ("onCategoryToggle", [a0, Bool(a1)]) => OnCategoryToggle(a0, a1)
      ("onRoleToggle", [a0, Bool(a1)]) => OnRoleToggle(a0, a1)
      ("onToggleFeaturedFirst", []) => OnToggleFeaturedFirst
      ("onToggleSortAsc", []) => OnToggleSortAsc
      ("onToggleSortByEnd", []) => OnToggleSortByEnd
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum RootMethod {
  HasActiveFilters
} derive(Eq, @debug.Debug)

///|
pub fn RootMethod::name(self : RootMethod) -> String {
  match self {
    HasActiveFilters => "hasActiveFilters"
  }
}

///|
/// Every `@when` handler the views name.
pub(all) enum RootWhen {
  FilterEntry
} derive(Eq, @debug.Debug)

///|
pub fn RootWhen::name(self : RootWhen) -> String {
  match self {
    FilterEntry => "filterEntry"
  }
}

///|
/// Every `@enrich-with` handler named INSIDE an `@each`.
pub(all) enum RootEnrich {
  EnrichCategoryBindings
  EnrichRoleBindings
} derive(Eq, @debug.Debug)

///|
pub fn RootEnrich::name(self : RootEnrich) -> String {
  match self {
    EnrichCategoryBindings => "enrichCategoryBindings"
    EnrichRoleBindings => "enrichRoleBindings"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for RootState with fn schema() {
  @component.SchemaInfo::new(
    name="Root",
    fingerprint="b2600c9777dc4864",
    fields=[
      @component.FieldInfo::new("allEntries", TyList(TyAny)),
      @component.FieldInfo::new("allCategories", TyList(TyText)),
      @component.FieldInfo::new("allRoles", TyList(TyText)),
      @component.FieldInfo::new("selectedCategories", TySet),
      @component.FieldInfo::new("selectedRoles", TySet),
      @component.FieldInfo::new("featuredFirst", TyBool),
      @component.FieldInfo::new("sortByEnd", TyBool),
      @component.FieldInfo::new("sortAsc", TyBool),
    ],
    inputs=[
      "clearFilters", "onCategoryToggle", "onRoleToggle", "onToggleFeaturedFirst",
      "onToggleSortAsc", "onToggleSortByEnd",
    ],
    methods=["hasActiveFilters"],
    view_names=["main"],
  )
}
