// GENERATED by `tutuca gen` from personal_site.tutu — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen personal_site.tutu --name PersonalSite --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the AltUrl component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct AltUrlState {
  label : String
  url : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..AltUrlState::zero(), label: "Counter" }
pub fn AltUrlState::zero() -> AltUrlState {
  { label: "", url: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for AltUrlState with fn encode(s) {
  { "label": Str(s.label), "url": Str(s.url) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for AltUrlState with fn decode(f) {
  guard f.get("label") is Some(Str(v_label)) else { return None }
  guard f.get("url") is Some(Str(v_url)) else { return None }
  Some({ label: v_label, url: v_url, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let alt_url_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let alt_url_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for AltUrlState with fn schema() {
  @component.SchemaInfo::new(
    name="AltUrl",
    fingerprint="d312eadc19e0ff9c",
    fields=[
      @component.FieldInfo::new("label", TyText),
      @component.FieldInfo::new("url", TyText),
    ],
    domains=[
      { field: "label", domain: DNonEmpty, },
      { field: "url", domain: DNonEmpty, },
    ],
    view_names=["main"],
  )
}

///|
/// The state of the PsEntry component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct PsEntryState {
  title : String
  description : String
  lang : String
  start_year : Int?
  end_year : Int?
  role : String?
  featured : Bool
  url : String
  alt_urls : Array[@tutuca.Value]
  categories : Array[String]
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..PsEntryState::zero(), label: "Counter" }
pub fn PsEntryState::zero() -> PsEntryState {
  {
    title: "",
    description: "",
    lang: "",
    start_year: None,
    end_year: None,
    role: None,
    featured: false,
    url: "",
    alt_urls: [],
    categories: [],
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for PsEntryState with fn encode(s) {
  let out : Map[String, @tutuca.Value] = Map([])
  out["title"] = Str(s.title)
  out["description"] = Str(s.description)
  out["lang"] = Str(s.lang)
  match s.start_year {
    Some(x) => out["start_year"] = Num(x.to_double())
    None => ()
  }
  match s.end_year {
    Some(x) => out["end_year"] = Num(x.to_double())
    None => ()
  }
  match s.role {
    Some(x) => out["role"] = Str(x)
    None => ()
  }
  out["featured"] = Bool(s.featured)
  out["url"] = Str(s.url)
  out["alt_urls"] = (List(s.alt_urls.map(x => x)) : @tutuca.Value)
  out["categories"] = (List(s.categories.map(x => Str(x))) : @tutuca.Value)
  out
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for PsEntryState with fn decode(f) {
  guard f.get("title") is Some(Str(v_title)) else { return None }
  guard f.get("description") is Some(Str(v_description)) else { return None }
  guard f.get("lang") is Some(Str(v_lang)) else { return None }
  let v_start_year : Int? = match f.get("start_year") {
    None | Some(Null) => None
    Some(t1_start_year) => {
      guard t1_start_year is Num(u3_start_year) else { return None }
      let x1_start_year = u3_start_year.to_int()
      Some(x1_start_year)
    }
  }
  let v_end_year : Int? = match f.get("end_year") {
    None | Some(Null) => None
    Some(t1_end_year) => {
      guard t1_end_year is Num(u3_end_year) else { return None }
      let x1_end_year = u3_end_year.to_int()
      Some(x1_end_year)
    }
  }
  let v_role : String? = match f.get("role") {
    None | Some(Null) => None
    Some(t1_role) => {
      guard t1_role is Str(x1_role) else { return None }
      Some(x1_role)
    }
  }
  guard f.get("featured") is Some(Bool(v_featured)) else { return None }
  guard f.get("url") is Some(Str(v_url)) else { return None }
  guard f.get("alt_urls") is Some(t1_alt_urls) else { return None }
  guard t1_alt_urls is List(u1_alt_urls) else { return None }
  let v_alt_urls : Array[@tutuca.Value] = []
  for e1_alt_urls in u1_alt_urls {
    let x1_alt_urls = e1_alt_urls
    v_alt_urls.push(x1_alt_urls)
  }
  guard f.get("categories") is Some(t1_categories) else { return None }
  guard t1_categories is List(u1_categories) else { return None }
  let v_categories : Array[String] = []
  for e1_categories in u1_categories {
    guard e1_categories is Str(x1_categories) else { return None }
    v_categories.push(x1_categories)
  }
  Some({
    title: v_title,
    description: v_description,
    lang: v_lang,
    start_year: v_start_year,
    end_year: v_end_year,
    role: v_role,
    featured: v_featured,
    url: v_url,
    alt_urls: v_alt_urls,
    categories: v_categories,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let ps_entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let ps_entry_global_style : String = ""

///|
/// Every callable the views name, plus the file's own `pred` / `compute` names — the bucket `compute~` keys.
pub(all) enum PsEntryCompute {
  IsSpanish
  LangFlag
  YearDisplay
} derive(Eq, @debug.Debug)

///|
pub fn PsEntryCompute::name(self : PsEntryCompute) -> String {
  match self {
    IsSpanish => "is_spanish"
    LangFlag => "lang_flag"
    YearDisplay => "year_display"
  }
}

///|
/// Every `@enrich-with` handler named INSIDE an `@each`.
pub(all) enum PsEntryEnrich {
  EnrichCategoryBadge
} derive(Eq, @debug.Debug)

///|
pub fn PsEntryEnrich::name(self : PsEntryEnrich) -> String {
  match self {
    EnrichCategoryBadge => "enrich_category_badge"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for PsEntryState with fn schema() {
  @component.SchemaInfo::new(
    name="PsEntry",
    fingerprint="7c2482919b353142",
    fields=[
      @component.FieldInfo::new("title", TyText),
      @component.FieldInfo::new("description", TyText),
      @component.FieldInfo::new("lang", TyText),
      @component.FieldInfo::new(
        "start_year",
        TyOption(TyInt(width=32, signed=true)),
      ),
      @component.FieldInfo::new(
        "end_year",
        TyOption(TyInt(width=32, signed=true)),
      ),
      @component.FieldInfo::new("role", TyOption(TyText)),
      @component.FieldInfo::new("featured", TyBool),
      @component.FieldInfo::new("url", TyText),
      @component.FieldInfo::new("alt_urls", TyList(TyComp(Some("AltUrl")))),
      @component.FieldInfo::new("categories", TyList(TyText)),
    ],
    methods=["is_spanish", "lang_flag", "year_display"],
    domains=[
      { field: "lang", domain: DOneOf([Str("en"), Str("es")]), },
      { field: "title", domain: DNonEmpty, },
      { field: "url", domain: DNonEmpty, },
    ],
    view_names=["main"],
  )
}

///|
/// The state of the Root component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct RootState {
  all_entries : Array[@tutuca.Value]
  all_categories : Array[String]
  all_roles : Array[String]
  selected_categories : Map[String, Bool]
  selected_roles : Map[String, Bool]
  featured_first : Bool
  sort_by_end : Bool
  sort_asc : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..RootState::zero(), label: "Counter" }
pub fn RootState::zero() -> RootState {
  {
    all_entries: [],
    all_categories: [],
    all_roles: [],
    selected_categories: Map([]),
    selected_roles: Map([]),
    featured_first: false,
    sort_by_end: false,
    sort_asc: false,
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for RootState with fn encode(s) {
  {
    "all_entries": (List(s.all_entries.map(x => x)) : @tutuca.Value),
    "all_categories": (List(s.all_categories.map(x => Str(x))) : @tutuca.Value),
    "all_roles": (List(s.all_roles.map(x => Str(x))) : @tutuca.Value),
    "selected_categories": {
      let m : Map[String, @tutuca.Value] = Map([])
      for k, b in s.selected_categories {
        m[k] = Bool(b)
      }
      (Map(m) : @tutuca.Value)
    },
    "selected_roles": {
      let m : Map[String, @tutuca.Value] = Map([])
      for k, b in s.selected_roles {
        m[k] = Bool(b)
      }
      (Map(m) : @tutuca.Value)
    },
    "featured_first": Bool(s.featured_first),
    "sort_by_end": Bool(s.sort_by_end),
    "sort_asc": Bool(s.sort_asc),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for RootState with fn decode(f) {
  guard f.get("all_entries") is Some(t1_all_entries) else { return None }
  guard t1_all_entries is List(u1_all_entries) else { return None }
  let v_all_entries : Array[@tutuca.Value] = []
  for e1_all_entries in u1_all_entries {
    let x1_all_entries = e1_all_entries
    v_all_entries.push(x1_all_entries)
  }
  guard f.get("all_categories") is Some(t1_all_categories) else { return None }
  guard t1_all_categories is List(u1_all_categories) else { return None }
  let v_all_categories : Array[String] = []
  for e1_all_categories in u1_all_categories {
    guard e1_all_categories is Str(x1_all_categories) else { return None }
    v_all_categories.push(x1_all_categories)
  }
  guard f.get("all_roles") is Some(t1_all_roles) else { return None }
  guard t1_all_roles is List(u1_all_roles) else { return None }
  let v_all_roles : Array[String] = []
  for e1_all_roles in u1_all_roles {
    guard e1_all_roles is Str(x1_all_roles) else { return None }
    v_all_roles.push(x1_all_roles)
  }
  guard f.get("selected_categories") is Some(t1_selected_categories) else {
    return None
  }
  guard t1_selected_categories is Map(u1_selected_categories) else {
    return None
  }
  let v_selected_categories : Map[String, Bool] = Map([])
  for k, x in u1_selected_categories {
    v_selected_categories[k] = x is Bool(true)
  }
  guard f.get("selected_roles") is Some(t1_selected_roles) else { return None }
  guard t1_selected_roles is Map(u1_selected_roles) else { return None }
  let v_selected_roles : Map[String, Bool] = Map([])
  for k, x in u1_selected_roles {
    v_selected_roles[k] = x is Bool(true)
  }
  guard f.get("featured_first") is Some(Bool(v_featured_first)) else {
    return None
  }
  guard f.get("sort_by_end") is Some(Bool(v_sort_by_end)) else { return None }
  guard f.get("sort_asc") is Some(Bool(v_sort_asc)) else { return None }
  Some({
    all_entries: v_all_entries,
    all_categories: v_all_categories,
    all_roles: v_all_roles,
    selected_categories: v_selected_categories,
    selected_roles: v_selected_roles,
    featured_first: v_featured_first,
    sort_by_end: v_sort_by_end,
    sort_asc: v_sort_asc,
  })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Every addressed name: the `message` the `spec:` section declares,
/// plus every bare `~on_<event>` handler the views write. One bucket holds
/// them
/// however they were sent, so one type covers it — where the two overlap
/// the schema's declared payload wins, and a view whose call sites
/// disagree with it is a build error.
pub(all) enum RootMsg {
  Init
  LoadDataOk(Array[@tutuca.Value])
  ClearFilters
  OnCategoryToggle(@tutuca.Value, Bool)
  OnRoleToggle(@tutuca.Value, Bool)
  OnToggleFeaturedFirst
  OnToggleSortAsc
  OnToggleSortByEnd
  /// A message the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// `load_data_ok`'s arguments, decoded at the declared types.
///
/// The same `dec_stmts` the state codec is written from, so a payload and
/// a field of the same declared type are read by one rule. `None` is a
/// mismatch anywhere in the shape, however deep.
fn root_msg_load_data_ok_of_args(args : Array[@tutuca.Value]) -> RootMsg? {
  guard args.length() == 1 else { return None }
  guard args.get(0) is Some(t1_0) else { return None }
  guard t1_0 is List(u1_0) else { return None }
  let a0 : Array[@tutuca.Value] = []
  for e1_0 in u1_0 {
    let x1_0 = e1_0
    a0.push(x1_0)
  }
  Some(LoadDataOk(a0))
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn RootMsg::from_dispatch(d : @component.Dispatch) -> RootMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("init", []) => Init
      ("load_data_ok", _) =>
        match root_msg_load_data_ok_of_args(args) {
          Some(v) => v
          None => {
            @tutuca.bad_payload(name, "Array[Any]", args)
            Unknown(name, args)
          }
        }
      ("clear_filters", []) => ClearFilters
      ("on_category_toggle", [a0, Bool(a1)]) => OnCategoryToggle(a0, a1)
      ("on_role_toggle", [a0, Bool(a1)]) => OnRoleToggle(a0, a1)
      ("on_toggle_featured_first", []) => OnToggleFeaturedFirst
      ("on_toggle_sort_asc", []) => OnToggleSortAsc
      ("on_toggle_sort_by_end", []) => OnToggleSortByEnd
      _ => {
        match name {
          "init" => @tutuca.bad_payload(name, "", args)
          "load_data_ok" => @tutuca.bad_payload(name, "Array[Any]", args)
          "clear_filters" => @tutuca.bad_payload(name, "", args)
          "on_category_toggle" => @tutuca.bad_payload(name, "Any, Bool", args)
          "on_role_toggle" => @tutuca.bad_payload(name, "Any, Bool", args)
          "on_toggle_featured_first" => @tutuca.bad_payload(name, "", args)
          "on_toggle_sort_asc" => @tutuca.bad_payload(name, "", args)
          "on_toggle_sort_by_end" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn RootMsg::to_dispatch(self : RootMsg) -> (String, Array[@tutuca.Value]) {
  match self {
    Init => ("init", [])
    LoadDataOk(a0) => ("load_data_ok", [(List(a0.map(x => x)) : @tutuca.Value)])
    ClearFilters => ("clear_filters", [])
    OnCategoryToggle(a0, a1) => ("on_category_toggle", [a0, Bool(a1)])
    OnRoleToggle(a0, a1) => ("on_role_toggle", [a0, Bool(a1)])
    OnToggleFeaturedFirst => ("on_toggle_featured_first", [])
    OnToggleSortAsc => ("on_toggle_sort_asc", [])
    OnToggleSortByEnd => ("on_toggle_sort_by_end", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn RootMsg::send(self : RootMsg, ctx : &@tutuca.Ctx) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let root_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let root_global_style : String = ""

///|
/// Every callable the views name, plus the file's own `pred` / `compute` names — the bucket `compute~` keys.
pub(all) enum RootCompute {
  HasActiveFilters
} derive(Eq, @debug.Debug)

///|
pub fn RootCompute::name(self : RootCompute) -> String {
  match self {
    HasActiveFilters => "has_active_filters"
  }
}

///|
/// Every `@when` handler the views name.
pub(all) enum RootWhen {
  FilterEntry
} derive(Eq, @debug.Debug)

///|
pub fn RootWhen::name(self : RootWhen) -> String {
  match self {
    FilterEntry => "filter_entry"
  }
}

///|
/// Every `@enrich-with` handler named INSIDE an `@each`.
pub(all) enum RootEnrich {
  EnrichCategoryBindings
  EnrichRoleBindings
} derive(Eq, @debug.Debug)

///|
pub fn RootEnrich::name(self : RootEnrich) -> String {
  match self {
    EnrichCategoryBindings => "enrich_category_bindings"
    EnrichRoleBindings => "enrich_role_bindings"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for RootState with fn schema() {
  @component.SchemaInfo::new(
    name="Root",
    fingerprint="254b9282c4cf43b6",
    fields=[
      @component.FieldInfo::new("all_entries", TyList(TyComp(Some("PsEntry")))),
      @component.FieldInfo::new("all_categories", TyList(TyText)),
      @component.FieldInfo::new("all_roles", TyList(TyText)),
      @component.FieldInfo::new("selected_categories", TySet),
      @component.FieldInfo::new("selected_roles", TySet),
      @component.FieldInfo::new("featured_first", TyBool),
      @component.FieldInfo::new("sort_by_end", TyBool),
      @component.FieldInfo::new("sort_asc", TyBool),
    ],
    view_handlers=[
      "clear_filters", "on_category_toggle", "on_role_toggle", "on_toggle_featured_first",
      "on_toggle_sort_asc", "on_toggle_sort_by_end",
    ],
    receives=["init", "load_data_ok"],
    methods=["has_active_filters"],
    view_names=["main"],
  )
}
