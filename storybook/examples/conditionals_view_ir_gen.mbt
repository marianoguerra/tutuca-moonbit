// GENERATED by `tutuca gen-views` from conditionals.html — DO NOT EDIT.
//
// The views of TrafficLight, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing traffic_light_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn traffic_light_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-2"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-primary"),
          "data-eid": LNum(0.0),
        }),
        childs: [Text({ val: "Next light" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "p",
        attrs: ConstAttrs(Map([])),
        childs: [
          Text({ val: "Light is: " }),
          Dom({
            tag: "code",
            attrs: ConstAttrs(Map([])),
            childs: [RenderText({ node_id: None, val: Method("light") })],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("advice") }),
        childs: [
          Text({ val: "\n    You must\n    " }),
          Show({
            node_id: None,
            val: Predicate(pred=Equals, args=[
              Method("light"),
              Const(lit=LStr("red"), from_macro=false),
            ]),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "STOP" })],
              ns: None,
            }),
          }),
          Text({ val: " " }),
          Show({
            node_id: None,
            val: Predicate(pred=Equals, args=[
              Method("light"),
              Const(lit=LStr("orange"), from_macro=false),
            ]),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "SLOW DOWN" })],
              ns: None,
            }),
          }),
          Text({ val: " " }),
          Show({
            node_id: None,
            val: Predicate(pred=Equals, args=[
              Method("light"),
              Const(lit=LStr("green"), from_macro=false),
            ]),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "GO" })],
              ns: None,
            }),
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("nextLight"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn traffic_light_views() -> Map[String, @anode.View] {
  { "main": traffic_light_main_view_ir() }
}

///|
/// The compiled `main` view.
fn tabbed_u_i_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({
          "role": LStr("tablist"),
          "class": LStr("tabs tabs-border"),
        }),
        childs: [
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("t-overview"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("tab"),
                  Const(lit=LStr("overview"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(0.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "Overview" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("t-features"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("tab"),
                  Const(lit=LStr("features"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(1.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "Features" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("t-pricing"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("tab"),
                  Const(lit=LStr("pricing"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(2.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "Pricing" })],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("tab"),
          Const(lit=LStr("overview"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("card p-3 border panel") }),
          childs: [
            Dom({
              tag: "h4",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "Overview" })],
              ns: None,
            }),
            Dom({
              tag: "p",
              attrs: ConstAttrs(Map([])),
              childs: [
                Text({ val: "A short summary of what this product does." }),
              ],
              ns: None,
            }),
          ],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("tab"),
          Const(lit=LStr("features"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("card p-3 border panel") }),
          childs: [
            Dom({
              tag: "h4",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "Features" })],
              ns: None,
            }),
            Dom({
              tag: "p",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "The list of features lives on this tab." })],
              ns: None,
            }),
          ],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("tab"),
          Const(lit=LStr("pricing"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("card p-3 border panel") }),
          childs: [
            Dom({
              tag: "h4",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "Pricing" })],
              ns: None,
            }),
            Dom({
              tag: "p",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "What it costs." })],
              ns: None,
            }),
          ],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setTab"),
            args: [Const(lit=LStr("overview"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setTab"),
            args: [Const(lit=LStr("features"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setTab"),
            args: [Const(lit=LStr("pricing"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn tabbed_u_i_views() -> Map[String, @anode.View] {
  { "main": tabbed_u_i_main_view_ir() }
}

///|
/// The compiled `main` view.
fn list_and_filter_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="type", val=Const(lit=LStr("search"), from_macro=false)),
          ConstA(name="class", val=Const(lit=LStr("input"), from_macro=false)),
          Plain(name="value", val=Field("query")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
          ConstA(
            name="placeholder",
            val=Const(lit=LStr("Filter entries"), from_macro=false),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "ul",
        attrs: ConstAttrs(Map([])),
        childs: [
          Each({
            node_id: 1,
            iter_info: {
              val: Field("items"),
              when_val: Some(HandlerName(name="filterItem", ns=Alter)),
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "li",
              attrs: ConstAttrs(Map([])),
              childs: [
                Dom({
                  tag: "span",
                  attrs: ConstAttrs(Map([])),
                  childs: [RenderText({ node_id: None, val: Bind("key") })],
                  ns: None,
                }),
                Text({ val: ": " }),
                RenderText({ node_id: Some(0), val: Bind("value") }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setQuery"), args: [Name("value")] },
          modifiers: [],
        },
        {
          name: "keydown",
          handler: { handler: Method("resetQuery"), args: [] },
          modifiers: ["cancel"],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn list_and_filter_views() -> Map[String, @anode.View] {
  { "main": list_and_filter_main_view_ir() }
}
