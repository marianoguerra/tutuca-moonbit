// GENERATED by `tutuca gen-views` from custom_collection.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views custom_collection.html --name CustomCollection --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the Playlist component.
///
/// Declared as `interface playlist` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct PlaylistState {
  songs : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..PlaylistState::zero(), label: "Counter" }
fn PlaylistState::zero() -> PlaylistState {
  PlaylistState::{ songs: Null }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn playlist_zero_args() -> Map[String, @tutuca.Value] {
  playlist_encode(PlaylistState::zero())
}

///|
/// Every field the Playlist state declares — typed field names for tests and inspectors.
pub(all) enum PlaylistField {
  Songs
} derive(Eq, @debug.Debug)

///|
pub fn PlaylistField::name(self : PlaylistField) -> String {
  match self {
    Songs => "songs"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn PlaylistField::kind(self : PlaylistField) -> @component.FieldKind {
  match self {
    Songs => @component.FAny
  }
}

///|
/// Every field, in declaration order.
pub let playlist_field_names : Array[String] = ["songs"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let playlist_schema_fingerprint : String = "cc5dc1cd9f4badee"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn playlist_encode(s : PlaylistState) -> Map[String, @tutuca.Value] {
  { "songs": s.songs }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn playlist_decode(f : Map[String, @tutuca.Value]) -> PlaylistState? {
  let v_songs = f.get("songs").unwrap_or(Null)
  Some(PlaylistState::{ songs: v_songs })
}

///|
/// The views declared in custom_collection.html.
pub(all) enum PlaylistView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn PlaylistView::name(self : PlaylistView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let playlist_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let playlist_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum PlaylistInput {
  RemoveSong
} derive(Eq, @debug.Debug)

///|
pub fn PlaylistInput::name(self : PlaylistInput) -> String {
  match self {
    RemoveSong => "removeSong"
  }
}

///|
pub fn PlaylistInput::parse(s : String) -> PlaylistInput? {
  match s {
    "removeSong" => Some(RemoveSong)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to custom_collection.html breaks every `match` over this type
/// until it is handled.
pub(all) enum PlaylistMsg {
  /// `@on.click="removeSong …"`
  RemoveSong(@tutuca.Value)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn PlaylistMsg::from_dispatch(d : @component.Dispatch) -> PlaylistMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("removeSong", [a0]) => RemoveSong(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] playlist_swap(
  f : (PlaylistInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([RemoveSong] : Array[PlaylistInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn playlist_schema() -> @component.SchemaInfo {
  {
    fingerprint: "cc5dc1cd9f4badee",
    fields: [
      {
        name: "songs",
        ty: "any",
        kind: @component.FAny,
        elem: Some("any"),
        slot: None,
        members: [],
      },
    ],
    inputs: ["removeSong"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the Song component.
///
/// Declared as `interface song` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct SongState {
  title : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SongState::zero(), label: "Counter" }
fn SongState::zero() -> SongState {
  SongState::{ title: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn song_zero_args() -> Map[String, @tutuca.Value] {
  song_encode(SongState::zero())
}

///|
/// Every field the Song state declares — typed field names for tests and inspectors.
pub(all) enum SongField {
  Title
} derive(Eq, @debug.Debug)

///|
pub fn SongField::name(self : SongField) -> String {
  match self {
    Title => "title"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn SongField::kind(self : SongField) -> @component.FieldKind {
  match self {
    Title => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let song_field_names : Array[String] = ["title"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let song_schema_fingerprint : String = "5d3ec9756ba331ba"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn song_encode(s : SongState) -> Map[String, @tutuca.Value] {
  { "title": Str(s.title) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn song_decode(f : Map[String, @tutuca.Value]) -> SongState? {
  guard f.get("title") is Some(Str(v_title)) else { return None }
  Some(SongState::{ title: v_title })
}

///|
/// The views declared in custom_collection.html.
pub(all) enum SongView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SongView::name(self : SongView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let song_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let song_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn song_schema() -> @component.SchemaInfo {
  {
    fingerprint: "5d3ec9756ba331ba",
    fields: [
      {
        name: "title",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}
