// GENERATED by `tutuca gen-views` from json.html — DO NOT EDIT.
//
// The views of JsonSelector, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing json_selector_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn json_selector_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "join gap-3 font-mono json-selector")],
      [
        @anode.h(
          "button",
          [
            @anode.attr("class", "btn btn-sm btn-soft btn-warning to-null"),
            @anode.eid(0),
          ],
          [@anode.text("null")],
        ),
        @anode.text(" "),
        @anode.h(
          "button",
          [
            @anode.attr("class", "btn btn-sm btn-soft btn-success to-bool"),
            @anode.eid(1),
          ],
          [@anode.text("bool")],
        ),
        @anode.text(" "),
        @anode.h(
          "button",
          [
            @anode.attr("class", "btn btn-sm btn-soft btn-primary to-number"),
            @anode.eid(2),
          ],
          [@anode.text("0")],
        ),
        @anode.text(" "),
        @anode.h(
          "button",
          [
            @anode.attr("class", "btn btn-sm btn-soft btn-accent to-string"),
            @anode.eid(3),
          ],
          [@anode.text("\"\"")],
        ),
        @anode.text(" "),
        @anode.h(
          "button",
          [
            @anode.attr("class", "btn btn-sm btn-soft btn-info to-array"),
            @anode.eid(4),
          ],
          [@anode.text("[]")],
        ),
        @anode.text(" "),
        @anode.h(
          "button",
          [
            @anode.attr("class", "btn btn-sm btn-soft btn-info to-object"),
            @anode.eid(5),
          ],
          [@anode.text("{}")],
        ),
      ],
    ),
    [
      [@anode.on("click", HandlerName(name="setToNull", ns=Receive))],
      [
        @anode.on("click", HandlerName(name="setToBool", ns=Receive), args=[
          EventPath(["isCtrl"]),
        ]),
      ],
      [@anode.on("click", HandlerName(name="setToNumber", ns=Receive))],
      [@anode.on("click", HandlerName(name="setToString", ns=Receive))],
      [@anode.on("click", HandlerName(name="setToArray", ns=Receive))],
      [@anode.on("click", HandlerName(name="setToObject", ns=Receive))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_selector_views() -> Map[String, @anode.View] {
  { "main": json_selector_main_view_ir() }
}

///|
/// The JsonSelector component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `JsonSelectorState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn json_selector_component(
  name? : String = "JsonSelector",
  views? : Map[String, @anode.View] = json_selector_views(),
  init? : JsonSelectorState = JsonSelectorState::zero(),
  common_style? : String = json_selector_common_style,
  global_style? : String = json_selector_global_style,
  update? : (JsonSelectorState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    JsonSelectorState,
  ],
  swap? : (JsonSelectorInput) -> ((
    JsonSelectorState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          (
            [
              SetToArray,
              SetToBool,
              SetToNull,
              SetToNumber,
              SetToObject,
              SetToString,
            ] : Array[JsonSelectorInput]),
          JsonSelectorInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn json_null_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "span",
      [@anode.attr("class", "text-warning text-xs font-mono p-3 json-null")],
      [@anode.text("null")],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_null_views() -> Map[String, @anode.View] {
  { "main": json_null_main_view_ir() }
}

///|
/// The JsonNull component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `JsonNullState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn json_null_component(
  name? : String = "JsonNull",
  views? : Map[String, @anode.View] = json_null_views(),
  init? : JsonNullState = JsonNullState::zero(),
  common_style? : String = json_null_common_style,
  global_style? : String = json_null_global_style,
  update? : (JsonNullState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    JsonNullState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn json_bool_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "button",
      [
        @anode.attr("data-bool", ""),
        @anode.if_attr(
          "class",
          Field("value"),
          then_val=@anode.const_str(
            "btn btn-sm btn-soft btn-success font-mono true",
          ),
          else_val=@anode.const_str(
            "btn btn-sm btn-soft btn-error font-mono false",
          ),
        ),
        @anode.eid(0),
      ],
      [
        @anode.show(Field("value"), @anode.h("span", [], [@anode.text("true")])),
        @anode.text(" "),
        @anode.hide(
          Field("value"),
          @anode.h("span", [], [@anode.text("false")]),
        ),
      ],
    ),
    [[@anode.on("click", HandlerName(name="toggleValue", ns=Receive))]],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_bool_views() -> Map[String, @anode.View] {
  { "main": json_bool_main_view_ir() }
}

///|
/// The JsonBool component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `JsonBoolState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn json_bool_component(
  name? : String = "JsonBool",
  views? : Map[String, @anode.View] = json_bool_views(),
  init? : JsonBoolState = JsonBoolState::zero(),
  common_style? : String = json_bool_common_style,
  global_style? : String = json_bool_global_style,
  update? : (JsonBoolState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    JsonBoolState,
  ],
  swap? : (JsonBoolInput) -> ((
    JsonBoolState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([ToggleValue] : Array[JsonBoolInput]),
          JsonBoolInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn json_string_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "input",
      [
        @anode.attr(
          "class", "input input-sm input-ghost text-green-500 font-mono json-string",
        ),
        @anode.dyn_attr("value", Field("value")),
        @anode.eid(0),
      ],
      [],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setValue", ns=Receive), args=[
          EventPath(["value"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_string_views() -> Map[String, @anode.View] {
  { "main": json_string_main_view_ir() }
}

///|
/// The JsonString component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `JsonStringState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn json_string_component(
  name? : String = "JsonString",
  views? : Map[String, @anode.View] = json_string_views(),
  init? : JsonStringState = JsonStringState::zero(),
  common_style? : String = json_string_common_style,
  global_style? : String = json_string_global_style,
  update? : (JsonStringState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    JsonStringState,
  ],
  swap? : (JsonStringInput) -> ((
    JsonStringState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetValue] : Array[JsonStringInput]),
          JsonStringInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn json_number_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "input",
      [
        @anode.attr("type", "number"),
        @anode.attr(
          "class", "input input-sm input-ghost text-primary font-mono json-number",
        ),
        @anode.dyn_attr("title", Field("value")),
        @anode.dyn_attr("value", Field("strValue")),
        @anode.eid(0),
      ],
      [],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setRawValue", ns=Receive), args=[
          EventPath(["value"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_number_views() -> Map[String, @anode.View] {
  { "main": json_number_main_view_ir() }
}

///|
/// The JsonNumber component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `JsonNumberState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn json_number_component(
  name? : String = "JsonNumber",
  views? : Map[String, @anode.View] = json_number_views(),
  init? : JsonNumberState = JsonNumberState::zero(),
  common_style? : String = json_number_common_style,
  global_style? : String = json_number_global_style,
  update? : (JsonNumberState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    JsonNumberState,
  ],
  swap? : (JsonNumberInput) -> ((
    JsonNumberState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetRawValue] : Array[JsonNumberInput]),
          JsonNumberInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn json_array_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "section",
      [
        @anode.attr(
          "class", "join join-vertical gap-3 pl-3 border-l-1 border-l-neutral-500 json-array",
        ),
      ],
      [
        @anode.h(
          "button",
          [
            @anode.attr("class", "btn btn-sm btn-soft btn-primary add"),
            @anode.eid(0),
          ],
          [@anode.text("+")],
        ),
        @anode.text(" "),
        @anode.h("div", [@anode.attr("class", "join join-vertical gap-3")], [
          @anode.each(
            1,
            Field("items"),
            @anode.h(
              "div",
              [
                @anode.attr(
                  "class", "join gap-3 justify-between items-center group entry",
                ),
                @anode.attr("draggable", "true"),
                @anode.attr("data-dragtype", "json-list-item"),
                @anode.attr("data-droptarget", "json-list-item"),
                @anode.eid(2),
              ],
              [
                @anode.render_it(0),
                @anode.text(" "),
                @anode.h(
                  "button",
                  [
                    @anode.attr(
                      "class", "btn btn-sm btn-soft btn-circle btn-error font-mono opacity-20 group-hover:opacity-100 rm",
                    ),
                    @anode.eid(1),
                  ],
                  [@anode.text("x")],
                ),
              ],
            ),
          ),
        ]),
      ],
    ),
    [
      [@anode.on("click", HandlerName(name="addItem", ns=Receive))],
      [
        @anode.on("click", HandlerName(name="removeInItemsAt", ns=Receive), args=[
          Bind("key"),
        ]),
      ],
      [
        @anode.on("drop", HandlerName(name="onDropOnItem", ns=Receive), args=[
          Bind("key"),
          EventPath(["dragInfo"]),
        ]),
      ],
    ],
    style="[data-draggingover=\"json-list-item\"] {\n  border-top: 4rem solid transparent;\n  transition: 0.15s border-top ease;\n}",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_array_views() -> Map[String, @anode.View] {
  { "main": json_array_main_view_ir() }
}

///|
/// The JsonArray component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `JsonArrayState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn json_array_component(
  name? : String = "JsonArray",
  views? : Map[String, @anode.View] = json_array_views(),
  init? : JsonArrayState = JsonArrayState::zero(),
  common_style? : String = json_array_common_style,
  global_style? : String = json_array_global_style,
  update? : (JsonArrayState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    JsonArrayState,
  ],
  swap? : (JsonArrayInput) -> ((
    JsonArrayState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([AddItem, OnDropOnItem, RemoveInItemsAt] : Array[JsonArrayInput]),
          JsonArrayInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn json_object_key_val_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "join gap-3 items-center justify-center json-kv")],
      [
        @anode.h(
          "input",
          [
            @anode.attr(
              "class", "input input-sm input-ghost text-green-500 font-mono json-key",
            ),
            @anode.dyn_attr("value", Field("key")),
            @anode.eid(0),
          ],
          [],
        ),
        @anode.text(" "),
        @anode.h(
          "span",
          [@anode.attr("class", "text-xs font-mono text-gray-500 colon")],
          [@anode.text(":")],
        ),
        @anode.text(" "),
        @anode.render(0, Field("value")),
      ],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setKey", ns=Receive), args=[
          EventPath(["value"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_object_key_val_views() -> Map[String, @anode.View] {
  { "main": json_object_key_val_main_view_ir() }
}

///|
/// The JsonObjectKeyVal component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `JsonObjectKeyValState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn json_object_key_val_component(
  name? : String = "JsonObjectKeyVal",
  views? : Map[String, @anode.View] = json_object_key_val_views(),
  init? : JsonObjectKeyValState = JsonObjectKeyValState::zero(),
  common_style? : String = json_object_key_val_common_style,
  global_style? : String = json_object_key_val_global_style,
  update? : (JsonObjectKeyValState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    JsonObjectKeyValState,
  ],
  swap? : (JsonObjectKeyValInput) -> ((
    JsonObjectKeyValState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetKey] : Array[JsonObjectKeyValInput]),
          JsonObjectKeyValInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn json_object_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "section",
      [
        @anode.attr(
          "class", "join join-vertical gap-3 pl-3 border-l-1 border-l-neutral-500 json-object",
        ),
      ],
      [
        @anode.h(
          "button",
          [
            @anode.attr(
              "class", "btn btn-sm btn-soft font-mono btn-primary add",
            ),
            @anode.eid(0),
          ],
          [@anode.text("+")],
        ),
        @anode.text(" "),
        @anode.h("div", [@anode.attr("class", "join join-vertical gap-3")], [
          @anode.each(
            1,
            Field("items"),
            @anode.h(
              "div",
              [
                @anode.attr(
                  "class", "join gap-3 justify-between items-center group entry",
                ),
                @anode.attr("draggable", "true"),
                @anode.attr("data-dragtype", "json-kv-item"),
                @anode.attr("data-droptarget", "json-kv-item"),
                @anode.eid(2),
              ],
              [
                @anode.render_it(0),
                @anode.text(" "),
                @anode.h(
                  "button",
                  [
                    @anode.attr(
                      "class", "btn btn-sm btn-soft btn-circle btn-error font-mono opacity-20 group-hover:opacity-100 rm",
                    ),
                    @anode.eid(1),
                  ],
                  [@anode.text("x")],
                ),
              ],
            ),
          ),
        ]),
      ],
    ),
    [
      [@anode.on("click", HandlerName(name="addItem", ns=Receive))],
      [
        @anode.on("click", HandlerName(name="removeInItemsAt", ns=Receive), args=[
          Bind("key"),
        ]),
      ],
      [
        @anode.on("drop", HandlerName(name="onDropOnItem", ns=Receive), args=[
          Bind("key"),
          EventPath(["dragInfo"]),
        ]),
      ],
    ],
    style="[data-draggingover=\"json-kv-item\"] {\n  border-top: 4rem solid transparent;\n  transition: 0.15s border-top ease;\n}",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_object_views() -> Map[String, @anode.View] {
  { "main": json_object_main_view_ir() }
}

///|
/// The JsonObject component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `JsonObjectState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn json_object_component(
  name? : String = "JsonObject",
  views? : Map[String, @anode.View] = json_object_views(),
  init? : JsonObjectState = JsonObjectState::zero(),
  common_style? : String = json_object_common_style,
  global_style? : String = json_object_global_style,
  update? : (JsonObjectState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    JsonObjectState,
  ],
  swap? : (JsonObjectInput) -> ((
    JsonObjectState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([AddItem, OnDropOnItem, RemoveInItemsAt] : Array[JsonObjectInput]),
          JsonObjectInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
