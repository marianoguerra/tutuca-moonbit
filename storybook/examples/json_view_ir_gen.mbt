// GENERATED by `tutuca gen-views` from json.html — DO NOT EDIT.
//
// The views of JsonSelector, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing json_selector_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn json_selector_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("join gap-3 font-mono json-selector"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm btn-soft btn-warning to-null"),
          "data-eid": LNum(0.0),
        }),
        childs: [Text({ val: "null" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm btn-soft btn-success to-bool"),
          "data-eid": LNum(1.0),
        }),
        childs: [Text({ val: "bool" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm btn-soft btn-primary to-number"),
          "data-eid": LNum(2.0),
        }),
        childs: [Text({ val: "0" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm btn-soft btn-accent to-string"),
          "data-eid": LNum(3.0),
        }),
        childs: [Text({ val: "\"\"" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm btn-soft btn-info to-array"),
          "data-eid": LNum(4.0),
        }),
        childs: [Text({ val: "[]" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm btn-soft btn-info to-object"),
          "data-eid": LNum(5.0),
        }),
        childs: [Text({ val: "{}" })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("setToNull"), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("setToBool"), args: [Name("isCtrl")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("setToNumber"), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 3,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("setToString"), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 4,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("setToArray"), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 5,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("setToObject"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_selector_views() -> Map[String, @anode.View] {
  { "main": json_selector_main_view_ir() }
}

///|
/// The compiled `main` view.
fn json_null_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "span",
    attrs: ConstAttrs({
      "class": LStr("text-warning text-xs font-mono p-3 json-null"),
      "data-vid": LStr("main"),
    }),
    childs: [Text({ val: "null" })],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_null_views() -> Map[String, @anode.View] {
  { "main": json_null_main_view_ir() }
}

///|
/// The compiled `main` view.
fn json_bool_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "button",
    attrs: DynAttrs([
      ConstA(name="data-bool", val=Const(lit=LStr(""), from_macro=false)),
      If({
        name: "class",
        cond_val: Field("value"),
        then_val: Some(
          Const(
            lit=LStr("btn btn-sm btn-soft btn-success font-mono true"),
            from_macro=false,
          ),
        ),
        else_val: Some(
          Const(
            lit=LStr("btn btn-sm btn-soft btn-error font-mono false"),
            from_macro=false,
          ),
        ),
      }),
      ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
      ConstA(name="data-vid", val=Const(lit=LStr("main"), from_macro=false)),
    ]),
    childs: [
      Show({
        node_id: None,
        val: Field("value"),
        node: Dom({
          tag: "span",
          attrs: ConstAttrs(Map([])),
          childs: [Text({ val: "true" })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Hide({
        node_id: None,
        val: Field("value"),
        node: Dom({
          tag: "span",
          attrs: ConstAttrs(Map([])),
          childs: [Text({ val: "false" })],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("toggleValue"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_bool_views() -> Map[String, @anode.View] {
  { "main": json_bool_main_view_ir() }
}

///|
/// The compiled `main` view.
fn json_string_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "input",
    attrs: DynAttrs([
      ConstA(
        name="class",
        val=Const(
          lit=LStr(
            "input input-sm input-ghost text-green-500 font-mono json-string",
          ),
          from_macro=false,
        ),
      ),
      Plain(name="value", val=Field("value")),
      ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
      ConstA(name="data-vid", val=Const(lit=LStr("main"), from_macro=false)),
    ]),
    childs: [],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setValue"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_string_views() -> Map[String, @anode.View] {
  { "main": json_string_main_view_ir() }
}

///|
/// The compiled `main` view.
fn json_number_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "input",
    attrs: DynAttrs([
      ConstA(name="type", val=Const(lit=LStr("number"), from_macro=false)),
      ConstA(
        name="class",
        val=Const(
          lit=LStr(
            "input input-sm input-ghost text-primary font-mono json-number",
          ),
          from_macro=false,
        ),
      ),
      Plain(name="title", val=Field("value")),
      Plain(name="value", val=Field("strValue")),
      ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
      ConstA(name="data-vid", val=Const(lit=LStr("main"), from_macro=false)),
    ]),
    childs: [],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setRawValue"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_number_views() -> Map[String, @anode.View] {
  { "main": json_number_main_view_ir() }
}

///|
/// The compiled `main` view.
fn json_array_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr(
        "join join-vertical gap-3 pl-3 border-l-1 border-l-neutral-500 json-array",
      ),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm btn-soft btn-primary add"),
          "data-eid": LNum(0.0),
        }),
        childs: [Text({ val: "+" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("join join-vertical gap-3") }),
        childs: [
          Each({
            node_id: 1,
            iter_info: {
              val: Field("items"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "div",
              attrs: ConstAttrs({
                "class": LStr(
                  "join gap-3 justify-between items-center group entry",
                ),
                "draggable": LStr("true"),
                "data-dragtype": LStr("json-list-item"),
                "data-droptarget": LStr("json-list-item"),
                "data-eid": LNum(2.0),
              }),
              childs: [
                RenderIt({ node_id: 0, view_val: None }),
                Text({ val: " " }),
                Dom({
                  tag: "button",
                  attrs: ConstAttrs({
                    "class": LStr(
                      "btn btn-sm btn-soft btn-circle btn-error font-mono opacity-20 group-hover:opacity-100 rm",
                    ),
                    "data-eid": LNum(1.0),
                  }),
                  childs: [Text({ val: "x" })],
                  ns: None,
                }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: HandlerName(name="addItem", ns=Input), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("removeInItemsAt"), args: [Bind("key")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "drop",
          handler: {
            handler: HandlerName(name="onDropOnItem", ns=Input),
            args: [Bind("key"), Name("dragInfo")],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="[data-draggingover=\"json-list-item\"] {\n  border-top: 4rem solid transparent;\n  transition: 0.15s border-top ease;\n}",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_array_views() -> Map[String, @anode.View] {
  { "main": json_array_main_view_ir() }
}

///|
/// The compiled `main` view.
fn json_object_key_val_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("join gap-3 items-center justify-center json-kv"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(
            name="class",
            val=Const(
              lit=LStr(
                "input input-sm input-ghost text-green-500 font-mono json-key",
              ),
              from_macro=false,
            ),
          ),
          Plain(name="value", val=Field("key")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs({
          "class": LStr("text-xs font-mono text-gray-500 colon"),
        }),
        childs: [Text({ val: ":" })],
        ns: None,
      }),
      Text({ val: " " }),
      Render({ node_id: 0, val: Field("value"), view_val: None }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setKey"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_object_key_val_views() -> Map[String, @anode.View] {
  { "main": json_object_key_val_main_view_ir() }
}

///|
/// The compiled `main` view.
fn json_object_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr(
        "join join-vertical gap-3 pl-3 border-l-1 border-l-neutral-500 json-object",
      ),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm btn-soft font-mono btn-primary add"),
          "data-eid": LNum(0.0),
        }),
        childs: [Text({ val: "+" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("join join-vertical gap-3") }),
        childs: [
          Each({
            node_id: 1,
            iter_info: {
              val: Field("items"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "div",
              attrs: ConstAttrs({
                "class": LStr(
                  "join gap-3 justify-between items-center group entry",
                ),
                "draggable": LStr("true"),
                "data-dragtype": LStr("json-kv-item"),
                "data-droptarget": LStr("json-kv-item"),
                "data-eid": LNum(2.0),
              }),
              childs: [
                RenderIt({ node_id: 0, view_val: None }),
                Text({ val: " " }),
                Dom({
                  tag: "button",
                  attrs: ConstAttrs({
                    "class": LStr(
                      "btn btn-sm btn-soft btn-circle btn-error font-mono opacity-20 group-hover:opacity-100 rm",
                    ),
                    "data-eid": LNum(1.0),
                  }),
                  childs: [Text({ val: "x" })],
                  ns: None,
                }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: HandlerName(name="addItem", ns=Input), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("removeInItemsAt"), args: [Bind("key")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "drop",
          handler: {
            handler: HandlerName(name="onDropOnItem", ns=Input),
            args: [Bind("key"), Name("dragInfo")],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="[data-draggingover=\"json-kv-item\"] {\n  border-top: 4rem solid transparent;\n  transition: 0.15s border-top ease;\n}",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn json_object_views() -> Map[String, @anode.View] {
  { "main": json_object_main_view_ir() }
}
