// GENERATED by `tutuca gen-views` from pseudo_x.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views pseudo_x.html --name PseudoX --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the TableRow component.
///
/// Declared as `interface table-row` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct TableRowState {
  name : String
  qty : Int
  price : Double
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..TableRowState::zero(), label: "Counter" }
pub fn TableRowState::zero() -> TableRowState {
  TableRowState::{ name: "", qty: 0, price: 0.0 }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for TableRowState with fn encode(s) {
  { "name": Str(s.name), "qty": Num(s.qty.to_double()), "price": Num(s.price) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for TableRowState with fn decode(f) {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("qty") is Some(Num(t1_qty)) else { return None }
  let v_qty = t1_qty.to_int()
  guard f.get("price") is Some(Num(v_price)) else { return None }
  Some(TableRowState::{ name: v_name, qty: v_qty, price: v_price })
}

///|
/// The file-level style — pass as `common_style~`.
pub let table_row_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let table_row_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for TableRowState with fn schema() {
  @component.SchemaInfo::new(
    name="TableRow",
    fingerprint="78cde2ca3599de8c",
    fields=[
      @component.FieldInfo::new("name", TyText),
      @component.FieldInfo::new("qty", TyInt),
      @component.FieldInfo::new("price", TyFloat),
    ],
    view_names=["main"],
  )
}

///|
/// The state of the ItemTable component.
///
/// Declared as `interface item-table` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ItemTableState {
  rows : Array[@tutuca.Value]
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ItemTableState::zero(), label: "Counter" }
pub fn ItemTableState::zero() -> ItemTableState {
  ItemTableState::{ rows: [] }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ItemTableState with fn encode(s) {
  { "rows": (List(s.rows.map(x => x)) : @tutuca.Value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ItemTableState with fn decode(f) {
  guard f.get("rows") is Some(t1_rows) else { return None }
  guard t1_rows is List(u1_rows) else { return None }
  let v_rows : Array[@tutuca.Value] = []
  for e1_rows in u1_rows {
    let x1_rows = e1_rows
    v_rows.push(x1_rows)
  }
  Some(ItemTableState::{ rows: v_rows })
}

///|
/// The file-level style — pass as `common_style~`.
pub let item_table_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let item_table_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ItemTableState with fn schema() {
  @component.SchemaInfo::new(
    name="ItemTable",
    fingerprint="413b1cfe818d4bc7",
    fields=[@component.FieldInfo::new("rows", TyList(TyAny))],
    view_names=["main"],
  )
}

///|
/// The state of the SelectOption component.
///
/// Declared as `interface select-option` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct SelectOptionState {
  value : String
  label : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SelectOptionState::zero(), label: "Counter" }
pub fn SelectOptionState::zero() -> SelectOptionState {
  SelectOptionState::{ value: "", label: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SelectOptionState with fn encode(s) {
  { "value": Str(s.value), "label": Str(s.label) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SelectOptionState with fn decode(f) {
  guard f.get("value") is Some(Str(v_value)) else { return None }
  guard f.get("label") is Some(Str(v_label)) else { return None }
  Some(SelectOptionState::{ value: v_value, label: v_label })
}

///|
/// The file-level style — pass as `common_style~`.
pub let select_option_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let select_option_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SelectOptionState with fn schema() {
  @component.SchemaInfo::new(
    name="SelectOption",
    fingerprint="07ec6c7cf74c56fc",
    fields=[
      @component.FieldInfo::new("value", TyText),
      @component.FieldInfo::new("label", TyText),
    ],
    view_names=["main"],
  )
}

///|
/// The state of the ItemSelect component.
///
/// Declared as `interface item-select` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ItemSelectState {
  options : Array[@tutuca.Value]
  current : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ItemSelectState::zero(), label: "Counter" }
pub fn ItemSelectState::zero() -> ItemSelectState {
  ItemSelectState::{ options: [], current: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ItemSelectState with fn encode(s) {
  {
    "options": (List(s.options.map(x => x)) : @tutuca.Value),
    "current": Str(s.current),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ItemSelectState with fn decode(f) {
  guard f.get("options") is Some(t1_options) else { return None }
  guard t1_options is List(u1_options) else { return None }
  let v_options : Array[@tutuca.Value] = []
  for e1_options in u1_options {
    let x1_options = e1_options
    v_options.push(x1_options)
  }
  guard f.get("current") is Some(Str(v_current)) else { return None }
  Some(ItemSelectState::{ options: v_options, current: v_current })
}

///|
/// The file-level style — pass as `common_style~`.
pub let item_select_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let item_select_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ItemSelectInput {
  SetCurrent
} derive(Eq, @debug.Debug)

///|
pub fn ItemSelectInput::name(self : ItemSelectInput) -> String {
  match self {
    SetCurrent => "setCurrent"
  }
}

///|
pub fn ItemSelectInput::parse(s : String) -> ItemSelectInput? {
  match s {
    "setCurrent" => Some(SetCurrent)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to pseudo_x.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ItemSelectMsg {
  /// `@on.input="setCurrent …"`
  SetCurrent(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ItemSelectMsg::from_dispatch(d : @component.Dispatch) -> ItemSelectMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setCurrent", [Str(a0)]) => SetCurrent(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ItemSelectState with fn schema() {
  @component.SchemaInfo::new(
    name="ItemSelect",
    fingerprint="c0c9027a3ae072e5",
    fields=[
      @component.FieldInfo::new("options", TyList(TyAny)),
      @component.FieldInfo::new("current", TyText),
    ],
    inputs=["setCurrent"],
    view_names=["main"],
  )
}

///|
/// The state of the PseudoXDemo component.
///
/// Declared as `interface pseudo-x-demo` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct PseudoXDemoState {}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..PseudoXDemoState::zero(), label: "Counter" }
pub fn PseudoXDemoState::zero() -> PseudoXDemoState {
  PseudoXDemoState::{  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for PseudoXDemoState with fn encode(_s) {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for PseudoXDemoState with fn decode(_f) {
  Some(PseudoXDemoState::{  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let pseudo_x_demo_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let pseudo_x_demo_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for PseudoXDemoState with fn schema() {
  @component.SchemaInfo::new(
    name="PseudoXDemo",
    fingerprint="32abcf05a4118ab4",
    fields=[
      @component.FieldInfo::new("table", TyComp(Some("ItemTable"))),
      @component.FieldInfo::new("select", TyComp(Some("ItemSelect"))),
    ],
    view_names=["main"],
  )
}
