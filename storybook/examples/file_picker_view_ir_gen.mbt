// GENERATED by `tutuca gen-views` from file_picker.html — DO NOT EDIT.
//
// The views of FilePicker, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing file_picker_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn file_picker_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h("label", [@anode.attr("class", "flex flex-col gap-1")], [
        @anode.h("span", [], [@anode.text("Pick a file")]),
        @anode.text(" "),
        @anode.h(
          "input",
          [
            @anode.attr("type", "file"),
            @anode.attr("class", "file-input"),
            @anode.eid(0),
          ],
          [],
        ),
      ]),
      @anode.text(" "),
      @anode.show(
        Field("hasFile"),
        @anode.h("table", [@anode.attr("class", "table")], [
          @anode.h("tbody", [], [
            @anode.h("tr", [], [
              @anode.h("th", [], [@anode.text("Name")]),
              @anode.h("td", [@anode.attr("class", "fname")], [
                @anode.dyn_text(Field("name")),
              ]),
            ]),
            @anode.h("tr", [], [
              @anode.h("th", [], [@anode.text("Size")]),
              @anode.h("td", [@anode.attr("class", "fsize")], [
                @anode.dyn_text(Method("sizeLabel")),
              ]),
            ]),
            @anode.h("tr", [], [
              @anode.h("th", [], [@anode.text("Type")]),
              @anode.h("td", [@anode.attr("class", "ftype")], [
                @anode.dyn_text(Method("typeLabel")),
              ]),
            ]),
            @anode.h("tr", [], [
              @anode.h("th", [], [@anode.text("Last modified")]),
              @anode.h("td", [@anode.attr("class", "fmodified")], [
                @anode.dyn_text(Method("lastModifiedLabel")),
              ]),
            ]),
          ]),
        ]),
      ),
      @anode.text(" "),
      @anode.hide(
        Field("hasFile"),
        @anode.h("p", [@anode.attr("class", "empty")], [
          @anode.text("No file selected yet — pick one above."),
        ]),
      ),
    ]),
    [
      [
        @anode.on("change", HandlerName(name="onPickFile", ns=Input), args=[
          Name("value"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn file_picker_views() -> Map[String, @anode.View] {
  { "main": file_picker_main_view_ir() }
}

///|
/// The FilePicker component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
fn file_picker_component(
  name? : String = "FilePicker",
  views? : Map[String, @anode.View] = file_picker_views(),
  init? : FilePickerState = FilePickerState::zero(),
  common_style? : String = file_picker_common_style,
  global_style? : String = file_picker_global_style,
  update? : (FilePickerState, @component.Dispatch, &@tutuca.Ctx) -> FilePickerState?,
  compute? : (FilePickerMethod) -> ((FilePickerState, Array[@tutuca.Value]) -> @tutuca.Value)?,
  swap? : (FilePickerInput) -> ((
    FilePickerState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) => file_picker_compute(f)
      None => Map([])
    },
    swap=match swap {
      Some(f) => file_picker_swap(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
    encode=file_picker_encode,
    decode=file_picker_decode,
    schema=file_picker_schema(),
  )
}
