// GENERATED by `tutuca gen-views` from file_picker.html — DO NOT EDIT.
//
// The views of FilePicker, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing file_picker_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn file_picker_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "label",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-1") }),
        childs: [
          Dom({
            tag: "span",
            attrs: ConstAttrs(Map([])),
            childs: [Text({ val: "Pick a file" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "input",
            attrs: ConstAttrs({
              "type": LStr("file"),
              "class": LStr("file-input"),
              "data-eid": LNum(0.0),
            }),
            childs: [],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Field("hasFile"),
        node: Dom({
          tag: "table",
          attrs: ConstAttrs({ "class": LStr("table") }),
          childs: [
            Dom({
              tag: "tbody",
              attrs: ConstAttrs(Map([])),
              childs: [
                Dom({
                  tag: "tr",
                  attrs: ConstAttrs(Map([])),
                  childs: [
                    Dom({
                      tag: "th",
                      attrs: ConstAttrs(Map([])),
                      childs: [Text({ val: "Name" })],
                      ns: None,
                    }),
                    Dom({
                      tag: "td",
                      attrs: ConstAttrs({ "class": LStr("fname") }),
                      childs: [
                        RenderText({ node_id: None, val: Field("name") }),
                      ],
                      ns: None,
                    }),
                  ],
                  ns: None,
                }),
                Dom({
                  tag: "tr",
                  attrs: ConstAttrs(Map([])),
                  childs: [
                    Dom({
                      tag: "th",
                      attrs: ConstAttrs(Map([])),
                      childs: [Text({ val: "Size" })],
                      ns: None,
                    }),
                    Dom({
                      tag: "td",
                      attrs: ConstAttrs({ "class": LStr("fsize") }),
                      childs: [
                        RenderText({ node_id: None, val: Method("sizeLabel") }),
                      ],
                      ns: None,
                    }),
                  ],
                  ns: None,
                }),
                Dom({
                  tag: "tr",
                  attrs: ConstAttrs(Map([])),
                  childs: [
                    Dom({
                      tag: "th",
                      attrs: ConstAttrs(Map([])),
                      childs: [Text({ val: "Type" })],
                      ns: None,
                    }),
                    Dom({
                      tag: "td",
                      attrs: ConstAttrs({ "class": LStr("ftype") }),
                      childs: [
                        RenderText({ node_id: None, val: Method("typeLabel") }),
                      ],
                      ns: None,
                    }),
                  ],
                  ns: None,
                }),
                Dom({
                  tag: "tr",
                  attrs: ConstAttrs(Map([])),
                  childs: [
                    Dom({
                      tag: "th",
                      attrs: ConstAttrs(Map([])),
                      childs: [Text({ val: "Last modified" })],
                      ns: None,
                    }),
                    Dom({
                      tag: "td",
                      attrs: ConstAttrs({ "class": LStr("fmodified") }),
                      childs: [
                        RenderText({
                          node_id: None,
                          val: Method("lastModifiedLabel"),
                        }),
                      ],
                      ns: None,
                    }),
                  ],
                  ns: None,
                }),
              ],
              ns: None,
            }),
          ],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Hide({
        node_id: None,
        val: Field("hasFile"),
        node: Dom({
          tag: "p",
          attrs: ConstAttrs({ "class": LStr("empty") }),
          childs: [Text({ val: "No file selected yet — pick one above." })],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "change",
          handler: {
            handler: HandlerName(name="onPickFile", ns=Input),
            args: [Name("value")],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn file_picker_views() -> Map[String, @anode.View] {
  { "main": file_picker_main_view_ir() }
}
