// GENERATED by `tutuca gen-views` from conditionals.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views conditionals.html --name Conditionals --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the TrafficLight component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct TrafficLightState {
  lightIndex : Int
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..TrafficLightState::zero(), label: "Counter" }
pub fn TrafficLightState::zero() -> TrafficLightState {
  { lightIndex: 0, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for TrafficLightState with fn encode(s) {
  { "lightIndex": Num(s.lightIndex.to_double()) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for TrafficLightState with fn decode(f) {
  guard f.get("lightIndex") is Some(Num(t1_lightIndex)) else { return None }
  let v_lightIndex = t1_lightIndex.to_int()
  Some({ lightIndex: v_lightIndex, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Every addressed name: the `message` variant the state block declares,
/// plus every bare `@on` handler the views write. One bucket holds them
/// however they were sent, so one type covers it — where the two overlap
/// the schema's declared payload wins, and a view whose call sites
/// disagree with it is a build error.
pub(all) enum TrafficLightMsg {
  NextLight
  /// A message the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn TrafficLightMsg::from_dispatch(
  d : @component.Dispatch,
) -> TrafficLightMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("nextLight", []) => NextLight
      _ => {
        match name {
          "nextLight" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn TrafficLightMsg::to_dispatch(
  self : TrafficLightMsg,
) -> (String, Array[@tutuca.Value]) {
  match self {
    NextLight => ("nextLight", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn TrafficLightMsg::send(
  self : TrafficLightMsg,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let traffic_light_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let traffic_light_global_style : String = ""

///|
/// Every `$name` the views call, plus the block's own `pred` / `compute` names — the bucket `compute~` keys.
pub(all) enum TrafficLightCompute {
  Light
} derive(Eq, @debug.Debug)

///|
pub fn TrafficLightCompute::name(self : TrafficLightCompute) -> String {
  match self {
    Light => "light"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for TrafficLightState with fn schema() {
  @component.SchemaInfo::new(
    name="TrafficLight",
    fingerprint="d35ce90bf3d27383",
    fields=[
      @component.FieldInfo::new("lightIndex", TyInt(width=32, signed=true)),
    ],
    view_handlers=["nextLight"],
    receives=["nextLight"],
    methods=["light"],
    domains=[{ field: "lightIndex", domain: DBetween(lo=DLit(0), hi=DLit(2)), }],
    view_names=["main"],
  )
}

///|
/// The state of the TabbedUI component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct TabbedUIState {
  tab : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..TabbedUIState::zero(), label: "Counter" }
pub fn TabbedUIState::zero() -> TabbedUIState {
  { tab: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for TabbedUIState with fn encode(s) {
  { "tab": Str(s.tab) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for TabbedUIState with fn decode(f) {
  guard f.get("tab") is Some(Str(v_tab)) else { return None }
  Some({ tab: v_tab, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let tabbed_u_i_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let tabbed_u_i_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for TabbedUIState with fn schema() {
  @component.SchemaInfo::new(
    name="TabbedUI",
    fingerprint="e232ce34c046543a",
    fields=[@component.FieldInfo::new("tab", TyText)],
    domains=[
      {
        field: "tab",
        domain: DOneOf([Str("overview"), Str("features"), Str("pricing")]),
      },
    ],
    ids=["t-features", "t-overview", "t-pricing"],
    view_names=["main"],
  )
}

///|
/// The state of the ListAndFilter component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct ListAndFilterState {
  items : Array[String]
  query : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   initial={ ..ListAndFilterState::zero(), label: "Counter" }
pub fn ListAndFilterState::zero() -> ListAndFilterState {
  { items: [], query: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ListAndFilterState with fn encode(s) {
  {
    "items": (List(s.items.map(x => Str(x))) : @tutuca.Value),
    "query": Str(s.query),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ListAndFilterState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[String] = []
  for e1_items in u1_items {
    guard e1_items is Str(x1_items) else { return None }
    v_items.push(x1_items)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  Some({ items: v_items, query: v_query, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let list_and_filter_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let list_and_filter_global_style : String = ""

///|
/// Every `@when` handler the views name.
pub(all) enum ListAndFilterWhen {
  FilterItem
} derive(Eq, @debug.Debug)

///|
pub fn ListAndFilterWhen::name(self : ListAndFilterWhen) -> String {
  match self {
    FilterItem => "filterItem"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ListAndFilterState with fn schema() {
  @component.SchemaInfo::new(
    name="ListAndFilter",
    fingerprint="afaa7379bfb55be9",
    fields=[
      @component.FieldInfo::new("items", TyList(TyText)),
      @component.FieldInfo::new("query", TyText),
    ],
    view_names=["main"],
  )
}
