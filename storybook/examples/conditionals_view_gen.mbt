// GENERATED by `tutuca gen-views` from conditionals.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views conditionals.html --name Conditionals --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the TrafficLight component.
///
/// Declared as `interface traffic-light` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct TrafficLightState {
  lightIndex : Int
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..TrafficLightState::zero(), label: "Counter" }
fn TrafficLightState::zero() -> TrafficLightState {
  TrafficLightState::{ lightIndex: 0 }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn traffic_light_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(TrafficLightState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the TrafficLight state declares — typed field names for tests and inspectors.
pub(all) enum TrafficLightField {
  LightIndex
} derive(Eq, @debug.Debug)

///|
pub fn TrafficLightField::name(self : TrafficLightField) -> String {
  match self {
    LightIndex => "lightIndex"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn TrafficLightField::kind(
  self : TrafficLightField,
) -> @component.FieldKind {
  match self {
    LightIndex => @component.FInt
  }
}

///|
/// Every field, in declaration order.
pub let traffic_light_field_names : Array[String] = ["lightIndex"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let traffic_light_schema_fingerprint : String = "641f8260005d94a3"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn traffic_light_encode(s : TrafficLightState) -> Map[String, @tutuca.Value] {
  { "lightIndex": Num(s.lightIndex.to_double()) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn traffic_light_decode(f : Map[String, @tutuca.Value]) -> TrafficLightState? {
  guard f.get("lightIndex") is Some(Num(t1_lightIndex)) else { return None }
  let v_lightIndex = t1_lightIndex.to_int()
  Some(TrafficLightState::{ lightIndex: v_lightIndex })
}

///|
/// The views declared in conditionals.html.
pub(all) enum TrafficLightView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn TrafficLightView::name(self : TrafficLightView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let traffic_light_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let traffic_light_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum TrafficLightInput {
  NextLight
} derive(Eq, @debug.Debug)

///|
pub fn TrafficLightInput::name(self : TrafficLightInput) -> String {
  match self {
    NextLight => "nextLight"
  }
}

///|
pub fn TrafficLightInput::parse(s : String) -> TrafficLightInput? {
  match s {
    "nextLight" => Some(NextLight)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to conditionals.html breaks every `match` over this type
/// until it is handled.
pub(all) enum TrafficLightMsg {
  /// `@on.click="nextLight …"`
  NextLight
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn TrafficLightMsg::from_dispatch(
  d : @component.Dispatch,
) -> TrafficLightMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("nextLight", []) => NextLight
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum TrafficLightMethod {
  Light
} derive(Eq, @debug.Debug)

///|
pub fn TrafficLightMethod::name(self : TrafficLightMethod) -> String {
  match self {
    Light => "light"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] traffic_light_compute(
  f : (TrafficLightMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([Light] : Array[TrafficLightMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] traffic_light_swap(
  f : (TrafficLightMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([Light] : Array[TrafficLightMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn traffic_light_schema() -> @component.SchemaInfo {
  {
    fingerprint: "641f8260005d94a3",
    fields: [
      {
        name: "lightIndex",
        ty: "s32",
        kind: @component.FInt,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: ["nextLight"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: ["light"],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the TabbedUI component.
///
/// Declared as `interface tabbed-u-i` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct TabbedUIState {
  tab : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..TabbedUIState::zero(), label: "Counter" }
fn TabbedUIState::zero() -> TabbedUIState {
  TabbedUIState::{ tab: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn tabbed_u_i_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(TabbedUIState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the TabbedUI state declares — typed field names for tests and inspectors.
pub(all) enum TabbedUIField {
  Tab
} derive(Eq, @debug.Debug)

///|
pub fn TabbedUIField::name(self : TabbedUIField) -> String {
  match self {
    Tab => "tab"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn TabbedUIField::kind(self : TabbedUIField) -> @component.FieldKind {
  match self {
    Tab => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let tabbed_u_i_field_names : Array[String] = ["tab"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let tabbed_u_i_schema_fingerprint : String = "91f1c8c673117b38"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn tabbed_u_i_encode(s : TabbedUIState) -> Map[String, @tutuca.Value] {
  { "tab": Str(s.tab) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn tabbed_u_i_decode(f : Map[String, @tutuca.Value]) -> TabbedUIState? {
  guard f.get("tab") is Some(Str(v_tab)) else { return None }
  Some(TabbedUIState::{ tab: v_tab })
}

///|
/// The views declared in conditionals.html.
pub(all) enum TabbedUIView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn TabbedUIView::name(self : TabbedUIView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let tabbed_u_i_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let tabbed_u_i_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum TabbedUIInput {
  SetTab
} derive(Eq, @debug.Debug)

///|
pub fn TabbedUIInput::name(self : TabbedUIInput) -> String {
  match self {
    SetTab => "setTab"
  }
}

///|
pub fn TabbedUIInput::parse(s : String) -> TabbedUIInput? {
  match s {
    "setTab" => Some(SetTab)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to conditionals.html breaks every `match` over this type
/// until it is handled.
pub(all) enum TabbedUIMsg {
  /// `@on.click="setTab …"`
  SetTab(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn TabbedUIMsg::from_dispatch(d : @component.Dispatch) -> TabbedUIMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setTab", [Str(a0)]) => SetTab(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Constant `id` attributes in the views — typed selectors for tests.
pub(all) enum TabbedUIId {
  TFeatures
  TOverview
  TPricing
} derive(Eq, @debug.Debug)

///|
pub fn TabbedUIId::id(self : TabbedUIId) -> String {
  match self {
    TFeatures => "t-features"
    TOverview => "t-overview"
    TPricing => "t-pricing"
  }
}

///|
/// The `#id` CSS selector for this element.
pub fn TabbedUIId::selector(self : TabbedUIId) -> String {
  "#" + self.id()
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn tabbed_u_i_schema() -> @component.SchemaInfo {
  {
    fingerprint: "91f1c8c673117b38",
    fields: [
      {
        name: "tab",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: ["setTab"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: ["t-features", "t-overview", "t-pricing"],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the ListAndFilter component.
///
/// Declared as `interface list-and-filter` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct ListAndFilterState {
  items : Array[@tutuca.Value]
  query : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ListAndFilterState::zero(), label: "Counter" }
fn ListAndFilterState::zero() -> ListAndFilterState {
  ListAndFilterState::{ items: [], query: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn list_and_filter_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(ListAndFilterState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the ListAndFilter state declares — typed field names for tests and inspectors.
pub(all) enum ListAndFilterField {
  Items
  Query
} derive(Eq, @debug.Debug)

///|
pub fn ListAndFilterField::name(self : ListAndFilterField) -> String {
  match self {
    Items => "items"
    Query => "query"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn ListAndFilterField::kind(
  self : ListAndFilterField,
) -> @component.FieldKind {
  match self {
    Items => @component.FList
    Query => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let list_and_filter_field_names : Array[String] = ["items", "query"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let list_and_filter_schema_fingerprint : String = "80f3b6c5cd0e5b4d"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn list_and_filter_encode(s : ListAndFilterState) -> Map[String, @tutuca.Value] {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "query": Str(s.query),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn list_and_filter_decode(
  f : Map[String, @tutuca.Value],
) -> ListAndFilterState? {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  Some(ListAndFilterState::{ items: v_items, query: v_query })
}

///|
/// The views declared in conditionals.html.
pub(all) enum ListAndFilterView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ListAndFilterView::name(self : ListAndFilterView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let list_and_filter_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let list_and_filter_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ListAndFilterInput {
  ResetQuery
  SetQuery
} derive(Eq, @debug.Debug)

///|
pub fn ListAndFilterInput::name(self : ListAndFilterInput) -> String {
  match self {
    ResetQuery => "resetQuery"
    SetQuery => "setQuery"
  }
}

///|
pub fn ListAndFilterInput::parse(s : String) -> ListAndFilterInput? {
  match s {
    "resetQuery" => Some(ResetQuery)
    "setQuery" => Some(SetQuery)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to conditionals.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ListAndFilterMsg {
  /// `@on.keydown="resetQuery …"`
  ResetQuery
  /// `@on.input="setQuery …"`
  SetQuery(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ListAndFilterMsg::from_dispatch(
  d : @component.Dispatch,
) -> ListAndFilterMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("resetQuery", []) => ResetQuery
      ("setQuery", [Str(a0)]) => SetQuery(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn list_and_filter_schema() -> @component.SchemaInfo {
  {
    fingerprint: "80f3b6c5cd0e5b4d",
    fields: [
      {
        name: "items",
        ty: "list<any>",
        kind: @component.FList,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "query",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: ["resetQuery", "setQuery"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}
