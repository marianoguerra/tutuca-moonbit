// GENERATED by `tutuca gen-views` from conditionals.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views conditionals.html --name Conditionals --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the TrafficLight component.
///
/// Declared as `interface traffic-light` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct TrafficLightState {
  lightIndex : Int
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..TrafficLightState::zero(), label: "Counter" }
pub fn TrafficLightState::zero() -> TrafficLightState {
  TrafficLightState::{ lightIndex: 0 }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for TrafficLightState with fn encode(s) {
  { "lightIndex": Num(s.lightIndex.to_double()) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for TrafficLightState with fn decode(f) {
  guard f.get("lightIndex") is Some(Num(t1_lightIndex)) else { return None }
  let v_lightIndex = t1_lightIndex.to_int()
  Some(TrafficLightState::{ lightIndex: v_lightIndex })
}

///|
/// The file-level style — pass as `common_style~`.
pub let traffic_light_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let traffic_light_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum TrafficLightInput {
  NextLight
} derive(Eq, @debug.Debug)

///|
pub fn TrafficLightInput::name(self : TrafficLightInput) -> String {
  match self {
    NextLight => "nextLight"
  }
}

///|
pub fn TrafficLightInput::parse(s : String) -> TrafficLightInput? {
  match s {
    "nextLight" => Some(NextLight)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to conditionals.html breaks every `match` over this type
/// until it is handled.
pub(all) enum TrafficLightMsg {
  /// `@on.click="nextLight …"`
  NextLight
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn TrafficLightMsg::from_dispatch(
  d : @component.Dispatch,
) -> TrafficLightMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("nextLight", []) => NextLight
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] traffic_light_swap(
  f : (TrafficLightInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([NextLight] : Array[TrafficLightInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `$name` the views call.
pub(all) enum TrafficLightMethod {
  Light
} derive(Eq, @debug.Debug)

///|
pub fn TrafficLightMethod::name(self : TrafficLightMethod) -> String {
  match self {
    Light => "light"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] traffic_light_compute(
  f : (TrafficLightMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([Light] : Array[TrafficLightMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for TrafficLightState with fn schema() {
  @component.SchemaInfo::new(
    name="TrafficLight",
    fingerprint="641f8260005d94a3",
    fields=[@component.FieldInfo::new("lightIndex", "s32", FInt)],
    inputs=["nextLight"],
    methods=["light"],
    view_names=["main"],
  )
}

///|
/// The state of the TabbedUI component.
///
/// Declared as `interface tabbed-u-i` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct TabbedUIState {
  tab : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..TabbedUIState::zero(), label: "Counter" }
pub fn TabbedUIState::zero() -> TabbedUIState {
  TabbedUIState::{ tab: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for TabbedUIState with fn encode(s) {
  { "tab": Str(s.tab) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for TabbedUIState with fn decode(f) {
  guard f.get("tab") is Some(Str(v_tab)) else { return None }
  Some(TabbedUIState::{ tab: v_tab })
}

///|
/// The file-level style — pass as `common_style~`.
pub let tabbed_u_i_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let tabbed_u_i_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum TabbedUIInput {
  SetTab
} derive(Eq, @debug.Debug)

///|
pub fn TabbedUIInput::name(self : TabbedUIInput) -> String {
  match self {
    SetTab => "setTab"
  }
}

///|
pub fn TabbedUIInput::parse(s : String) -> TabbedUIInput? {
  match s {
    "setTab" => Some(SetTab)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to conditionals.html breaks every `match` over this type
/// until it is handled.
pub(all) enum TabbedUIMsg {
  /// `@on.click="setTab …"`
  SetTab(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn TabbedUIMsg::from_dispatch(d : @component.Dispatch) -> TabbedUIMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setTab", [Str(a0)]) => SetTab(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] tabbed_u_i_swap(
  f : (TabbedUIInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetTab] : Array[TabbedUIInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for TabbedUIState with fn schema() {
  @component.SchemaInfo::new(
    name="TabbedUI",
    fingerprint="91f1c8c673117b38",
    fields=[@component.FieldInfo::new("tab", "string", FText)],
    inputs=["setTab"],
    ids=["t-features", "t-overview", "t-pricing"],
    view_names=["main"],
  )
}

///|
/// The state of the ListAndFilter component.
///
/// Declared as `interface list-and-filter` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ListAndFilterState {
  items : Array[@tutuca.Value]
  query : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ListAndFilterState::zero(), label: "Counter" }
pub fn ListAndFilterState::zero() -> ListAndFilterState {
  ListAndFilterState::{ items: [], query: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ListAndFilterState with fn encode(s) {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "query": Str(s.query),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ListAndFilterState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  Some(ListAndFilterState::{ items: v_items, query: v_query })
}

///|
/// The file-level style — pass as `common_style~`.
pub let list_and_filter_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let list_and_filter_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ListAndFilterInput {
  ResetQuery
  SetQuery
} derive(Eq, @debug.Debug)

///|
pub fn ListAndFilterInput::name(self : ListAndFilterInput) -> String {
  match self {
    ResetQuery => "resetQuery"
    SetQuery => "setQuery"
  }
}

///|
pub fn ListAndFilterInput::parse(s : String) -> ListAndFilterInput? {
  match s {
    "resetQuery" => Some(ResetQuery)
    "setQuery" => Some(SetQuery)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to conditionals.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ListAndFilterMsg {
  /// `@on.keydown="resetQuery …"`
  ResetQuery
  /// `@on.input="setQuery …"`
  SetQuery(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ListAndFilterMsg::from_dispatch(
  d : @component.Dispatch,
) -> ListAndFilterMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("resetQuery", []) => ResetQuery
      ("setQuery", [Str(a0)]) => SetQuery(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_and_filter_swap(
  f : (ListAndFilterInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListAndFilterInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `@when` handler the views name.
pub(all) enum ListAndFilterWhen {
  FilterItem
} derive(Eq, @debug.Debug)

///|
pub fn ListAndFilterWhen::name(self : ListAndFilterWhen) -> String {
  match self {
    FilterItem => "filterItem"
  }
}

///|
/// Build `when~` from an exhaustive match over the names the views use.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_and_filter_when(
  f : (ListAndFilterWhen) -> ((S, @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Bool)?,
) -> Map[String, (S, @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Bool] {
  let out : Map[
    String,
    (S, @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Bool,
  ] = Map([])
  for m in ([FilterItem] : Array[ListAndFilterWhen]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ListAndFilterState with fn schema() {
  @component.SchemaInfo::new(
    name="ListAndFilter",
    fingerprint="80f3b6c5cd0e5b4d",
    fields=[
      @component.FieldInfo::new("items", "list<any>", FList, elem="any"),
      @component.FieldInfo::new("query", "string", FText),
    ],
    inputs=["resetQuery", "setQuery"],
    view_names=["main"],
  )
}
