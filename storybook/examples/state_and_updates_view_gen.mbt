// GENERATED by `tutuca gen-views` from state_and_updates.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views state_and_updates.html --name StateAndUpdates --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the AttributeBinding component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct AttributeBindingState {
  str : String
  num : Int
  bool : Bool
  notSet : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..AttributeBindingState::zero(), label: "Counter" }
pub fn AttributeBindingState::zero() -> AttributeBindingState {
  { str: "", num: 0, bool: false, notSet: Null, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for AttributeBindingState with fn encode(s) {
  {
    "str": Str(s.str),
    "num": Num(s.num.to_double()),
    "bool": Bool(s.bool),
    "notSet": s.notSet,
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for AttributeBindingState with fn decode(f) {
  guard f.get("str") is Some(Str(v_str)) else { return None }
  guard f.get("num") is Some(Num(t1_num)) else { return None }
  let v_num = t1_num.to_int()
  guard f.get("bool") is Some(Bool(v_bool)) else { return None }
  let v_notSet = f.get("notSet").unwrap_or(Null)
  Some({ str: v_str, num: v_num, bool: v_bool, notSet: v_notSet, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum AttributeBindingReceive {
  SetRawNumber(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn AttributeBindingReceive::from_dispatch(
  d : @component.Dispatch,
) -> AttributeBindingReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("setRawNumber", [Str(a0)]) => SetRawNumber(a0)
      _ => {
        match name {
          "setRawNumber" => @tutuca.bad_payload(name, "String", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn AttributeBindingReceive::to_dispatch(
  self : AttributeBindingReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    SetRawNumber(a0) => ("setRawNumber", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn AttributeBindingReceive::send(
  self : AttributeBindingReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let attribute_binding_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let attribute_binding_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum AttributeBindingInput {
  SetRawNumber
} derive(Eq, @debug.Debug)

///|
pub fn AttributeBindingInput::name(self : AttributeBindingInput) -> String {
  match self {
    SetRawNumber => "setRawNumber"
  }
}

///|
pub fn AttributeBindingInput::parse(s : String) -> AttributeBindingInput? {
  match s {
    "setRawNumber" => Some(SetRawNumber)
    _ => None
  }
}

///|
/// The VIEW-named half of @component.Dispatch's `Receive` arm, decoded.
/// Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to state_and_updates.html breaks every `match` over this type
/// until it is handled.
pub(all) enum AttributeBindingMsg {
  /// `@on.input="setRawNumber …"`
  SetRawNumber(String)
  /// A dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for an `Intent` — a routed name is raised by component code and
/// never written in a view, so the views cannot type it. A `Receive` the
/// views do not name decodes to `Unknown`, which is what the sibling
/// `<T>Receive` (the schema's half of the same bucket) is for.
pub fn AttributeBindingMsg::from_dispatch(
  d : @component.Dispatch,
) -> AttributeBindingMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("setRawNumber", [Str(a0)]) => SetRawNumber(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for AttributeBindingState with fn schema() {
  @component.SchemaInfo::new(
    name="AttributeBinding",
    fingerprint="1e94422931fa608c",
    fields=[
      @component.FieldInfo::new("str", TyText),
      @component.FieldInfo::new("num", TyInt(width=32, signed=true)),
      @component.FieldInfo::new("bool", TyBool),
      @component.FieldInfo::new("notSet", TyAny),
    ],
    view_handlers=["setRawNumber"],
    view_names=["main"],
  )
}

///|
/// The state of the ConditionalAttributes component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct ConditionalAttributesState {
  isActive : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ConditionalAttributesState::zero(), label: "Counter" }
pub fn ConditionalAttributesState::zero() -> ConditionalAttributesState {
  { isActive: false, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ConditionalAttributesState with fn encode(s) {
  { "isActive": Bool(s.isActive) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ConditionalAttributesState with fn decode(f) {
  guard f.get("isActive") is Some(Bool(v_isActive)) else { return None }
  Some({ isActive: v_isActive, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let conditional_attributes_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let conditional_attributes_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ConditionalAttributesState with fn schema() {
  @component.SchemaInfo::new(
    name="ConditionalAttributes",
    fingerprint="3a518a385250bb4d",
    fields=[@component.FieldInfo::new("isActive", TyBool)],
    view_names=["main"],
  )
}

///|
/// The state of the EventModifiers component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct EventModifiersState {
  query : String
  lastSentSearch : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..EventModifiersState::zero(), label: "Counter" }
pub fn EventModifiersState::zero() -> EventModifiersState {
  { query: "", lastSentSearch: Null, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for EventModifiersState with fn encode(s) {
  { "query": Str(s.query), "lastSentSearch": s.lastSentSearch }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for EventModifiersState with fn decode(f) {
  guard f.get("query") is Some(Str(v_query)) else { return None }
  let v_lastSentSearch = f.get("lastSentSearch").unwrap_or(Null)
  Some({ query: v_query, lastSentSearch: v_lastSentSearch, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum EventModifiersReceive {
  OnInput(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn EventModifiersReceive::from_dispatch(
  d : @component.Dispatch,
) -> EventModifiersReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("onInput", [Str(a0)]) => OnInput(a0)
      _ => {
        match name {
          "onInput" => @tutuca.bad_payload(name, "String", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn EventModifiersReceive::to_dispatch(
  self : EventModifiersReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    OnInput(a0) => ("onInput", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn EventModifiersReceive::send(
  self : EventModifiersReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let event_modifiers_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let event_modifiers_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum EventModifiersInput {
  OnInput
} derive(Eq, @debug.Debug)

///|
pub fn EventModifiersInput::name(self : EventModifiersInput) -> String {
  match self {
    OnInput => "onInput"
  }
}

///|
pub fn EventModifiersInput::parse(s : String) -> EventModifiersInput? {
  match s {
    "onInput" => Some(OnInput)
    _ => None
  }
}

///|
/// The VIEW-named half of @component.Dispatch's `Receive` arm, decoded.
/// Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to state_and_updates.html breaks every `match` over this type
/// until it is handled.
pub(all) enum EventModifiersMsg {
  /// `@on.input="onInput …"`
  OnInput(String)
  /// A dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for an `Intent` — a routed name is raised by component code and
/// never written in a view, so the views cannot type it. A `Receive` the
/// views do not name decodes to `Unknown`, which is what the sibling
/// `<T>Receive` (the schema's half of the same bucket) is for.
pub fn EventModifiersMsg::from_dispatch(
  d : @component.Dispatch,
) -> EventModifiersMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("onInput", [Str(a0)]) => OnInput(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for EventModifiersState with fn schema() {
  @component.SchemaInfo::new(
    name="EventModifiers",
    fingerprint="56cb6951381a404e",
    fields=[
      @component.FieldInfo::new("query", TyText),
      @component.FieldInfo::new("lastSentSearch", TyAny),
    ],
    view_handlers=["onInput"],
    view_names=["main"],
  )
}

///|
/// The state of the ShowHide component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct ShowHideState {
  isOpen : Bool
  count : Int
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ShowHideState::zero(), label: "Counter" }
pub fn ShowHideState::zero() -> ShowHideState {
  { isOpen: false, count: 0, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ShowHideState with fn encode(s) {
  { "isOpen": Bool(s.isOpen), "count": Num(s.count.to_double()) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ShowHideState with fn decode(f) {
  guard f.get("isOpen") is Some(Bool(v_isOpen)) else { return None }
  guard f.get("count") is Some(Num(t1_count)) else { return None }
  let v_count = t1_count.to_int()
  Some({ isOpen: v_isOpen, count: v_count, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum ShowHideReceive {
  IncCount
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn ShowHideReceive::from_dispatch(
  d : @component.Dispatch,
) -> ShowHideReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("incCount", []) => IncCount
      _ => {
        match name {
          "incCount" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn ShowHideReceive::to_dispatch(
  self : ShowHideReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    IncCount => ("incCount", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn ShowHideReceive::send(
  self : ShowHideReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let show_hide_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let show_hide_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ShowHideInput {
  IncCount
} derive(Eq, @debug.Debug)

///|
pub fn ShowHideInput::name(self : ShowHideInput) -> String {
  match self {
    IncCount => "incCount"
  }
}

///|
pub fn ShowHideInput::parse(s : String) -> ShowHideInput? {
  match s {
    "incCount" => Some(IncCount)
    _ => None
  }
}

///|
/// The VIEW-named half of @component.Dispatch's `Receive` arm, decoded.
/// Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to state_and_updates.html breaks every `match` over this type
/// until it is handled.
///
/// Absent, because the script block answers them: incCount.
/// `update` is consulted only for a dispatch the block did not claim, so
/// an arm for one of these could never run.
pub(all) enum ShowHideMsg {
  /// A dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for an `Intent` — a routed name is raised by component code and
/// never written in a view, so the views cannot type it. A `Receive` the
/// views do not name decodes to `Unknown`, which is what the sibling
/// `<T>Receive` (the schema's half of the same bucket) is for.
pub fn ShowHideMsg::from_dispatch(d : @component.Dispatch) -> ShowHideMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum ShowHideMethod {
  Label
} derive(Eq, @debug.Debug)

///|
pub fn ShowHideMethod::name(self : ShowHideMethod) -> String {
  match self {
    Label => "label"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ShowHideState with fn schema() {
  @component.SchemaInfo::new(
    name="ShowHide",
    fingerprint="330b85e924a25a8e",
    fields=[
      @component.FieldInfo::new("isOpen", TyBool),
      @component.FieldInfo::new("count", TyInt(width=32, signed=true)),
    ],
    view_handlers=["incCount"],
    methods=["label"],
    domains=[{ field: "count", domain: DCompare(op=DGe, rhs=DLit(0)), }],
    view_names=["main"],
  )
}
