// GENERATED by `tutuca gen-views` from dynamic.html — DO NOT EDIT.
//
// The views of SelectorEntry, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing selector_entry_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn selector_entry_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({ "class": LStr("flex gap-3"), "data-vid": LStr("main") }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(
            name="class",
            val=Const(lit=LStr("input val"), from_macro=false),
          ),
          Plain(name="value", val=Field("value")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(
            name="class",
            val=Const(lit=LStr("input lbl"), from_macro=false),
          ),
          Plain(name="value", val=Field("label")),
          ConstA(name="data-eid", val=Const(lit=LNum(1.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setValue"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setLabel"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn selector_entry_views() -> Map[String, @anode.View] {
  { "main": selector_entry_main_view_ir() }
}

///|
/// The compiled `main` view.
fn selector_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "select",
    attrs: DynAttrs([
      ConstA(name="class", val=Const(lit=LStr("select"), from_macro=false)),
      Plain(name="value", val=Field("selectedValue")),
      ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
      ConstA(name="data-vid", val=Const(lit=LStr("main"), from_macro=false)),
    ]),
    childs: [
      Each({
        node_id: 0,
        iter_info: {
          val: Dyn("entries"),
          when_val: None,
          loop_with_val: None,
          enrich_with_val: None,
        },
        node: Dom({
          tag: "option",
          attrs: DynAttrs([
            Plain(name="value", val=BindMember(name="value", prop="value")),
          ]),
          childs: [
            RenderText({
              node_id: None,
              val: BindMember(name="value", prop="label"),
            }),
          ],
          ns: None,
        }),
        from_render_each: false,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: {
            handler: Method("setSelectedValue"),
            args: [Name("value")],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn selector_views() -> Map[String, @anode.View] {
  { "main": selector_main_view_ir() }
}

///|
/// The compiled `main` view.
fn entry_editor_and_selector_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex gap-3 justify-center") }),
        childs: [Render({ node_id: 0, val: Field("selector"), view_val: None })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-soft btn-success add"),
          "data-eid": LNum(0.0),
        }),
        childs: [Text({ val: "Add Entry" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-3 w-full") }),
        childs: [
          Each({
            node_id: 2,
            iter_info: {
              val: Field("items"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "div",
              attrs: ConstAttrs({
                "class": LStr(
                  "flex gap-3 justify-center items-center w-full row",
                ),
              }),
              childs: [
                RenderIt({ node_id: 1, view_val: None }),
                Text({ val: " " }),
                Dom({
                  tag: "button",
                  attrs: ConstAttrs({
                    "class": LStr(
                      "btn btn-soft btn-sm btn-error btn-circle font-mono rm",
                    ),
                    "data-eid": LNum(1.0),
                  }),
                  childs: [Text({ val: "x" })],
                  ns: None,
                }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: {
            handler: HandlerName(name="onAddItem", ns=Input),
            args: [],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("removeInItemsAt"), args: [Bind("key")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn entry_editor_and_selector_views() -> Map[String, @anode.View] {
  { "main": entry_editor_and_selector_main_view_ir() }
}

///|
/// The compiled `main` view.
fn sheet_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("card bg-base-200 p-3 gap-2 sheet"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "span",
        attrs: ConstAttrs({ "class": LStr("text-xs opacity-60 tag") }),
        childs: [Text({ val: "Sheet (rendered via *active)" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(
            name="class",
            val=Const(lit=LStr("input input-bordered title"), from_macro=false),
          ),
          Plain(name="value", val=Field("title")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setTitle"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn sheet_views() -> Map[String, @anode.View] {
  { "main": sheet_main_view_ir() }
}

///|
/// The compiled `main` view.
fn toolbar_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr(
        "border border-dashed border-warning rounded p-3 gap-2 toolbar",
      ),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "span",
        attrs: ConstAttrs({ "class": LStr("text-xs opacity-60 tag") }),
        childs: [Text({ val: "Toolbar (consumer)" })],
        ns: None,
      }),
      Text({ val: " " }),
      Render({ node_id: 0, val: Dyn("active"), view_val: None }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn toolbar_views() -> Map[String, @anode.View] {
  { "main": toolbar_main_view_ir() }
}

///|
/// The compiled `main` view.
fn panel_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("border border-dashed border-info rounded p-3 gap-2 panel"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "span",
        attrs: ConstAttrs({ "class": LStr("text-xs opacity-60 tag") }),
        childs: [Text({ val: "Panel (intermediate)" })],
        ns: None,
      }),
      Text({ val: " " }),
      Render({ node_id: 0, val: Field("toolbar"), view_val: None }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn panel_views() -> Map[String, @anode.View] {
  { "main": panel_main_view_ir() }
}

///|
/// The compiled `main` view.
fn workspace_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3 p-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("alert") }),
        childs: [
          Dom({
            tag: "span",
            attrs: ConstAttrs(Map([])),
            childs: [
              Text({ val: "Workspace.sheet.title = " }),
              Dom({
                tag: "b",
                attrs: ConstAttrs({ "class": LStr("echo") }),
                childs: [
                  RenderText({ node_id: None, val: Method("sheetTitle") }),
                ],
                ns: None,
              }),
            ],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Render({ node_id: 0, val: Field("panel"), view_val: None }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn workspace_views() -> Map[String, @anode.View] {
  { "main": workspace_main_view_ir() }
}
