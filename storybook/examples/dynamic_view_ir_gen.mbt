// GENERATED by `tutuca gen-views` from dynamic.html — DO NOT EDIT.
//
// The views of SelectorEntry, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing selector_entry_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn selector_entry_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex gap-3")], [
      @anode.h(
        "input",
        [
          @anode.attr("class", "input val"),
          @anode.dyn_attr("value", Field("value")),
          @anode.eid(0),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h(
        "input",
        [
          @anode.attr("class", "input lbl"),
          @anode.dyn_attr("value", Field("label")),
          @anode.eid(1),
        ],
        [],
      ),
    ]),
    [
      [
        @anode.on("input", HandlerName(name="setValue", ns=Input), args=[
          Name("value"),
        ]),
      ],
      [
        @anode.on("input", HandlerName(name="setLabel", ns=Input), args=[
          Name("value"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn selector_entry_views() -> Map[String, @anode.View] {
  { "main": selector_entry_main_view_ir() }
}

///|
/// The SelectorEntry component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn selector_entry_component(
  name? : String = "SelectorEntry",
  views? : Map[String, @anode.View] = selector_entry_views(),
  init? : SelectorEntryState = SelectorEntryState::zero(),
  common_style? : String = selector_entry_common_style,
  global_style? : String = selector_entry_global_style,
  update? : (SelectorEntryState, @component.Dispatch, &@tutuca.Ctx) -> SelectorEntryState?,
  swap? : (SelectorEntryInput) -> ((
    SelectorEntryState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => selector_entry_swap(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn selector_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "select",
      [
        @anode.attr("class", "select"),
        @anode.dyn_attr("value", Field("selectedValue")),
        @anode.eid(0),
      ],
      [
        @anode.each(
          0,
          Dyn("entries"),
          @anode.h(
            "option",
            [@anode.dyn_attr("value", BindMember(name="value", prop="value"))],
            [@anode.dyn_text(BindMember(name="value", prop="label"))],
          ),
        ),
      ],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setSelectedValue", ns=Input), args=[
          Name("value"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn selector_views() -> Map[String, @anode.View] {
  { "main": selector_main_view_ir() }
}

///|
/// The Selector component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn selector_component(
  name? : String = "Selector",
  views? : Map[String, @anode.View] = selector_views(),
  init? : SelectorState = SelectorState::zero(),
  common_style? : String = selector_common_style,
  global_style? : String = selector_global_style,
  update? : (SelectorState, @component.Dispatch, &@tutuca.Ctx) -> SelectorState?,
  swap? : (SelectorInput) -> ((
    SelectorState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => selector_swap(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn entry_editor_and_selector_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h("div", [@anode.attr("class", "flex gap-3 justify-center")], [
        @anode.render(0, Field("selector")),
      ]),
      @anode.text(" "),
      @anode.h(
        "button",
        [@anode.attr("class", "btn btn-soft btn-success add"), @anode.eid(0)],
        [@anode.text("Add Entry")],
      ),
      @anode.text(" "),
      @anode.h("div", [@anode.attr("class", "flex flex-col gap-3 w-full")], [
        @anode.each(
          2,
          Field("items"),
          @anode.h(
            "div",
            [
              @anode.attr(
                "class", "flex gap-3 justify-center items-center w-full row",
              ),
            ],
            [
              @anode.render_it(1),
              @anode.text(" "),
              @anode.h(
                "button",
                [
                  @anode.attr(
                    "class", "btn btn-soft btn-sm btn-error btn-circle font-mono rm",
                  ),
                  @anode.eid(1),
                ],
                [@anode.text("x")],
              ),
            ],
          ),
        ),
      ]),
    ]),
    [
      [@anode.on("click", HandlerName(name="onAddItem", ns=Input))],
      [
        @anode.on("click", HandlerName(name="removeInItemsAt", ns=Input), args=[
          Bind("key"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn entry_editor_and_selector_views() -> Map[String, @anode.View] {
  { "main": entry_editor_and_selector_main_view_ir() }
}

///|
/// The EntryEditorAndSelector component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn entry_editor_and_selector_component(
  name? : String = "EntryEditorAndSelector",
  views? : Map[String, @anode.View] = entry_editor_and_selector_views(),
  init? : EntryEditorAndSelectorState = EntryEditorAndSelectorState::zero(),
  common_style? : String = entry_editor_and_selector_common_style,
  global_style? : String = entry_editor_and_selector_global_style,
  update? : (EntryEditorAndSelectorState, @component.Dispatch, &@tutuca.Ctx) -> EntryEditorAndSelectorState?,
  swap? : (EntryEditorAndSelectorInput) -> ((
    EntryEditorAndSelectorState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => entry_editor_and_selector_swap(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn sheet_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "card bg-base-200 p-3 gap-2 sheet")],
      [
        @anode.h("span", [@anode.attr("class", "text-xs opacity-60 tag")], [
          @anode.text("Sheet (rendered via *active)"),
        ]),
        @anode.text(" "),
        @anode.h(
          "input",
          [
            @anode.attr("class", "input input-bordered title"),
            @anode.dyn_attr("value", Field("title")),
            @anode.eid(0),
          ],
          [],
        ),
      ],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setTitle", ns=Input), args=[
          Name("value"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn sheet_views() -> Map[String, @anode.View] {
  { "main": sheet_main_view_ir() }
}

///|
/// The Sheet component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn sheet_component(
  name? : String = "Sheet",
  views? : Map[String, @anode.View] = sheet_views(),
  init? : SheetState = SheetState::zero(),
  common_style? : String = sheet_common_style,
  global_style? : String = sheet_global_style,
  update? : (SheetState, @component.Dispatch, &@tutuca.Ctx) -> SheetState?,
  swap? : (SheetInput) -> ((SheetState, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => sheet_swap(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn toolbar_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "border border-dashed border-warning rounded p-3 gap-2 toolbar",
        ),
      ],
      [
        @anode.h("span", [@anode.attr("class", "text-xs opacity-60 tag")], [
          @anode.text("Toolbar (consumer)"),
        ]),
        @anode.text(" "),
        @anode.render(0, Dyn("active")),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn toolbar_views() -> Map[String, @anode.View] {
  { "main": toolbar_main_view_ir() }
}

///|
/// The Toolbar component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn toolbar_component(
  name? : String = "Toolbar",
  views? : Map[String, @anode.View] = toolbar_views(),
  init? : ToolbarState = ToolbarState::zero(),
  common_style? : String = toolbar_common_style,
  global_style? : String = toolbar_global_style,
  update? : (ToolbarState, @component.Dispatch, &@tutuca.Ctx) -> ToolbarState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn panel_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "border border-dashed border-info rounded p-3 gap-2 panel",
        ),
      ],
      [
        @anode.h("span", [@anode.attr("class", "text-xs opacity-60 tag")], [
          @anode.text("Panel (intermediate)"),
        ]),
        @anode.text(" "),
        @anode.render(0, Field("toolbar")),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn panel_views() -> Map[String, @anode.View] {
  { "main": panel_main_view_ir() }
}

///|
/// The Panel component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn panel_component(
  name? : String = "Panel",
  views? : Map[String, @anode.View] = panel_views(),
  init? : PanelState = PanelState::zero(),
  common_style? : String = panel_common_style,
  global_style? : String = panel_global_style,
  update? : (PanelState, @component.Dispatch, &@tutuca.Ctx) -> PanelState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn workspace_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-3 p-3")], [
      @anode.h("div", [@anode.attr("class", "alert")], [
        @anode.h("span", [], [
          @anode.text("Workspace.sheet.title = "),
          @anode.h("b", [@anode.attr("class", "echo")], [
            @anode.dyn_text(Method("sheetTitle")),
          ]),
        ]),
      ]),
      @anode.text(" "),
      @anode.render(0, Field("panel")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn workspace_views() -> Map[String, @anode.View] {
  { "main": workspace_main_view_ir() }
}

///|
/// The Workspace component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn workspace_component(
  name? : String = "Workspace",
  views? : Map[String, @anode.View] = workspace_views(),
  init? : WorkspaceState = WorkspaceState::zero(),
  common_style? : String = workspace_common_style,
  global_style? : String = workspace_global_style,
  update? : (WorkspaceState, @component.Dispatch, &@tutuca.Ctx) -> WorkspaceState?,
  compute? : (WorkspaceMethod) -> ((WorkspaceState, Array[@tutuca.Value]) -> @tutuca.Value)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) => workspace_compute(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}
