// GENERATED by `tutuca gen-views` from filter_paginate.html — DO NOT EDIT.
//
// The views of Person, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing person_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn person_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "person flex gap-2 items-center grow")],
      [
        @anode.h(
          "input",
          [
            @anode.attr("class", "input input-sm grow name"),
            @anode.dyn_attr("value", Field("name")),
            @anode.eid(0),
            @anode.attr("placeholder", "name"),
          ],
          [],
        ),
        @anode.text(" "),
        @anode.h(
          "input",
          [
            @anode.attr("class", "input input-sm grow email"),
            @anode.dyn_attr("value", Field("email")),
            @anode.eid(1),
            @anode.attr("placeholder", "email"),
          ],
          [],
        ),
        @anode.text(" "),
        @anode.h(
          "label",
          [@anode.attr("class", "label cursor-pointer gap-1 chk-label")],
          [
            @anode.h(
              "input",
              [
                @anode.attr("type", "checkbox"),
                @anode.attr("class", "checkbox checkbox-sm active"),
                @anode.dyn_attr("checked", Field("active")),
                @anode.eid(2),
              ],
              [],
            ),
            @anode.text("\n    active\n  "),
          ],
        ),
      ],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setName", ns=Input), args=[
          Name("value"),
        ]),
      ],
      [
        @anode.on("input", HandlerName(name="setEmail", ns=Input), args=[
          Name("value"),
        ]),
      ],
      [
        @anode.on("input", HandlerName(name="setActive", ns=Input), args=[
          Name("value"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn person_views() -> Map[String, @anode.View] {
  { "main": person_main_view_ir() }
}

///|
/// The Person component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn person_component(
  name? : String = "Person",
  views? : Map[String, @anode.View] = person_views(),
  init? : PersonState = PersonState::zero(),
  common_style? : String = person_common_style,
  global_style? : String = person_global_style,
  update? : (PersonState, @component.Dispatch, &@tutuca.Ctx) -> PersonState?,
  swap? : (PersonInput) -> ((PersonState, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => person_swap(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn strategies_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-6")], [
      @anode.h("div", [@anode.attr("class", "flex flex-col gap-1")], [
        @anode.h("h3", [@anode.attr("class", "font-bold")], [
          @anode.text("1. Naive — two independent scans"),
        ]),
        @anode.render(0, Field("naive")),
      ]),
      @anode.h("div", [@anode.attr("class", "flex flex-col gap-1")], [
        @anode.h("h3", [@anode.attr("class", "font-bold")], [
          @anode.text("2. Shared — one count + one partial collect"),
        ]),
        @anode.render(1, Field("shared")),
      ]),
      @anode.h("div", [@anode.attr("class", "flex flex-col gap-1")], [
        @anode.h("h3", [@anode.attr("class", "font-bold")], [
          @anode.text(
            "3. Coupled — one scan; loop-with forwards the enrich's keys",
          ),
        ]),
        @anode.render(2, Field("coupled")),
      ]),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn strategies_views() -> Map[String, @anode.View] {
  { "main": strategies_main_view_ir() }
}

///|
/// The Strategies component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn strategies_component(
  name? : String = "Strategies",
  views? : Map[String, @anode.View] = strategies_views(),
  init? : StrategiesState = StrategiesState::zero(),
  common_style? : String = strategies_common_style,
  global_style? : String = strategies_global_style,
  update? : (StrategiesState, @component.Dispatch, &@tutuca.Ctx) -> StrategiesState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn naive_people_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.scope(
      2,
      HandlerName(name="pagerInfo", ns=Alter),
      @anode.h(
        "section",
        [
          @anode.attr("class", "flex flex-col gap-3"),
          @anode.attr_num("data-nid", 2.0),
        ],
        [
          @anode.h(
            "input",
            [
              @anode.attr("type", "search"),
              @anode.attr("class", "input q"),
              @anode.dyn_attr("value", Field("query")),
              @anode.eid(0),
              @anode.attr("placeholder", "Filter by name or email"),
            ],
            [],
          ),
          @anode.text(" "),
          @anode.h("ul", [@anode.attr("class", "flex flex-col gap-2")], [
            @anode.each(
              1,
              Field("items"),
              @anode.h(
                "li",
                [@anode.attr("class", "flex gap-2 items-center row")],
                [
                  @anode.h(
                    "span",
                    [@anode.attr("class", "badge badge-neutral")],
                    [@anode.dyn_text(Bind("key"))],
                  ),
                  @anode.text(" "),
                  @anode.render_it(0),
                  @anode.text(" "),
                  @anode.h(
                    "button",
                    [
                      @anode.attr(
                        "class", "btn btn-sm btn-error btn-outline rm",
                      ),
                      @anode.eid(1),
                    ],
                    [@anode.text("x")],
                  ),
                ],
              ),
              when_val=HandlerName(name="onlyMatches", ns=Alter),
              loop_with_val=HandlerName(name="tablePage", ns=Alter),
            ),
          ]),
          @anode.h("div", [@anode.attr("class", "join")], [
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn join-item prev"),
                @anode.eid(2),
                @anode.dyn_attr("disabled", Bind("isFirst")),
              ],
              [@anode.text("Prev")],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [@anode.attr("class", "btn join-item btn-disabled pglabel")],
              [@anode.dyn_text(Bind("pageLabel"))],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn join-item next"),
                @anode.eid(3),
                @anode.dyn_attr("disabled", Bind("isLast")),
              ],
              [@anode.text("Next")],
            ),
          ]),
        ],
      ),
    ),
    [
      [
        @anode.on("input", HandlerName(name="search", ns=Input), args=[
          Name("value"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="removeInItemsAt", ns=Input), args=[
          Bind("key"),
        ]),
      ],
      [@anode.on("click", HandlerName(name="prev", ns=Input))],
      [@anode.on("click", HandlerName(name="next", ns=Input))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn naive_people_views() -> Map[String, @anode.View] {
  { "main": naive_people_main_view_ir() }
}

///|
/// The NaivePeople component: `component()` with everything the view file already
/// states filled in — its name, its compiled views, its styles, its schema
/// and its direct codec. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn naive_people_component(
  name? : String = "NaivePeople",
  views? : Map[String, @anode.View] = naive_people_views(),
  init? : NaivePeopleState = NaivePeopleState::zero(),
  common_style? : String = naive_people_common_style,
  global_style? : String = naive_people_global_style,
  update? : (NaivePeopleState, @component.Dispatch, &@tutuca.Ctx) -> NaivePeopleState?,
  swap? : (NaivePeopleInput) -> ((
    NaivePeopleState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  when? : (NaivePeopleWhen) -> ((
    NaivePeopleState,
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Bool)?,
  enrich_scope? : (NaivePeopleEnrichScope) -> ((NaivePeopleState) -> Map[
    String,
    @tutuca.Value,
  ])?,
  loop_with? : (NaivePeopleLoopWith) -> ((
    NaivePeopleState,
    @tutuca.Value,
    @component.LoopCtx,
  ) -> @component.LoopWith)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => naive_people_swap(f)
      None => Map([])
    },
    when=match when {
      Some(f) => naive_people_when(f)
      None => Map([])
    },
    enrich_scope=match enrich_scope {
      Some(f) => naive_people_enrich_scope(f)
      None => Map([])
    },
    loop_with=match loop_with {
      Some(f) => naive_people_loop_with(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn shared_people_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.scope(
      2,
      HandlerName(name="pagerInfo", ns=Alter),
      @anode.h(
        "section",
        [
          @anode.attr("class", "flex flex-col gap-3"),
          @anode.attr_num("data-nid", 2.0),
        ],
        [
          @anode.h(
            "input",
            [
              @anode.attr("type", "search"),
              @anode.attr("class", "input q"),
              @anode.dyn_attr("value", Field("query")),
              @anode.eid(0),
              @anode.attr("placeholder", "Filter by name or email"),
            ],
            [],
          ),
          @anode.text(" "),
          @anode.h("ul", [@anode.attr("class", "flex flex-col gap-2")], [
            @anode.each(
              1,
              Field("items"),
              @anode.h(
                "li",
                [@anode.attr("class", "flex gap-2 items-center row")],
                [
                  @anode.h(
                    "span",
                    [@anode.attr("class", "badge badge-neutral")],
                    [@anode.dyn_text(Bind("key"))],
                  ),
                  @anode.text(" "),
                  @anode.render_it(0),
                  @anode.text(" "),
                  @anode.h(
                    "button",
                    [
                      @anode.attr(
                        "class", "btn btn-sm btn-error btn-outline rm",
                      ),
                      @anode.eid(1),
                    ],
                    [@anode.text("x")],
                  ),
                ],
              ),
              when_val=HandlerName(name="onlyMatches", ns=Alter),
              loop_with_val=HandlerName(name="page", ns=Alter),
            ),
          ]),
          @anode.h("div", [@anode.attr("class", "join")], [
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn join-item prev"),
                @anode.eid(2),
                @anode.dyn_attr("disabled", Bind("isFirst")),
              ],
              [@anode.text("Prev")],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [@anode.attr("class", "btn join-item btn-disabled pglabel")],
              [@anode.dyn_text(Bind("pageLabel"))],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn join-item next"),
                @anode.eid(3),
                @anode.dyn_attr("disabled", Bind("isLast")),
              ],
              [@anode.text("Next")],
            ),
          ]),
        ],
      ),
    ),
    [
      [
        @anode.on("input", HandlerName(name="search", ns=Input), args=[
          Name("value"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="removeInItemsAt", ns=Input), args=[
          Bind("key"),
        ]),
      ],
      [@anode.on("click", HandlerName(name="prev", ns=Input))],
      [@anode.on("click", HandlerName(name="next", ns=Input))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn shared_people_views() -> Map[String, @anode.View] {
  { "main": shared_people_main_view_ir() }
}

///|
/// The compiled `main` view.
fn coupled_people_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.scope(
      2,
      HandlerName(name="pagerInfo", ns=Alter),
      @anode.h(
        "section",
        [
          @anode.attr("class", "flex flex-col gap-3"),
          @anode.attr_num("data-nid", 2.0),
        ],
        [
          @anode.h(
            "input",
            [
              @anode.attr("type", "search"),
              @anode.attr("class", "input q"),
              @anode.dyn_attr("value", Field("query")),
              @anode.eid(0),
              @anode.attr("placeholder", "Filter by name or email"),
            ],
            [],
          ),
          @anode.text(" "),
          @anode.h("ul", [@anode.attr("class", "flex flex-col gap-2")], [
            @anode.each(
              1,
              Field("items"),
              @anode.h(
                "li",
                [@anode.attr("class", "flex gap-2 items-center row")],
                [
                  @anode.h(
                    "span",
                    [@anode.attr("class", "badge badge-neutral")],
                    [@anode.dyn_text(Bind("key"))],
                  ),
                  @anode.text(" "),
                  @anode.render_it(0),
                  @anode.text(" "),
                  @anode.h(
                    "button",
                    [
                      @anode.attr(
                        "class", "btn btn-sm btn-error btn-outline rm",
                      ),
                      @anode.eid(1),
                    ],
                    [@anode.text("x")],
                  ),
                ],
              ),
              loop_with_val=HandlerName(name="forwardKeys", ns=Alter),
            ),
          ]),
          @anode.h("div", [@anode.attr("class", "join")], [
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn join-item prev"),
                @anode.eid(2),
                @anode.dyn_attr("disabled", Bind("isFirst")),
              ],
              [@anode.text("Prev")],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [@anode.attr("class", "btn join-item btn-disabled pglabel")],
              [@anode.dyn_text(Bind("pageLabel"))],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn join-item next"),
                @anode.eid(3),
                @anode.dyn_attr("disabled", Bind("isLast")),
              ],
              [@anode.text("Next")],
            ),
          ]),
        ],
      ),
    ),
    [
      [
        @anode.on("input", HandlerName(name="search", ns=Input), args=[
          Name("value"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="removeInItemsAt", ns=Input), args=[
          Bind("key"),
        ]),
      ],
      [@anode.on("click", HandlerName(name="prev", ns=Input))],
      [@anode.on("click", HandlerName(name="next", ns=Input))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn coupled_people_views() -> Map[String, @anode.View] {
  { "main": coupled_people_main_view_ir() }
}
