// GENERATED by `tutuca gen-views` from filter_paginate.html — DO NOT EDIT.
//
// The views of Person, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing person_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn person_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("person flex gap-2 items-center grow"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(
            name="class",
            val=Const(lit=LStr("input input-sm grow name"), from_macro=false),
          ),
          Plain(name="value", val=Field("name")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
          ConstA(
            name="placeholder",
            val=Const(lit=LStr("name"), from_macro=false),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(
            name="class",
            val=Const(lit=LStr("input input-sm grow email"), from_macro=false),
          ),
          Plain(name="value", val=Field("email")),
          ConstA(name="data-eid", val=Const(lit=LNum(1.0), from_macro=false)),
          ConstA(
            name="placeholder",
            val=Const(lit=LStr("email"), from_macro=false),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "label",
        attrs: ConstAttrs({
          "class": LStr("label cursor-pointer gap-1 chk-label"),
        }),
        childs: [
          Dom({
            tag: "input",
            attrs: DynAttrs([
              ConstA(
                name="type",
                val=Const(lit=LStr("checkbox"), from_macro=false),
              ),
              ConstA(
                name="class",
                val=Const(
                  lit=LStr("checkbox checkbox-sm active"),
                  from_macro=false,
                ),
              ),
              Plain(name="checked", val=Field("active")),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(2.0), from_macro=false),
              ),
            ]),
            childs: [],
            ns: None,
          }),
          Text({ val: "\n    active\n  " }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setName"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setEmail"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setActive"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn person_views() -> Map[String, @anode.View] {
  { "main": person_main_view_ir() }
}

///|
/// The compiled `main` view.
fn strategies_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-6"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-1") }),
        childs: [
          Dom({
            tag: "h3",
            attrs: ConstAttrs({ "class": LStr("font-bold") }),
            childs: [Text({ val: "1. Naive — two independent scans" })],
            ns: None,
          }),
          Render({ node_id: 0, val: Field("naive"), view_val: None }),
        ],
        ns: None,
      }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-1") }),
        childs: [
          Dom({
            tag: "h3",
            attrs: ConstAttrs({ "class": LStr("font-bold") }),
            childs: [
              Text({ val: "2. Shared — one count + one partial collect" }),
            ],
            ns: None,
          }),
          Render({ node_id: 1, val: Field("shared"), view_val: None }),
        ],
        ns: None,
      }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-1") }),
        childs: [
          Dom({
            tag: "h3",
            attrs: ConstAttrs({ "class": LStr("font-bold") }),
            childs: [
              Text({
                val: "3. Coupled — one scan; loop-with forwards the enrich's keys",
              }),
            ],
            ns: None,
          }),
          Render({ node_id: 2, val: Field("coupled"), view_val: None }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn strategies_views() -> Map[String, @anode.View] {
  { "main": strategies_main_view_ir() }
}
