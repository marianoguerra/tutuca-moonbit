// GENERATED by `tutuca gen-views` from collections.html — DO NOT EDIT.
//
// The views of RenderWithScope, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing render_with_scope_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn render_with_scope_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h(
        "input",
        [
          @anode.attr("class", "input"),
          @anode.dyn_attr("value", Field("text")),
          @anode.eid(0),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.scope(
        0,
        HandlerName(name="enrichScope", ns=Alter),
        @anode.h("div", [@anode.attr_num("data-nid", 0.0)], [
          @anode.h("p", [], [
            @anode.text("Text: "),
            @anode.h("span", [@anode.attr("class", "v-text")], [
              @anode.dyn_text(Field("text")),
            ]),
          ]),
          @anode.h("p", [], [
            @anode.text("Len: "),
            @anode.h("span", [@anode.attr("class", "v-len")], [
              @anode.dyn_text(Bind("len")),
            ]),
          ]),
          @anode.h("p", [], [
            @anode.text("Upper: "),
            @anode.h("span", [@anode.attr("class", "v-upper")], [
              @anode.dyn_text(Bind("upper")),
            ]),
          ]),
        ]),
      ),
    ]),
    [
      [
        @anode.on("input", HandlerName(name="setText", ns=Input), args=[
          Name("value"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn render_with_scope_views() -> Map[String, @anode.View] {
  { "main": render_with_scope_main_view_ir() }
}

///|
/// The RenderWithScope component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `RenderWithScopeState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn render_with_scope_component(
  name? : String = "RenderWithScope",
  views? : Map[String, @anode.View] = render_with_scope_views(),
  init? : RenderWithScopeState = RenderWithScopeState::zero(),
  common_style? : String = render_with_scope_common_style,
  global_style? : String = render_with_scope_global_style,
  update? : (RenderWithScopeState, @component.Dispatch, &@tutuca.Ctx) -> RenderWithScopeState?,
  swap? : (RenderWithScopeInput) -> ((
    RenderWithScopeState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  enrich_scope? : (RenderWithScopeEnrichScope) -> ((RenderWithScopeState) -> Map[
    String,
    @tutuca.Value,
  ])?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => render_with_scope_swap(f)
      None => Map([])
    },
    enrich_scope=match enrich_scope {
      Some(f) => render_with_scope_enrich_scope(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn list_filter_enrich_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h(
        "input",
        [
          @anode.attr("type", "search"),
          @anode.attr("class", "input"),
          @anode.dyn_attr("value", Field("query")),
          @anode.eid(0),
          @anode.attr("placeholder", "Filter entries"),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h("ul", [], [
        @anode.each(
          2,
          Field("items"),
          @anode.h("li", [], [
            @anode.h("span", [], [@anode.dyn_text(Bind("key"))]),
            @anode.text(": "),
            @anode.dyn_text(Bind("value"), id=0),
            @anode.text(" ("),
            @anode.dyn_text(Bind("count"), id=1),
            @anode.text(" characters)\n    "),
          ]),
          when_val=HandlerName(name="filterItem", ns=Alter),
          enrich_with_val=HandlerName(name="enrichItem", ns=Alter),
        ),
      ]),
    ]),
    [
      [
        @anode.on("input", HandlerName(name="setQuery", ns=Input), args=[
          Name("value"),
        ]),
        @anode.on("keydown", HandlerName(name="resetQuery", ns=Input), modifiers=[
          "cancel",
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn list_filter_enrich_views() -> Map[String, @anode.View] {
  { "main": list_filter_enrich_main_view_ir() }
}

///|
/// The ListFilterEnrich component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `ListFilterEnrichState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn list_filter_enrich_component(
  name? : String = "ListFilterEnrich",
  views? : Map[String, @anode.View] = list_filter_enrich_views(),
  init? : ListFilterEnrichState = ListFilterEnrichState::zero(),
  common_style? : String = list_filter_enrich_common_style,
  global_style? : String = list_filter_enrich_global_style,
  update? : (ListFilterEnrichState, @component.Dispatch, &@tutuca.Ctx) -> ListFilterEnrichState?,
  swap? : (ListFilterEnrichInput) -> ((
    ListFilterEnrichState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  when? : (ListFilterEnrichWhen) -> ((
    ListFilterEnrichState,
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Bool)?,
  enrich? : (ListFilterEnrichEnrich) -> ((
    ListFilterEnrichState,
    Map[String, @tutuca.Value],
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Unit)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => list_filter_enrich_swap(f)
      None => Map([])
    },
    when=match when {
      Some(f) => list_filter_enrich_when(f)
      None => Map([])
    },
    enrich=match enrich {
      Some(f) => list_filter_enrich_enrich(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn list_filter_enrich_with_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h(
        "input",
        [
          @anode.attr("type", "search"),
          @anode.attr("class", "input"),
          @anode.dyn_attr("value", Field("query")),
          @anode.eid(0),
          @anode.attr("placeholder", "Filter entries"),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h("ul", [], [
        @anode.each(
          3,
          Field("items"),
          @anode.h("li", [], [
            @anode.h("span", [], [@anode.dyn_text(Bind("key"))]),
            @anode.text(": "),
            @anode.dyn_text(Bind("value"), id=0),
            @anode.text(" ("),
            @anode.dyn_text(Bind("count"), id=1),
            @anode.text(" / "),
            @anode.dyn_text(Bind("total"), id=2),
            @anode.text(")\n    "),
          ]),
          when_val=HandlerName(name="filterItem", ns=Alter),
          loop_with_val=HandlerName(name="getIterData", ns=Alter),
          enrich_with_val=HandlerName(name="enrichItem", ns=Alter),
        ),
      ]),
    ]),
    [
      [
        @anode.on("input", HandlerName(name="setQuery", ns=Input), args=[
          Name("value"),
        ]),
        @anode.on("keydown", HandlerName(name="resetQuery", ns=Input), modifiers=[
          "cancel",
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn list_filter_enrich_with_views() -> Map[String, @anode.View] {
  { "main": list_filter_enrich_with_main_view_ir() }
}

///|
/// The ListFilterEnrichWith component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `ListFilterEnrichWithState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn list_filter_enrich_with_component(
  name? : String = "ListFilterEnrichWith",
  views? : Map[String, @anode.View] = list_filter_enrich_with_views(),
  init? : ListFilterEnrichWithState = ListFilterEnrichWithState::zero(),
  common_style? : String = list_filter_enrich_with_common_style,
  global_style? : String = list_filter_enrich_with_global_style,
  update? : (ListFilterEnrichWithState, @component.Dispatch, &@tutuca.Ctx) -> ListFilterEnrichWithState?,
  swap? : (ListFilterEnrichWithInput) -> ((
    ListFilterEnrichWithState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  when? : (ListFilterEnrichWithWhen) -> ((
    ListFilterEnrichWithState,
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Bool)?,
  enrich? : (ListFilterEnrichWithEnrich) -> ((
    ListFilterEnrichWithState,
    Map[String, @tutuca.Value],
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Unit)?,
  loop_with? : (ListFilterEnrichWithLoopWith) -> ((
    ListFilterEnrichWithState,
    @tutuca.Value,
    @component.LoopCtx,
  ) -> @component.LoopWith)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) => list_filter_enrich_with_swap(f)
      None => Map([])
    },
    when=match when {
      Some(f) => list_filter_enrich_with_when(f)
      None => Map([])
    },
    enrich=match enrich {
      Some(f) => list_filter_enrich_with_enrich(f)
      None => Map([])
    },
    loop_with=match loop_with {
      Some(f) => list_filter_enrich_with_loop_with(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}
