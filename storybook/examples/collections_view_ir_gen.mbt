// GENERATED by `tutuca gen-views` from collections.html — DO NOT EDIT.
//
// The views of RenderWithScope, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing render_with_scope_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn render_with_scope_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="class", val=Const(lit=LStr("input"), from_macro=false)),
          Plain(name="value", val=Field("text")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Scope({
        node_id: Some(0),
        val: HandlerName(name="enrichScope", ns=Alter),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "data-nid": LNum(0.0) }),
          childs: [
            Dom({
              tag: "p",
              attrs: ConstAttrs(Map([])),
              childs: [
                Text({ val: "Text: " }),
                Dom({
                  tag: "span",
                  attrs: ConstAttrs({ "class": LStr("v-text") }),
                  childs: [RenderText({ node_id: None, val: Field("text") })],
                  ns: None,
                }),
              ],
              ns: None,
            }),
            Dom({
              tag: "p",
              attrs: ConstAttrs(Map([])),
              childs: [
                Text({ val: "Len: " }),
                Dom({
                  tag: "span",
                  attrs: ConstAttrs({ "class": LStr("v-len") }),
                  childs: [RenderText({ node_id: None, val: Bind("len") })],
                  ns: None,
                }),
              ],
              ns: None,
            }),
            Dom({
              tag: "p",
              attrs: ConstAttrs(Map([])),
              childs: [
                Text({ val: "Upper: " }),
                Dom({
                  tag: "span",
                  attrs: ConstAttrs({ "class": LStr("v-upper") }),
                  childs: [RenderText({ node_id: None, val: Bind("upper") })],
                  ns: None,
                }),
              ],
              ns: None,
            }),
          ],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setText"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn render_with_scope_views() -> Map[String, @anode.View] {
  { "main": render_with_scope_main_view_ir() }
}

///|
/// The compiled `main` view.
fn list_filter_enrich_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="type", val=Const(lit=LStr("search"), from_macro=false)),
          ConstA(name="class", val=Const(lit=LStr("input"), from_macro=false)),
          Plain(name="value", val=Field("query")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
          ConstA(
            name="placeholder",
            val=Const(lit=LStr("Filter entries"), from_macro=false),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "ul",
        attrs: ConstAttrs(Map([])),
        childs: [
          Each({
            node_id: 2,
            iter_info: {
              val: Field("items"),
              when_val: Some(HandlerName(name="filterItem", ns=Alter)),
              loop_with_val: None,
              enrich_with_val: Some(HandlerName(name="enrichItem", ns=Alter)),
            },
            node: Dom({
              tag: "li",
              attrs: ConstAttrs(Map([])),
              childs: [
                Dom({
                  tag: "span",
                  attrs: ConstAttrs(Map([])),
                  childs: [RenderText({ node_id: None, val: Bind("key") })],
                  ns: None,
                }),
                Text({ val: ": " }),
                RenderText({ node_id: Some(0), val: Bind("value") }),
                Text({ val: " (" }),
                RenderText({ node_id: Some(1), val: Bind("count") }),
                Text({ val: " characters)\n    " }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setQuery"), args: [Name("value")] },
          modifiers: [],
        },
        {
          name: "keydown",
          handler: { handler: Method("resetQuery"), args: [] },
          modifiers: ["cancel"],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn list_filter_enrich_views() -> Map[String, @anode.View] {
  { "main": list_filter_enrich_main_view_ir() }
}

///|
/// The compiled `main` view.
fn list_filter_enrich_with_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="type", val=Const(lit=LStr("search"), from_macro=false)),
          ConstA(name="class", val=Const(lit=LStr("input"), from_macro=false)),
          Plain(name="value", val=Field("query")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
          ConstA(
            name="placeholder",
            val=Const(lit=LStr("Filter entries"), from_macro=false),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "ul",
        attrs: ConstAttrs(Map([])),
        childs: [
          Each({
            node_id: 3,
            iter_info: {
              val: Field("items"),
              when_val: Some(HandlerName(name="filterItem", ns=Alter)),
              loop_with_val: Some(HandlerName(name="getIterData", ns=Alter)),
              enrich_with_val: Some(HandlerName(name="enrichItem", ns=Alter)),
            },
            node: Dom({
              tag: "li",
              attrs: ConstAttrs(Map([])),
              childs: [
                Dom({
                  tag: "span",
                  attrs: ConstAttrs(Map([])),
                  childs: [RenderText({ node_id: None, val: Bind("key") })],
                  ns: None,
                }),
                Text({ val: ": " }),
                RenderText({ node_id: Some(0), val: Bind("value") }),
                Text({ val: " (" }),
                RenderText({ node_id: Some(1), val: Bind("count") }),
                Text({ val: " / " }),
                RenderText({ node_id: Some(2), val: Bind("total") }),
                Text({ val: ")\n    " }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setQuery"), args: [Name("value")] },
          modifiers: [],
        },
        {
          name: "keydown",
          handler: { handler: Method("resetQuery"), args: [] },
          modifiers: ["cancel"],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn list_filter_enrich_with_views() -> Map[String, @anode.View] {
  { "main": list_filter_enrich_with_main_view_ir() }
}
