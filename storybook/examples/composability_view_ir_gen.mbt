// GENERATED by `tutuca gen-views` from composability.html — DO NOT EDIT.
//
// The views of Composability, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing composability_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn composability_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-4"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({
          "role": LStr("tablist"),
          "class": LStr("tabs tabs-border"),
        }),
        childs: [
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("tab-todo"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("activeSection"),
                  Const(lit=LStr("todo"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(0.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "To-Do" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("tab-json"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("activeSection"),
                  Const(lit=LStr("json"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(1.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "JSON Editor" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("tab-tree"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("activeSection"),
                  Const(lit=LStr("tree"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(2.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "Tree" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("tab-personal-site"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("activeSection"),
                  Const(lit=LStr("personalSite"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(3.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "Personal Site" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("tab-dnd"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("activeSection"),
                  Const(lit=LStr("dnd"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(4.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "Drag and Drop" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(name="role", val=Const(lit=LStr("tab"), from_macro=false)),
              ConstA(
                name="id",
                val=Const(lit=LStr("tab-visual-wasm"), from_macro=false),
              ),
              If({
                name: "class",
                cond_val: Predicate(pred=Equals, args=[
                  Field("activeSection"),
                  Const(lit=LStr("visualWasm"), from_macro=false),
                ]),
                then_val: Some(
                  Const(lit=LStr("tab tab-active"), from_macro=false),
                ),
                else_val: Some(Const(lit=LStr("tab"), from_macro=false)),
              }),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(5.0), from_macro=false),
              ),
            ]),
            childs: [Text({ val: "Visual Wasm" })],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("activeSection"),
          Const(lit=LStr("todo"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("pane-todo") }),
          childs: [Render({ node_id: 0, val: Field("todo"), view_val: None })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("activeSection"),
          Const(lit=LStr("json"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("pane-json") }),
          childs: [Render({ node_id: 1, val: Field("json"), view_val: None })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("activeSection"),
          Const(lit=LStr("tree"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("pane-tree") }),
          childs: [Render({ node_id: 2, val: Field("tree"), view_val: None })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("activeSection"),
          Const(lit=LStr("personalSite"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("pane-personal-site") }),
          childs: [
            Render({ node_id: 3, val: Field("personalSite"), view_val: None }),
          ],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("activeSection"),
          Const(lit=LStr("dnd"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("pane-dnd") }),
          childs: [Render({ node_id: 4, val: Field("dnd"), view_val: None })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=Equals, args=[
          Field("activeSection"),
          Const(lit=LStr("visualWasm"), from_macro=false),
        ]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("pane-visual-wasm") }),
          childs: [
            Render({ node_id: 5, val: Field("visualWasm"), view_val: None }),
          ],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setActiveSection"),
            args: [Const(lit=LStr("todo"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setActiveSection"),
            args: [Const(lit=LStr("json"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setActiveSection"),
            args: [Const(lit=LStr("tree"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 3,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setActiveSection"),
            args: [Const(lit=LStr("personalSite"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 4,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setActiveSection"),
            args: [Const(lit=LStr("dnd"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 5,
      handlers: [
        {
          name: "click",
          handler: {
            handler: Method("setActiveSection"),
            args: [Const(lit=LStr("visualWasm"), from_macro=false)],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn composability_views() -> Map[String, @anode.View] {
  { "main": composability_main_view_ir() }
}
