// GENERATED by `tutuca gen` from composability.tutu — DO NOT EDIT.
//
// The views of Composability, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing composability_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `spec:` section, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let composability_spec_source : String =
  #|spec:
  #|  Composability:
  #|    field active_section :: String
  #|    field todo :: Instance.of(Items)
  #|    field json :: Instance.of(JsonArray)
  #|    field tree :: Instance.of(TreeRoot)
  #|    field personal_site :: Instance.of(Root)
  #|    field visual_wasm :: Instance.of(Module)
  #|    field dnd :: Instance
  #|
  #|    where it.active_section is_one_of ["todo", "json", "tree", "personalSite", "dnd", "visualWasm"]

///|
/// The compiled `main` view.
fn composability_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-4")], [
      @anode.h(
        "div",
        [
          @anode.attr("role", "tablist"),
          @anode.attr("class", "tabs tabs-border"),
        ],
        [
          @anode.h(
            "button",
            [
              @anode.attr("role", "tab"),
              @anode.attr("id", "tab-todo"),
              @anode.if_attr(
                "class",
                EChain(
                  family=FCompare,
                  ops=["is"],
                  operands=[
                    @tutuca.Expr::field("active_section"),
                    @anode.const_str("todo"),
                  ],
                  span=@tutuca.no_span,
                ),
                then_val=@anode.const_str("tab tab-active"),
                else_val=@anode.const_str("tab"),
              ),
              @anode.eid(0),
            ],
            [@anode.text("To-Do")],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("role", "tab"),
              @anode.attr("id", "tab-json"),
              @anode.if_attr(
                "class",
                EChain(
                  family=FCompare,
                  ops=["is"],
                  operands=[
                    @tutuca.Expr::field("active_section"),
                    @anode.const_str("json"),
                  ],
                  span=@tutuca.no_span,
                ),
                then_val=@anode.const_str("tab tab-active"),
                else_val=@anode.const_str("tab"),
              ),
              @anode.eid(1),
            ],
            [@anode.text("JSON Editor")],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("role", "tab"),
              @anode.attr("id", "tab-tree"),
              @anode.if_attr(
                "class",
                EChain(
                  family=FCompare,
                  ops=["is"],
                  operands=[
                    @tutuca.Expr::field("active_section"),
                    @anode.const_str("tree"),
                  ],
                  span=@tutuca.no_span,
                ),
                then_val=@anode.const_str("tab tab-active"),
                else_val=@anode.const_str("tab"),
              ),
              @anode.eid(2),
            ],
            [@anode.text("Tree")],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("role", "tab"),
              @anode.attr("id", "tab-personal-site"),
              @anode.if_attr(
                "class",
                EChain(
                  family=FCompare,
                  ops=["is"],
                  operands=[
                    @tutuca.Expr::field("active_section"),
                    @anode.const_str("personalSite"),
                  ],
                  span=@tutuca.no_span,
                ),
                then_val=@anode.const_str("tab tab-active"),
                else_val=@anode.const_str("tab"),
              ),
              @anode.eid(3),
            ],
            [@anode.text("Personal Site")],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("role", "tab"),
              @anode.attr("id", "tab-dnd"),
              @anode.if_attr(
                "class",
                EChain(
                  family=FCompare,
                  ops=["is"],
                  operands=[
                    @tutuca.Expr::field("active_section"),
                    @anode.const_str("dnd"),
                  ],
                  span=@tutuca.no_span,
                ),
                then_val=@anode.const_str("tab tab-active"),
                else_val=@anode.const_str("tab"),
              ),
              @anode.eid(4),
            ],
            [@anode.text("Drag and Drop")],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("role", "tab"),
              @anode.attr("id", "tab-visual-wasm"),
              @anode.if_attr(
                "class",
                EChain(
                  family=FCompare,
                  ops=["is"],
                  operands=[
                    @tutuca.Expr::field("active_section"),
                    @anode.const_str("visualWasm"),
                  ],
                  span=@tutuca.no_span,
                ),
                then_val=@anode.const_str("tab tab-active"),
                else_val=@anode.const_str("tab"),
              ),
              @anode.eid(5),
            ],
            [@anode.text("Visual Wasm")],
          ),
        ],
      ),
      @anode.text(" "),
      @anode.show(
        EChain(
          family=FCompare,
          ops=["is"],
          operands=[
            @tutuca.Expr::field("active_section"),
            @anode.const_str("todo"),
          ],
          span=@tutuca.no_span,
        ),
        @anode.h("div", [@anode.attr("class", "pane-todo")], [
          @anode.render(0, @tutuca.Expr::field("todo")),
        ]),
      ),
      @anode.text(" "),
      @anode.show(
        EChain(
          family=FCompare,
          ops=["is"],
          operands=[
            @tutuca.Expr::field("active_section"),
            @anode.const_str("json"),
          ],
          span=@tutuca.no_span,
        ),
        @anode.h("div", [@anode.attr("class", "pane-json")], [
          @anode.render(1, @tutuca.Expr::field("json")),
        ]),
      ),
      @anode.text(" "),
      @anode.show(
        EChain(
          family=FCompare,
          ops=["is"],
          operands=[
            @tutuca.Expr::field("active_section"),
            @anode.const_str("tree"),
          ],
          span=@tutuca.no_span,
        ),
        @anode.h("div", [@anode.attr("class", "pane-tree")], [
          @anode.render(2, @tutuca.Expr::field("tree")),
        ]),
      ),
      @anode.text(" "),
      @anode.show(
        EChain(
          family=FCompare,
          ops=["is"],
          operands=[
            @tutuca.Expr::field("active_section"),
            @anode.const_str("personalSite"),
          ],
          span=@tutuca.no_span,
        ),
        @anode.h("div", [@anode.attr("class", "pane-personal-site")], [
          @anode.render(3, @tutuca.Expr::field("personal_site")),
        ]),
      ),
      @anode.text(" "),
      @anode.show(
        EChain(
          family=FCompare,
          ops=["is"],
          operands=[
            @tutuca.Expr::field("active_section"),
            @anode.const_str("dnd"),
          ],
          span=@tutuca.no_span,
        ),
        @anode.h("div", [@anode.attr("class", "pane-dnd")], [
          @anode.render(4, @tutuca.Expr::field("dnd")),
        ]),
      ),
      @anode.text(" "),
      @anode.show(
        EChain(
          family=FCompare,
          ops=["is"],
          operands=[
            @tutuca.Expr::field("active_section"),
            @anode.const_str("visualWasm"),
          ],
          span=@tutuca.no_span,
        ),
        @anode.h("div", [@anode.attr("class", "pane-visual-wasm")], [
          @anode.render(5, @tutuca.Expr::field("visual_wasm")),
        ]),
      ),
    ]),
    [
      [
        @anode.on(
          "click",
          @anode.const_null(),
          action=@anode.set_property_action(
            "active_section",
            @anode.const_str("todo"),
          ),
        ),
      ],
      [
        @anode.on(
          "click",
          @anode.const_null(),
          action=@anode.set_property_action(
            "active_section",
            @anode.const_str("json"),
          ),
        ),
      ],
      [
        @anode.on(
          "click",
          @anode.const_null(),
          action=@anode.set_property_action(
            "active_section",
            @anode.const_str("tree"),
          ),
        ),
      ],
      [
        @anode.on(
          "click",
          @anode.const_null(),
          action=@anode.set_property_action(
            "active_section",
            @anode.const_str("personalSite"),
          ),
        ),
      ],
      [
        @anode.on(
          "click",
          @anode.const_null(),
          action=@anode.set_property_action(
            "active_section",
            @anode.const_str("dnd"),
          ),
        ),
      ],
      [
        @anode.on(
          "click",
          @anode.const_null(),
          action=@anode.set_property_action(
            "active_section",
            @anode.const_str("visualWasm"),
          ),
        ),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn composability_views() -> Map[String, @anode.View] {
  { "main": composability_main_view_ir() }
}

///|
/// The Composability component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `ComposabilityState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn composability_component(
  name? : String = "Composability",
  views? : Map[String, @anode.View] = composability_views(),
  initial? : ComposabilityState = ComposabilityState::zero(),
  common_style? : String = composability_common_style,
  global_style? : String = composability_global_style,
  update? : (ComposabilityState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    ComposabilityState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=composability_spec_source,
  )
}
