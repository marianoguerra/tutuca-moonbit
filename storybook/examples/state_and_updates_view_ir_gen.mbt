// GENERATED by `tutuca gen-views` from state_and_updates.html — DO NOT EDIT.
//
// The views of AttributeBinding, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing attribute_binding_views() as `views~` means the template parser
// never runs at startup.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn attribute_binding_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h(
        "input",
        [
          @anode.attr("class", "input s"),
          @anode.dyn_attr("value", Field("str")),
          @anode.eid(0),
          @anode.dyn_attr(
            "title",
            StrTpl([Some(@anode.const_str("Content is ")), Some(Field("str"))]),
          ),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h(
        "input",
        [
          @anode.attr("class", "input n"),
          @anode.attr("type", "number"),
          @anode.dyn_attr("value", Field("num")),
          @anode.eid(1),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h(
        "input",
        [
          @anode.attr("class", "checkbox b"),
          @anode.attr("type", "checkbox"),
          @anode.dyn_attr("checked", Field("bool")),
          @anode.eid(2),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h("p", [], [
        @anode.text("String: "),
        @anode.h("span", [@anode.attr("class", "o-str")], [
          @anode.dyn_text(Field("str")),
        ]),
      ]),
      @anode.h("p", [], [
        @anode.text("Number: "),
        @anode.h("span", [@anode.attr("class", "o-num")], [
          @anode.dyn_text(Field("num")),
        ]),
      ]),
      @anode.h("p", [], [
        @anode.text("Boolean: "),
        @anode.h("span", [@anode.attr("class", "o-bool")], [
          @anode.dyn_text(Field("bool")),
        ]),
      ]),
    ]),
    [
      [@anode.on("input", Method("setStr"), args=[Name("value")])],
      [@anode.on("input", Method("setRawNumber"), args=[Name("value")])],
      [@anode.on("input", Method("setBool"), args=[Name("value")])],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn attribute_binding_views() -> Map[String, @anode.View] {
  { "main": attribute_binding_main_view_ir() }
}

///|
/// The compiled `main` view.
fn conditional_attributes_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [], [
      @anode.h(
        "button",
        [
          @anode.if_attr(
            "class",
            Field("isActive"),
            then_val=@anode.const_str("btn btn-success"),
            else_val=@anode.const_str("btn btn-ghost"),
          ),
          @anode.if_attr(
            "title",
            Field("isActive"),
            then_val=@anode.const_str("Click to disable"),
            else_val=@anode.const_str("Click to enable"),
          ),
          @anode.eid(0),
        ],
        [
          @anode.show(
            Field("isActive"),
            @anode.h("span", [@anode.attr("class", "on")], [
              @anode.text("Enabled"),
            ]),
          ),
          @anode.text(" "),
          @anode.hide(
            Field("isActive"),
            @anode.h("span", [@anode.attr("class", "off")], [
              @anode.text("Disabled"),
            ]),
          ),
        ],
      ),
    ]),
    [[@anode.on("click", Method("toggleIsActive"))]],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn conditional_attributes_views() -> Map[String, @anode.View] {
  { "main": conditional_attributes_main_view_ir() }
}

///|
/// The compiled `main` view.
fn event_modifiers_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h(
        "input",
        [
          @anode.attr("type", "search"),
          @anode.attr("class", "input"),
          @anode.dyn_attr("value", Field("query")),
          @anode.eid(0),
          @anode.attr(
            "placeholder", "Search Query (Enter to send, Esc to clear)",
          ),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.show(
        Predicate(pred=IsTruthy, args=[Field("lastSentSearch")]),
        @anode.h("p", [@anode.attr("class", "sent")], [
          @anode.text("\n    Search: \""),
          @anode.h("span", [@anode.attr("class", "sent-q")], [
            @anode.dyn_text(Field("lastSentSearch")),
          ]),
          @anode.text("\"\n  "),
        ]),
      ),
    ]),
    [
      [
        @anode.on("input", HandlerName(name="onInput", ns=Input), args=[
          Name("value"),
        ]),
        @anode.on(
          "keydown",
          Method("setLastSentSearch"),
          args=[Name("value")],
          modifiers=["send"],
        ),
        @anode.on("keydown", Method("resetQuery"), modifiers=["cancel"]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn event_modifiers_views() -> Map[String, @anode.View] {
  { "main": event_modifiers_main_view_ir() }
}

///|
/// The compiled `main` view.
fn show_hide_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-2")], [
      @anode.h(
        "button",
        [@anode.attr("class", "btn btn-primary toggle-btn"), @anode.eid(0)],
        [@anode.dyn_text(Method("label"))],
      ),
      @anode.text(" "),
      @anode.show(
        Field("isOpen"),
        @anode.h("div", [@anode.attr("class", "card p-3 border details")], [
          @anode.h("p", [], [
            @anode.text("Details panel — only visible when "),
            @anode.h("code", [], [@anode.text("isOpen")]),
            @anode.text(" is true."),
          ]),
          @anode.text(" "),
          @anode.h(
            "button",
            [@anode.attr("class", "btn btn-sm bump"), @anode.eid(1)],
            [@anode.text("Click me")],
          ),
          @anode.text(" "),
          @anode.h("p", [], [
            @anode.text("Clicked "),
            @anode.dyn_text(Field("count"), id=0),
            @anode.text(" times."),
          ]),
        ]),
      ),
      @anode.text(" "),
      @anode.hide(
        Field("isOpen"),
        @anode.h("p", [@anode.attr("class", "text-sm opacity-60 hint")], [
          @anode.text("(details are hidden)"),
        ]),
      ),
      @anode.text(" "),
      @anode.h("p", [@anode.attr("class", "peek")], [
        @anode.text("Count, only when open: "),
        @anode.show(
          Field("isOpen"),
          @anode.dyn_text(Field("count"), id=1),
          id=2,
        ),
      ]),
    ]),
    [
      [@anode.on("click", Method("toggleIsOpen"))],
      [@anode.on("click", Method("incCount"))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn show_hide_views() -> Map[String, @anode.View] {
  { "main": show_hide_main_view_ir() }
}
