// GENERATED by `tutuca gen-views` from state_and_updates.html — DO NOT EDIT.
//
// The views of AttributeBinding, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing attribute_binding_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn attribute_binding_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="class", val=Const(lit=LStr("input s"), from_macro=false)),
          Plain(name="value", val=Field("str")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
          Plain(
            name="title",
            val=StrTpl([
              Some(Const(lit=LStr("Content is "), from_macro=false)),
              Some(Field("str")),
            ]),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="class", val=Const(lit=LStr("input n"), from_macro=false)),
          ConstA(name="type", val=Const(lit=LStr("number"), from_macro=false)),
          Plain(name="value", val=Field("num")),
          ConstA(name="data-eid", val=Const(lit=LNum(1.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(
            name="class",
            val=Const(lit=LStr("checkbox b"), from_macro=false),
          ),
          ConstA(name="type", val=Const(lit=LStr("checkbox"), from_macro=false)),
          Plain(name="checked", val=Field("bool")),
          ConstA(name="data-eid", val=Const(lit=LNum(2.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "p",
        attrs: ConstAttrs(Map([])),
        childs: [
          Text({ val: "String: " }),
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "class": LStr("o-str") }),
            childs: [RenderText({ node_id: None, val: Field("str") })],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs(Map([])),
        childs: [
          Text({ val: "Number: " }),
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "class": LStr("o-num") }),
            childs: [RenderText({ node_id: None, val: Field("num") })],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs(Map([])),
        childs: [
          Text({ val: "Boolean: " }),
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "class": LStr("o-bool") }),
            childs: [RenderText({ node_id: None, val: Field("bool") })],
            ns: None,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setStr"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setRawNumber"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setBool"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn attribute_binding_views() -> Map[String, @anode.View] {
  { "main": attribute_binding_main_view_ir() }
}

///|
/// The compiled `main` view.
fn conditional_attributes_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({ "data-vid": LStr("main") }),
    childs: [
      Dom({
        tag: "button",
        attrs: DynAttrs([
          If({
            name: "class",
            cond_val: Field("isActive"),
            then_val: Some(Const(lit=LStr("btn btn-success"), from_macro=false)),
            else_val: Some(Const(lit=LStr("btn btn-ghost"), from_macro=false)),
          }),
          If({
            name: "title",
            cond_val: Field("isActive"),
            then_val: Some(
              Const(lit=LStr("Click to disable"), from_macro=false),
            ),
            else_val: Some(Const(lit=LStr("Click to enable"), from_macro=false)),
          }),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
        ]),
        childs: [
          Show({
            node_id: None,
            val: Field("isActive"),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs({ "class": LStr("on") }),
              childs: [Text({ val: "Enabled" })],
              ns: None,
            }),
          }),
          Text({ val: " " }),
          Hide({
            node_id: None,
            val: Field("isActive"),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs({ "class": LStr("off") }),
              childs: [Text({ val: "Disabled" })],
              ns: None,
            }),
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("toggleIsActive"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn conditional_attributes_views() -> Map[String, @anode.View] {
  { "main": conditional_attributes_main_view_ir() }
}

///|
/// The compiled `main` view.
fn event_modifiers_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="type", val=Const(lit=LStr("search"), from_macro=false)),
          ConstA(name="class", val=Const(lit=LStr("input"), from_macro=false)),
          Plain(name="value", val=Field("query")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
          ConstA(
            name="placeholder",
            val=Const(
              lit=LStr("Search Query (Enter to send, Esc to clear)"),
              from_macro=false,
            ),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=IsTruthy, args=[Field("lastSentSearch")]),
        node: Dom({
          tag: "p",
          attrs: ConstAttrs({ "class": LStr("sent") }),
          childs: [
            Text({ val: "\n    Search: \"" }),
            Dom({
              tag: "span",
              attrs: ConstAttrs({ "class": LStr("sent-q") }),
              childs: [
                RenderText({ node_id: None, val: Field("lastSentSearch") }),
              ],
              ns: None,
            }),
            Text({ val: "\"\n  " }),
          ],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: {
            handler: HandlerName(name="onInput", ns=Input),
            args: [Name("value")],
          },
          modifiers: [],
        },
        {
          name: "keydown",
          handler: {
            handler: Method("setLastSentSearch"),
            args: [Name("value")],
          },
          modifiers: ["send"],
        },
        {
          name: "keydown",
          handler: { handler: Method("resetQuery"), args: [] },
          modifiers: ["cancel"],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn event_modifiers_views() -> Map[String, @anode.View] {
  { "main": event_modifiers_main_view_ir() }
}

///|
/// The compiled `main` view.
fn show_hide_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-2"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-primary toggle-btn"),
          "data-eid": LNum(0.0),
        }),
        childs: [RenderText({ node_id: None, val: Method("label") })],
        ns: None,
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Field("isOpen"),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("card p-3 border details") }),
          childs: [
            Dom({
              tag: "p",
              attrs: ConstAttrs(Map([])),
              childs: [
                Text({ val: "Details panel — only visible when " }),
                Dom({
                  tag: "code",
                  attrs: ConstAttrs(Map([])),
                  childs: [Text({ val: "isOpen" })],
                  ns: None,
                }),
                Text({ val: " is true." }),
              ],
              ns: None,
            }),
            Text({ val: " " }),
            Dom({
              tag: "button",
              attrs: ConstAttrs({
                "class": LStr("btn btn-sm bump"),
                "data-eid": LNum(1.0),
              }),
              childs: [Text({ val: "Click me" })],
              ns: None,
            }),
            Text({ val: " " }),
            Dom({
              tag: "p",
              attrs: ConstAttrs(Map([])),
              childs: [
                Text({ val: "Clicked " }),
                RenderText({ node_id: Some(0), val: Field("count") }),
                Text({ val: " times." }),
              ],
              ns: None,
            }),
          ],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Hide({
        node_id: None,
        val: Field("isOpen"),
        node: Dom({
          tag: "p",
          attrs: ConstAttrs({ "class": LStr("text-sm opacity-60 hint") }),
          childs: [Text({ val: "(details are hidden)" })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("peek") }),
        childs: [
          Text({ val: "Count, only when open: " }),
          Show({
            node_id: Some(2),
            val: Field("isOpen"),
            node: RenderText({ node_id: Some(1), val: Field("count") }),
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("toggleIsOpen"), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("incCount"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn show_hide_views() -> Map[String, @anode.View] {
  { "main": show_hide_main_view_ir() }
}
