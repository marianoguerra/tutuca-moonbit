// GENERATED by `tutuca gen-views` from rendering.html — DO NOT EDIT.
//
// The views of PushView, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing push_view_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn push_view_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex justify-between") }),
        childs: [
          Dom({
            tag: "input",
            attrs: DynAttrs([
              ConstA(
                name="type",
                val=Const(lit=LStr("search"), from_macro=false),
              ),
              ConstA(
                name="class",
                val=Const(lit=LStr("input"), from_macro=false),
              ),
              Plain(name="value", val=Field("query")),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(0.0), from_macro=false),
              ),
              ConstA(
                name="placeholder",
                val=Const(lit=LStr("Filter entries"), from_macro=false),
              ),
            ]),
            childs: [],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: ConstAttrs({
              "class": LStr("btn bnt-sm btn-primary toggle-btn"),
              "data-eid": LNum(1.0),
            }),
            childs: [
              RenderText({ node_id: None, val: Method("getToggleLabel") }),
            ],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      PushView({
        node_id: None,
        val: Field("view"),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({
            "class": LStr(
              "flex flex-col gap-3 max-h-[40vh] overflow-y-auto pr-3 rows",
            ),
          }),
          childs: [
            Each({
              node_id: 1,
              iter_info: {
                val: Field("items"),
                when_val: Some(HandlerName(name="filterItem", ns=Alter)),
                loop_with_val: None,
                enrich_with_val: None,
              },
              node: RenderIt({ node_id: 0, view_val: None }),
              from_render_each: true,
            }),
          ],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setQuery"), args: [Name("value")] },
          modifiers: [],
        },
        {
          name: "keydown",
          handler: { handler: Method("resetQuery"), args: [] },
          modifiers: ["cancel"],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("toggleView"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn push_view_views() -> Map[String, @anode.View] {
  { "main": push_view_main_view_ir() }
}

///|
/// The compiled `main` view.
fn seq_item_access_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="type", val=Const(lit=LStr("range"), from_macro=false)),
          ConstA(name="min", val=Const(lit=LStr("0"), from_macro=false)),
          Plain(name="max", val=Method("getMaxIndex")),
          Plain(name="value", val=Field("currentIndex")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Render({
        node_id: 0,
        val: SeqAccess(seq="byIndex", key="currentIndex"),
        view_val: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "select",
        attrs: DynAttrs([
          ConstA(name="class", val=Const(lit=LStr("select"), from_macro=false)),
          Plain(name="value", val=Field("currentKey")),
          ConstA(name="data-eid", val=Const(lit=LNum(1.0), from_macro=false)),
        ]),
        childs: [
          Each({
            node_id: 1,
            iter_info: {
              val: Field("byKey"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "option",
              attrs: DynAttrs([Plain(name="value", val=Bind("key"))]),
              childs: [
                RenderText({
                  node_id: None,
                  val: BindMember(name="value", prop="title"),
                }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Render({
        node_id: 2,
        val: SeqAccess(seq="byKey", key="currentKey"),
        view_val: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: {
            handler: Method("setCurrentIndex"),
            args: [Name("valueAsInt")],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setCurrentKey"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn seq_item_access_views() -> Map[String, @anode.View] {
  { "main": seq_item_access_main_view_ir() }
}

///|
/// The compiled `main` view.
fn pagination_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("join") }),
        childs: [
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(
                name="class",
                val=Const(lit=LStr("btn join-item prev"), from_macro=false),
              ),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(0.0), from_macro=false),
              ),
              Plain(name="disabled", val=Method("isFirstPage")),
            ]),
            childs: [Text({ val: "‹ Prev" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: ConstAttrs({
              "class": LStr("btn join-item btn-disabled label"),
            }),
            childs: [RenderText({ node_id: None, val: Method("pageLabel") })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: DynAttrs([
              ConstA(
                name="class",
                val=Const(lit=LStr("btn join-item next"), from_macro=false),
              ),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(1.0), from_macro=false),
              ),
              Plain(name="disabled", val=Method("isLastPage")),
            ]),
            childs: [Text({ val: "Next ›" })],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "ul",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-1") }),
        childs: [
          Each({
            node_id: 1,
            iter_info: {
              val: Field("items"),
              when_val: None,
              loop_with_val: Some(HandlerName(name="paginate", ns=Alter)),
              enrich_with_val: None,
            },
            node: Dom({
              tag: "li",
              attrs: ConstAttrs({ "class": LStr("flex gap-2 items-center") }),
              childs: [
                Dom({
                  tag: "span",
                  attrs: ConstAttrs({ "class": LStr("badge badge-neutral") }),
                  childs: [RenderText({ node_id: None, val: Bind("key") })],
                  ns: None,
                }),
                Text({ val: " " }),
                RenderText({ node_id: Some(0), val: Bind("value") }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: HandlerName(name="prev", ns=Input), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: HandlerName(name="next", ns=Input), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn pagination_views() -> Map[String, @anode.View] {
  { "main": pagination_main_view_ir() }
}
