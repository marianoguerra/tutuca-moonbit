// GENERATED by `tutuca gen-views` from web_component.html — DO NOT EDIT.
//
// The views of EmojiPicker, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing emoji_picker_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let emoji_picker_spec_source : String =
  #|  state {
  #|    current : Any
  #|    isPickerVisible : Bool
  #|  }

///|
/// The compiled `main` view.
fn emoji_picker_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "section",
      [
        @anode.eid(1),
        @anode.attr("class", "join join-vertical gap-3 items-center"),
      ],
      [
        @anode.hide(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("current")]),
          @anode.h("p", [@anode.attr("class", "hint")], [
            @anode.text("\n    Select the "),
            @anode.h("em", [], [@anode.text("Toggle Picker")]),
            @anode.text(
              " button and select an emoji, it will be\n    displayed here\n  ",
            ),
          ]),
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("current")]),
          @anode.h("p", [@anode.attr("class", "label")], [
            @anode.text("Selected emoji:"),
          ]),
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::app("truthy?", [@tutuca.Expr::field("current")]),
          @anode.h("span", [@anode.attr("class", "current text-9xl")], [
            @anode.dyn_text(@tutuca.Expr::field("current")),
          ]),
        ),
        @anode.text(" "),
        @anode.h(
          "button",
          [@anode.attr("class", "btn btn-sm toggle-btn"), @anode.eid(0)],
          [@anode.text("\n    Toggle Picker\n  ")],
        ),
        @anode.text(" "),
        @anode.show(
          @tutuca.Expr::field("isPickerVisible"),
          @anode.h("emoji-picker", [], []),
        ),
      ],
    ),
    [
      [
        @anode.on(
          "click",
          @anode.const_null(),
          action=@anode.set_property_action(
            "isPickerVisible",
            @tutuca.Expr::app("not", [@tutuca.Expr::field("isPickerVisible")]),
          ),
        ),
      ],
      [
        @anode.on("emoji-click", @tutuca.Expr::name("onEmojiClick"), args=[
          @tutuca.Expr::event_path(["value"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn emoji_picker_views() -> Map[String, @anode.View] {
  { "main": emoji_picker_main_view_ir() }
}

///|
/// The EmojiPicker component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `EmojiPickerState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn emoji_picker_component(
  name? : String = "EmojiPicker",
  views? : Map[String, @anode.View] = emoji_picker_views(),
  init? : EmojiPickerState = EmojiPickerState::zero(),
  common_style? : String = emoji_picker_common_style,
  global_style? : String = emoji_picker_global_style,
  update? : (EmojiPickerState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    EmojiPickerState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=emoji_picker_spec_source,
  )
}
