// GENERATED by `tutuca gen-views` from web_component.html — DO NOT EDIT.
//
// The views of EmojiPicker, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing emoji_picker_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn emoji_picker_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "data-eid": LNum(1.0),
      "class": LStr("join join-vertical gap-3 items-center"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Hide({
        node_id: None,
        val: Predicate(pred=IsTruthy, args=[Field("current")]),
        node: Dom({
          tag: "p",
          attrs: ConstAttrs({ "class": LStr("hint") }),
          childs: [
            Text({ val: "\n    Select the " }),
            Dom({
              tag: "em",
              attrs: ConstAttrs(Map([])),
              childs: [Text({ val: "Toggle Picker" })],
              ns: None,
            }),
            Text({
              val: " button and select an emoji, it will be\n    displayed here\n  ",
            }),
          ],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=IsTruthy, args=[Field("current")]),
        node: Dom({
          tag: "p",
          attrs: ConstAttrs({ "class": LStr("label") }),
          childs: [Text({ val: "Selected emoji:" })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=IsTruthy, args=[Field("current")]),
        node: Dom({
          tag: "span",
          attrs: ConstAttrs({ "class": LStr("current text-9xl") }),
          childs: [RenderText({ node_id: None, val: Field("current") })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-sm toggle-btn"),
          "data-eid": LNum(0.0),
        }),
        childs: [Text({ val: "\n    Toggle Picker\n  " })],
        ns: None,
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Field("isPickerVisible"),
        node: Dom({
          tag: "emoji-picker",
          attrs: ConstAttrs(Map([])),
          childs: [],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("toggleIsPickerVisible"), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "emoji-click",
          handler: {
            handler: HandlerName(name="onEmojiClick", ns=Input),
            args: [Name("value")],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn emoji_picker_views() -> Map[String, @anode.View] {
  { "main": emoji_picker_main_view_ir() }
}
