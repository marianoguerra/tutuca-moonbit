// GENERATED by `tutuca gen-views` from basics.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views basics.html --name Basics --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the MinimumViableComponent component.
///
/// Declared as `interface minimum-viable-component` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct MinimumViableComponentState {}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..MinimumViableComponentState::zero(), label: "Counter" }
fn MinimumViableComponentState::zero() -> MinimumViableComponentState {
  MinimumViableComponentState::{  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn minimum_viable_component_zero_args() -> Map[String, @tutuca.Value] {
  @component.Fields::encode(MinimumViableComponentState::zero())
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let minimum_viable_component_schema_fingerprint : String = "0f2138963e0265a7"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for MinimumViableComponentState with fn encode(_s) {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for MinimumViableComponentState with fn decode(_f) {
  Some(MinimumViableComponentState::{  })
}

///|
/// The views declared in basics.html.
pub(all) enum MinimumViableComponentView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn MinimumViableComponentView::name(
  self : MinimumViableComponentView,
) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let minimum_viable_component_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let minimum_viable_component_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for MinimumViableComponentState with fn schema() {
  @component.SchemaInfo::new(
    name="MinimumViableComponent",
    fingerprint="0f2138963e0265a7",
    view_names=["main"],
  )
}

///|
/// The state of the StaticViewComponent component.
///
/// Declared as `interface static-view-component` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct StaticViewComponentState {}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..StaticViewComponentState::zero(), label: "Counter" }
fn StaticViewComponentState::zero() -> StaticViewComponentState {
  StaticViewComponentState::{  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn static_view_component_zero_args() -> Map[String, @tutuca.Value] {
  @component.Fields::encode(StaticViewComponentState::zero())
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let static_view_component_schema_fingerprint : String = "a2006cc55bf0a041"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for StaticViewComponentState with fn encode(_s) {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for StaticViewComponentState with fn decode(_f) {
  Some(StaticViewComponentState::{  })
}

///|
/// The views declared in basics.html.
pub(all) enum StaticViewComponentView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn StaticViewComponentView::name(self : StaticViewComponentView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let static_view_component_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let static_view_component_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for StaticViewComponentState with fn schema() {
  @component.SchemaInfo::new(
    name="StaticViewComponent",
    fingerprint="a2006cc55bf0a041",
    view_names=["main"],
  )
}

///|
/// The state of the TextDirective component.
///
/// Declared as `interface text-directive` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct TextDirectiveState {
  str : String
  num : Int
  bool : Bool
  notSet : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..TextDirectiveState::zero(), label: "Counter" }
fn TextDirectiveState::zero() -> TextDirectiveState {
  TextDirectiveState::{ str: "", num: 0, bool: false, notSet: Null }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn text_directive_zero_args() -> Map[String, @tutuca.Value] {
  @component.Fields::encode(TextDirectiveState::zero())
}

///|
/// Every field the TextDirective state declares — typed field names for tests and inspectors.
pub(all) enum TextDirectiveField {
  Str
  Num
  Bool
  NotSet
} derive(Eq, @debug.Debug)

///|
pub fn TextDirectiveField::name(self : TextDirectiveField) -> String {
  match self {
    Str => "str"
    Num => "num"
    Bool => "bool"
    NotSet => "notSet"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn TextDirectiveField::kind(
  self : TextDirectiveField,
) -> @component.FieldKind {
  match self {
    Str => FText
    Num => FInt
    Bool => FBool
    NotSet => FAny
  }
}

///|
/// Every field, in declaration order.
pub let text_directive_field_names : Array[String] = [
  "str", "num", "bool", "notSet",
]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let text_directive_schema_fingerprint : String = "f6fb027d2388083c"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for TextDirectiveState with fn encode(s) {
  {
    "str": Str(s.str),
    "num": Num(s.num.to_double()),
    "bool": Bool(s.bool),
    "notSet": s.notSet,
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for TextDirectiveState with fn decode(f) {
  guard f.get("str") is Some(Str(v_str)) else { return None }
  guard f.get("num") is Some(Num(t1_num)) else { return None }
  let v_num = t1_num.to_int()
  guard f.get("bool") is Some(Bool(v_bool)) else { return None }
  let v_notSet = f.get("notSet").unwrap_or(Null)
  Some(TextDirectiveState::{
    str: v_str,
    num: v_num,
    bool: v_bool,
    notSet: v_notSet,
  })
}

///|
/// The views declared in basics.html.
pub(all) enum TextDirectiveView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn TextDirectiveView::name(self : TextDirectiveView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let text_directive_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let text_directive_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum TextDirectiveMethod {
  GetStrUpper
} derive(Eq, @debug.Debug)

///|
pub fn TextDirectiveMethod::name(self : TextDirectiveMethod) -> String {
  match self {
    GetStrUpper => "getStrUpper"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] text_directive_compute(
  f : (TextDirectiveMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([GetStrUpper] : Array[TextDirectiveMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for TextDirectiveState with fn schema() {
  @component.SchemaInfo::new(
    name="TextDirective",
    fingerprint="f6fb027d2388083c",
    fields=[
      @component.FieldInfo::new("str", "string", FText),
      @component.FieldInfo::new("num", "s32", FInt),
      @component.FieldInfo::new("bool", "bool", FBool),
      @component.FieldInfo::new("notSet", "any", FAny, elem="any"),
    ],
    methods=["getStrUpper"],
    view_names=["main"],
  )
}

///|
/// The state of the DangerSetInnerHtml component.
///
/// Declared as `interface danger-set-inner-html` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct DangerSetInnerHtmlState {
  content : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..DangerSetInnerHtmlState::zero(), label: "Counter" }
fn DangerSetInnerHtmlState::zero() -> DangerSetInnerHtmlState {
  DangerSetInnerHtmlState::{ content: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn danger_set_inner_html_zero_args() -> Map[String, @tutuca.Value] {
  @component.Fields::encode(DangerSetInnerHtmlState::zero())
}

///|
/// Every field the DangerSetInnerHtml state declares — typed field names for tests and inspectors.
pub(all) enum DangerSetInnerHtmlField {
  Content
} derive(Eq, @debug.Debug)

///|
pub fn DangerSetInnerHtmlField::name(self : DangerSetInnerHtmlField) -> String {
  match self {
    Content => "content"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn DangerSetInnerHtmlField::kind(
  self : DangerSetInnerHtmlField,
) -> @component.FieldKind {
  match self {
    Content => FText
  }
}

///|
/// Every field, in declaration order.
pub let danger_set_inner_html_field_names : Array[String] = ["content"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let danger_set_inner_html_schema_fingerprint : String = "20e28deaad6a8c77"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for DangerSetInnerHtmlState with fn encode(s) {
  { "content": Str(s.content) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for DangerSetInnerHtmlState with fn decode(f) {
  guard f.get("content") is Some(Str(v_content)) else { return None }
  Some(DangerSetInnerHtmlState::{ content: v_content })
}

///|
/// The views declared in basics.html.
pub(all) enum DangerSetInnerHtmlView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn DangerSetInnerHtmlView::name(self : DangerSetInnerHtmlView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let danger_set_inner_html_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let danger_set_inner_html_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for DangerSetInnerHtmlState with fn schema() {
  @component.SchemaInfo::new(
    name="DangerSetInnerHtml",
    fingerprint="20e28deaad6a8c77",
    fields=[@component.FieldInfo::new("content", "string", FText)],
    view_names=["main"],
  )
}
