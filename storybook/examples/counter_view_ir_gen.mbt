// GENERATED by `tutuca gen-views` from counter.html — DO NOT EDIT.
//
// The views of Counter, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing counter_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn counter_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("join join-vertical"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-error"),
          "data-eid": LNum(0.0),
        }),
        childs: [Text({ val: "-" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("stats") }),
        childs: [
          Dom({
            tag: "div",
            attrs: ConstAttrs({ "class": LStr("stat text-center") }),
            childs: [
              Dom({
                tag: "div",
                attrs: ConstAttrs({ "class": LStr("stat-title") }),
                childs: [Text({ val: "Count" })],
                ns: None,
              }),
              Dom({
                tag: "div",
                attrs: ConstAttrs({ "class": LStr("stat-value") }),
                childs: [RenderText({ node_id: None, val: Field("count") })],
                ns: None,
              }),
              Dom({
                tag: "div",
                attrs: ConstAttrs({ "class": LStr("stat-desc") }),
                childs: [Text({ val: "Current Count" })],
                ns: None,
              }),
            ],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-success"),
          "data-eid": LNum(1.0),
        }),
        childs: [Text({ val: "+" })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: HandlerName(name="dec", ns=Input), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("inc"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn counter_views() -> Map[String, @anode.View] {
  { "main": counter_main_view_ir() }
}
