// GENERATED by `tutuca gen-views` from dnd.html — DO NOT EDIT.
//
// The views of DnDExample, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing dn_d_example_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn dn_d_example_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="type", val=Const(lit=LStr("search"), from_macro=false)),
          ConstA(name="class", val=Const(lit=LStr("input"), from_macro=false)),
          Plain(name="value", val=Field("query")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
          ConstA(
            name="placeholder",
            val=Const(lit=LStr("Filter entries"), from_macro=false),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "div",
        attrs: ConstAttrs(Map([])),
        childs: [
          Each({
            node_id: 1,
            iter_info: {
              val: Field("items"),
              when_val: Some(HandlerName(name="filterItem", ns=Alter)),
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "div",
              attrs: ConstAttrs({
                "class": LStr("cursor-grab row"),
                "draggable": LStr("true"),
                "data-dragtype": LStr("my-list-item"),
                "data-droptarget": LStr("my-list-item"),
                "data-eid": LNum(1.0),
              }),
              childs: [
                Dom({
                  tag: "span",
                  attrs: ConstAttrs({ "class": LStr("k") }),
                  childs: [RenderText({ node_id: None, val: Bind("key") })],
                  ns: None,
                }),
                Text({ val: ": " }),
                RenderText({ node_id: Some(0), val: Bind("value") }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setQuery"), args: [Name("value")] },
          modifiers: [],
        },
        {
          name: "keydown",
          handler: { handler: Method("resetQuery"), args: [] },
          modifiers: ["cancel"],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "drop",
          handler: {
            handler: HandlerName(name="onDropOnItem", ns=Input),
            args: [Bind("key"), Name("dragInfo")],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="\n    [data-dragging=\"1\"] { opacity: 0.5; }\n    [data-draggingover=\"my-list-item\"] {\n      border-top: 2rem solid transparent;\n      outline: 1px solid #77777777;\n      transition: 0.15s border-top ease;\n    }\n  ",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn dn_d_example_views() -> Map[String, @anode.View] {
  { "main": dn_d_example_main_view_ir() }
}
