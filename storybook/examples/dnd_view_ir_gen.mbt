// GENERATED by `tutuca gen-views` from dnd.html — DO NOT EDIT.
//
// The views of DnDExample, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing dn_d_example_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let dn_d_example_spec_source : String =
  #|  state {
  #|    items : Array[String]
  #|    query : String
  #|  }

///|
/// The compiled `main` view.
fn dn_d_example_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h(
        "input",
        [
          @anode.attr("type", "search"),
          @anode.attr("class", "input"),
          @anode.dyn_attr("value", @tutuca.Expr::field("query")),
          @anode.eid(0),
          @anode.attr("placeholder", "Filter entries"),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h("div", [], [
        @anode.each(
          1,
          @tutuca.Expr::field("items"),
          @anode.h(
            "div",
            [
              @anode.attr("class", "cursor-grab row"),
              @anode.attr("draggable", "true"),
              @anode.attr("data-dragtype", "my-list-item"),
              @anode.attr("data-droptarget", "my-list-item"),
              @anode.eid(1),
            ],
            [
              @anode.h("span", [@anode.attr("class", "k")], [
                @anode.dyn_text(@tutuca.Expr::bind("key")),
              ]),
              @anode.text(": "),
              @anode.dyn_text(@tutuca.Expr::bind("value"), id=0),
            ],
          ),
          when_val=@tutuca.Expr::handler_name("filterItem", Alter),
        ),
      ]),
    ]),
    [
      [
        @anode.on("input", @tutuca.Expr::handler_name("setQuery", Receive), args=[
          @tutuca.Expr::event_path(["value"]),
        ]),
        @anode.on(
          "keydown",
          @tutuca.Expr::handler_name("resetQuery", Receive),
          modifiers=["cancel"],
        ),
      ],
      [
        @anode.on("drop", @tutuca.Expr::handler_name("onDropOnItem", Receive), args=[
          @tutuca.Expr::bind("key"),
          @tutuca.Expr::event_path(["dragInfo"]),
        ]),
      ],
    ],
    style="\n    [data-dragging=\"1\"] { opacity: 0.5; }\n    [data-draggingover=\"my-list-item\"] {\n      border-top: 2rem solid transparent;\n      outline: 1px solid #77777777;\n      transition: 0.15s border-top ease;\n    }\n  ",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn dn_d_example_views() -> Map[String, @anode.View] {
  { "main": dn_d_example_main_view_ir() }
}

///|
/// The DnDExample component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `DnDExampleState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn dn_d_example_component(
  name? : String = "DnDExample",
  views? : Map[String, @anode.View] = dn_d_example_views(),
  init? : DnDExampleState = DnDExampleState::zero(),
  common_style? : String = dn_d_example_common_style,
  global_style? : String = dn_d_example_global_style,
  update? : (DnDExampleState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    DnDExampleState,
  ],
  swap? : (DnDExampleInput) -> ((
    DnDExampleState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  when? : (DnDExampleWhen) -> ((
    DnDExampleState,
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
    &@tutuca.Stack,
  ) -> Bool)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([OnDropOnItem, ResetQuery, SetQuery] : Array[DnDExampleInput]),
          DnDExampleInput::name,
          f,
        )
      None => Map([])
    },
    when=match when {
      Some(f) =>
        @component.build_bucket(
          ([FilterItem] : Array[DnDExampleWhen]),
          DnDExampleWhen::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=dn_d_example_spec_source,
  )
}
