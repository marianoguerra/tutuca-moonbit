// GENERATED by `tutuca gen-views` from request.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views request.html --name RequestExample --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the RequestExample component.
///
/// Declared as `interface request-example` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct RequestExampleState {
  items : Array[@tutuca.Value]
  query : String
  view : String
  isLoading : Bool
  error : @tutuca.Value
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..RequestExampleState::zero(), label: "Counter" }
fn RequestExampleState::zero() -> RequestExampleState {
  RequestExampleState::{
    items: [],
    query: "",
    view: "",
    isLoading: false,
    error: Null,
  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn request_example_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(RequestExampleState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the RequestExample state declares — typed field names for tests and inspectors.
pub(all) enum RequestExampleField {
  Items
  Query
  View
  IsLoading
  Error
} derive(Eq, @debug.Debug)

///|
pub fn RequestExampleField::name(self : RequestExampleField) -> String {
  match self {
    Items => "items"
    Query => "query"
    View => "view"
    IsLoading => "isLoading"
    Error => "error"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn RequestExampleField::kind(
  self : RequestExampleField,
) -> @component.FieldKind {
  match self {
    Items => @component.FList
    Query => @component.FText
    View => @component.FText
    IsLoading => @component.FBool
    Error => @component.FAny
  }
}

///|
/// Every field, in declaration order.
pub let request_example_field_names : Array[String] = [
  "items", "query", "view", "isLoading", "error",
]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn request_example_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let request_example_schema_fingerprint : String = "fb42c79b675f798e"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn request_example_encode(
  s : RequestExampleState,
) -> Map[String, @tutuca.Value] {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "query": Str(s.query),
    "view": Str(s.view),
    "isLoading": Bool(s.isLoading),
    "error": s.error,
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn request_example_decode(
  f : Map[String, @tutuca.Value],
) -> RequestExampleState? {
  guard f.get("items") is Some(List(l_items)) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for x in l_items {
    v_items.push(x)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  guard f.get("view") is Some(Str(v_view)) else { return None }
  guard f.get("isLoading") is Some(Bool(v_isLoading)) else { return None }
  let v_error = f.get("error").unwrap_or(Null)
  Some(RequestExampleState::{
    items: v_items,
    query: v_query,
    view: v_view,
    isLoading: v_isLoading,
    error: v_error,
  })
}

///|
/// The views declared in request.html.
pub(all) enum RequestExampleView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn RequestExampleView::name(self : RequestExampleView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let request_example_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let request_example_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum RequestExampleInput {
  LoadAnotherWay
} derive(Eq, @debug.Debug)

///|
pub fn RequestExampleInput::name(self : RequestExampleInput) -> String {
  match self {
    LoadAnotherWay => "loadAnotherWay"
  }
}

///|
pub fn RequestExampleInput::parse(s : String) -> RequestExampleInput? {
  match s {
    "loadAnotherWay" => Some(LoadAnotherWay)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to request.html breaks every `match` over this type
/// until it is handled.
pub(all) enum RequestExampleMsg {
  /// `@on.click="loadAnotherWay …"`
  LoadAnotherWay
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn RequestExampleMsg::from_dispatch(
  d : @component.Dispatch,
) -> RequestExampleMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("loadAnotherWay", []) => LoadAnotherWay
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum RequestExampleMethod {
  ResetQuery
  SetQuery
  ToggleView
} derive(Eq, @debug.Debug)

///|
pub fn RequestExampleMethod::name(self : RequestExampleMethod) -> String {
  match self {
    ResetQuery => "resetQuery"
    SetQuery => "setQuery"
    ToggleView => "toggleView"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] request_example_mutate(
  f : (RequestExampleMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([ResetQuery, SetQuery, ToggleView] : Array[RequestExampleMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] request_example_compute(
  f : (RequestExampleMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([ResetQuery, SetQuery, ToggleView] : Array[RequestExampleMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] request_example_swap(
  f : (RequestExampleMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ResetQuery, SetQuery, ToggleView] : Array[RequestExampleMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}
