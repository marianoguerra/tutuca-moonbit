// GENERATED by `tutuca gen-views` from basics.html — DO NOT EDIT.
//
// The views of MinimumViableComponent, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing minimum_viable_component_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn minimum_viable_component_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Comment({ val: "Empty View in ANode.parse" })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn minimum_viable_component_views() -> Map[String, @anode.View] {
  { "main": minimum_viable_component_main_view_ir() }
}

///|
/// The compiled `main` view.
fn static_view_component_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "p",
    attrs: ConstAttrs({ "data-vid": LStr("main") }),
    childs: [Text({ val: "Hello, world!" })],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn static_view_component_views() -> Map[String, @anode.View] {
  { "main": static_view_component_main_view_ir() }
}

///|
/// The compiled `main` view.
fn text_directive_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("grid grid-cols-[auto_auto] gap-x-4 gap-y-2 items-center"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "span",
        attrs: ConstAttrs(Map([])),
        childs: [Text({ val: "String:" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs({ "class": LStr("v-str") }),
        childs: [RenderText({ node_id: None, val: Field("str") })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs(Map([])),
        childs: [Text({ val: "Number:" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs({ "class": LStr("v-num") }),
        childs: [
          RenderText({ node_id: None, val: Field("num") }),
          Text({ val: " <- text directive is prepended" }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs(Map([])),
        childs: [Text({ val: "Boolean:" })],
        ns: None,
      }),
      Text({ val: " " }),
      RenderText({ node_id: Some(0), val: Field("bool") }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs(Map([])),
        childs: [Text({ val: "notSet:" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs({ "class": LStr("v-notset") }),
        childs: [RenderText({ node_id: None, val: Field("notSet") })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs(Map([])),
        childs: [Text({ val: "Method Call:" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs({ "class": LStr("v-method") }),
        childs: [RenderText({ node_id: None, val: Method("getStrUpper") })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn text_directive_views() -> Map[String, @anode.View] {
  { "main": text_directive_main_view_ir() }
}

///|
/// The compiled `main` view.
fn danger_set_inner_html_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({ "data-vid": LStr("main") }),
    childs: [
      Dom({
        tag: "div",
        attrs: DynAttrs([
          ConstA(name="class", val=Const(lit=LStr("raw"), from_macro=false)),
          RawHtml(val=Some(Field("content"))),
        ]),
        childs: [],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn danger_set_inner_html_views() -> Map[String, @anode.View] {
  { "main": danger_set_inner_html_main_view_ir() }
}
