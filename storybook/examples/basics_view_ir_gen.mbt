// GENERATED by `tutuca gen-views` from basics.html — DO NOT EDIT.
//
// The views of MinimumViableComponent, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing minimum_viable_component_views() as `views~` means the template parser
// never runs at startup.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn minimum_viable_component_main_view_ir() -> @anode.View {
  @anode.View::from_ir("main", @anode.comment("Empty View in ANode.parse"), [])
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn minimum_viable_component_views() -> Map[String, @anode.View] {
  { "main": minimum_viable_component_main_view_ir() }
}

///|
/// The compiled `main` view.
fn static_view_component_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("p", [], [@anode.text("Hello, world!")]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn static_view_component_views() -> Map[String, @anode.View] {
  { "main": static_view_component_main_view_ir() }
}

///|
/// The compiled `main` view.
fn text_directive_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "grid grid-cols-[auto_auto] gap-x-4 gap-y-2 items-center",
        ),
      ],
      [
        @anode.h("span", [], [@anode.text("String:")]),
        @anode.text(" "),
        @anode.h("span", [@anode.attr("class", "v-str")], [
          @anode.dyn_text(Field("str")),
        ]),
        @anode.text(" "),
        @anode.h("span", [], [@anode.text("Number:")]),
        @anode.text(" "),
        @anode.h("span", [@anode.attr("class", "v-num")], [
          @anode.dyn_text(Field("num")),
          @anode.text(" <- text directive is prepended"),
        ]),
        @anode.text(" "),
        @anode.h("span", [], [@anode.text("Boolean:")]),
        @anode.text(" "),
        @anode.dyn_text(Field("bool"), id=0),
        @anode.text(" "),
        @anode.h("span", [], [@anode.text("notSet:")]),
        @anode.text(" "),
        @anode.h("span", [@anode.attr("class", "v-notset")], [
          @anode.dyn_text(Field("notSet")),
        ]),
        @anode.text(" "),
        @anode.h("span", [], [@anode.text("Method Call:")]),
        @anode.text(" "),
        @anode.h("span", [@anode.attr("class", "v-method")], [
          @anode.dyn_text(Method("getStrUpper")),
        ]),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn text_directive_views() -> Map[String, @anode.View] {
  { "main": text_directive_main_view_ir() }
}

///|
/// The compiled `main` view.
fn danger_set_inner_html_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [], [
      @anode.h(
        "div",
        [@anode.attr("class", "raw"), @anode.raw_html(val=Field("content"))],
        [],
      ),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn danger_set_inner_html_views() -> Map[String, @anode.View] {
  { "main": danger_set_inner_html_main_view_ir() }
}
