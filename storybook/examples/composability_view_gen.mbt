// GENERATED by `tutuca gen-views` from composability.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views composability.html --name Composability --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the Composability component.
///
/// Declared as `interface composability` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct ComposabilityState {
  activeSection : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ComposabilityState::zero(), label: "Counter" }
fn ComposabilityState::zero() -> ComposabilityState {
  ComposabilityState::{ activeSection: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn composability_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(ComposabilityState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the Composability state declares — typed field names for tests and inspectors.
pub(all) enum ComposabilityField {
  ActiveSection
  Todo
  Json
  Tree
  PersonalSite
  VisualWasm
  Dnd
} derive(Eq, @debug.Debug)

///|
pub fn ComposabilityField::name(self : ComposabilityField) -> String {
  match self {
    ActiveSection => "activeSection"
    Todo => "todo"
    Json => "json"
    Tree => "tree"
    PersonalSite => "personalSite"
    VisualWasm => "visualWasm"
    Dnd => "dnd"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn ComposabilityField::kind(
  self : ComposabilityField,
) -> @component.FieldKind {
  match self {
    ActiveSection => @component.FText
    Todo => @component.FComp(comp="", args=Map([]))
    Json => @component.FComp(comp="", args=Map([]))
    Tree => @component.FComp(comp="", args=Map([]))
    PersonalSite => @component.FComp(comp="", args=Map([]))
    VisualWasm => @component.FComp(comp="", args=Map([]))
    Dnd => @component.FComp(comp="", args=Map([]))
  }
}

///|
/// Every field, in declaration order.
pub let composability_field_names : Array[String] = [
  "activeSection", "todo", "json", "tree", "personalSite", "visualWasm", "dnd",
]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn composability_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let composability_schema_fingerprint : String = "a3b3ea198bfa01dd"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn composability_encode(s : ComposabilityState) -> Map[String, @tutuca.Value] {
  { "activeSection": Str(s.activeSection) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn composability_decode(f : Map[String, @tutuca.Value]) -> ComposabilityState? {
  guard f.get("activeSection") is Some(Str(v_activeSection)) else {
    return None
  }
  Some(ComposabilityState::{ activeSection: v_activeSection })
}

///|
/// The views declared in composability.html.
pub(all) enum ComposabilityView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ComposabilityView::name(self : ComposabilityView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let composability_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let composability_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum ComposabilityMethod {
  SetActiveSection
} derive(Eq, @debug.Debug)

///|
pub fn ComposabilityMethod::name(self : ComposabilityMethod) -> String {
  match self {
    SetActiveSection => "setActiveSection"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] composability_mutate(
  f : (ComposabilityMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetActiveSection] : Array[ComposabilityMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] composability_compute(
  f : (ComposabilityMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetActiveSection] : Array[ComposabilityMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] composability_swap(
  f : (ComposabilityMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetActiveSection] : Array[ComposabilityMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Constant `id` attributes in the views — typed selectors for tests.
pub(all) enum ComposabilityId {
  TabDnd
  TabJson
  TabPersonalSite
  TabTodo
  TabTree
  TabVisualWasm
} derive(Eq, @debug.Debug)

///|
pub fn ComposabilityId::id(self : ComposabilityId) -> String {
  match self {
    TabDnd => "tab-dnd"
    TabJson => "tab-json"
    TabPersonalSite => "tab-personal-site"
    TabTodo => "tab-todo"
    TabTree => "tab-tree"
    TabVisualWasm => "tab-visual-wasm"
  }
}

///|
/// The `#id` CSS selector for this element.
pub fn ComposabilityId::selector(self : ComposabilityId) -> String {
  "#" + self.id()
}
