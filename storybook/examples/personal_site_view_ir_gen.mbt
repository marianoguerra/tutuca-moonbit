// GENERATED by `tutuca gen-views` from personal_site.html — DO NOT EDIT.
//
// The views of AltUrl, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing alt_url_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn alt_url_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "a",
    attrs: DynAttrs([
      ConstA(
        name="class",
        val=Const(
          lit=LStr(
            "badge badge-ghost badge-sm cursor-pointer gap-1 no-underline hover:bg-base-200 alt-url",
          ),
          from_macro=false,
        ),
      ),
      Plain(name="href", val=Field("url")),
      ConstA(name="target", val=Const(lit=LStr("_blank"), from_macro=false)),
      ConstA(name="data-vid", val=Const(lit=LStr("main"), from_macro=false)),
    ]),
    childs: [
      Text({ val: "🔗 " }),
      Dom({
        tag: "span",
        attrs: ConstAttrs(Map([])),
        childs: [RenderText({ node_id: None, val: Field("label") })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn alt_url_views() -> Map[String, @anode.View] {
  { "main": alt_url_main_view_ir() }
}

///|
/// The compiled `main` view.
fn ps_entry_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr(
        "card bg-base-200 shadow-sm p-2 sm:p-3 flex flex-col gap-2 ps-entry",
      ),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({
          "class": LStr("flex gap-1 sm:gap-2 flex-wrap items-center"),
        }),
        childs: [
          Show({
            node_id: None,
            val: Field("featured"),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs({ "class": LStr("star") }),
              childs: [Text({ val: "⭐" })],
              ns: None,
            }),
          }),
          Text({ val: " " }),
          Show({
            node_id: None,
            val: Method("isSpanish"),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs({ "class": LStr("flag") }),
              childs: [RenderText({ node_id: None, val: Method("langFlag") })],
              ns: None,
            }),
          }),
          Text({ val: " " }),
          Show({
            node_id: None,
            val: Predicate(pred=IsTruthy, args=[Field("startYear")]),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs({
                "class": LStr(
                  "badge badge-ghost badge-sm border-0 font-bold years",
                ),
              }),
              childs: [
                RenderText({ node_id: None, val: Method("yearDisplay") }),
              ],
              ns: None,
            }),
          }),
          Text({ val: " " }),
          Show({
            node_id: None,
            val: Field("role"),
            node: Dom({
              tag: "span",
              attrs: ConstAttrs({
                "class": LStr(
                  "badge badge-neutral badge-sm border-0 font-bold role",
                ),
              }),
              childs: [RenderText({ node_id: None, val: Field("role") })],
              ns: None,
            }),
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "a",
        attrs: DynAttrs([
          Plain(name="href", val=Field("url")),
          ConstA(name="target", val=Const(lit=LStr("_blank"), from_macro=false)),
          ConstA(
            name="class",
            val=Const(lit=LStr("link link-hover"), from_macro=false),
          ),
        ]),
        childs: [
          Dom({
            tag: "h2",
            attrs: ConstAttrs({ "class": LStr("font-bold title") }),
            childs: [RenderText({ node_id: None, val: Field("title") })],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("text-sm opacity-70 desc") }),
        childs: [RenderText({ node_id: None, val: Field("description") })],
        ns: None,
      }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({
          "class": LStr("flex gap-1 sm:gap-2 flex-wrap cats"),
        }),
        childs: [
          Each({
            node_id: 0,
            iter_info: {
              val: Field("categories"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: Some(
                HandlerName(name="enrichCategoryBadge", ns=Alter),
              ),
            },
            node: Dom({
              tag: "span",
              attrs: DynAttrs([Plain(name="class", val=Bind("badgeClass"))]),
              childs: [RenderText({ node_id: None, val: Bind("value") })],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Hide({
        node_id: None,
        val: Predicate(pred=IsEmpty, args=[Field("altUrls")]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("alts") }),
          childs: [
            Dom({
              tag: "ul",
              attrs: ConstAttrs(Map([])),
              childs: [
                Each({
                  node_id: 2,
                  iter_info: {
                    val: Field("altUrls"),
                    when_val: None,
                    loop_with_val: None,
                    enrich_with_val: None,
                  },
                  node: Dom({
                    tag: "li",
                    attrs: ConstAttrs(Map([])),
                    childs: [RenderIt({ node_id: 1, view_val: None })],
                    ns: None,
                  }),
                  from_render_each: false,
                }),
              ],
              ns: None,
            }),
          ],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn ps_entry_views() -> Map[String, @anode.View] {
  { "main": ps_entry_main_view_ir() }
}

///|
/// The compiled `main` view.
fn root_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("ps-root flex flex-col gap-4"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("card bg-base-200 p-4 controls") }),
        childs: [
          Dom({
            tag: "p",
            attrs: ConstAttrs({ "class": LStr("text-sm mb-4") }),
            childs: [
              Text({
                val: "💡 Click tags to toggle. Solo / unsolo toggle with ",
              }),
              Dom({
                tag: "kbd",
                attrs: ConstAttrs({ "class": LStr("text-mono") }),
                childs: [Text({ val: "alt/option + click" })],
                ns: None,
              }),
              Text({ val: "." }),
            ],
            ns: None,
          }),
          Dom({
            tag: "div",
            attrs: ConstAttrs({
              "class": LStr(
                "grid grid-cols-1 sm:grid-cols-[auto_1fr] gap-y-1 sm:gap-x-3 sm:gap-y-3 items-start",
              ),
            }),
            childs: [
              Dom({
                tag: "span",
                attrs: ConstAttrs({
                  "class": LStr("font-semibold text-sm pt-1"),
                }),
                childs: [Text({ val: "Categories" })],
                ns: None,
              }),
              Text({ val: " " }),
              Dom({
                tag: "div",
                attrs: ConstAttrs({
                  "class": LStr("cat-facets flex flex-wrap gap-2"),
                }),
                childs: [
                  Each({
                    node_id: 0,
                    iter_info: {
                      val: Field("allCategories"),
                      when_val: None,
                      loop_with_val: None,
                      enrich_with_val: Some(
                        HandlerName(name="enrichCategoryBindings", ns=Alter),
                      ),
                    },
                    node: Dom({
                      tag: "button",
                      attrs: DynAttrs([
                        Plain(name="class", val=Bind("btnClass")),
                        ConstA(
                          name="data-eid",
                          val=Const(lit=LNum(0.0), from_macro=false),
                        ),
                      ]),
                      childs: [
                        RenderText({ node_id: None, val: Bind("value") }),
                      ],
                      ns: None,
                    }),
                    from_render_each: false,
                  }),
                ],
                ns: None,
              }),
              Text({ val: " " }),
              Dom({
                tag: "span",
                attrs: ConstAttrs({
                  "class": LStr("font-semibold text-sm pt-1"),
                }),
                childs: [Text({ val: "Roles" })],
                ns: None,
              }),
              Text({ val: " " }),
              Dom({
                tag: "div",
                attrs: ConstAttrs({
                  "class": LStr("role-facets flex flex-wrap gap-2"),
                }),
                childs: [
                  Each({
                    node_id: 1,
                    iter_info: {
                      val: Field("allRoles"),
                      when_val: None,
                      loop_with_val: None,
                      enrich_with_val: Some(
                        HandlerName(name="enrichRoleBindings", ns=Alter),
                      ),
                    },
                    node: Dom({
                      tag: "button",
                      attrs: DynAttrs([
                        Plain(name="class", val=Bind("btnClass")),
                        ConstA(
                          name="data-eid",
                          val=Const(lit=LNum(1.0), from_macro=false),
                        ),
                      ]),
                      childs: [
                        RenderText({ node_id: None, val: Bind("value") }),
                      ],
                      ns: None,
                    }),
                    from_render_each: false,
                  }),
                ],
                ns: None,
              }),
              Text({ val: " " }),
              Dom({
                tag: "span",
                attrs: ConstAttrs({
                  "class": LStr("font-semibold text-sm pt-1"),
                }),
                childs: [Text({ val: "Sort By" })],
                ns: None,
              }),
              Text({ val: " " }),
              Dom({
                tag: "div",
                attrs: ConstAttrs({
                  "class": LStr("flex flex-wrap items-center gap-2"),
                }),
                childs: [
                  Dom({
                    tag: "label",
                    attrs: ConstAttrs({
                      "class": LStr("swap btn btn-xs btn-outline btn-primary"),
                    }),
                    childs: [
                      Dom({
                        tag: "input",
                        attrs: DynAttrs([
                          ConstA(
                            name="type",
                            val=Const(lit=LStr("checkbox"), from_macro=false),
                          ),
                          ConstA(
                            name="class",
                            val=Const(lit=LStr("sort-end"), from_macro=false),
                          ),
                          Plain(name="checked", val=Field("sortByEnd")),
                          ConstA(
                            name="data-eid",
                            val=Const(lit=LNum(2.0), from_macro=false),
                          ),
                        ]),
                        childs: [],
                        ns: None,
                      }),
                      Text({ val: " " }),
                      Dom({
                        tag: "span",
                        attrs: ConstAttrs({ "class": LStr("swap-on") }),
                        childs: [Text({ val: "End Year" })],
                        ns: None,
                      }),
                      Text({ val: " " }),
                      Dom({
                        tag: "span",
                        attrs: ConstAttrs({ "class": LStr("swap-off") }),
                        childs: [Text({ val: "Start Year" })],
                        ns: None,
                      }),
                    ],
                    ns: None,
                  }),
                  Text({ val: " " }),
                  Dom({
                    tag: "label",
                    attrs: ConstAttrs({
                      "class": LStr("swap btn btn-xs btn-outline btn-primary"),
                    }),
                    childs: [
                      Dom({
                        tag: "input",
                        attrs: DynAttrs([
                          ConstA(
                            name="type",
                            val=Const(lit=LStr("checkbox"), from_macro=false),
                          ),
                          ConstA(
                            name="class",
                            val=Const(lit=LStr("sort-asc"), from_macro=false),
                          ),
                          Plain(name="checked", val=Field("sortAsc")),
                          ConstA(
                            name="data-eid",
                            val=Const(lit=LNum(3.0), from_macro=false),
                          ),
                        ]),
                        childs: [],
                        ns: None,
                      }),
                      Text({ val: " " }),
                      Dom({
                        tag: "span",
                        attrs: ConstAttrs({ "class": LStr("swap-on") }),
                        childs: [Text({ val: "↑ Oldest first" })],
                        ns: None,
                      }),
                      Text({ val: " " }),
                      Dom({
                        tag: "span",
                        attrs: ConstAttrs({ "class": LStr("swap-off") }),
                        childs: [Text({ val: "↓ Newest first" })],
                        ns: None,
                      }),
                    ],
                    ns: None,
                  }),
                  Text({ val: " " }),
                  Dom({
                    tag: "label",
                    attrs: ConstAttrs({
                      "class": LStr("flex items-center gap-2 cursor-pointer"),
                    }),
                    childs: [
                      Dom({
                        tag: "input",
                        attrs: DynAttrs([
                          ConstA(
                            name="type",
                            val=Const(lit=LStr("checkbox"), from_macro=false),
                          ),
                          ConstA(
                            name="class",
                            val=Const(
                              lit=LStr("toggle toggle-sm feat-first"),
                              from_macro=false,
                            ),
                          ),
                          Plain(name="checked", val=Field("featuredFirst")),
                          ConstA(
                            name="data-eid",
                            val=Const(lit=LNum(4.0), from_macro=false),
                          ),
                        ]),
                        childs: [],
                        ns: None,
                      }),
                      Text({ val: " " }),
                      Dom({
                        tag: "span",
                        attrs: ConstAttrs({ "class": LStr("text-sm") }),
                        childs: [Text({ val: "Featured first" })],
                        ns: None,
                      }),
                    ],
                    ns: None,
                  }),
                ],
                ns: None,
              }),
            ],
            ns: None,
          }),
          Dom({
            tag: "div",
            attrs: ConstAttrs({
              "class": LStr("flex flex-wrap items-center gap-4 mt-4"),
            }),
            childs: [
              Show({
                node_id: None,
                val: Method("hasActiveFilters"),
                node: Dom({
                  tag: "button",
                  attrs: ConstAttrs({
                    "class": LStr("btn btn-outline btn-sm clear-filters"),
                    "data-eid": LNum(5.0),
                  }),
                  childs: [Text({ val: "Clear Filters" })],
                  ns: None,
                }),
              }),
            ],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("entries flex flex-col gap-3") }),
        childs: [
          Each({
            node_id: 3,
            iter_info: {
              val: Field("allEntries"),
              when_val: Some(HandlerName(name="filterEntry", ns=Alter)),
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "div",
              attrs: ConstAttrs({ "class": LStr("entry-row") }),
              childs: [RenderIt({ node_id: 2, view_val: None })],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: {
            handler: HandlerName(name="onCategoryToggle", ns=Input),
            args: [Bind("key"), Name("isAlt")],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: {
            handler: HandlerName(name="onRoleToggle", ns=Input),
            args: [Bind("key"), Name("isAlt")],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "change",
          handler: {
            handler: HandlerName(name="onToggleSortByEnd", ns=Input),
            args: [],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 3,
      handlers: [
        {
          name: "change",
          handler: {
            handler: HandlerName(name="onToggleSortAsc", ns=Input),
            args: [],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 4,
      handlers: [
        {
          name: "change",
          handler: {
            handler: HandlerName(name="onToggleFeaturedFirst", ns=Input),
            args: [],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 5,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("clearFilters"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn root_views() -> Map[String, @anode.View] {
  { "main": root_main_view_ir() }
}
