// GENERATED by `tutuca gen-views` from styles.html — DO NOT EDIT.
//
// The views of StylesExample, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing styles_example_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn styles_example_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [], [
      @anode.h("p", [], [@anode.text("View: main")]),
      @anode.h("p", [@anode.attr("class", "mine")], [@anode.text("My Style")]),
      @anode.h("p", [@anode.attr("class", "common")], [
        @anode.text("Common Style"),
      ]),
      @anode.h("p", [@anode.attr("class", "styles-example-global-class")], [
        @anode.text("Global Style"),
      ]),
    ]),
    [],
    style=".mine { color: red; }",
  )
}

///|
/// The compiled `one` view.
fn styles_example_one_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "one",
    @anode.h("section", [], [
      @anode.h("p", [], [@anode.text("View: one")]),
      @anode.h("p", [@anode.attr("class", "mine")], [@anode.text("My Style")]),
      @anode.h("p", [@anode.attr("class", "common")], [
        @anode.text("Common Style"),
      ]),
      @anode.h("p", [@anode.attr("class", "styles-example-global-class")], [
        @anode.text("Global Style"),
      ]),
    ]),
    [],
  )
}

///|
/// The compiled `two` view.
fn styles_example_two_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "two",
    @anode.h("section", [], [
      @anode.h("p", [], [@anode.text("View: two")]),
      @anode.h("p", [@anode.attr("class", "mine")], [@anode.text("My Style")]),
      @anode.h("p", [@anode.attr("class", "common")], [
        @anode.text("Common Style"),
      ]),
      @anode.h("p", [@anode.attr("class", "styles-example-global-class")], [
        @anode.text("Global Style"),
      ]),
    ]),
    [],
    style=".mine { color: orange; text-decoration: underline; }",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn styles_example_views() -> Map[String, @anode.View] {
  {
    "main": styles_example_main_view_ir(),
    "one": styles_example_one_view_ir(),
    "two": styles_example_two_view_ir(),
  }
}

///|
/// The StylesExample component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `StylesExampleState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn styles_example_component(
  name? : String = "StylesExample",
  views? : Map[String, @anode.View] = styles_example_views(),
  init? : StylesExampleState = StylesExampleState::zero(),
  common_style? : String = styles_example_common_style,
  global_style? : String = styles_example_global_style,
  update? : (StylesExampleState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    StylesExampleState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn styles_example_root_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.render(0, Field("value")),
      @anode.text(" "),
      @anode.render(1, Field("value"), view=@anode.const_str("one")),
      @anode.text(" "),
      @anode.render(2, Field("value"), view=@anode.const_str("two")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn styles_example_root_views() -> Map[String, @anode.View] {
  { "main": styles_example_root_main_view_ir() }
}

///|
/// The StylesExampleRoot component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `StylesExampleRootState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn styles_example_root_component(
  name? : String = "StylesExampleRoot",
  views? : Map[String, @anode.View] = styles_example_root_views(),
  init? : StylesExampleRootState = StylesExampleRootState::zero(),
  common_style? : String = styles_example_root_common_style,
  global_style? : String = styles_example_root_global_style,
  update? : (StylesExampleRootState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    StylesExampleRootState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
