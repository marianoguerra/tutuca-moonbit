// GENERATED by `tutuca gen-views` from styles.html — DO NOT EDIT.
//
// The views of StylesExample, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing styles_example_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn styles_example_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({ "data-vid": LStr("main") }),
    childs: [
      Dom({
        tag: "p",
        attrs: ConstAttrs(Map([])),
        childs: [Text({ val: "View: main" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("mine") }),
        childs: [Text({ val: "My Style" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("common") }),
        childs: [Text({ val: "Common Style" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("styles-example-global-class") }),
        childs: [Text({ val: "Global Style" })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style=".mine { color: red; }",
  )
}

///|
/// The compiled `one` view.
fn styles_example_one_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({ "data-vid": LStr("one") }),
    childs: [
      Dom({
        tag: "p",
        attrs: ConstAttrs(Map([])),
        childs: [Text({ val: "View: one" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("mine") }),
        childs: [Text({ val: "My Style" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("common") }),
        childs: [Text({ val: "Common Style" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("styles-example-global-class") }),
        childs: [Text({ val: "Global Style" })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "one",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The compiled `two` view.
fn styles_example_two_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({ "data-vid": LStr("two") }),
    childs: [
      Dom({
        tag: "p",
        attrs: ConstAttrs(Map([])),
        childs: [Text({ val: "View: two" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("mine") }),
        childs: [Text({ val: "My Style" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("common") }),
        childs: [Text({ val: "Common Style" })],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("styles-example-global-class") }),
        childs: [Text({ val: "Global Style" })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "two",
    root,
    @anode.ParseContext::from_ir(root, events),
    style=".mine { color: orange; text-decoration: underline; }",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn styles_example_views() -> Map[String, @anode.View] {
  {
    "main": styles_example_main_view_ir(),
    "one": styles_example_one_view_ir(),
    "two": styles_example_two_view_ir(),
  }
}

///|
/// The compiled `main` view.
fn styles_example_root_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Render({ node_id: 0, val: Field("value"), view_val: None }),
      Text({ val: " " }),
      Render({
        node_id: 1,
        val: Field("value"),
        view_val: Some(Const(lit=LStr("one"), from_macro=false)),
      }),
      Text({ val: " " }),
      Render({
        node_id: 2,
        val: Field("value"),
        view_val: Some(Const(lit=LStr("two"), from_macro=false)),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn styles_example_root_views() -> Map[String, @anode.View] {
  { "main": styles_example_root_main_view_ir() }
}
