// GENERATED by `tutuca gen-views` from web_component.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views web_component.html --name EmojiPicker --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the EmojiPicker component.
///
/// Declared as `interface emoji-picker` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct EmojiPickerState {
  current : @tutuca.Value
  isPickerVisible : Bool
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..EmojiPickerState::zero(), label: "Counter" }
fn EmojiPickerState::zero() -> EmojiPickerState {
  EmojiPickerState::{ current: Null, isPickerVisible: false }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn emoji_picker_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(EmojiPickerState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the EmojiPicker state declares — typed field names for tests and inspectors.
pub(all) enum EmojiPickerField {
  Current
  IsPickerVisible
} derive(Eq, @debug.Debug)

///|
pub fn EmojiPickerField::name(self : EmojiPickerField) -> String {
  match self {
    Current => "current"
    IsPickerVisible => "isPickerVisible"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn EmojiPickerField::kind(self : EmojiPickerField) -> @component.FieldKind {
  match self {
    Current => @component.FAny
    IsPickerVisible => @component.FBool
  }
}

///|
/// Every field, in declaration order.
pub let emoji_picker_field_names : Array[String] = [
  "current", "isPickerVisible",
]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn emoji_picker_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let emoji_picker_schema_fingerprint : String = "4ae49bac65c482b5"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn emoji_picker_encode(s : EmojiPickerState) -> Map[String, @tutuca.Value] {
  { "current": s.current, "isPickerVisible": Bool(s.isPickerVisible) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn emoji_picker_decode(f : Map[String, @tutuca.Value]) -> EmojiPickerState? {
  let v_current = f.get("current").unwrap_or(Null)
  guard f.get("isPickerVisible") is Some(Bool(v_isPickerVisible)) else {
    return None
  }
  Some(EmojiPickerState::{
    current: v_current,
    isPickerVisible: v_isPickerVisible,
  })
}

///|
/// The views declared in web_component.html.
pub(all) enum EmojiPickerView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn EmojiPickerView::name(self : EmojiPickerView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let emoji_picker_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let emoji_picker_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum EmojiPickerInput {
  OnEmojiClick
  ToggleIsPickerVisible
} derive(Eq, @debug.Debug)

///|
pub fn EmojiPickerInput::name(self : EmojiPickerInput) -> String {
  match self {
    OnEmojiClick => "onEmojiClick"
    ToggleIsPickerVisible => "toggleIsPickerVisible"
  }
}

///|
pub fn EmojiPickerInput::parse(s : String) -> EmojiPickerInput? {
  match s {
    "onEmojiClick" => Some(OnEmojiClick)
    "toggleIsPickerVisible" => Some(ToggleIsPickerVisible)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to web_component.html breaks every `match` over this type
/// until it is handled.
pub(all) enum EmojiPickerMsg {
  /// `@on.emoji-click="onEmojiClick …"`
  OnEmojiClick(String)
  /// `@on.click="toggleIsPickerVisible …"`
  ToggleIsPickerVisible
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn EmojiPickerMsg::from_dispatch(
  d : @component.Dispatch,
) -> EmojiPickerMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onEmojiClick", [Str(a0)]) => OnEmojiClick(a0)
      ("toggleIsPickerVisible", []) => ToggleIsPickerVisible
      _ => Unknown(name, args)
    },
  )
}
