// GENERATED by `tutuca gen-views` from web_component.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views web_component.html --name EmojiPicker --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The views declared in web_component.html.
pub(all) enum EmojiPickerView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn EmojiPickerView::name(self : EmojiPickerView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let emoji_picker_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let emoji_picker_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum EmojiPickerInput {
  OnEmojiClick
} derive(Eq, @debug.Debug)

///|
pub fn EmojiPickerInput::name(self : EmojiPickerInput) -> String {
  match self {
    OnEmojiClick => "onEmojiClick"
  }
}

///|
pub fn EmojiPickerInput::parse(s : String) -> EmojiPickerInput? {
  match s {
    "onEmojiClick" => Some(OnEmojiClick)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to web_component.html breaks every `match` over this type
/// until it is handled.
pub(all) enum EmojiPickerMsg {
  /// `@on.emoji-click="onEmojiClick …"`
  OnEmojiClick(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn EmojiPickerMsg::from_dispatch(
  d : @component.Dispatch,
) -> EmojiPickerMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onEmojiClick", [Str(a0)]) => OnEmojiClick(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum EmojiPickerMethod {
  ToggleIsPickerVisible
} derive(Eq, @debug.Debug)

///|
pub fn EmojiPickerMethod::name(self : EmojiPickerMethod) -> String {
  match self {
    ToggleIsPickerVisible => "toggleIsPickerVisible"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] emoji_picker_mutate(
  f : (EmojiPickerMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([ToggleIsPickerVisible] : Array[EmojiPickerMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] emoji_picker_compute(
  f : (EmojiPickerMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([ToggleIsPickerVisible] : Array[EmojiPickerMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] emoji_picker_swap(
  f : (EmojiPickerMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ToggleIsPickerVisible] : Array[EmojiPickerMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let emoji_picker_fields : Array[String] = ["current", "isPickerVisible"]

///|
/// The fields in `emoji_picker_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] emoji_picker_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  emoji_picker_fields.filter(f => !present.contains(f))
}
