// GENERATED by `tutuca gen` from nested_state.tutu — DO NOT EDIT.
//
// The views of NestedState, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing nested_state_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `spec:` section, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let nested_state_spec_source : String =
  #|state NestedState {
  #|  title: String
  #|  labels: Array[Label]
  #|  priority: Priority
  #|  due: Due
  #|  note: String?
  #|  size: (Int, Int)
  #|  where title is nonempty
  #|}
  #|enum Due { Unscheduled, OnDay(Int) }
  #|struct Label { text: String, done: Bool }
  #|enum Priority { Low, High }

///|
/// The compiled `main` view.
fn nested_state_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "card bg-base-200 p-4")], [
      @anode.h("h3", [@anode.attr("class", "card-title")], [
        @anode.dyn_text(@tutuca.Expr::field("title"), id=0),
      ]),
      @anode.h("ul", [@anode.attr("class", "menu")], [
        @anode.each(
          2,
          @tutuca.Expr::field("labels"),
          @anode.h("li", [], [
            @anode.h(
              "button",
              [@anode.attr("class", "btn btn-xs"), @anode.eid(0)],
              [
                @anode.h("span", [], [
                  @anode.dyn_text(
                    @tutuca.Expr::bind_member("value", "text"),
                    id=1,
                  ),
                ]),
              ],
            ),
          ]),
        ),
      ]),
      @anode.text(" "),
      @anode.h(
        "button",
        [@anode.attr("class", "btn btn-sm btn-primary"), @anode.eid(1)],
        [@anode.text(" bump priority ")],
      ),
    ]),
    [
      [
        @anode.on("click", @tutuca.Expr::name("toggle_label"), args=[
          @tutuca.Expr::bind("key"),
        ]),
      ],
      [@anode.on("click", @tutuca.Expr::name("bump_priority"))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn nested_state_views() -> Map[String, @anode.View] {
  { "main": nested_state_main_view_ir() }
}

///|
/// The NestedState component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `NestedStateState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn nested_state_component(
  name? : String = "NestedState",
  views? : Map[String, @anode.View] = nested_state_views(),
  initial? : NestedStateState = NestedStateState::zero(),
  common_style? : String = nested_state_common_style,
  global_style? : String = nested_state_global_style,
  update? : (NestedStateState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    NestedStateState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    initial~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=nested_state_spec_source,
  )
}
