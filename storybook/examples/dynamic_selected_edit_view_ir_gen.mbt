// GENERATED by `tutuca gen-views` from dynamic_selected_edit.html — DO NOT EDIT.
//
// The views of DseRoot, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing dse_root_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn dse_root_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3 p-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("alert") }),
        childs: [
          Dom({
            tag: "span",
            attrs: ConstAttrs(Map([])),
            childs: [
              Text({ val: "Selected key = " }),
              Dom({
                tag: "b",
                attrs: ConstAttrs({ "class": LStr("selected-key") }),
                childs: [
                  RenderText({ node_id: None, val: Field("selectedKey") }),
                ],
                ns: None,
              }),
            ],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-2") }),
        childs: [
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "class": LStr("text-xs opacity-60") }),
            childs: [
              Text({
                val: "Entries — rendered from the *items dynamic (owner):",
              }),
            ],
            ns: None,
          }),
          Text({ val: " " }),
          Each({
            node_id: 1,
            iter_info: {
              val: Dyn("items"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "div",
              attrs: ConstAttrs({
                "class": LStr("flex gap-3 items-center owner-row"),
              }),
              childs: [
                RenderIt({ node_id: 0, view_val: None }),
                Text({ val: " " }),
                Dom({
                  tag: "button",
                  attrs: ConstAttrs({
                    "class": LStr("btn btn-xs btn-soft select"),
                    "data-eid": LNum(0.0),
                  }),
                  childs: [Text({ val: "select" })],
                  ns: None,
                }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Render({ node_id: 2, val: Field("editor"), view_val: None }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: {
            handler: HandlerName(name="selectItem", ns=Input),
            args: [Bind("key")],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn dse_root_views() -> Map[String, @anode.View] {
  { "main": dse_root_main_view_ir() }
}

///|
/// The compiled `main` view.
fn dse_editor_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("card bg-base-200 p-3 gap-3 editor"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "span",
        attrs: ConstAttrs({ "class": LStr("text-xs opacity-60") }),
        childs: [Text({ val: "Editor (child component)" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-1") }),
        childs: [
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "class": LStr("text-xs opacity-60") }),
            childs: [
              Text({ val: "All entries — rendered from the *items lookup:" }),
            ],
            ns: None,
          }),
          Text({ val: " " }),
          Each({
            node_id: 1,
            iter_info: {
              val: Dyn("items"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "div",
              attrs: ConstAttrs({
                "class": LStr("flex gap-3 flex-wrap child-items"),
              }),
              childs: [RenderIt({ node_id: 0, view_val: None })],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-1") }),
        childs: [
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "class": LStr("text-xs opacity-60") }),
            childs: [Text({ val: "Editing the *selected entry:" })],
            ns: None,
          }),
          Text({ val: " " }),
          Render({
            node_id: 2,
            val: Dyn("selected"),
            view_val: Some(Const(lit=LStr("edit"), from_macro=false)),
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn dse_editor_views() -> Map[String, @anode.View] {
  { "main": dse_editor_main_view_ir() }
}

///|
/// The compiled `main` view.
fn dse_entry_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "span",
    attrs: ConstAttrs({
      "class": LStr("font-mono entry"),
      "data-vid": LStr("main"),
    }),
    childs: [RenderText({ node_id: None, val: Field("name") })],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The compiled `edit` view.
fn dse_entry_edit_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "input",
    attrs: DynAttrs([
      ConstA(
        name="class",
        val=Const(
          lit=LStr("input input-bordered w-full edit-name"),
          from_macro=false,
        ),
      ),
      Plain(name="value", val=Field("name")),
      ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
      ConstA(
        name="placeholder",
        val=Const(lit=LStr("Entry name"), from_macro=false),
      ),
      ConstA(name="data-vid", val=Const(lit=LStr("edit"), from_macro=false)),
    ]),
    childs: [],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setName"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "edit",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn dse_entry_views() -> Map[String, @anode.View] {
  { "main": dse_entry_main_view_ir(), "edit": dse_entry_edit_view_ir() }
}
