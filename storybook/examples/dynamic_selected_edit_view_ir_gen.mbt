// GENERATED by `tutuca gen-views` from dynamic_selected_edit.html — DO NOT EDIT.
//
// The views of DseRoot, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing dse_root_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn dse_root_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-3 p-3")], [
      @anode.h("div", [@anode.attr("class", "alert")], [
        @anode.h("span", [], [
          @anode.text("Selected key = "),
          @anode.h("b", [@anode.attr("class", "selected-key")], [
            @anode.dyn_text(Field("selectedKey")),
          ]),
        ]),
      ]),
      @anode.h("div", [@anode.attr("class", "flex flex-col gap-2")], [
        @anode.h("span", [@anode.attr("class", "text-xs opacity-60")], [
          @anode.text("Entries — rendered from the *items dynamic (owner):"),
        ]),
        @anode.text(" "),
        @anode.each(
          1,
          Dyn("items"),
          @anode.h(
            "div",
            [@anode.attr("class", "flex gap-3 items-center owner-row")],
            [
              @anode.render_it(0),
              @anode.text(" "),
              @anode.h(
                "button",
                [
                  @anode.attr("class", "btn btn-xs btn-soft select"),
                  @anode.eid(0),
                ],
                [@anode.text("select")],
              ),
            ],
          ),
        ),
      ]),
      @anode.text(" "),
      @anode.render(2, Field("editor")),
    ]),
    [
      [
        @anode.on("click", HandlerName(name="selectItem", ns=Input), args=[
          Bind("key"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn dse_root_views() -> Map[String, @anode.View] {
  { "main": dse_root_main_view_ir() }
}

///|
/// The DseRoot component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `DseRootState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn dse_root_component(
  name? : String = "DseRoot",
  views? : Map[String, @anode.View] = dse_root_views(),
  init? : DseRootState = DseRootState::zero(),
  common_style? : String = dse_root_common_style,
  global_style? : String = dse_root_global_style,
  update? : (DseRootState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    DseRootState,
  ],
  swap? : (DseRootInput) -> ((DseRootState, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SelectItem] : Array[DseRootInput]),
          DseRootInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn dse_editor_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [@anode.attr("class", "card bg-base-200 p-3 gap-3 editor")],
      [
        @anode.h("span", [@anode.attr("class", "text-xs opacity-60")], [
          @anode.text("Editor (child component)"),
        ]),
        @anode.text(" "),
        @anode.h("div", [@anode.attr("class", "flex flex-col gap-1")], [
          @anode.h("span", [@anode.attr("class", "text-xs opacity-60")], [
            @anode.text("All entries — rendered from the *items lookup:"),
          ]),
          @anode.text(" "),
          @anode.each(
            1,
            Dyn("items"),
            @anode.h(
              "div",
              [@anode.attr("class", "flex gap-3 flex-wrap child-items")],
              [@anode.render_it(0)],
            ),
          ),
        ]),
        @anode.h("div", [@anode.attr("class", "flex flex-col gap-1")], [
          @anode.h("span", [@anode.attr("class", "text-xs opacity-60")], [
            @anode.text("Editing the *selected entry:"),
          ]),
          @anode.text(" "),
          @anode.render(2, Dyn("selected"), view=@anode.const_str("edit")),
        ]),
      ],
    ),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn dse_editor_views() -> Map[String, @anode.View] {
  { "main": dse_editor_main_view_ir() }
}

///|
/// The DseEditor component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `DseEditorState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn dse_editor_component(
  name? : String = "DseEditor",
  views? : Map[String, @anode.View] = dse_editor_views(),
  init? : DseEditorState = DseEditorState::zero(),
  common_style? : String = dse_editor_common_style,
  global_style? : String = dse_editor_global_style,
  update? : (DseEditorState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    DseEditorState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn dse_entry_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("span", [@anode.attr("class", "font-mono entry")], [
      @anode.dyn_text(Field("name")),
    ]),
    [],
  )
}

///|
/// The compiled `edit` view.
fn dse_entry_edit_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "edit",
    @anode.h(
      "input",
      [
        @anode.attr("class", "input input-bordered w-full edit-name"),
        @anode.dyn_attr("value", Field("name")),
        @anode.eid(0),
        @anode.attr("placeholder", "Entry name"),
      ],
      [],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setName", ns=Input), args=[
          EventPath(["value"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn dse_entry_views() -> Map[String, @anode.View] {
  { "main": dse_entry_main_view_ir(), "edit": dse_entry_edit_view_ir() }
}

///|
/// The DseEntry component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `DseEntryState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn dse_entry_component(
  name? : String = "DseEntry",
  views? : Map[String, @anode.View] = dse_entry_views(),
  init? : DseEntryState = DseEntryState::zero(),
  common_style? : String = dse_entry_common_style,
  global_style? : String = dse_entry_global_style,
  update? : (DseEntryState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    DseEntryState,
  ],
  swap? : (DseEntryInput) -> ((
    DseEntryState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetName] : Array[DseEntryInput]),
          DseEntryInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
