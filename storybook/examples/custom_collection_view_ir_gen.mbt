// GENERATED by `tutuca gen-views` from custom_collection.html — DO NOT EDIT.
//
// The views of Playlist, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing playlist_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn playlist_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "ul",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-1"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Each({
        node_id: 1,
        iter_info: {
          val: Field("songs"),
          when_val: None,
          loop_with_val: None,
          enrich_with_val: None,
        },
        node: Dom({
          tag: "li",
          attrs: ConstAttrs({ "class": LStr("flex gap-2 items-center") }),
          childs: [
            RenderIt({ node_id: 0, view_val: None }),
            Text({ val: " " }),
            Dom({
              tag: "button",
              attrs: ConstAttrs({
                "class": LStr("btn btn-xs rm"),
                "data-eid": LNum(0.0),
              }),
              childs: [Text({ val: "remove" })],
              ns: None,
            }),
          ],
          ns: None,
        }),
        from_render_each: false,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("removeSong"), args: [Bind("key")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn playlist_views() -> Map[String, @anode.View] {
  { "main": playlist_main_view_ir() }
}

///|
/// The compiled `main` view.
fn song_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "span",
    attrs: ConstAttrs({ "class": LStr("song"), "data-vid": LStr("main") }),
    childs: [RenderText({ node_id: None, val: Field("title") })],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn song_views() -> Map[String, @anode.View] {
  { "main": song_main_view_ir() }
}
