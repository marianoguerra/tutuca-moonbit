// GENERATED by `tutuca gen-views` from custom_collection.html — DO NOT EDIT.
//
// The views of Playlist, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing playlist_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn playlist_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("ul", [@anode.attr("class", "flex flex-col gap-1")], [
      @anode.each(
        1,
        Field("songs"),
        @anode.h("li", [@anode.attr("class", "flex gap-2 items-center")], [
          @anode.render_it(0),
          @anode.text(" "),
          @anode.h(
            "button",
            [@anode.attr("class", "btn btn-xs rm"), @anode.eid(0)],
            [@anode.text("remove")],
          ),
        ]),
      ),
    ]),
    [
      [
        @anode.on("click", HandlerName(name="removeSong", ns=Input), args=[
          Bind("key"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn playlist_views() -> Map[String, @anode.View] {
  { "main": playlist_main_view_ir() }
}

///|
/// The Playlist component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `PlaylistState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn playlist_component(
  name? : String = "Playlist",
  views? : Map[String, @anode.View] = playlist_views(),
  init? : PlaylistState = PlaylistState::zero(),
  common_style? : String = playlist_common_style,
  global_style? : String = playlist_global_style,
  update? : (PlaylistState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    PlaylistState,
  ],
  swap? : (PlaylistInput) -> ((
    PlaylistState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([RemoveSong] : Array[PlaylistInput]),
          PlaylistInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn song_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("span", [@anode.attr("class", "song")], [
      @anode.dyn_text(Field("title")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn song_views() -> Map[String, @anode.View] {
  { "main": song_main_view_ir() }
}

///|
/// The Song component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SongState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn song_component(
  name? : String = "Song",
  views? : Map[String, @anode.View] = song_views(),
  init? : SongState = SongState::zero(),
  common_style? : String = song_common_style,
  global_style? : String = song_global_style,
  update? : (SongState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SongState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
