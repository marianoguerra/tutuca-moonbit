// GENERATED by `tutuca gen-views` from graphics.html — DO NOT EDIT.
//
// The views of SwatchPicker, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing swatch_picker_views() as `views~` means the template parser
// never runs at startup.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn swatch_picker_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-2")], [
      @anode.h(
        "svg",
        [@anode.attr("viewBox", "0 0 380 130"), @anode.attr("role", "img")],
        [
          @anode.h(
            "rect",
            [
              @anode.attr("class", "preview"),
              @anode.attr("x", "20"),
              @anode.attr("y", "12"),
              @anode.attr("width", "340"),
              @anode.attr("height", "52"),
              @anode.attr("rx", "8"),
              @anode.dyn_attr("fill", Field("color")),
            ],
            [],
            ns=Svg,
          ),
          @anode.text(" "),
          @anode.each(
            0,
            Field("palette"),
            @anode.h(
              "circle",
              [
                @anode.dyn_attr("cx", Bind("cx")),
                @anode.attr("cy", "98"),
                @anode.attr("r", "18"),
                @anode.dyn_attr("fill", Bind("value")),
                @anode.attr("stroke-width", "3"),
                @anode.dyn_attr("stroke", Bind("ring")),
                @anode.eid(0),
              ],
              [],
              ns=Svg,
            ),
            enrich_with_val=HandlerName(name="swatch", ns=Alter),
          ),
        ],
        ns=Svg,
      ),
      @anode.text(" "),
      @anode.h("p", [@anode.attr("class", "text-sm label")], [
        @anode.text("Selected: "),
        @anode.dyn_text(Field("color"), id=1),
      ]),
    ]),
    [[@anode.on("click", Method("setColor"), args=[Bind("value")])]],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn swatch_picker_views() -> Map[String, @anode.View] {
  { "main": swatch_picker_main_view_ir() }
}

///|
/// The compiled `main` view.
fn quadratic_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h("div", [@anode.attr("class", "flex gap-3 text-sm")], [
        @anode.h("label", [@anode.attr("class", "flex items-center gap-1")], [
          @anode.text("a "),
          @anode.h(
            "input",
            [
              @anode.attr("type", "number"),
              @anode.attr("class", "input input-sm w-16 in-a"),
              @anode.dyn_attr("value", Field("a")),
              @anode.eid(0),
            ],
            [],
          ),
        ]),
        @anode.text(" "),
        @anode.h("label", [@anode.attr("class", "flex items-center gap-1")], [
          @anode.text("b "),
          @anode.h(
            "input",
            [
              @anode.attr("type", "number"),
              @anode.attr("class", "input input-sm w-16 in-b"),
              @anode.dyn_attr("value", Field("b")),
              @anode.eid(1),
            ],
            [],
          ),
        ]),
        @anode.text(" "),
        @anode.h("label", [@anode.attr("class", "flex items-center gap-1")], [
          @anode.text("c "),
          @anode.h(
            "input",
            [
              @anode.attr("type", "number"),
              @anode.attr("class", "input input-sm w-16 in-c"),
              @anode.dyn_attr("value", Field("c")),
              @anode.eid(2),
            ],
            [],
          ),
        ]),
      ]),
      @anode.text(" "),
      @anode.h(
        "math",
        [@anode.attr("display", "block")],
        [
          @anode.h(
            "mn",
            [@anode.attr("class", "m-a")],
            [@anode.dyn_text(Field("a"))],
            ns=MathML,
          ),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("⁢")], ns=MathML),
          @anode.text(" "),
          @anode.h(
            "msup",
            [],
            [
              @anode.h("mi", [], [@anode.text("x")], ns=MathML),
              @anode.h("mn", [], [@anode.text("2")], ns=MathML),
            ],
            ns=MathML,
          ),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("+")], ns=MathML),
          @anode.text(" "),
          @anode.h(
            "mn",
            [@anode.attr("class", "m-b")],
            [@anode.dyn_text(Field("b"))],
            ns=MathML,
          ),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("⁢")], ns=MathML),
          @anode.text(" "),
          @anode.h("mi", [], [@anode.text("x")], ns=MathML),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("+")], ns=MathML),
          @anode.text(" "),
          @anode.h(
            "mn",
            [@anode.attr("class", "m-c")],
            [@anode.dyn_text(Field("c"))],
            ns=MathML,
          ),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("=")], ns=MathML),
          @anode.text(" "),
          @anode.h("mn", [], [@anode.text("0")], ns=MathML),
        ],
        ns=MathML,
      ),
      @anode.text(" "),
      @anode.h("p", [@anode.attr("class", "text-sm")], [
        @anode.text("Discriminant: "),
        @anode.h("span", [@anode.attr("class", "disc")], [
          @anode.dyn_text(Method("discriminant")),
        ]),
      ]),
      @anode.h("p", [@anode.attr("class", "text-sm")], [
        @anode.text("There are "),
        @anode.h("span", [@anode.attr("class", "classify")], [
          @anode.dyn_text(Method("classify")),
        ]),
        @anode.text("."),
      ]),
    ]),
    [
      [@anode.on("input", Method("setA"), args=[Name("valueAsInt")])],
      [@anode.on("input", Method("setB"), args=[Name("valueAsInt")])],
      [@anode.on("input", Method("setC"), args=[Name("valueAsInt")])],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn quadratic_views() -> Map[String, @anode.View] {
  { "main": quadratic_main_view_ir() }
}
