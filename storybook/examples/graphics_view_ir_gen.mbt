// GENERATED by `tutuca gen-views` from graphics.html — DO NOT EDIT.
//
// The views of SwatchPicker, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing swatch_picker_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let swatch_picker_spec_source : String =
  #|  state SwatchPicker {
  #|    color : String
  #|    palette : Array[String]
  #|
  #|    where palette is nonempty
  #|
  #|    invariant colorIsOnThePalette
  #|      format $'{.color} is not one of the swatches' {
  #|        has .palette .color
  #|      }
  #|  }
  #|
  #|  state Quadratic {
  #|    a : Int
  #|    b : Int
  #|    c : Int
  #|  }

///|
/// The compiled `main` view.
fn swatch_picker_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-2")], [
      @anode.h(
        "svg",
        [@anode.attr("viewBox", "0 0 380 130"), @anode.attr("role", "img")],
        [
          @anode.h(
            "rect",
            [
              @anode.attr("class", "preview"),
              @anode.attr("x", "20"),
              @anode.attr("y", "12"),
              @anode.attr("width", "340"),
              @anode.attr("height", "52"),
              @anode.attr("rx", "8"),
              @anode.dyn_attr("fill", @tutuca.Expr::field("color")),
            ],
            [],
            ns=Svg,
          ),
          @anode.text(" "),
          @anode.each(
            0,
            @tutuca.Expr::field("palette"),
            @anode.h(
              "circle",
              [
                @anode.dyn_attr("cx", @tutuca.Expr::bind("cx")),
                @anode.attr("cy", "98"),
                @anode.attr("r", "18"),
                @anode.dyn_attr("fill", @tutuca.Expr::bind("value")),
                @anode.attr("stroke-width", "3"),
                @anode.dyn_attr("stroke", @tutuca.Expr::bind("ring")),
                @anode.eid(0),
              ],
              [],
              ns=Svg,
            ),
            enrich_with_val=@tutuca.Expr::handler_name("swatch", Alter),
          ),
        ],
        ns=Svg,
      ),
      @anode.text(" "),
      @anode.h("p", [@anode.attr("class", "text-sm label")], [
        @anode.text("Selected: "),
        @anode.dyn_text(@tutuca.Expr::field("color"), id=1),
      ]),
    ]),
    [
      [
        @anode.on("click", @tutuca.Expr::handler_name("setColor", Receive), args=[
          @tutuca.Expr::bind("value"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn swatch_picker_views() -> Map[String, @anode.View] {
  { "main": swatch_picker_main_view_ir() }
}

///|
/// `invariant colorIsOnThePalette` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn swatch_picker_compute_colorIsOnThePalette(
  s : SwatchPickerState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(s.palette.contains(s.color)) : @tutuca.Value)
}

///|
/// The `compute` names the `<script type="tutuca/script">` block answers.
/// GENERATED — edit the block, not this.
fn swatch_picker_inline_compute() -> Map[
  String,
  (SwatchPickerState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  { "colorIsOnThePalette": swatch_picker_compute_colorIsOnThePalette }
}

///|
/// The SwatchPicker component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SwatchPickerState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn swatch_picker_component(
  name? : String = "SwatchPicker",
  views? : Map[String, @anode.View] = swatch_picker_views(),
  init? : SwatchPickerState = SwatchPickerState::zero(),
  common_style? : String = swatch_picker_common_style,
  global_style? : String = swatch_picker_global_style,
  update? : (SwatchPickerState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SwatchPickerState,
  ],
  swap? : (SwatchPickerInput) -> ((
    SwatchPickerState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  enrich? : (SwatchPickerEnrich) -> ((
    SwatchPickerState,
    Map[String, @tutuca.Value],
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
    &@tutuca.Stack,
  ) -> Unit)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    compute=swatch_picker_inline_compute(),
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetColor] : Array[SwatchPickerInput]),
          SwatchPickerInput::name,
          f,
        )
      None => Map([])
    },
    enrich=match enrich {
      Some(f) =>
        @component.build_bucket(
          ([Swatch] : Array[SwatchPickerEnrich]),
          SwatchPickerEnrich::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=swatch_picker_spec_source,
  )
}

///|
/// The compiled `main` view.
fn quadratic_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h("div", [@anode.attr("class", "flex gap-3 text-sm")], [
        @anode.h("label", [@anode.attr("class", "flex items-center gap-1")], [
          @anode.text("a "),
          @anode.h(
            "input",
            [
              @anode.attr("type", "number"),
              @anode.attr("class", "input input-sm w-16 in-a"),
              @anode.dyn_attr("value", @tutuca.Expr::field("a")),
              @anode.eid(0),
            ],
            [],
          ),
        ]),
        @anode.text(" "),
        @anode.h("label", [@anode.attr("class", "flex items-center gap-1")], [
          @anode.text("b "),
          @anode.h(
            "input",
            [
              @anode.attr("type", "number"),
              @anode.attr("class", "input input-sm w-16 in-b"),
              @anode.dyn_attr("value", @tutuca.Expr::field("b")),
              @anode.eid(1),
            ],
            [],
          ),
        ]),
        @anode.text(" "),
        @anode.h("label", [@anode.attr("class", "flex items-center gap-1")], [
          @anode.text("c "),
          @anode.h(
            "input",
            [
              @anode.attr("type", "number"),
              @anode.attr("class", "input input-sm w-16 in-c"),
              @anode.dyn_attr("value", @tutuca.Expr::field("c")),
              @anode.eid(2),
            ],
            [],
          ),
        ]),
      ]),
      @anode.text(" "),
      @anode.h(
        "math",
        [@anode.attr("display", "block")],
        [
          @anode.h(
            "mn",
            [@anode.attr("class", "m-a")],
            [@anode.dyn_text(@tutuca.Expr::field("a"))],
            ns=MathML,
          ),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("⁢")], ns=MathML),
          @anode.text(" "),
          @anode.h(
            "msup",
            [],
            [
              @anode.h("mi", [], [@anode.text("x")], ns=MathML),
              @anode.h("mn", [], [@anode.text("2")], ns=MathML),
            ],
            ns=MathML,
          ),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("+")], ns=MathML),
          @anode.text(" "),
          @anode.h(
            "mn",
            [@anode.attr("class", "m-b")],
            [@anode.dyn_text(@tutuca.Expr::field("b"))],
            ns=MathML,
          ),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("⁢")], ns=MathML),
          @anode.text(" "),
          @anode.h("mi", [], [@anode.text("x")], ns=MathML),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("+")], ns=MathML),
          @anode.text(" "),
          @anode.h(
            "mn",
            [@anode.attr("class", "m-c")],
            [@anode.dyn_text(@tutuca.Expr::field("c"))],
            ns=MathML,
          ),
          @anode.text(" "),
          @anode.h("mo", [], [@anode.text("=")], ns=MathML),
          @anode.text(" "),
          @anode.h("mn", [], [@anode.text("0")], ns=MathML),
        ],
        ns=MathML,
      ),
      @anode.text(" "),
      @anode.h("p", [@anode.attr("class", "text-sm")], [
        @anode.text("Discriminant: "),
        @anode.h("span", [@anode.attr("class", "disc")], [
          @anode.dyn_text(@tutuca.Expr::method_("discriminant")),
        ]),
      ]),
      @anode.h("p", [@anode.attr("class", "text-sm")], [
        @anode.text("There are "),
        @anode.h("span", [@anode.attr("class", "classify")], [
          @anode.dyn_text(@tutuca.Expr::method_("classify")),
        ]),
        @anode.text("."),
      ]),
    ]),
    [
      [
        @anode.on("input", @tutuca.Expr::handler_name("setA", Receive), args=[
          @tutuca.Expr::event_path(["valueAsInt"]),
        ]),
      ],
      [
        @anode.on("input", @tutuca.Expr::handler_name("setB", Receive), args=[
          @tutuca.Expr::event_path(["valueAsInt"]),
        ]),
      ],
      [
        @anode.on("input", @tutuca.Expr::handler_name("setC", Receive), args=[
          @tutuca.Expr::event_path(["valueAsInt"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn quadratic_views() -> Map[String, @anode.View] {
  { "main": quadratic_main_view_ir() }
}

///|
/// The Quadratic component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `QuadraticState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn quadratic_component(
  name? : String = "Quadratic",
  views? : Map[String, @anode.View] = quadratic_views(),
  init? : QuadraticState = QuadraticState::zero(),
  common_style? : String = quadratic_common_style,
  global_style? : String = quadratic_global_style,
  update? : (QuadraticState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    QuadraticState,
  ],
  compute? : (QuadraticMethod) -> ((
    QuadraticState,
    Array[@tutuca.Value],
    &@tutuca.Stack,
  ) -> @tutuca.Value)?,
  swap? : (QuadraticInput) -> ((
    QuadraticState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) =>
        @component.build_bucket(
          ([Classify, Discriminant] : Array[QuadraticMethod]),
          QuadraticMethod::name,
          f,
        )
      None => Map([])
    },
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetA, SetB, SetC] : Array[QuadraticInput]),
          QuadraticInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=swatch_picker_spec_source,
  )
}
