// GENERATED by `tutuca gen-views` from graphics.html — DO NOT EDIT.
//
// The views of SwatchPicker, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing swatch_picker_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn swatch_picker_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-2"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "svg",
        attrs: ConstAttrs({
          "viewBox": LStr("0 0 380 130"),
          "role": LStr("img"),
        }),
        childs: [
          Dom({
            tag: "rect",
            attrs: DynAttrs([
              ConstA(
                name="class",
                val=Const(lit=LStr("preview"), from_macro=false),
              ),
              ConstA(name="x", val=Const(lit=LStr("20"), from_macro=false)),
              ConstA(name="y", val=Const(lit=LStr("12"), from_macro=false)),
              ConstA(name="width", val=Const(lit=LStr("340"), from_macro=false)),
              ConstA(name="height", val=Const(lit=LStr("52"), from_macro=false)),
              ConstA(name="rx", val=Const(lit=LStr("8"), from_macro=false)),
              Plain(name="fill", val=Field("color")),
            ]),
            childs: [],
            ns: Some(Svg),
          }),
          Text({ val: " " }),
          Each({
            node_id: 0,
            iter_info: {
              val: Field("palette"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: Some(HandlerName(name="swatch", ns=Alter)),
            },
            node: Dom({
              tag: "circle",
              attrs: DynAttrs([
                Plain(name="cx", val=Bind("cx")),
                ConstA(name="cy", val=Const(lit=LStr("98"), from_macro=false)),
                ConstA(name="r", val=Const(lit=LStr("18"), from_macro=false)),
                Plain(name="fill", val=Bind("value")),
                ConstA(
                  name="stroke-width",
                  val=Const(lit=LStr("3"), from_macro=false),
                ),
                Plain(name="stroke", val=Bind("ring")),
                ConstA(
                  name="data-eid",
                  val=Const(lit=LNum(0.0), from_macro=false),
                ),
              ]),
              childs: [],
              ns: Some(Svg),
            }),
            from_render_each: false,
          }),
        ],
        ns: Some(Svg),
      }),
      Text({ val: " " }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("text-sm label") }),
        childs: [
          Text({ val: "Selected: " }),
          RenderText({ node_id: Some(1), val: Field("color") }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("setColor"), args: [Bind("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn swatch_picker_views() -> Map[String, @anode.View] {
  { "main": swatch_picker_main_view_ir() }
}

///|
/// The compiled `main` view.
fn quadratic_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex gap-3 text-sm") }),
        childs: [
          Dom({
            tag: "label",
            attrs: ConstAttrs({ "class": LStr("flex items-center gap-1") }),
            childs: [
              Text({ val: "a " }),
              Dom({
                tag: "input",
                attrs: DynAttrs([
                  ConstA(
                    name="type",
                    val=Const(lit=LStr("number"), from_macro=false),
                  ),
                  ConstA(
                    name="class",
                    val=Const(
                      lit=LStr("input input-sm w-16 in-a"),
                      from_macro=false,
                    ),
                  ),
                  Plain(name="value", val=Field("a")),
                  ConstA(
                    name="data-eid",
                    val=Const(lit=LNum(0.0), from_macro=false),
                  ),
                ]),
                childs: [],
                ns: None,
              }),
            ],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "label",
            attrs: ConstAttrs({ "class": LStr("flex items-center gap-1") }),
            childs: [
              Text({ val: "b " }),
              Dom({
                tag: "input",
                attrs: DynAttrs([
                  ConstA(
                    name="type",
                    val=Const(lit=LStr("number"), from_macro=false),
                  ),
                  ConstA(
                    name="class",
                    val=Const(
                      lit=LStr("input input-sm w-16 in-b"),
                      from_macro=false,
                    ),
                  ),
                  Plain(name="value", val=Field("b")),
                  ConstA(
                    name="data-eid",
                    val=Const(lit=LNum(1.0), from_macro=false),
                  ),
                ]),
                childs: [],
                ns: None,
              }),
            ],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "label",
            attrs: ConstAttrs({ "class": LStr("flex items-center gap-1") }),
            childs: [
              Text({ val: "c " }),
              Dom({
                tag: "input",
                attrs: DynAttrs([
                  ConstA(
                    name="type",
                    val=Const(lit=LStr("number"), from_macro=false),
                  ),
                  ConstA(
                    name="class",
                    val=Const(
                      lit=LStr("input input-sm w-16 in-c"),
                      from_macro=false,
                    ),
                  ),
                  Plain(name="value", val=Field("c")),
                  ConstA(
                    name="data-eid",
                    val=Const(lit=LNum(2.0), from_macro=false),
                  ),
                ]),
                childs: [],
                ns: None,
              }),
            ],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "math",
        attrs: ConstAttrs({ "display": LStr("block") }),
        childs: [
          Dom({
            tag: "mn",
            attrs: ConstAttrs({ "class": LStr("m-a") }),
            childs: [RenderText({ node_id: None, val: Field("a") })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mo",
            attrs: ConstAttrs(Map([])),
            childs: [Text({ val: "⁢" })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "msup",
            attrs: ConstAttrs(Map([])),
            childs: [
              Dom({
                tag: "mi",
                attrs: ConstAttrs(Map([])),
                childs: [Text({ val: "x" })],
                ns: Some(MathML),
              }),
              Dom({
                tag: "mn",
                attrs: ConstAttrs(Map([])),
                childs: [Text({ val: "2" })],
                ns: Some(MathML),
              }),
            ],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mo",
            attrs: ConstAttrs(Map([])),
            childs: [Text({ val: "+" })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mn",
            attrs: ConstAttrs({ "class": LStr("m-b") }),
            childs: [RenderText({ node_id: None, val: Field("b") })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mo",
            attrs: ConstAttrs(Map([])),
            childs: [Text({ val: "⁢" })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mi",
            attrs: ConstAttrs(Map([])),
            childs: [Text({ val: "x" })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mo",
            attrs: ConstAttrs(Map([])),
            childs: [Text({ val: "+" })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mn",
            attrs: ConstAttrs({ "class": LStr("m-c") }),
            childs: [RenderText({ node_id: None, val: Field("c") })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mo",
            attrs: ConstAttrs(Map([])),
            childs: [Text({ val: "=" })],
            ns: Some(MathML),
          }),
          Text({ val: " " }),
          Dom({
            tag: "mn",
            attrs: ConstAttrs(Map([])),
            childs: [Text({ val: "0" })],
            ns: Some(MathML),
          }),
        ],
        ns: Some(MathML),
      }),
      Text({ val: " " }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("text-sm") }),
        childs: [
          Text({ val: "Discriminant: " }),
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "class": LStr("disc") }),
            childs: [RenderText({ node_id: None, val: Method("discriminant") })],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("text-sm") }),
        childs: [
          Text({ val: "There are " }),
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "class": LStr("classify") }),
            childs: [RenderText({ node_id: None, val: Method("classify") })],
            ns: None,
          }),
          Text({ val: "." }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setA"), args: [Name("valueAsInt")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setB"), args: [Name("valueAsInt")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setC"), args: [Name("valueAsInt")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn quadratic_views() -> Map[String, @anode.View] {
  { "main": quadratic_main_view_ir() }
}
