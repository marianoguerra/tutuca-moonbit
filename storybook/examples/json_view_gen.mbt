// GENERATED by `tutuca gen-views` from json.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views json.html --name Json --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the JsonSelector component.
///
/// Declared as `interface json-selector` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct JsonSelectorState {} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonSelectorState::zero(), label: "Counter" }
fn JsonSelectorState::zero() -> JsonSelectorState {
  JsonSelectorState::{  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn json_selector_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(JsonSelectorState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn json_selector_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let json_selector_schema_fingerprint : String = "823721696e1e4913"

///|
/// The views declared in json.html.
pub(all) enum JsonSelectorView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn JsonSelectorView::name(self : JsonSelectorView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_selector_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_selector_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum JsonSelectorMethod {
  SetToArray
  SetToBool
  SetToNull
  SetToNumber
  SetToObject
  SetToString
} derive(Eq, @debug.Debug)

///|
pub fn JsonSelectorMethod::name(self : JsonSelectorMethod) -> String {
  match self {
    SetToArray => "setToArray"
    SetToBool => "setToBool"
    SetToNull => "setToNull"
    SetToNumber => "setToNumber"
    SetToObject => "setToObject"
    SetToString => "setToString"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_selector_mutate(
  f : (JsonSelectorMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for
    m in (
      [SetToArray, SetToBool, SetToNull, SetToNumber, SetToObject, SetToString] :
      Array[JsonSelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_selector_compute(
  f : (JsonSelectorMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for
    m in (
      [SetToArray, SetToBool, SetToNull, SetToNumber, SetToObject, SetToString] :
      Array[JsonSelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_selector_swap(
  f : (JsonSelectorMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for
    m in (
      [SetToArray, SetToBool, SetToNull, SetToNumber, SetToObject, SetToString] :
      Array[JsonSelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// The state of the JsonNull component.
///
/// Declared as `interface json-null` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct JsonNullState {} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonNullState::zero(), label: "Counter" }
fn JsonNullState::zero() -> JsonNullState {
  JsonNullState::{  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn json_null_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(JsonNullState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn json_null_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let json_null_schema_fingerprint : String = "28cebc724be3edcf"

///|
/// The views declared in json.html.
pub(all) enum JsonNullView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn JsonNullView::name(self : JsonNullView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_null_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_null_global_style : String = ""

///|
/// The state of the JsonBool component.
///
/// Declared as `interface json-bool` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct JsonBoolState {
  value : Bool
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonBoolState::zero(), label: "Counter" }
fn JsonBoolState::zero() -> JsonBoolState {
  JsonBoolState::{ value: false }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn json_bool_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(JsonBoolState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the JsonBool state declares — typed field names for tests and inspectors.
pub(all) enum JsonBoolField {
  Value
} derive(Eq, @debug.Debug)

///|
pub fn JsonBoolField::name(self : JsonBoolField) -> String {
  match self {
    Value => "value"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn JsonBoolField::kind(self : JsonBoolField) -> @component.FieldKind {
  match self {
    Value => @component.FBool
  }
}

///|
/// Every field, in declaration order.
pub let json_bool_field_names : Array[String] = ["value"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn json_bool_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let json_bool_schema_fingerprint : String = "d4b8a46eb24ceada"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn json_bool_encode(s : JsonBoolState) -> Map[String, @tutuca.Value] {
  { "value": Bool(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn json_bool_decode(f : Map[String, @tutuca.Value]) -> JsonBoolState? {
  guard f.get("value") is Some(Bool(v_value)) else { return None }
  Some(JsonBoolState::{ value: v_value })
}

///|
/// The views declared in json.html.
pub(all) enum JsonBoolView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn JsonBoolView::name(self : JsonBoolView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_bool_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_bool_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum JsonBoolMethod {
  ToggleValue
} derive(Eq, @debug.Debug)

///|
pub fn JsonBoolMethod::name(self : JsonBoolMethod) -> String {
  match self {
    ToggleValue => "toggleValue"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_bool_mutate(
  f : (JsonBoolMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([ToggleValue] : Array[JsonBoolMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_bool_compute(
  f : (JsonBoolMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([ToggleValue] : Array[JsonBoolMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_bool_swap(
  f : (JsonBoolMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ToggleValue] : Array[JsonBoolMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// The state of the JsonString component.
///
/// Declared as `interface json-string` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct JsonStringState {
  value : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonStringState::zero(), label: "Counter" }
fn JsonStringState::zero() -> JsonStringState {
  JsonStringState::{ value: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn json_string_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(JsonStringState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the JsonString state declares — typed field names for tests and inspectors.
pub(all) enum JsonStringField {
  Value
} derive(Eq, @debug.Debug)

///|
pub fn JsonStringField::name(self : JsonStringField) -> String {
  match self {
    Value => "value"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn JsonStringField::kind(self : JsonStringField) -> @component.FieldKind {
  match self {
    Value => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let json_string_field_names : Array[String] = ["value"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn json_string_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let json_string_schema_fingerprint : String = "56e6fad878f71b0c"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn json_string_encode(s : JsonStringState) -> Map[String, @tutuca.Value] {
  { "value": Str(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn json_string_decode(f : Map[String, @tutuca.Value]) -> JsonStringState? {
  guard f.get("value") is Some(Str(v_value)) else { return None }
  Some(JsonStringState::{ value: v_value })
}

///|
/// The views declared in json.html.
pub(all) enum JsonStringView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn JsonStringView::name(self : JsonStringView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_string_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_string_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum JsonStringMethod {
  SetValue
} derive(Eq, @debug.Debug)

///|
pub fn JsonStringMethod::name(self : JsonStringMethod) -> String {
  match self {
    SetValue => "setValue"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_string_mutate(
  f : (JsonStringMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetValue] : Array[JsonStringMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_string_compute(
  f : (JsonStringMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetValue] : Array[JsonStringMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_string_swap(
  f : (JsonStringMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetValue] : Array[JsonStringMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// The state of the JsonNumber component.
///
/// Declared as `interface json-number` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct JsonNumberState {
  strValue : String
  value : Double
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonNumberState::zero(), label: "Counter" }
fn JsonNumberState::zero() -> JsonNumberState {
  JsonNumberState::{ strValue: "", value: 0.0 }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn json_number_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(JsonNumberState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the JsonNumber state declares — typed field names for tests and inspectors.
pub(all) enum JsonNumberField {
  StrValue
  Value
} derive(Eq, @debug.Debug)

///|
pub fn JsonNumberField::name(self : JsonNumberField) -> String {
  match self {
    StrValue => "strValue"
    Value => "value"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn JsonNumberField::kind(self : JsonNumberField) -> @component.FieldKind {
  match self {
    StrValue => @component.FText
    Value => @component.FFloat
  }
}

///|
/// Every field, in declaration order.
pub let json_number_field_names : Array[String] = ["strValue", "value"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn json_number_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let json_number_schema_fingerprint : String = "f0f29389d0ef3ee2"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn json_number_encode(s : JsonNumberState) -> Map[String, @tutuca.Value] {
  { "strValue": Str(s.strValue), "value": Num(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn json_number_decode(f : Map[String, @tutuca.Value]) -> JsonNumberState? {
  guard f.get("strValue") is Some(Str(v_strValue)) else { return None }
  guard f.get("value") is Some(Num(v_value)) else { return None }
  Some(JsonNumberState::{ strValue: v_strValue, value: v_value })
}

///|
/// The views declared in json.html.
pub(all) enum JsonNumberView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn JsonNumberView::name(self : JsonNumberView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_number_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_number_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum JsonNumberMethod {
  SetRawValue
} derive(Eq, @debug.Debug)

///|
pub fn JsonNumberMethod::name(self : JsonNumberMethod) -> String {
  match self {
    SetRawValue => "setRawValue"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_number_mutate(
  f : (JsonNumberMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetRawValue] : Array[JsonNumberMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_number_compute(
  f : (JsonNumberMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetRawValue] : Array[JsonNumberMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_number_swap(
  f : (JsonNumberMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetRawValue] : Array[JsonNumberMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// The state of the JsonArray component.
///
/// Declared as `interface json-array` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct JsonArrayState {
  items : Array[@tutuca.Value]
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonArrayState::zero(), label: "Counter" }
fn JsonArrayState::zero() -> JsonArrayState {
  JsonArrayState::{ items: [] }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn json_array_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(JsonArrayState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the JsonArray state declares — typed field names for tests and inspectors.
pub(all) enum JsonArrayField {
  Items
} derive(Eq, @debug.Debug)

///|
pub fn JsonArrayField::name(self : JsonArrayField) -> String {
  match self {
    Items => "items"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn JsonArrayField::kind(self : JsonArrayField) -> @component.FieldKind {
  match self {
    Items => @component.FList
  }
}

///|
/// Every field, in declaration order.
pub let json_array_field_names : Array[String] = ["items"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn json_array_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let json_array_schema_fingerprint : String = "52ac0f26bb122de2"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn json_array_encode(s : JsonArrayState) -> Map[String, @tutuca.Value] {
  { "items": (List(s.items.map(x => x)) : @tutuca.Value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn json_array_decode(f : Map[String, @tutuca.Value]) -> JsonArrayState? {
  guard f.get("items") is Some(List(l_items)) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for x in l_items {
    v_items.push(x)
  }
  Some(JsonArrayState::{ items: v_items })
}

///|
/// The views declared in json.html.
pub(all) enum JsonArrayView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn JsonArrayView::name(self : JsonArrayView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_array_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_array_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonArrayInput {
  AddItem
  OnDropOnItem
} derive(Eq, @debug.Debug)

///|
pub fn JsonArrayInput::name(self : JsonArrayInput) -> String {
  match self {
    AddItem => "addItem"
    OnDropOnItem => "onDropOnItem"
  }
}

///|
pub fn JsonArrayInput::parse(s : String) -> JsonArrayInput? {
  match s {
    "addItem" => Some(AddItem)
    "onDropOnItem" => Some(OnDropOnItem)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonArrayMsg {
  /// `@on.click="addItem …"`
  AddItem
  /// `@on.drop="onDropOnItem …"`
  OnDropOnItem(@tutuca.Value, @tutuca.Value)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonArrayMsg::from_dispatch(d : @component.Dispatch) -> JsonArrayMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("addItem", []) => AddItem
      ("onDropOnItem", [a0, a1]) => OnDropOnItem(a0, a1)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum JsonArrayMethod {
  RemoveInItemsAt
} derive(Eq, @debug.Debug)

///|
pub fn JsonArrayMethod::name(self : JsonArrayMethod) -> String {
  match self {
    RemoveInItemsAt => "removeInItemsAt"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_array_mutate(
  f : (JsonArrayMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([RemoveInItemsAt] : Array[JsonArrayMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_array_compute(
  f : (JsonArrayMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([RemoveInItemsAt] : Array[JsonArrayMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_array_swap(
  f : (JsonArrayMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([RemoveInItemsAt] : Array[JsonArrayMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// The state of the JsonObjectKeyVal component.
///
/// Declared as `interface json-object-key-val` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct JsonObjectKeyValState {
  key : String
  value : @tutuca.Value
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonObjectKeyValState::zero(), label: "Counter" }
fn JsonObjectKeyValState::zero() -> JsonObjectKeyValState {
  JsonObjectKeyValState::{ key: "", value: Null }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn json_object_key_val_zero_args() -> Map[String, @tutuca.Value] {
  match
    @tutuca.Value::from_json(ToJson::to_json(JsonObjectKeyValState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the JsonObjectKeyVal state declares — typed field names for tests and inspectors.
pub(all) enum JsonObjectKeyValField {
  Key
  Value
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectKeyValField::name(self : JsonObjectKeyValField) -> String {
  match self {
    Key => "key"
    Value => "value"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn JsonObjectKeyValField::kind(
  self : JsonObjectKeyValField,
) -> @component.FieldKind {
  match self {
    Key => @component.FText
    Value => @component.FAny
  }
}

///|
/// Every field, in declaration order.
pub let json_object_key_val_field_names : Array[String] = ["key", "value"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn json_object_key_val_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let json_object_key_val_schema_fingerprint : String = "f20288305b90ddee"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn json_object_key_val_encode(
  s : JsonObjectKeyValState,
) -> Map[String, @tutuca.Value] {
  { "key": Str(s.key), "value": s.value }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn json_object_key_val_decode(
  f : Map[String, @tutuca.Value],
) -> JsonObjectKeyValState? {
  guard f.get("key") is Some(Str(v_key)) else { return None }
  let v_value = f.get("value").unwrap_or(Null)
  Some(JsonObjectKeyValState::{ key: v_key, value: v_value })
}

///|
/// The views declared in json.html.
pub(all) enum JsonObjectKeyValView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectKeyValView::name(self : JsonObjectKeyValView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_object_key_val_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_object_key_val_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum JsonObjectKeyValMethod {
  SetKey
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectKeyValMethod::name(self : JsonObjectKeyValMethod) -> String {
  match self {
    SetKey => "setKey"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_object_key_val_mutate(
  f : (JsonObjectKeyValMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetKey] : Array[JsonObjectKeyValMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_object_key_val_compute(
  f : (JsonObjectKeyValMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetKey] : Array[JsonObjectKeyValMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_object_key_val_swap(
  f : (JsonObjectKeyValMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetKey] : Array[JsonObjectKeyValMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// The state of the JsonObject component.
///
/// Declared as `interface json-object` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct JsonObjectState {
  items : Array[@tutuca.Value]
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonObjectState::zero(), label: "Counter" }
fn JsonObjectState::zero() -> JsonObjectState {
  JsonObjectState::{ items: [] }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn json_object_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(JsonObjectState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the JsonObject state declares — typed field names for tests and inspectors.
pub(all) enum JsonObjectField {
  Items
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectField::name(self : JsonObjectField) -> String {
  match self {
    Items => "items"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn JsonObjectField::kind(self : JsonObjectField) -> @component.FieldKind {
  match self {
    Items => @component.FList
  }
}

///|
/// Every field, in declaration order.
pub let json_object_field_names : Array[String] = ["items"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn json_object_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let json_object_schema_fingerprint : String = "25415f50b72a94c2"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn json_object_encode(s : JsonObjectState) -> Map[String, @tutuca.Value] {
  { "items": (List(s.items.map(x => x)) : @tutuca.Value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn json_object_decode(f : Map[String, @tutuca.Value]) -> JsonObjectState? {
  guard f.get("items") is Some(List(l_items)) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for x in l_items {
    v_items.push(x)
  }
  Some(JsonObjectState::{ items: v_items })
}

///|
/// The views declared in json.html.
pub(all) enum JsonObjectView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectView::name(self : JsonObjectView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_object_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_object_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonObjectInput {
  AddItem
  OnDropOnItem
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectInput::name(self : JsonObjectInput) -> String {
  match self {
    AddItem => "addItem"
    OnDropOnItem => "onDropOnItem"
  }
}

///|
pub fn JsonObjectInput::parse(s : String) -> JsonObjectInput? {
  match s {
    "addItem" => Some(AddItem)
    "onDropOnItem" => Some(OnDropOnItem)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonObjectMsg {
  /// `@on.click="addItem …"`
  AddItem
  /// `@on.drop="onDropOnItem …"`
  OnDropOnItem(@tutuca.Value, @tutuca.Value)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonObjectMsg::from_dispatch(d : @component.Dispatch) -> JsonObjectMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("addItem", []) => AddItem
      ("onDropOnItem", [a0, a1]) => OnDropOnItem(a0, a1)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum JsonObjectMethod {
  RemoveInItemsAt
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectMethod::name(self : JsonObjectMethod) -> String {
  match self {
    RemoveInItemsAt => "removeInItemsAt"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_object_mutate(
  f : (JsonObjectMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([RemoveInItemsAt] : Array[JsonObjectMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_object_compute(
  f : (JsonObjectMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([RemoveInItemsAt] : Array[JsonObjectMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] json_object_swap(
  f : (JsonObjectMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([RemoveInItemsAt] : Array[JsonObjectMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}
