// GENERATED by `tutuca gen-views` from json.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views json.html --name Json --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the JsonSelector component.
///
/// Declared as `interface json-selector` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct JsonSelectorState {}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonSelectorState::zero(), label: "Counter" }
pub fn JsonSelectorState::zero() -> JsonSelectorState {
  JsonSelectorState::{  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonSelectorState with fn encode(_s) {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonSelectorState with fn decode(_f) {
  Some(JsonSelectorState::{  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_selector_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_selector_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonSelectorInput {
  SetToArray
  SetToBool
  SetToNull
  SetToNumber
  SetToObject
  SetToString
} derive(Eq, @debug.Debug)

///|
pub fn JsonSelectorInput::name(self : JsonSelectorInput) -> String {
  match self {
    SetToArray => "setToArray"
    SetToBool => "setToBool"
    SetToNull => "setToNull"
    SetToNumber => "setToNumber"
    SetToObject => "setToObject"
    SetToString => "setToString"
  }
}

///|
pub fn JsonSelectorInput::parse(s : String) -> JsonSelectorInput? {
  match s {
    "setToArray" => Some(SetToArray)
    "setToBool" => Some(SetToBool)
    "setToNull" => Some(SetToNull)
    "setToNumber" => Some(SetToNumber)
    "setToObject" => Some(SetToObject)
    "setToString" => Some(SetToString)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonSelectorMsg {
  /// `@on.click="setToArray …"`
  SetToArray
  /// `@on.click="setToBool …"`
  SetToBool(Bool)
  /// `@on.click="setToNull …"`
  SetToNull
  /// `@on.click="setToNumber …"`
  SetToNumber
  /// `@on.click="setToObject …"`
  SetToObject
  /// `@on.click="setToString …"`
  SetToString
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonSelectorMsg::from_dispatch(
  d : @component.Dispatch,
) -> JsonSelectorMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setToArray", []) => SetToArray
      ("setToBool", [Bool(a0)]) => SetToBool(a0)
      ("setToNull", []) => SetToNull
      ("setToNumber", []) => SetToNumber
      ("setToObject", []) => SetToObject
      ("setToString", []) => SetToString
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonSelectorState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonSelector",
    fingerprint="823721696e1e4913",
    inputs=[
      "setToArray", "setToBool", "setToNull", "setToNumber", "setToObject", "setToString",
    ],
    view_names=["main"],
  )
}

///|
/// The state of the JsonNull component.
///
/// Declared as `interface json-null` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct JsonNullState {}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonNullState::zero(), label: "Counter" }
pub fn JsonNullState::zero() -> JsonNullState {
  JsonNullState::{  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonNullState with fn encode(_s) {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonNullState with fn decode(_f) {
  Some(JsonNullState::{  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_null_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_null_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonNullState with fn schema() {
  @component.SchemaInfo::new(name="JsonNull", fingerprint="28cebc724be3edcf", view_names=[
    "main",
  ])
}

///|
/// The state of the JsonBool component.
///
/// Declared as `interface json-bool` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct JsonBoolState {
  value : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonBoolState::zero(), label: "Counter" }
pub fn JsonBoolState::zero() -> JsonBoolState {
  JsonBoolState::{ value: false }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonBoolState with fn encode(s) {
  { "value": Bool(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonBoolState with fn decode(f) {
  guard f.get("value") is Some(Bool(v_value)) else { return None }
  Some(JsonBoolState::{ value: v_value })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_bool_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_bool_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonBoolInput {
  ToggleValue
} derive(Eq, @debug.Debug)

///|
pub fn JsonBoolInput::name(self : JsonBoolInput) -> String {
  match self {
    ToggleValue => "toggleValue"
  }
}

///|
pub fn JsonBoolInput::parse(s : String) -> JsonBoolInput? {
  match s {
    "toggleValue" => Some(ToggleValue)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonBoolMsg {
  /// `@on.click="toggleValue …"`
  ToggleValue
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonBoolMsg::from_dispatch(d : @component.Dispatch) -> JsonBoolMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("toggleValue", []) => ToggleValue
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonBoolState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonBool",
    fingerprint="d4b8a46eb24ceada",
    fields=[@component.FieldInfo::new("value", "bool", FBool)],
    inputs=["toggleValue"],
    view_names=["main"],
  )
}

///|
/// The state of the JsonString component.
///
/// Declared as `interface json-string` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct JsonStringState {
  value : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonStringState::zero(), label: "Counter" }
pub fn JsonStringState::zero() -> JsonStringState {
  JsonStringState::{ value: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonStringState with fn encode(s) {
  { "value": Str(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonStringState with fn decode(f) {
  guard f.get("value") is Some(Str(v_value)) else { return None }
  Some(JsonStringState::{ value: v_value })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_string_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_string_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonStringInput {
  SetValue
} derive(Eq, @debug.Debug)

///|
pub fn JsonStringInput::name(self : JsonStringInput) -> String {
  match self {
    SetValue => "setValue"
  }
}

///|
pub fn JsonStringInput::parse(s : String) -> JsonStringInput? {
  match s {
    "setValue" => Some(SetValue)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonStringMsg {
  /// `@on.input="setValue …"`
  SetValue(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonStringMsg::from_dispatch(d : @component.Dispatch) -> JsonStringMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setValue", [Str(a0)]) => SetValue(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonStringState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonString",
    fingerprint="56e6fad878f71b0c",
    fields=[@component.FieldInfo::new("value", "string", FText)],
    inputs=["setValue"],
    view_names=["main"],
  )
}

///|
/// The state of the JsonNumber component.
///
/// Declared as `interface json-number` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct JsonNumberState {
  strValue : String
  value : Double
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonNumberState::zero(), label: "Counter" }
pub fn JsonNumberState::zero() -> JsonNumberState {
  JsonNumberState::{ strValue: "", value: 0.0 }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonNumberState with fn encode(s) {
  { "strValue": Str(s.strValue), "value": Num(s.value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonNumberState with fn decode(f) {
  guard f.get("strValue") is Some(Str(v_strValue)) else { return None }
  guard f.get("value") is Some(Num(v_value)) else { return None }
  Some(JsonNumberState::{ strValue: v_strValue, value: v_value })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_number_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_number_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonNumberInput {
  SetRawValue
} derive(Eq, @debug.Debug)

///|
pub fn JsonNumberInput::name(self : JsonNumberInput) -> String {
  match self {
    SetRawValue => "setRawValue"
  }
}

///|
pub fn JsonNumberInput::parse(s : String) -> JsonNumberInput? {
  match s {
    "setRawValue" => Some(SetRawValue)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonNumberMsg {
  /// `@on.input="setRawValue …"`
  SetRawValue(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonNumberMsg::from_dispatch(d : @component.Dispatch) -> JsonNumberMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setRawValue", [Str(a0)]) => SetRawValue(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonNumberState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonNumber",
    fingerprint="f0f29389d0ef3ee2",
    fields=[
      @component.FieldInfo::new("strValue", "string", FText),
      @component.FieldInfo::new("value", "f64", FFloat),
    ],
    inputs=["setRawValue"],
    view_names=["main"],
  )
}

///|
/// The state of the JsonArray component.
///
/// Declared as `interface json-array` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct JsonArrayState {
  items : Array[@tutuca.Value]
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonArrayState::zero(), label: "Counter" }
pub fn JsonArrayState::zero() -> JsonArrayState {
  JsonArrayState::{ items: [] }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonArrayState with fn encode(s) {
  { "items": (List(s.items.map(x => x)) : @tutuca.Value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonArrayState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  Some(JsonArrayState::{ items: v_items })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_array_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_array_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonArrayInput {
  AddItem
  OnDropOnItem
  RemoveInItemsAt
} derive(Eq, @debug.Debug)

///|
pub fn JsonArrayInput::name(self : JsonArrayInput) -> String {
  match self {
    AddItem => "addItem"
    OnDropOnItem => "onDropOnItem"
    RemoveInItemsAt => "removeInItemsAt"
  }
}

///|
pub fn JsonArrayInput::parse(s : String) -> JsonArrayInput? {
  match s {
    "addItem" => Some(AddItem)
    "onDropOnItem" => Some(OnDropOnItem)
    "removeInItemsAt" => Some(RemoveInItemsAt)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonArrayMsg {
  /// `@on.click="addItem …"`
  AddItem
  /// `@on.drop="onDropOnItem …"`
  OnDropOnItem(@tutuca.Value, @tutuca.Value)
  /// `@on.click="removeInItemsAt …"`
  RemoveInItemsAt(@tutuca.Value)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonArrayMsg::from_dispatch(d : @component.Dispatch) -> JsonArrayMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("addItem", []) => AddItem
      ("onDropOnItem", [a0, a1]) => OnDropOnItem(a0, a1)
      ("removeInItemsAt", [a0]) => RemoveInItemsAt(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonArrayState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonArray",
    fingerprint="52ac0f26bb122de2",
    fields=[@component.FieldInfo::new("items", "list<any>", FList, elem="any")],
    inputs=["addItem", "onDropOnItem", "removeInItemsAt"],
    view_names=["main"],
  )
}

///|
/// The state of the JsonObjectKeyVal component.
///
/// Declared as `interface json-object-key-val` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct JsonObjectKeyValState {
  key : String
  value : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonObjectKeyValState::zero(), label: "Counter" }
pub fn JsonObjectKeyValState::zero() -> JsonObjectKeyValState {
  JsonObjectKeyValState::{ key: "", value: Null }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonObjectKeyValState with fn encode(s) {
  { "key": Str(s.key), "value": s.value }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonObjectKeyValState with fn decode(f) {
  guard f.get("key") is Some(Str(v_key)) else { return None }
  let v_value = f.get("value").unwrap_or(Null)
  Some(JsonObjectKeyValState::{ key: v_key, value: v_value })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_object_key_val_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_object_key_val_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonObjectKeyValInput {
  SetKey
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectKeyValInput::name(self : JsonObjectKeyValInput) -> String {
  match self {
    SetKey => "setKey"
  }
}

///|
pub fn JsonObjectKeyValInput::parse(s : String) -> JsonObjectKeyValInput? {
  match s {
    "setKey" => Some(SetKey)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonObjectKeyValMsg {
  /// `@on.input="setKey …"`
  SetKey(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonObjectKeyValMsg::from_dispatch(
  d : @component.Dispatch,
) -> JsonObjectKeyValMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setKey", [Str(a0)]) => SetKey(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonObjectKeyValState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonObjectKeyVal",
    fingerprint="f20288305b90ddee",
    fields=[
      @component.FieldInfo::new("key", "string", FText),
      @component.FieldInfo::new("value", "any", FAny, elem="any"),
    ],
    inputs=["setKey"],
    view_names=["main"],
  )
}

///|
/// The state of the JsonObject component.
///
/// Declared as `interface json-object` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct JsonObjectState {
  items : Array[@tutuca.Value]
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..JsonObjectState::zero(), label: "Counter" }
pub fn JsonObjectState::zero() -> JsonObjectState {
  JsonObjectState::{ items: [] }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for JsonObjectState with fn encode(s) {
  { "items": (List(s.items.map(x => x)) : @tutuca.Value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for JsonObjectState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  Some(JsonObjectState::{ items: v_items })
}

///|
/// The file-level style — pass as `common_style~`.
pub let json_object_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let json_object_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum JsonObjectInput {
  AddItem
  OnDropOnItem
  RemoveInItemsAt
} derive(Eq, @debug.Debug)

///|
pub fn JsonObjectInput::name(self : JsonObjectInput) -> String {
  match self {
    AddItem => "addItem"
    OnDropOnItem => "onDropOnItem"
    RemoveInItemsAt => "removeInItemsAt"
  }
}

///|
pub fn JsonObjectInput::parse(s : String) -> JsonObjectInput? {
  match s {
    "addItem" => Some(AddItem)
    "onDropOnItem" => Some(OnDropOnItem)
    "removeInItemsAt" => Some(RemoveInItemsAt)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to json.html breaks every `match` over this type
/// until it is handled.
pub(all) enum JsonObjectMsg {
  /// `@on.click="addItem …"`
  AddItem
  /// `@on.drop="onDropOnItem …"`
  OnDropOnItem(@tutuca.Value, @tutuca.Value)
  /// `@on.click="removeInItemsAt …"`
  RemoveInItemsAt(@tutuca.Value)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn JsonObjectMsg::from_dispatch(d : @component.Dispatch) -> JsonObjectMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("addItem", []) => AddItem
      ("onDropOnItem", [a0, a1]) => OnDropOnItem(a0, a1)
      ("removeInItemsAt", [a0]) => RemoveInItemsAt(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for JsonObjectState with fn schema() {
  @component.SchemaInfo::new(
    name="JsonObject",
    fingerprint="25415f50b72a94c2",
    fields=[@component.FieldInfo::new("items", "list<any>", FList, elem="any")],
    inputs=["addItem", "onDropOnItem", "removeInItemsAt"],
    view_names=["main"],
  )
}
