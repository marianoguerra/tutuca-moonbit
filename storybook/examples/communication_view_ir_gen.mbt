// GENERATED by `tutuca gen-views` from communication.html — DO NOT EDIT.
//
// The views of Status, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing status_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn status_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Show({
    node_id: None,
    val: Predicate(pred=IsTruthy, args=[Field("message")]),
    node: Dom({
      tag: "div",
      attrs: ConstAttrs({
        "class": LStr("alert alert-info"),
        "data-vid": LStr("main"),
      }),
      childs: [
        Dom({
          tag: "span",
          attrs: ConstAttrs({ "class": LStr("msg") }),
          childs: [RenderText({ node_id: None, val: Field("message") })],
          ns: None,
        }),
        Text({ val: " " }),
        Dom({
          tag: "small",
          attrs: ConstAttrs(Map([])),
          childs: [
            Text({ val: "(flashed " }),
            RenderText({ node_id: Some(0), val: Field("count") }),
            Text({ val: " times)" }),
          ],
          ns: None,
        }),
      ],
      ns: None,
    }),
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn status_views() -> Map[String, @anode.View] {
  { "main": status_main_view_ir() }
}

///|
/// The compiled `main` view.
fn send_receive_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-2"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Render({ node_id: 0, val: Field("status"), view_val: None }),
      Text({ val: " " }),
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="class", val=Const(lit=LStr("input"), from_macro=false)),
          Plain(name="value", val=Field("draft")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
          ConstA(
            name="placeholder",
            val=Const(
              lit=LStr("Type a message and press Enter"),
              from_macro=false,
            ),
          ),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex gap-2") }),
        childs: [
          Dom({
            tag: "button",
            attrs: ConstAttrs({
              "class": LStr("btn btn-primary send"),
              "data-eid": LNum(1.0),
            }),
            childs: [Text({ val: "Send" })],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "button",
            attrs: ConstAttrs({
              "class": LStr("btn btn-ghost clear"),
              "data-eid": LNum(2.0),
            }),
            childs: [Text({ val: "Clear status" })],
            ns: None,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setDraft"), args: [Name("value")] },
          modifiers: [],
        },
        {
          name: "keydown",
          handler: { handler: HandlerName(name="submit", ns=Input), args: [] },
          modifiers: ["send"],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: HandlerName(name="submit", ns=Input), args: [] },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "click",
          handler: {
            handler: HandlerName(name="clearStatus", ns=Input),
            args: [],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn send_receive_views() -> Map[String, @anode.View] {
  { "main": send_receive_main_view_ir() }
}

///|
/// The compiled `main` view.
fn tree_item_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({ "data-vid": LStr("main") }),
    childs: [
      Dom({
        tag: "p",
        attrs: DynAttrs([
          ConstA(name="class", val=Const(lit=LStr("head"), from_macro=false)),
          If({
            name: "class",
            cond_val: Field("isOpen"),
            then_val: Some(
              StrTpl([
                Some(Const(lit=LStr("head open type-"), from_macro=false)),
                Some(Field("type")),
              ]),
            ),
            else_val: Some(
              StrTpl([
                Some(Const(lit=LStr("head closed type-"), from_macro=false)),
                Some(Field("type")),
              ]),
            ),
          }),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
        ]),
        childs: [RenderText({ node_id: None, val: Field("label") })],
        ns: None,
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Method("areChildsVisible"),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({
            "class": LStr("pl-3 pt-1 flex flex-col gap-2 kids"),
          }),
          childs: [
            Each({
              node_id: 1,
              iter_info: {
                val: Field("items"),
                when_val: None,
                loop_with_val: None,
                enrich_with_val: None,
              },
              node: RenderIt({ node_id: 0, view_val: None }),
              from_render_each: true,
            }),
          ],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: {
            handler: HandlerName(name="onItemClick", ns=Input),
            args: [],
          },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="user-select: none;\n.head { cursor: pointer; }\n.head:before { content: \"⏺️\"; margin-right: 0.25rem; }\n.head.type-file:before { content: \"📄️\"; }\n.head.open.type-dir:before { content: \"📂️\"; }\n.head.closed.type-dir:before { content: \"📁️\"; }",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn tree_item_views() -> Map[String, @anode.View] {
  { "main": tree_item_main_view_ir() }
}

///|
/// The compiled `main` view.
fn tree_root_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Render({ node_id: 0, val: Field("root"), view_val: None }),
      Text({ val: " " }),
      Dom({
        tag: "ul",
        attrs: ConstAttrs({
          "class": LStr("flex flex-col gap-2 max-h-[40vh] overflow-y-auto log"),
        }),
        childs: [
          Each({
            node_id: 2,
            iter_info: {
              val: Field("log"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "li",
              attrs: ConstAttrs(Map([])),
              childs: [RenderText({ node_id: Some(1), val: Bind("value") })],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn tree_root_views() -> Map[String, @anode.View] {
  { "main": tree_root_main_view_ir() }
}
