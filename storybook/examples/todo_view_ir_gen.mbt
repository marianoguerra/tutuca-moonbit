// GENERATED by `tutuca gen-views` from todo.html — DO NOT EDIT.
//
// The views of Item, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing item_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn item_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex gap-3 items-center"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="type", val=Const(lit=LStr("checkbox"), from_macro=false)),
          ConstA(
            name="class",
            val=Const(lit=LStr("checkbox"), from_macro=false),
          ),
          Plain(name="checked", val=Field("completed")),
          ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
        ]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "input",
        attrs: DynAttrs([
          ConstA(name="class", val=Const(lit=LStr("input"), from_macro=false)),
          Plain(name="value", val=Field("text")),
          ConstA(name="data-eid", val=Const(lit=LNum(1.0), from_macro=false)),
          Plain(name="disabled", val=Field("completed")),
        ]),
        childs: [],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setCompleted"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setText"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn item_views() -> Map[String, @anode.View] {
  { "main": item_main_view_ir() }
}

///|
/// The compiled `main` view.
fn items_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "button",
        attrs: ConstAttrs({
          "class": LStr("btn btn-soft btn-success add"),
          "data-eid": LNum(0.0),
        }),
        childs: [Text({ val: "Add Task" })],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("flex flex-col gap-3 w-full") }),
        childs: [
          Each({
            node_id: 1,
            iter_info: {
              val: Field("items"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: Dom({
              tag: "div",
              attrs: ConstAttrs({
                "class": LStr(
                  "flex gap-3 justify-center items-center w-full row",
                ),
              }),
              childs: [
                RenderIt({ node_id: 0, view_val: None }),
                Text({ val: " " }),
                Dom({
                  tag: "button",
                  attrs: ConstAttrs({
                    "class": LStr(
                      "btn btn-soft btn-sm btn-error btn-circle font-mono rm",
                    ),
                    "data-eid": LNum(1.0),
                  }),
                  childs: [Text({ val: "x" })],
                  ns: None,
                }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "click",
          handler: {
            handler: HandlerName(name="onAddItem", ns=Input),
            args: [],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("removeInItemsAt"), args: [Bind("key")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn items_views() -> Map[String, @anode.View] {
  { "main": items_main_view_ir() }
}
