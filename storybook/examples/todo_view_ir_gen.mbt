// GENERATED by `tutuca gen-views` from todo.html — DO NOT EDIT.
//
// The views of Item, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing item_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let item_spec_source : String =
  #|  state Item {
  #|    completed : Bool
  #|    text : String
  #|  }
  #|
  #|  state Items {
  #|    items : Array[Item]
  #|  }

///|
/// The compiled `main` view.
fn item_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex gap-3 items-center")], [
      @anode.h(
        "input",
        [
          @anode.attr("type", "checkbox"),
          @anode.attr("class", "checkbox"),
          @anode.dyn_attr("checked", @tutuca.Expr::field("completed")),
          @anode.eid(0),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h(
        "input",
        [
          @anode.attr("class", "input"),
          @anode.dyn_attr("value", @tutuca.Expr::field("text")),
          @anode.eid(1),
          @anode.dyn_attr("disabled", @tutuca.Expr::field("completed")),
        ],
        [],
      ),
    ]),
    [
      [
        @anode.on(
          "input",
          @tutuca.Expr::handler_name("setCompleted", Receive),
          args=[@tutuca.Expr::event_path(["value"])],
        ),
      ],
      [
        @anode.on("input", @tutuca.Expr::handler_name("setText", Receive), args=[
          @tutuca.Expr::event_path(["value"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn item_views() -> Map[String, @anode.View] {
  { "main": item_main_view_ir() }
}

///|
/// The Item component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `ItemState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn item_component(
  name? : String = "Item",
  views? : Map[String, @anode.View] = item_views(),
  init? : ItemState = ItemState::zero(),
  common_style? : String = item_common_style,
  global_style? : String = item_global_style,
  update? : (ItemState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    ItemState,
  ],
  swap? : (ItemInput) -> ((ItemState, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetCompleted, SetText] : Array[ItemInput]),
          ItemInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=item_spec_source,
  )
}

///|
/// The compiled `main` view.
fn items_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.h(
        "button",
        [@anode.attr("class", "btn btn-soft btn-success add"), @anode.eid(0)],
        [@anode.text("Add Task")],
      ),
      @anode.text(" "),
      @anode.h("div", [@anode.attr("class", "flex flex-col gap-3 w-full")], [
        @anode.each(
          1,
          @tutuca.Expr::field("items"),
          @anode.h(
            "div",
            [
              @anode.attr(
                "class", "flex gap-3 justify-center items-center w-full row",
              ),
            ],
            [
              @anode.render_it(0),
              @anode.text(" "),
              @anode.h(
                "button",
                [
                  @anode.attr(
                    "class", "btn btn-soft btn-sm btn-error btn-circle font-mono rm",
                  ),
                  @anode.eid(1),
                ],
                [@anode.text("x")],
              ),
            ],
          ),
        ),
      ]),
    ]),
    [
      [@anode.on("click", @tutuca.Expr::handler_name("onAddItem", Receive))],
      [
        @anode.on(
          "click",
          @tutuca.Expr::handler_name("removeInItemsAt", Receive),
          args=[@tutuca.Expr::bind("key")],
        ),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn items_views() -> Map[String, @anode.View] {
  { "main": items_main_view_ir() }
}

///|
/// The Items component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `ItemsState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn items_component(
  name? : String = "Items",
  views? : Map[String, @anode.View] = items_views(),
  init? : ItemsState = ItemsState::zero(),
  common_style? : String = items_common_style,
  global_style? : String = items_global_style,
  update? : (ItemsState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    ItemsState,
  ],
  swap? : (ItemsInput) -> ((ItemsState, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([OnAddItem, RemoveInItemsAt] : Array[ItemsInput]),
          ItemsInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=item_spec_source,
  )
}
