// GENERATED by `tutuca gen-views` from request.html — DO NOT EDIT.
//
// The views of RequestExample, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing request_example_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn request_example_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Show({
        node_id: None,
        val: Field("isLoading"),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({
            "class": LStr("alert alert-info alert-outline loading"),
          }),
          childs: [Text({ val: "Loading" })],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Hide({
        node_id: None,
        val: Field("isLoading"),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("flex justify-between") }),
          childs: [
            Dom({
              tag: "input",
              attrs: DynAttrs([
                ConstA(
                  name="type",
                  val=Const(lit=LStr("search"), from_macro=false),
                ),
                ConstA(
                  name="class",
                  val=Const(lit=LStr("input"), from_macro=false),
                ),
                Plain(name="value", val=Field("query")),
                ConstA(
                  name="data-eid",
                  val=Const(lit=LNum(0.0), from_macro=false),
                ),
                ConstA(
                  name="placeholder",
                  val=Const(lit=LStr("Filter entries"), from_macro=false),
                ),
              ]),
              childs: [],
              ns: None,
            }),
            Text({ val: " " }),
            Dom({
              tag: "button",
              attrs: ConstAttrs({
                "class": LStr("btn bnt-sm btn-primary btn-outline another"),
                "data-eid": LNum(1.0),
              }),
              childs: [Text({ val: "Load Another Way" })],
              ns: None,
            }),
            Text({ val: " " }),
            Dom({
              tag: "button",
              attrs: ConstAttrs({
                "class": LStr("btn bnt-sm btn-primary toggle-btn"),
                "data-eid": LNum(2.0),
              }),
              childs: [RenderText({ node_id: None, val: Field("view") })],
              ns: None,
            }),
          ],
          ns: None,
        }),
      }),
      Text({ val: " " }),
      Hide({
        node_id: None,
        val: Field("isLoading"),
        node: PushView({
          node_id: None,
          val: Field("view"),
          node: Dom({
            tag: "div",
            attrs: ConstAttrs({
              "class": LStr(
                "flex flex-col gap-3 max-h-[40vh] overflow-y-auto pr-3 rows",
              ),
            }),
            childs: [
              Each({
                node_id: 1,
                iter_info: {
                  val: Field("items"),
                  when_val: Some(HandlerName(name="filterItem", ns=Alter)),
                  loop_with_val: None,
                  enrich_with_val: None,
                },
                node: RenderIt({ node_id: 0, view_val: None }),
                from_render_each: true,
              }),
            ],
            ns: None,
          }),
        }),
      }),
      Text({ val: " " }),
      Show({
        node_id: None,
        val: Predicate(pred=IsTruthy, args=[Field("error")]),
        node: Dom({
          tag: "div",
          attrs: ConstAttrs({ "class": LStr("error") }),
          childs: [RenderText({ node_id: Some(2), val: Field("error") })],
          ns: None,
        }),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setQuery"), args: [Name("value")] },
          modifiers: [],
        },
        {
          name: "keydown",
          handler: { handler: Method("resetQuery"), args: [] },
          modifiers: ["cancel"],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "click",
          handler: {
            handler: HandlerName(name="loadAnotherWay", ns=Input),
            args: [],
          },
          modifiers: [],
        },
      ],
    },
    {
      id: 2,
      handlers: [
        {
          name: "click",
          handler: { handler: Method("toggleView"), args: [] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn request_example_views() -> Map[String, @anode.View] {
  { "main": request_example_main_view_ir() }
}
