// GENERATED by `tutuca gen-views` from request.html — DO NOT EDIT.
//
// The views of RequestExample, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing request_example_views() as `views~` means the template parser
// never runs at startup.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn request_example_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.show(
        Field("isLoading"),
        @anode.h(
          "div",
          [@anode.attr("class", "alert alert-info alert-outline loading")],
          [@anode.text("Loading")],
        ),
      ),
      @anode.text(" "),
      @anode.hide(
        Field("isLoading"),
        @anode.h("div", [@anode.attr("class", "flex justify-between")], [
          @anode.h(
            "input",
            [
              @anode.attr("type", "search"),
              @anode.attr("class", "input"),
              @anode.dyn_attr("value", Field("query")),
              @anode.eid(0),
              @anode.attr("placeholder", "Filter entries"),
            ],
            [],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("class", "btn bnt-sm btn-primary btn-outline another"),
              @anode.eid(1),
            ],
            [@anode.text("Load Another Way")],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("class", "btn bnt-sm btn-primary toggle-btn"),
              @anode.eid(2),
            ],
            [@anode.dyn_text(Field("view"))],
          ),
        ]),
      ),
      @anode.text(" "),
      @anode.hide(
        Field("isLoading"),
        @anode.push_view(
          Field("view"),
          @anode.h(
            "div",
            [
              @anode.attr(
                "class", "flex flex-col gap-3 max-h-[40vh] overflow-y-auto pr-3 rows",
              ),
            ],
            [
              @anode.each(
                1,
                Field("items"),
                @anode.render_it(0),
                when_val=HandlerName(name="filterItem", ns=Alter),
                from_render_each=true,
              ),
            ],
          ),
        ),
      ),
      @anode.text(" "),
      @anode.show(
        Predicate(pred=IsTruthy, args=[Field("error")]),
        @anode.h("div", [@anode.attr("class", "error")], [
          @anode.dyn_text(Field("error"), id=2),
        ]),
      ),
    ]),
    [
      [
        @anode.on("input", Method("setQuery"), args=[Name("value")]),
        @anode.on("keydown", Method("resetQuery"), modifiers=["cancel"]),
      ],
      [@anode.on("click", HandlerName(name="loadAnotherWay", ns=Input))],
      [@anode.on("click", Method("toggleView"))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn request_example_views() -> Map[String, @anode.View] {
  { "main": request_example_main_view_ir() }
}
