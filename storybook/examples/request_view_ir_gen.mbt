// GENERATED by `tutuca gen-views` from request.html — DO NOT EDIT.
//
// The views of RequestExample, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing request_example_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn request_example_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.show(
        Field("isLoading"),
        @anode.h(
          "div",
          [@anode.attr("class", "alert alert-info alert-outline loading")],
          [@anode.text("Loading")],
        ),
      ),
      @anode.text(" "),
      @anode.hide(
        Field("isLoading"),
        @anode.h("div", [@anode.attr("class", "flex justify-between")], [
          @anode.h(
            "input",
            [
              @anode.attr("type", "search"),
              @anode.attr("class", "input"),
              @anode.dyn_attr("value", Field("query")),
              @anode.eid(0),
              @anode.attr("placeholder", "Filter entries"),
            ],
            [],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("class", "btn bnt-sm btn-primary btn-outline another"),
              @anode.eid(1),
            ],
            [@anode.text("Load Another Way")],
          ),
          @anode.text(" "),
          @anode.h(
            "button",
            [
              @anode.attr("class", "btn bnt-sm btn-primary toggle-btn"),
              @anode.eid(2),
            ],
            [@anode.dyn_text(Field("view"))],
          ),
        ]),
      ),
      @anode.text(" "),
      @anode.hide(
        Field("isLoading"),
        @anode.push_view(
          Field("view"),
          @anode.h(
            "div",
            [
              @anode.attr(
                "class", "flex flex-col gap-3 max-h-[40vh] overflow-y-auto pr-3 rows",
              ),
            ],
            [
              @anode.each(
                1,
                Field("items"),
                @anode.render_it(0),
                when_val=HandlerName(name="filterItem", ns=Alter),
                from_render_each=true,
              ),
            ],
          ),
        ),
      ),
      @anode.text(" "),
      @anode.show(
        App(name="truthy?", args=[Field("error")]),
        @anode.h("div", [@anode.attr("class", "error")], [
          @anode.dyn_text(Field("error"), id=2),
        ]),
      ),
    ]),
    [
      [
        @anode.on("input", HandlerName(name="setQuery", ns=Input), args=[
          EventPath(["value"]),
        ]),
        @anode.on("keydown", HandlerName(name="resetQuery", ns=Input), modifiers=[
          "cancel",
        ]),
      ],
      [@anode.on("click", HandlerName(name="loadAnotherWay", ns=Input))],
      [@anode.on("click", HandlerName(name="toggleView", ns=Input))],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn request_example_views() -> Map[String, @anode.View] {
  { "main": request_example_main_view_ir() }
}

///|
/// The dispatches the `<script type="tutuca/script">` block answers.
///
/// Composed AHEAD of the author's `update~`, which stays optional and is
/// consulted only for a name this does not answer. GENERATED — edit the
/// block, not this.
fn request_example_inline_update(
  s : RequestExampleState,
  msg : @component.Dispatch,
  ctx : &@tutuca.Ctx,
) -> @component.Update[RequestExampleState] {
  match msg {
    Receive("toggleView", _args) => {
      let s = {
        ..s,
        view: if (Str(s.view) : @tutuca.Value) == (Str("main") : @tutuca.Value) {
          "edit"
        } else {
          "main"
        },
      }
      Next(s)
    }
    Receive("init", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "loadData",
          [],
          @tutuca.IntentOpts::new(
            route=[@tutuca.Leg::Lex],
            on_ok_name="loadDataOk",
            on_error_name="loadDataError",
            on_unhandled_name="loadDataUnhandled",
          ),
        )
      })
      let s = { ..s, isLoading: true }
      for f in effects {
        f()
      }
      Next(s)
    }
    _ => Unhandled
  }
}

///|
/// The RequestExample component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `RequestExampleState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn request_example_component(
  name? : String = "RequestExample",
  views? : Map[String, @anode.View] = request_example_views(),
  init? : RequestExampleState = RequestExampleState::zero(),
  common_style? : String = request_example_common_style,
  global_style? : String = request_example_global_style,
  update? : (RequestExampleState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    RequestExampleState,
  ],
  swap? : (RequestExampleInput) -> ((
    RequestExampleState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  when? : (RequestExampleWhen) -> ((
    RequestExampleState,
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Bool)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update=(s, msg, ctx) => {
      match request_example_inline_update(s, msg, ctx) {
        Unhandled =>
          match update {
            Some(u) => u(s, msg, ctx)
            None => Unhandled
          }
        answered => answered
      }
    },
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          (
            [LoadAnotherWay, ResetQuery, SetQuery, ToggleView] :
            Array[RequestExampleInput]),
          RequestExampleInput::name,
          f,
        )
      None => Map([])
    },
    when=match when {
      Some(f) =>
        @component.build_bucket(
          ([FilterItem] : Array[RequestExampleWhen]),
          RequestExampleWhen::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
