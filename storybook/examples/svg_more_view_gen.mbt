// GENERATED by `tutuca gen-views` from svg_more.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views svg_more.html --name SvgMore --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state for the components in svg_more.mbt, which build
/// their views in MoonBit rather than here — a macro body is
/// registered at runtime, so it cannot be compiled ahead of time.
/// No <template>: this file declares state and nothing else.
///
/// Declared as `interface bar-chart` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct BarChartState {
  values : Array[@tutuca.Value]
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..BarChartState::zero(), label: "Counter" }
fn BarChartState::zero() -> BarChartState {
  BarChartState::{ values: [] }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn bar_chart_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(BarChartState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the BarChart state declares — typed field names for tests and inspectors.
pub(all) enum BarChartField {
  Values
} derive(Eq, @debug.Debug)

///|
pub fn BarChartField::name(self : BarChartField) -> String {
  match self {
    Values => "values"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn BarChartField::kind(self : BarChartField) -> @component.FieldKind {
  match self {
    Values => @component.FList
  }
}

///|
/// Every field, in declaration order.
pub let bar_chart_field_names : Array[String] = ["values"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let bar_chart_schema_fingerprint : String = "6bab0bb9bb7de924"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn bar_chart_encode(s : BarChartState) -> Map[String, @tutuca.Value] {
  { "values": (List(s.values.map(x => x)) : @tutuca.Value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn bar_chart_decode(f : Map[String, @tutuca.Value]) -> BarChartState? {
  guard f.get("values") is Some(List(l_values)) else { return None }
  let v_values : Array[@tutuca.Value] = []
  for x in l_values {
    v_values.push(x)
  }
  Some(BarChartState::{ values: v_values })
}

///|
/// The state of the IconGallery component.
///
/// Declared as `interface icon-gallery` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct IconGalleryState {
  size : Int
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..IconGalleryState::zero(), label: "Counter" }
fn IconGalleryState::zero() -> IconGalleryState {
  IconGalleryState::{ size: 0 }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn icon_gallery_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(IconGalleryState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the IconGallery state declares — typed field names for tests and inspectors.
pub(all) enum IconGalleryField {
  Size
} derive(Eq, @debug.Debug)

///|
pub fn IconGalleryField::name(self : IconGalleryField) -> String {
  match self {
    Size => "size"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn IconGalleryField::kind(self : IconGalleryField) -> @component.FieldKind {
  match self {
    Size => @component.FInt
  }
}

///|
/// Every field, in declaration order.
pub let icon_gallery_field_names : Array[String] = ["size"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let icon_gallery_schema_fingerprint : String = "17ce7599eac0af6f"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn icon_gallery_encode(s : IconGalleryState) -> Map[String, @tutuca.Value] {
  { "size": Num(s.size.to_double()) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn icon_gallery_decode(f : Map[String, @tutuca.Value]) -> IconGalleryState? {
  guard f.get("size") is Some(Num(n_size)) else { return None }
  let v_size = n_size.to_int()
  Some(IconGalleryState::{ size: v_size })
}
