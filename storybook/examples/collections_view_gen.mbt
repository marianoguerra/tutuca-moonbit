// GENERATED by `tutuca gen-views` from collections.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views collections.html --name Collections --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the RenderWithScope component.
///
/// Declared as `interface render-with-scope` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct RenderWithScopeState {
  text : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..RenderWithScopeState::zero(), label: "Counter" }
fn RenderWithScopeState::zero() -> RenderWithScopeState {
  RenderWithScopeState::{ text: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn render_with_scope_zero_args() -> Map[String, @tutuca.Value] {
  match
    @tutuca.Value::from_json(ToJson::to_json(RenderWithScopeState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the RenderWithScope state declares — typed field names for tests and inspectors.
pub(all) enum RenderWithScopeField {
  Text
} derive(Eq, @debug.Debug)

///|
pub fn RenderWithScopeField::name(self : RenderWithScopeField) -> String {
  match self {
    Text => "text"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn RenderWithScopeField::kind(
  self : RenderWithScopeField,
) -> @component.FieldKind {
  match self {
    Text => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let render_with_scope_field_names : Array[String] = ["text"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn render_with_scope_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let render_with_scope_schema_fingerprint : String = "25c803b695d1cdde"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn render_with_scope_encode(
  s : RenderWithScopeState,
) -> Map[String, @tutuca.Value] {
  { "text": Str(s.text) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn render_with_scope_decode(
  f : Map[String, @tutuca.Value],
) -> RenderWithScopeState? {
  guard f.get("text") is Some(Str(v_text)) else { return None }
  Some(RenderWithScopeState::{ text: v_text })
}

///|
/// The views declared in collections.html.
pub(all) enum RenderWithScopeView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn RenderWithScopeView::name(self : RenderWithScopeView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let render_with_scope_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let render_with_scope_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum RenderWithScopeMethod {
  SetText
} derive(Eq, @debug.Debug)

///|
pub fn RenderWithScopeMethod::name(self : RenderWithScopeMethod) -> String {
  match self {
    SetText => "setText"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] render_with_scope_mutate(
  f : (RenderWithScopeMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetText] : Array[RenderWithScopeMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] render_with_scope_compute(
  f : (RenderWithScopeMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetText] : Array[RenderWithScopeMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] render_with_scope_swap(
  f : (RenderWithScopeMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetText] : Array[RenderWithScopeMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// The state of the ListFilterEnrich component.
///
/// Declared as `interface list-filter-enrich` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct ListFilterEnrichState {
  items : Array[@tutuca.Value]
  query : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ListFilterEnrichState::zero(), label: "Counter" }
fn ListFilterEnrichState::zero() -> ListFilterEnrichState {
  ListFilterEnrichState::{ items: [], query: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn list_filter_enrich_zero_args() -> Map[String, @tutuca.Value] {
  match
    @tutuca.Value::from_json(ToJson::to_json(ListFilterEnrichState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the ListFilterEnrich state declares — typed field names for tests and inspectors.
pub(all) enum ListFilterEnrichField {
  Items
  Query
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichField::name(self : ListFilterEnrichField) -> String {
  match self {
    Items => "items"
    Query => "query"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn ListFilterEnrichField::kind(
  self : ListFilterEnrichField,
) -> @component.FieldKind {
  match self {
    Items => @component.FList
    Query => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let list_filter_enrich_field_names : Array[String] = ["items", "query"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn list_filter_enrich_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let list_filter_enrich_schema_fingerprint : String = "d9bbfb6a84864d29"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn list_filter_enrich_encode(
  s : ListFilterEnrichState,
) -> Map[String, @tutuca.Value] {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "query": Str(s.query),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn list_filter_enrich_decode(
  f : Map[String, @tutuca.Value],
) -> ListFilterEnrichState? {
  guard f.get("items") is Some(List(l_items)) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for x in l_items {
    v_items.push(x)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  Some(ListFilterEnrichState::{ items: v_items, query: v_query })
}

///|
/// The views declared in collections.html.
pub(all) enum ListFilterEnrichView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichView::name(self : ListFilterEnrichView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let list_filter_enrich_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let list_filter_enrich_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum ListFilterEnrichMethod {
  ResetQuery
  SetQuery
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichMethod::name(self : ListFilterEnrichMethod) -> String {
  match self {
    ResetQuery => "resetQuery"
    SetQuery => "setQuery"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_mutate(
  f : (ListFilterEnrichMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListFilterEnrichMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_compute(
  f : (ListFilterEnrichMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListFilterEnrichMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_swap(
  f : (ListFilterEnrichMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListFilterEnrichMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// The state of the ListFilterEnrichWith component.
///
/// Declared as `interface list-filter-enrich-with` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct ListFilterEnrichWithState {
  items : Array[@tutuca.Value]
  query : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ListFilterEnrichWithState::zero(), label: "Counter" }
fn ListFilterEnrichWithState::zero() -> ListFilterEnrichWithState {
  ListFilterEnrichWithState::{ items: [], query: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn list_filter_enrich_with_zero_args() -> Map[String, @tutuca.Value] {
  match
    @tutuca.Value::from_json(ToJson::to_json(ListFilterEnrichWithState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the ListFilterEnrichWith state declares — typed field names for tests and inspectors.
pub(all) enum ListFilterEnrichWithField {
  Items
  Query
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichWithField::name(
  self : ListFilterEnrichWithField,
) -> String {
  match self {
    Items => "items"
    Query => "query"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn ListFilterEnrichWithField::kind(
  self : ListFilterEnrichWithField,
) -> @component.FieldKind {
  match self {
    Items => @component.FList
    Query => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let list_filter_enrich_with_field_names : Array[String] = ["items", "query"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn list_filter_enrich_with_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let list_filter_enrich_with_schema_fingerprint : String = "4b8fcd058a698178"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn list_filter_enrich_with_encode(
  s : ListFilterEnrichWithState,
) -> Map[String, @tutuca.Value] {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "query": Str(s.query),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn list_filter_enrich_with_decode(
  f : Map[String, @tutuca.Value],
) -> ListFilterEnrichWithState? {
  guard f.get("items") is Some(List(l_items)) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for x in l_items {
    v_items.push(x)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  Some(ListFilterEnrichWithState::{ items: v_items, query: v_query })
}

///|
/// The views declared in collections.html.
pub(all) enum ListFilterEnrichWithView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichWithView::name(
  self : ListFilterEnrichWithView,
) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let list_filter_enrich_with_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let list_filter_enrich_with_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum ListFilterEnrichWithMethod {
  ResetQuery
  SetQuery
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichWithMethod::name(
  self : ListFilterEnrichWithMethod,
) -> String {
  match self {
    ResetQuery => "resetQuery"
    SetQuery => "setQuery"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_with_mutate(
  f : (ListFilterEnrichWithMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListFilterEnrichWithMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_with_compute(
  f : (ListFilterEnrichWithMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListFilterEnrichWithMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_with_swap(
  f : (ListFilterEnrichWithMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListFilterEnrichWithMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}
