// GENERATED by `tutuca gen-views` from collections.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views collections.html --name Collections --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the RenderWithScope component.
///
/// Declared as `interface render-with-scope` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct RenderWithScopeState {
  text : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..RenderWithScopeState::zero(), label: "Counter" }
pub fn RenderWithScopeState::zero() -> RenderWithScopeState {
  RenderWithScopeState::{ text: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for RenderWithScopeState with fn encode(s) {
  { "text": Str(s.text) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for RenderWithScopeState with fn decode(f) {
  guard f.get("text") is Some(Str(v_text)) else { return None }
  Some(RenderWithScopeState::{ text: v_text })
}

///|
/// The file-level style — pass as `common_style~`.
pub let render_with_scope_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let render_with_scope_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum RenderWithScopeInput {
  SetText
} derive(Eq, @debug.Debug)

///|
pub fn RenderWithScopeInput::name(self : RenderWithScopeInput) -> String {
  match self {
    SetText => "setText"
  }
}

///|
pub fn RenderWithScopeInput::parse(s : String) -> RenderWithScopeInput? {
  match s {
    "setText" => Some(SetText)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to collections.html breaks every `match` over this type
/// until it is handled.
pub(all) enum RenderWithScopeMsg {
  /// `@on.input="setText …"`
  SetText(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn RenderWithScopeMsg::from_dispatch(
  d : @component.Dispatch,
) -> RenderWithScopeMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setText", [Str(a0)]) => SetText(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] render_with_scope_swap(
  f : (RenderWithScopeInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetText] : Array[RenderWithScopeInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `@enrich-with` handler named with no `@each` around it.
pub(all) enum RenderWithScopeEnrichScope {
  EnrichScope
} derive(Eq, @debug.Debug)

///|
pub fn RenderWithScopeEnrichScope::name(
  self : RenderWithScopeEnrichScope,
) -> String {
  match self {
    EnrichScope => "enrichScope"
  }
}

///|
/// Build `enrich_scope~` from an exhaustive match over the names the views use.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] render_with_scope_enrich_scope(
  f : (RenderWithScopeEnrichScope) -> ((S) -> Map[String, @tutuca.Value])?,
) -> Map[String, (S) -> Map[String, @tutuca.Value]] {
  let out : Map[String, (S) -> Map[String, @tutuca.Value]] = Map([])
  for m in ([EnrichScope] : Array[RenderWithScopeEnrichScope]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for RenderWithScopeState with fn schema() {
  @component.SchemaInfo::new(
    name="RenderWithScope",
    fingerprint="25c803b695d1cdde",
    fields=[@component.FieldInfo::new("text", "string", FText)],
    inputs=["setText"],
    view_names=["main"],
  )
}

///|
/// The state of the ListFilterEnrich component.
///
/// Declared as `interface list-filter-enrich` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ListFilterEnrichState {
  items : Array[@tutuca.Value]
  query : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ListFilterEnrichState::zero(), label: "Counter" }
pub fn ListFilterEnrichState::zero() -> ListFilterEnrichState {
  ListFilterEnrichState::{ items: [], query: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ListFilterEnrichState with fn encode(s) {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "query": Str(s.query),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ListFilterEnrichState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  Some(ListFilterEnrichState::{ items: v_items, query: v_query })
}

///|
/// The file-level style — pass as `common_style~`.
pub let list_filter_enrich_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let list_filter_enrich_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ListFilterEnrichInput {
  ResetQuery
  SetQuery
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichInput::name(self : ListFilterEnrichInput) -> String {
  match self {
    ResetQuery => "resetQuery"
    SetQuery => "setQuery"
  }
}

///|
pub fn ListFilterEnrichInput::parse(s : String) -> ListFilterEnrichInput? {
  match s {
    "resetQuery" => Some(ResetQuery)
    "setQuery" => Some(SetQuery)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to collections.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ListFilterEnrichMsg {
  /// `@on.keydown="resetQuery …"`
  ResetQuery
  /// `@on.input="setQuery …"`
  SetQuery(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ListFilterEnrichMsg::from_dispatch(
  d : @component.Dispatch,
) -> ListFilterEnrichMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("resetQuery", []) => ResetQuery
      ("setQuery", [Str(a0)]) => SetQuery(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_swap(
  f : (ListFilterEnrichInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListFilterEnrichInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `@when` handler the views name.
pub(all) enum ListFilterEnrichWhen {
  FilterItem
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichWhen::name(self : ListFilterEnrichWhen) -> String {
  match self {
    FilterItem => "filterItem"
  }
}

///|
/// Build `when~` from an exhaustive match over the names the views use.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_when(
  f : (ListFilterEnrichWhen) -> ((
    S,
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Bool)?,
) -> Map[String, (S, @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Bool] {
  let out : Map[
    String,
    (S, @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Bool,
  ] = Map([])
  for m in ([FilterItem] : Array[ListFilterEnrichWhen]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `@enrich-with` handler named INSIDE an `@each`.
pub(all) enum ListFilterEnrichEnrich {
  EnrichItem
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichEnrich::name(self : ListFilterEnrichEnrich) -> String {
  match self {
    EnrichItem => "enrichItem"
  }
}

///|
/// Build `enrich~` from an exhaustive match over the names the views use.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_enrich(
  f : (ListFilterEnrichEnrich) -> ((
    S,
    Map[String, @tutuca.Value],
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Unit)?,
) -> Map[
  String,
  (S, Map[String, @tutuca.Value], @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Unit,
] {
  let out : Map[
    String,
    (S, Map[String, @tutuca.Value], @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Unit,
  ] = Map([])
  for m in ([EnrichItem] : Array[ListFilterEnrichEnrich]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ListFilterEnrichState with fn schema() {
  @component.SchemaInfo::new(
    name="ListFilterEnrich",
    fingerprint="d9bbfb6a84864d29",
    fields=[
      @component.FieldInfo::new("items", "list<any>", FList, elem="any"),
      @component.FieldInfo::new("query", "string", FText),
    ],
    inputs=["resetQuery", "setQuery"],
    view_names=["main"],
  )
}

///|
/// The state of the ListFilterEnrichWith component.
///
/// Declared as `interface list-filter-enrich-with` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ListFilterEnrichWithState {
  items : Array[@tutuca.Value]
  query : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ListFilterEnrichWithState::zero(), label: "Counter" }
pub fn ListFilterEnrichWithState::zero() -> ListFilterEnrichWithState {
  ListFilterEnrichWithState::{ items: [], query: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ListFilterEnrichWithState with fn encode(s) {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "query": Str(s.query),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ListFilterEnrichWithState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  Some(ListFilterEnrichWithState::{ items: v_items, query: v_query })
}

///|
/// The file-level style — pass as `common_style~`.
pub let list_filter_enrich_with_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let list_filter_enrich_with_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ListFilterEnrichWithInput {
  ResetQuery
  SetQuery
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichWithInput::name(
  self : ListFilterEnrichWithInput,
) -> String {
  match self {
    ResetQuery => "resetQuery"
    SetQuery => "setQuery"
  }
}

///|
pub fn ListFilterEnrichWithInput::parse(
  s : String,
) -> ListFilterEnrichWithInput? {
  match s {
    "resetQuery" => Some(ResetQuery)
    "setQuery" => Some(SetQuery)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to collections.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ListFilterEnrichWithMsg {
  /// `@on.keydown="resetQuery …"`
  ResetQuery
  /// `@on.input="setQuery …"`
  SetQuery(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ListFilterEnrichWithMsg::from_dispatch(
  d : @component.Dispatch,
) -> ListFilterEnrichWithMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("resetQuery", []) => ResetQuery
      ("setQuery", [Str(a0)]) => SetQuery(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_with_swap(
  f : (ListFilterEnrichWithInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ResetQuery, SetQuery] : Array[ListFilterEnrichWithInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `@when` handler the views name.
pub(all) enum ListFilterEnrichWithWhen {
  FilterItem
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichWithWhen::name(
  self : ListFilterEnrichWithWhen,
) -> String {
  match self {
    FilterItem => "filterItem"
  }
}

///|
/// Build `when~` from an exhaustive match over the names the views use.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_with_when(
  f : (ListFilterEnrichWithWhen) -> ((
    S,
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Bool)?,
) -> Map[String, (S, @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Bool] {
  let out : Map[
    String,
    (S, @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Bool,
  ] = Map([])
  for m in ([FilterItem] : Array[ListFilterEnrichWithWhen]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `@enrich-with` handler named INSIDE an `@each`.
pub(all) enum ListFilterEnrichWithEnrich {
  EnrichItem
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichWithEnrich::name(
  self : ListFilterEnrichWithEnrich,
) -> String {
  match self {
    EnrichItem => "enrichItem"
  }
}

///|
/// Build `enrich~` from an exhaustive match over the names the views use.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_with_enrich(
  f : (ListFilterEnrichWithEnrich) -> ((
    S,
    Map[String, @tutuca.Value],
    @tutuca.Value,
    @tutuca.Value,
    @tutuca.Value,
  ) -> Unit)?,
) -> Map[
  String,
  (S, Map[String, @tutuca.Value], @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Unit,
] {
  let out : Map[
    String,
    (S, Map[String, @tutuca.Value], @tutuca.Value, @tutuca.Value, @tutuca.Value) -> Unit,
  ] = Map([])
  for m in ([EnrichItem] : Array[ListFilterEnrichWithEnrich]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `@loop-with` handler the views name.
pub(all) enum ListFilterEnrichWithLoopWith {
  GetIterData
} derive(Eq, @debug.Debug)

///|
pub fn ListFilterEnrichWithLoopWith::name(
  self : ListFilterEnrichWithLoopWith,
) -> String {
  match self {
    GetIterData => "getIterData"
  }
}

///|
/// Build `loop_with~` from an exhaustive match over the names the views use.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] list_filter_enrich_with_loop_with(
  f : (ListFilterEnrichWithLoopWith) -> ((S, @tutuca.Value, @component.LoopCtx) -> @component.LoopWith)?,
) -> Map[String, (S, @tutuca.Value, @component.LoopCtx) -> @component.LoopWith] {
  let out : Map[
    String,
    (S, @tutuca.Value, @component.LoopCtx) -> @component.LoopWith,
  ] = Map([])
  for m in ([GetIterData] : Array[ListFilterEnrichWithLoopWith]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ListFilterEnrichWithState with fn schema() {
  @component.SchemaInfo::new(
    name="ListFilterEnrichWith",
    fingerprint="4b8fcd058a698178",
    fields=[
      @component.FieldInfo::new("items", "list<any>", FList, elem="any"),
      @component.FieldInfo::new("query", "string", FText),
    ],
    inputs=["resetQuery", "setQuery"],
    view_names=["main"],
  )
}
