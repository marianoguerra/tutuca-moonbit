// GENERATED by `tutuca gen-views` from render_child.html — DO NOT EDIT.
//
// The views of Greeting, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing greeting_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn greeting_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "p",
    attrs: ConstAttrs({
      "class": LStr("p-2 border rounded"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Text({ val: "\n  Hello, " }),
      Dom({
        tag: "strong",
        attrs: ConstAttrs(Map([])),
        childs: [RenderText({ node_id: None, val: Field("name") })],
        ns: None,
      }),
      Text({ val: "!\n" }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn greeting_views() -> Map[String, @anode.View] {
  { "main": greeting_main_view_ir() }
}

///|
/// The compiled `main` view.
fn page_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-2"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "h2",
        attrs: ConstAttrs({ "class": LStr("text-lg") }),
        childs: [Text({ val: "Page header" })],
        ns: None,
      }),
      Text({ val: " " }),
      Render({ node_id: 0, val: Field("greeting"), view_val: None }),
      Text({ val: " " }),
      Dom({
        tag: "p",
        attrs: ConstAttrs({ "class": LStr("opacity-60") }),
        childs: [Text({ val: "— end of page —" })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn page_views() -> Map[String, @anode.View] {
  { "main": page_main_view_ir() }
}

///|
/// The compiled `main` view.
fn entry_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("card bg-base-100 shadow-sm"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("card-body") }),
        childs: [
          Dom({
            tag: "h2",
            attrs: ConstAttrs({ "class": LStr("card-title") }),
            childs: [RenderText({ node_id: None, val: Field("title") })],
            ns: None,
          }),
          Dom({
            tag: "p",
            attrs: ConstAttrs(Map([])),
            childs: [RenderText({ node_id: None, val: Field("description") })],
            ns: None,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The compiled `edit` view.
fn entry_edit_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({
      "class": LStr("card bg-base-100 shadow-sm gap-3"),
      "data-vid": LStr("edit"),
    }),
    childs: [
      Dom({
        tag: "div",
        attrs: ConstAttrs({ "class": LStr("card-body") }),
        childs: [
          Dom({
            tag: "input",
            attrs: DynAttrs([
              ConstA(
                name="class",
                val=Const(lit=LStr("input title"), from_macro=false),
              ),
              Plain(name="value", val=Field("title")),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(0.0), from_macro=false),
              ),
            ]),
            childs: [],
            ns: None,
          }),
          Text({ val: " " }),
          Dom({
            tag: "input",
            attrs: DynAttrs([
              ConstA(
                name="class",
                val=Const(lit=LStr("input desc"), from_macro=false),
              ),
              Plain(name="value", val=Field("description")),
              ConstA(
                name="data-eid",
                val=Const(lit=LNum(1.0), from_macro=false),
              ),
            ]),
            childs: [],
            ns: None,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setTitle"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
    {
      id: 1,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setDescription"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "edit",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn entry_views() -> Map[String, @anode.View] {
  { "main": entry_main_view_ir(), "edit": entry_edit_view_ir() }
}

///|
/// The compiled `main` view.
fn multiple_views_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "section",
    attrs: ConstAttrs({
      "class": LStr("flex flex-col gap-3"),
      "data-vid": LStr("main"),
    }),
    childs: [
      Render({ node_id: 0, val: Field("value"), view_val: None }),
      Text({ val: " " }),
      Render({
        node_id: 1,
        val: Field("value"),
        view_val: Some(Const(lit=LStr("edit"), from_macro=false)),
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn multiple_views_views() -> Map[String, @anode.View] {
  { "main": multiple_views_main_view_ir() }
}
