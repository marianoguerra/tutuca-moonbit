// GENERATED by `tutuca gen-views` from render_child.html — DO NOT EDIT.
//
// The views of Greeting, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing greeting_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn greeting_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("p", [@anode.attr("class", "p-2 border rounded")], [
      @anode.text("\n  Hello, "),
      @anode.h("strong", [], [@anode.dyn_text(Field("name"))]),
      @anode.text("!\n"),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn greeting_views() -> Map[String, @anode.View] {
  { "main": greeting_main_view_ir() }
}

///|
/// The Greeting component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `GreetingState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn greeting_component(
  name? : String = "Greeting",
  views? : Map[String, @anode.View] = greeting_views(),
  init? : GreetingState = GreetingState::zero(),
  common_style? : String = greeting_common_style,
  global_style? : String = greeting_global_style,
  update? : (GreetingState, @component.Dispatch, &@tutuca.Ctx) -> GreetingState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn page_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-2")], [
      @anode.h("h2", [@anode.attr("class", "text-lg")], [
        @anode.text("Page header"),
      ]),
      @anode.text(" "),
      @anode.render(0, Field("greeting")),
      @anode.text(" "),
      @anode.h("p", [@anode.attr("class", "opacity-60")], [
        @anode.text("— end of page —"),
      ]),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn page_views() -> Map[String, @anode.View] {
  { "main": page_main_view_ir() }
}

///|
/// The Page component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `PageState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn page_component(
  name? : String = "Page",
  views? : Map[String, @anode.View] = page_views(),
  init? : PageState = PageState::zero(),
  common_style? : String = page_common_style,
  global_style? : String = page_global_style,
  update? : (PageState, @component.Dispatch, &@tutuca.Ctx) -> PageState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn entry_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "card bg-base-100 shadow-sm")], [
      @anode.h("div", [@anode.attr("class", "card-body")], [
        @anode.h("h2", [@anode.attr("class", "card-title")], [
          @anode.dyn_text(Field("title")),
        ]),
        @anode.h("p", [], [@anode.dyn_text(Field("description"))]),
      ]),
    ]),
    [],
  )
}

///|
/// The compiled `edit` view.
fn entry_edit_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "edit",
    @anode.h(
      "div",
      [@anode.attr("class", "card bg-base-100 shadow-sm gap-3")],
      [
        @anode.h("div", [@anode.attr("class", "card-body")], [
          @anode.h(
            "input",
            [
              @anode.attr("class", "input title"),
              @anode.dyn_attr("value", Field("title")),
              @anode.eid(0),
            ],
            [],
          ),
          @anode.text(" "),
          @anode.h(
            "input",
            [
              @anode.attr("class", "input desc"),
              @anode.dyn_attr("value", Field("description")),
              @anode.eid(1),
            ],
            [],
          ),
        ]),
      ],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setTitle", ns=Input), args=[
          Name("value"),
        ]),
      ],
      [
        @anode.on("input", HandlerName(name="setDescription", ns=Input), args=[
          Name("value"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn entry_views() -> Map[String, @anode.View] {
  { "main": entry_main_view_ir(), "edit": entry_edit_view_ir() }
}

///|
/// The Entry component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `EntryState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn entry_component(
  name? : String = "Entry",
  views? : Map[String, @anode.View] = entry_views(),
  init? : EntryState = EntryState::zero(),
  common_style? : String = entry_common_style,
  global_style? : String = entry_global_style,
  update? : (EntryState, @component.Dispatch, &@tutuca.Ctx) -> EntryState?,
  compute? : (EntryMethod) -> ((EntryState, Array[@tutuca.Value]) -> @tutuca.Value)?,
  swap? : (EntryInput) -> ((EntryState, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) => entry_compute(f)
      None => Map([])
    },
    swap=match swap {
      Some(f) => entry_swap(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn multiple_views_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [@anode.attr("class", "flex flex-col gap-3")], [
      @anode.render(0, Field("value")),
      @anode.text(" "),
      @anode.render(1, Field("value"), view=@anode.const_str("edit")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn multiple_views_views() -> Map[String, @anode.View] {
  { "main": multiple_views_main_view_ir() }
}

///|
/// The MultipleViews component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `MultipleViewsState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn multiple_views_component(
  name? : String = "MultipleViews",
  views? : Map[String, @anode.View] = multiple_views_views(),
  init? : MultipleViewsState = MultipleViewsState::zero(),
  common_style? : String = multiple_views_common_style,
  global_style? : String = multiple_views_global_style,
  update? : (MultipleViewsState, @component.Dispatch, &@tutuca.Ctx) -> MultipleViewsState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}
