// GENERATED by `tutuca gen-views` from pseudo_x.html — DO NOT EDIT.
//
// The views of TableRow, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing table_row_views() as
// `views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn table_row_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "tr",
    attrs: ConstAttrs({ "data-vid": LStr("main") }),
    childs: [
      Dom({
        tag: "td",
        attrs: ConstAttrs({ "class": LStr("name") }),
        childs: [RenderText({ node_id: None, val: Field("name") })],
        ns: None,
      }),
      Dom({
        tag: "td",
        attrs: ConstAttrs({ "class": LStr("qty") }),
        childs: [RenderText({ node_id: None, val: Field("qty") })],
        ns: None,
      }),
      Dom({
        tag: "td",
        attrs: ConstAttrs({ "class": LStr("price") }),
        childs: [RenderText({ node_id: None, val: Field("price") })],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn table_row_views() -> Map[String, @anode.View] {
  { "main": table_row_main_view_ir() }
}

///|
/// The compiled `main` view.
fn item_table_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "table",
    attrs: ConstAttrs({ "class": LStr("table"), "data-vid": LStr("main") }),
    childs: [
      Dom({
        tag: "thead",
        attrs: ConstAttrs(Map([])),
        childs: [
          Dom({
            tag: "tr",
            attrs: ConstAttrs(Map([])),
            childs: [
              Dom({
                tag: "th",
                attrs: ConstAttrs(Map([])),
                childs: [Text({ val: "Name" })],
                ns: None,
              }),
              Dom({
                tag: "th",
                attrs: ConstAttrs(Map([])),
                childs: [Text({ val: "Qty" })],
                ns: None,
              }),
              Dom({
                tag: "th",
                attrs: ConstAttrs(Map([])),
                childs: [Text({ val: "Price" })],
                ns: None,
              }),
            ],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Dom({
        tag: "tbody",
        attrs: ConstAttrs(Map([])),
        childs: [
          Each({
            node_id: 1,
            iter_info: {
              val: Field("rows"),
              when_val: None,
              loop_with_val: None,
              enrich_with_val: None,
            },
            node: RenderIt({ node_id: 0, view_val: None }),
            from_render_each: true,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn item_table_views() -> Map[String, @anode.View] {
  { "main": item_table_main_view_ir() }
}

///|
/// The compiled `main` view.
fn select_option_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "option",
    attrs: DynAttrs([
      Plain(name="value", val=Field("value")),
      ConstA(name="data-vid", val=Const(lit=LStr("main"), from_macro=false)),
    ]),
    childs: [RenderText({ node_id: None, val: Field("label") })],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn select_option_views() -> Map[String, @anode.View] {
  { "main": select_option_main_view_ir() }
}

///|
/// The compiled `main` view.
fn item_select_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "select",
    attrs: DynAttrs([
      ConstA(name="class", val=Const(lit=LStr("select"), from_macro=false)),
      Plain(name="value", val=Field("current")),
      ConstA(name="data-eid", val=Const(lit=LNum(0.0), from_macro=false)),
      ConstA(name="data-vid", val=Const(lit=LStr("main"), from_macro=false)),
    ]),
    childs: [
      Each({
        node_id: 1,
        iter_info: {
          val: Field("options"),
          when_val: None,
          loop_with_val: None,
          enrich_with_val: None,
        },
        node: RenderIt({ node_id: 0, view_val: None }),
        from_render_each: true,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        {
          name: "input",
          handler: { handler: Method("setCurrent"), args: [Name("value")] },
          modifiers: [],
        },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn item_select_views() -> Map[String, @anode.View] {
  { "main": item_select_main_view_ir() }
}

///|
/// The compiled `main` view.
fn pseudo_x_demo_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({ "data-vid": LStr("main") }),
    childs: [
      Render({ node_id: 0, val: Field("table"), view_val: None }),
      Text({ val: " " }),
      Render({ node_id: 1, val: Field("select"), view_val: None }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = []
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn pseudo_x_demo_views() -> Map[String, @anode.View] {
  { "main": pseudo_x_demo_main_view_ir() }
}
