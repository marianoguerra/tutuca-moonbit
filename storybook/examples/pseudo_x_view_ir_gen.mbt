// GENERATED by `tutuca gen-views` from pseudo_x.html — DO NOT EDIT.
//
// The views of TableRow, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing table_row_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn table_row_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("tr", [], [
      @anode.h("td", [@anode.attr("class", "name")], [
        @anode.dyn_text(Field("name")),
      ]),
      @anode.h("td", [@anode.attr("class", "qty")], [
        @anode.dyn_text(Field("qty")),
      ]),
      @anode.h("td", [@anode.attr("class", "price")], [
        @anode.dyn_text(Field("price")),
      ]),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn table_row_views() -> Map[String, @anode.View] {
  { "main": table_row_main_view_ir() }
}

///|
/// The TableRow component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `TableRowState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn table_row_component(
  name? : String = "TableRow",
  views? : Map[String, @anode.View] = table_row_views(),
  init? : TableRowState = TableRowState::zero(),
  common_style? : String = table_row_common_style,
  global_style? : String = table_row_global_style,
  update? : (TableRowState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    TableRowState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn item_table_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("table", [@anode.attr("class", "table")], [
      @anode.h("thead", [], [
        @anode.h("tr", [], [
          @anode.h("th", [], [@anode.text("Name")]),
          @anode.h("th", [], [@anode.text("Qty")]),
          @anode.h("th", [], [@anode.text("Price")]),
        ]),
      ]),
      @anode.h("tbody", [], [
        @anode.each(
          1,
          Field("rows"),
          @anode.render_it(0),
          from_render_each=true,
        ),
      ]),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn item_table_views() -> Map[String, @anode.View] {
  { "main": item_table_main_view_ir() }
}

///|
/// The ItemTable component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `ItemTableState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn item_table_component(
  name? : String = "ItemTable",
  views? : Map[String, @anode.View] = item_table_views(),
  init? : ItemTableState = ItemTableState::zero(),
  common_style? : String = item_table_common_style,
  global_style? : String = item_table_global_style,
  update? : (ItemTableState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    ItemTableState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn select_option_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("option", [@anode.dyn_attr("value", Field("value"))], [
      @anode.dyn_text(Field("label")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn select_option_views() -> Map[String, @anode.View] {
  { "main": select_option_main_view_ir() }
}

///|
/// The SelectOption component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SelectOptionState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn select_option_component(
  name? : String = "SelectOption",
  views? : Map[String, @anode.View] = select_option_views(),
  init? : SelectOptionState = SelectOptionState::zero(),
  common_style? : String = select_option_common_style,
  global_style? : String = select_option_global_style,
  update? : (SelectOptionState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    SelectOptionState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn item_select_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "select",
      [
        @anode.attr("class", "select"),
        @anode.dyn_attr("value", Field("current")),
        @anode.eid(0),
      ],
      [
        @anode.each(
          1,
          Field("options"),
          @anode.render_it(0),
          from_render_each=true,
        ),
      ],
    ),
    [
      [
        @anode.on("input", HandlerName(name="setCurrent", ns=Receive), args=[
          EventPath(["value"]),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn item_select_views() -> Map[String, @anode.View] {
  { "main": item_select_main_view_ir() }
}

///|
/// The ItemSelect component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `ItemSelectState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn item_select_component(
  name? : String = "ItemSelect",
  views? : Map[String, @anode.View] = item_select_views(),
  init? : ItemSelectState = ItemSelectState::zero(),
  common_style? : String = item_select_common_style,
  global_style? : String = item_select_global_style,
  update? : (ItemSelectState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    ItemSelectState,
  ],
  swap? : (ItemSelectInput) -> ((
    ItemSelectState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([SetCurrent] : Array[ItemSelectInput]),
          ItemSelectInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn pseudo_x_demo_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [], [
      @anode.render(0, Field("table")),
      @anode.text(" "),
      @anode.render(1, Field("select")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn pseudo_x_demo_views() -> Map[String, @anode.View] {
  { "main": pseudo_x_demo_main_view_ir() }
}

///|
/// The PseudoXDemo component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `PseudoXDemoState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn pseudo_x_demo_component(
  name? : String = "PseudoXDemo",
  views? : Map[String, @anode.View] = pseudo_x_demo_views(),
  init? : PseudoXDemoState = PseudoXDemoState::zero(),
  common_style? : String = pseudo_x_demo_common_style,
  global_style? : String = pseudo_x_demo_global_style,
  update? : (PseudoXDemoState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    PseudoXDemoState,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
