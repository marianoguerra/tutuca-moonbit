// GENERATED by `tutuca gen-views` from communication.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views communication.html --name Communication --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The views declared in communication.html.
pub(all) enum StatusView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn StatusView::name(self : StatusView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let status_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let status_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let status_fields : Array[String] = ["count", "message"]

///|
/// The fields in `status_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] status_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  status_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in communication.html.
pub(all) enum SendReceiveView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SendReceiveView::name(self : SendReceiveView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let send_receive_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let send_receive_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum SendReceiveInput {
  ClearStatus
  Submit
} derive(Eq, @debug.Debug)

///|
pub fn SendReceiveInput::name(self : SendReceiveInput) -> String {
  match self {
    ClearStatus => "clearStatus"
    Submit => "submit"
  }
}

///|
pub fn SendReceiveInput::parse(s : String) -> SendReceiveInput? {
  match s {
    "clearStatus" => Some(ClearStatus)
    "submit" => Some(Submit)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to communication.html breaks every `match` over this type
/// until it is handled.
pub(all) enum SendReceiveMsg {
  /// `@on.click="clearStatus …"`
  ClearStatus
  /// `@on.keydown, @on.click="submit …"`
  Submit
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn SendReceiveMsg::of_dispatch(d : @component.Dispatch) -> SendReceiveMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("clearStatus", []) => ClearStatus
      ("submit", []) => Submit
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum SendReceiveMethod {
  SetDraft
} derive(Eq, @debug.Debug)

///|
pub fn SendReceiveMethod::name(self : SendReceiveMethod) -> String {
  match self {
    SetDraft => "setDraft"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] send_receive_mutate(
  f : (SendReceiveMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetDraft] : Array[SendReceiveMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] send_receive_compute(
  f : (SendReceiveMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetDraft] : Array[SendReceiveMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] send_receive_swap(
  f : (SendReceiveMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetDraft] : Array[SendReceiveMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let send_receive_fields : Array[String] = ["draft", "status"]

///|
/// The fields in `send_receive_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] send_receive_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  send_receive_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in communication.html.
pub(all) enum TreeItemView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn TreeItemView::name(self : TreeItemView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let tree_item_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let tree_item_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum TreeItemInput {
  OnItemClick
} derive(Eq, @debug.Debug)

///|
pub fn TreeItemInput::name(self : TreeItemInput) -> String {
  match self {
    OnItemClick => "onItemClick"
  }
}

///|
pub fn TreeItemInput::parse(s : String) -> TreeItemInput? {
  match s {
    "onItemClick" => Some(OnItemClick)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to communication.html breaks every `match` over this type
/// until it is handled.
pub(all) enum TreeItemMsg {
  /// `@on.click="onItemClick …"`
  OnItemClick
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn TreeItemMsg::of_dispatch(d : @component.Dispatch) -> TreeItemMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onItemClick", []) => OnItemClick
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum TreeItemMethod {
  AreChildsVisible
} derive(Eq, @debug.Debug)

///|
pub fn TreeItemMethod::name(self : TreeItemMethod) -> String {
  match self {
    AreChildsVisible => "areChildsVisible"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] tree_item_mutate(
  f : (TreeItemMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([AreChildsVisible] : Array[TreeItemMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] tree_item_compute(
  f : (TreeItemMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([AreChildsVisible] : Array[TreeItemMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] tree_item_swap(
  f : (TreeItemMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([AreChildsVisible] : Array[TreeItemMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let tree_item_fields : Array[String] = ["isOpen", "items", "label", "type"]

///|
/// The fields in `tree_item_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] tree_item_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  tree_item_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in communication.html.
pub(all) enum TreeRootView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn TreeRootView::name(self : TreeRootView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let tree_root_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let tree_root_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let tree_root_fields : Array[String] = ["log", "root"]

///|
/// The fields in `tree_root_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] tree_root_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  tree_root_fields.filter(f => !present.contains(f))
}
