// GENERATED by `tutuca gen-views` from communication.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views communication.html --name Communication --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the Status component.
///
/// Declared as `interface status` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct StatusState {
  message : String
  count : Int
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..StatusState::zero(), label: "Counter" }
fn StatusState::zero() -> StatusState {
  StatusState::{ message: "", count: 0 }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn status_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(StatusState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the Status state declares — typed field names for tests and inspectors.
pub(all) enum StatusField {
  Message
  Count
} derive(Eq, @debug.Debug)

///|
pub fn StatusField::name(self : StatusField) -> String {
  match self {
    Message => "message"
    Count => "count"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn StatusField::kind(self : StatusField) -> @component.FieldKind {
  match self {
    Message => @component.FText
    Count => @component.FInt
  }
}

///|
/// Every field, in declaration order.
pub let status_field_names : Array[String] = ["message", "count"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let status_schema_fingerprint : String = "818952001ae7566e"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn status_encode(s : StatusState) -> Map[String, @tutuca.Value] {
  { "message": Str(s.message), "count": Num(s.count.to_double()) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn status_decode(f : Map[String, @tutuca.Value]) -> StatusState? {
  guard f.get("message") is Some(Str(v_message)) else { return None }
  guard f.get("count") is Some(Num(t1_count)) else { return None }
  let v_count = t1_count.to_int()
  Some(StatusState::{ message: v_message, count: v_count })
}

///|
/// The views declared in communication.html.
pub(all) enum StatusView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn StatusView::name(self : StatusView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let status_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let status_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn status_schema() -> @component.SchemaInfo {
  {
    fingerprint: "818952001ae7566e",
    fields: [
      {
        name: "message",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "count",
        ty: "s32",
        kind: @component.FInt,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the SendReceive component.
///
/// Declared as `interface send-receive` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct SendReceiveState {
  draft : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SendReceiveState::zero(), label: "Counter" }
fn SendReceiveState::zero() -> SendReceiveState {
  SendReceiveState::{ draft: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn send_receive_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(SendReceiveState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the SendReceive state declares — typed field names for tests and inspectors.
pub(all) enum SendReceiveField {
  Draft
  Status
} derive(Eq, @debug.Debug)

///|
pub fn SendReceiveField::name(self : SendReceiveField) -> String {
  match self {
    Draft => "draft"
    Status => "status"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn SendReceiveField::kind(self : SendReceiveField) -> @component.FieldKind {
  match self {
    Draft => @component.FText
    Status => @component.FComp(comp="", args=Map([]))
  }
}

///|
/// Every field, in declaration order.
pub let send_receive_field_names : Array[String] = ["draft", "status"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let send_receive_schema_fingerprint : String = "af6010efd88e64f0"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn send_receive_encode(s : SendReceiveState) -> Map[String, @tutuca.Value] {
  { "draft": Str(s.draft) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn send_receive_decode(f : Map[String, @tutuca.Value]) -> SendReceiveState? {
  guard f.get("draft") is Some(Str(v_draft)) else { return None }
  Some(SendReceiveState::{ draft: v_draft })
}

///|
/// The views declared in communication.html.
pub(all) enum SendReceiveView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SendReceiveView::name(self : SendReceiveView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let send_receive_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let send_receive_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum SendReceiveInput {
  ClearStatus
  SetDraft
  Submit
} derive(Eq, @debug.Debug)

///|
pub fn SendReceiveInput::name(self : SendReceiveInput) -> String {
  match self {
    ClearStatus => "clearStatus"
    SetDraft => "setDraft"
    Submit => "submit"
  }
}

///|
pub fn SendReceiveInput::parse(s : String) -> SendReceiveInput? {
  match s {
    "clearStatus" => Some(ClearStatus)
    "setDraft" => Some(SetDraft)
    "submit" => Some(Submit)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to communication.html breaks every `match` over this type
/// until it is handled.
pub(all) enum SendReceiveMsg {
  /// `@on.click="clearStatus …"`
  ClearStatus
  /// `@on.input="setDraft …"`
  SetDraft(String)
  /// `@on.keydown, @on.click="submit …"`
  Submit
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn SendReceiveMsg::from_dispatch(
  d : @component.Dispatch,
) -> SendReceiveMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("clearStatus", []) => ClearStatus
      ("setDraft", [Str(a0)]) => SetDraft(a0)
      ("submit", []) => Submit
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn send_receive_schema() -> @component.SchemaInfo {
  {
    fingerprint: "af6010efd88e64f0",
    fields: [
      {
        name: "draft",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "status",
        ty: "component",
        kind: @component.FComp(comp="", args=Map([])),
        elem: Some("any"),
        slot: Some(""),
        members: [],
      },
    ],
    inputs: ["clearStatus", "setDraft", "submit"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the TreeItem component.
///
/// Declared as `interface tree-item` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct TreeItemState {
  type_ : String
  label : String
  isOpen : Bool
  items : Array[@tutuca.Value]
} derive (
  ToJson(fields(type_(rename="type"))),
  FromJson(fields(type_(rename="type"))),
)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..TreeItemState::zero(), label: "Counter" }
fn TreeItemState::zero() -> TreeItemState {
  TreeItemState::{ type_: "", label: "", isOpen: false, items: [] }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn tree_item_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(TreeItemState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the TreeItem state declares — typed field names for tests and inspectors.
pub(all) enum TreeItemField {
  Type
  Label
  IsOpen
  Items
} derive(Eq, @debug.Debug)

///|
pub fn TreeItemField::name(self : TreeItemField) -> String {
  match self {
    Type => "type"
    Label => "label"
    IsOpen => "isOpen"
    Items => "items"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn TreeItemField::kind(self : TreeItemField) -> @component.FieldKind {
  match self {
    Type => @component.FText
    Label => @component.FText
    IsOpen => @component.FBool
    Items => @component.FList
  }
}

///|
/// Every field, in declaration order.
pub let tree_item_field_names : Array[String] = [
  "type", "label", "isOpen", "items",
]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let tree_item_schema_fingerprint : String = "e6692133fe7c7e9a"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn tree_item_encode(s : TreeItemState) -> Map[String, @tutuca.Value] {
  {
    "type": Str(s.type_),
    "label": Str(s.label),
    "isOpen": Bool(s.isOpen),
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn tree_item_decode(f : Map[String, @tutuca.Value]) -> TreeItemState? {
  guard f.get("type") is Some(Str(v_type_)) else { return None }
  guard f.get("label") is Some(Str(v_label)) else { return None }
  guard f.get("isOpen") is Some(Bool(v_isOpen)) else { return None }
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  Some(TreeItemState::{
    type_: v_type_,
    label: v_label,
    isOpen: v_isOpen,
    items: v_items,
  })
}

///|
/// The views declared in communication.html.
pub(all) enum TreeItemView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn TreeItemView::name(self : TreeItemView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let tree_item_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let tree_item_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum TreeItemInput {
  OnItemClick
} derive(Eq, @debug.Debug)

///|
pub fn TreeItemInput::name(self : TreeItemInput) -> String {
  match self {
    OnItemClick => "onItemClick"
  }
}

///|
pub fn TreeItemInput::parse(s : String) -> TreeItemInput? {
  match s {
    "onItemClick" => Some(OnItemClick)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to communication.html breaks every `match` over this type
/// until it is handled.
pub(all) enum TreeItemMsg {
  /// `@on.click="onItemClick …"`
  OnItemClick
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn TreeItemMsg::from_dispatch(d : @component.Dispatch) -> TreeItemMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onItemClick", []) => OnItemClick
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum TreeItemMethod {
  AreChildsVisible
} derive(Eq, @debug.Debug)

///|
pub fn TreeItemMethod::name(self : TreeItemMethod) -> String {
  match self {
    AreChildsVisible => "areChildsVisible"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] tree_item_compute(
  f : (TreeItemMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([AreChildsVisible] : Array[TreeItemMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] tree_item_swap(
  f : (TreeItemMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([AreChildsVisible] : Array[TreeItemMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn tree_item_schema() -> @component.SchemaInfo {
  {
    fingerprint: "e6692133fe7c7e9a",
    fields: [
      {
        name: "type",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "label",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "isOpen",
        ty: "bool",
        kind: @component.FBool,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "items",
        ty: "list<any>",
        kind: @component.FList,
        elem: Some("any"),
        slot: None,
        members: [],
      },
    ],
    inputs: ["onItemClick"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: ["areChildsVisible"],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the TreeRoot component.
///
/// Declared as `interface tree-root` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct TreeRootState {
  log : Array[@tutuca.Value]
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..TreeRootState::zero(), label: "Counter" }
fn TreeRootState::zero() -> TreeRootState {
  TreeRootState::{ log: [] }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn tree_root_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(TreeRootState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the TreeRoot state declares — typed field names for tests and inspectors.
pub(all) enum TreeRootField {
  Log
  Root
} derive(Eq, @debug.Debug)

///|
pub fn TreeRootField::name(self : TreeRootField) -> String {
  match self {
    Log => "log"
    Root => "root"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn TreeRootField::kind(self : TreeRootField) -> @component.FieldKind {
  match self {
    Log => @component.FList
    Root => @component.FComp(comp="", args=Map([]))
  }
}

///|
/// Every field, in declaration order.
pub let tree_root_field_names : Array[String] = ["log", "root"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let tree_root_schema_fingerprint : String = "956f1bda15ce4557"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn tree_root_encode(s : TreeRootState) -> Map[String, @tutuca.Value] {
  { "log": (List(s.log.map(x => x)) : @tutuca.Value) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn tree_root_decode(f : Map[String, @tutuca.Value]) -> TreeRootState? {
  guard f.get("log") is Some(t1_log) else { return None }
  guard t1_log is List(u1_log) else { return None }
  let v_log : Array[@tutuca.Value] = []
  for e1_log in u1_log {
    let x1_log = e1_log
    v_log.push(x1_log)
  }
  Some(TreeRootState::{ log: v_log })
}

///|
/// The views declared in communication.html.
pub(all) enum TreeRootView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn TreeRootView::name(self : TreeRootView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let tree_root_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let tree_root_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn tree_root_schema() -> @component.SchemaInfo {
  {
    fingerprint: "956f1bda15ce4557",
    fields: [
      {
        name: "log",
        ty: "list<any>",
        kind: @component.FList,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "root",
        ty: "component",
        kind: @component.FComp(comp="", args=Map([])),
        elem: Some("any"),
        slot: Some(""),
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}
