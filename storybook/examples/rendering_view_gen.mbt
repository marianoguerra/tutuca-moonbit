// GENERATED by `tutuca gen-views` from rendering.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views rendering.html --name Rendering --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the PushView component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct PushViewState {
  items : Array[@tutuca.Value]
  query : String
  view : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..PushViewState::zero(), label: "Counter" }
pub fn PushViewState::zero() -> PushViewState {
  { items: [], query: "", view: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for PushViewState with fn encode(s) {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "query": Str(s.query),
    "view": Str(s.view),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for PushViewState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("query") is Some(Str(v_query)) else { return None }
  guard f.get("view") is Some(Str(v_view)) else { return None }
  Some({ items: v_items, query: v_query, view: v_view, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum PushViewReceive {
  ToggleView
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn PushViewReceive::from_dispatch(
  d : @component.Dispatch,
) -> PushViewReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("toggleView", []) => ToggleView
      _ => {
        match name {
          "toggleView" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn PushViewReceive::to_dispatch(
  self : PushViewReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    ToggleView => ("toggleView", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn PushViewReceive::send(
  self : PushViewReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let push_view_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let push_view_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum PushViewInput {
  ToggleView
} derive(Eq, @debug.Debug)

///|
pub fn PushViewInput::name(self : PushViewInput) -> String {
  match self {
    ToggleView => "toggleView"
  }
}

///|
pub fn PushViewInput::parse(s : String) -> PushViewInput? {
  match s {
    "toggleView" => Some(ToggleView)
    _ => None
  }
}

///|
/// The VIEW-named half of @component.Dispatch's `Receive` arm, decoded.
/// Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to rendering.html breaks every `match` over this type
/// until it is handled.
///
/// Absent, because the script block answers them: toggleView.
/// `update` is consulted only for a dispatch the block did not claim, so
/// an arm for one of these could never run.
pub(all) enum PushViewMsg {
  /// A dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for an `Intent` — a routed name is raised by component code and
/// never written in a view, so the views cannot type it. A `Receive` the
/// views do not name decodes to `Unknown`, which is what the sibling
/// `<T>Receive` (the schema's half of the same bucket) is for.
pub fn PushViewMsg::from_dispatch(d : @component.Dispatch) -> PushViewMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum PushViewMethod {
  GetToggleLabel
} derive(Eq, @debug.Debug)

///|
pub fn PushViewMethod::name(self : PushViewMethod) -> String {
  match self {
    GetToggleLabel => "getToggleLabel"
  }
}

///|
/// Every `@when` handler the views name.
pub(all) enum PushViewWhen {
  FilterItem
} derive(Eq, @debug.Debug)

///|
pub fn PushViewWhen::name(self : PushViewWhen) -> String {
  match self {
    FilterItem => "filterItem"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for PushViewState with fn schema() {
  @component.SchemaInfo::new(
    name="PushView",
    fingerprint="5907771e77308ed8",
    fields=[
      @component.FieldInfo::new("items", TyList(TyComp(Some("Entry")))),
      @component.FieldInfo::new("query", TyText),
      @component.FieldInfo::new("view", TyText),
    ],
    view_handlers=["toggleView"],
    methods=["getToggleLabel"],
    domains=[{ field: "view", domain: DOneOf([Str("main"), Str("edit")]), }],
    view_names=["main"],
  )
}

///|
/// The state of the SeqItemAccess component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct SeqItemAccessState {
  byKey : Map[String, @tutuca.Value]
  byIndex : Array[@tutuca.Value]
  currentKey : @tutuca.Value
  currentIndex : Int
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SeqItemAccessState::zero(), label: "Counter" }
pub fn SeqItemAccessState::zero() -> SeqItemAccessState {
  { byKey: Map([]), byIndex: [], currentKey: Null, currentIndex: 0, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for SeqItemAccessState with fn encode(s) {
  {
    "byKey": {
      let m : Map[String, @tutuca.Value] = Map([])
      for k, x in s.byKey {
        m[k] = x
      }
      (Map(m) : @tutuca.Value)
    },
    "byIndex": (List(s.byIndex.map(x => x)) : @tutuca.Value),
    "currentKey": s.currentKey,
    "currentIndex": Num(s.currentIndex.to_double()),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for SeqItemAccessState with fn decode(f) {
  guard f.get("byKey") is Some(t1_byKey) else { return None }
  guard t1_byKey is Map(u1_byKey) else { return None }
  let v_byKey : Map[String, @tutuca.Value] = Map([])
  for k, e1_byKey in u1_byKey {
    let x1_byKey = e1_byKey
    v_byKey[k] = x1_byKey
  }
  guard f.get("byIndex") is Some(t1_byIndex) else { return None }
  guard t1_byIndex is List(u1_byIndex) else { return None }
  let v_byIndex : Array[@tutuca.Value] = []
  for e1_byIndex in u1_byIndex {
    let x1_byIndex = e1_byIndex
    v_byIndex.push(x1_byIndex)
  }
  let v_currentKey = f.get("currentKey").unwrap_or(Null)
  guard f.get("currentIndex") is Some(Num(t1_currentIndex)) else { return None }
  let v_currentIndex = t1_currentIndex.to_int()
  Some({
    byKey: v_byKey,
    byIndex: v_byIndex,
    currentKey: v_currentKey,
    currentIndex: v_currentIndex,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let seq_item_access_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let seq_item_access_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum SeqItemAccessMethod {
  GetMaxIndex
} derive(Eq, @debug.Debug)

///|
pub fn SeqItemAccessMethod::name(self : SeqItemAccessMethod) -> String {
  match self {
    GetMaxIndex => "getMaxIndex"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for SeqItemAccessState with fn schema() {
  @component.SchemaInfo::new(
    name="SeqItemAccess",
    fingerprint="5028d75eea4ab82e",
    fields=[
      @component.FieldInfo::new("byKey", TyOMap(TyComp(Some("Entry")))),
      @component.FieldInfo::new("byIndex", TyList(TyComp(Some("Entry")))),
      @component.FieldInfo::new("currentKey", TyAny),
      @component.FieldInfo::new("currentIndex", TyInt(width=32, signed=true)),
    ],
    methods=["getMaxIndex"],
    domains=[
      {
        field: "currentIndex",
        domain: DIndexOf(field="byIndex", allow_none=true),
      },
      { field: "currentKey", domain: DKeyOf(field="byKey"), },
    ],
    view_names=["main"],
  )
}

///|
/// The state of the Pagination component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct PaginationState {
  items : Array[String]
  page : Int
  pageSize : Int
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..PaginationState::zero(), label: "Counter" }
pub fn PaginationState::zero() -> PaginationState {
  { items: [], page: 0, pageSize: 0, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for PaginationState with fn encode(s) {
  {
    "items": (List(s.items.map(x => Str(x))) : @tutuca.Value),
    "page": Num(s.page.to_double()),
    "pageSize": Num(s.pageSize.to_double()),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for PaginationState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[String] = []
  for e1_items in u1_items {
    guard e1_items is Str(x1_items) else { return None }
    v_items.push(x1_items)
  }
  guard f.get("page") is Some(Num(t1_page)) else { return None }
  let v_page = t1_page.to_int()
  guard f.get("pageSize") is Some(Num(t1_pageSize)) else { return None }
  let v_pageSize = t1_pageSize.to_int()
  Some({ items: v_items, page: v_page, pageSize: v_pageSize, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum PaginationReceive {
  Next
  Prev
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn PaginationReceive::from_dispatch(
  d : @component.Dispatch,
) -> PaginationReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("next", []) => Next
      ("prev", []) => Prev
      _ => {
        match name {
          "next" => @tutuca.bad_payload(name, "", args)
          "prev" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn PaginationReceive::to_dispatch(
  self : PaginationReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    Next => ("next", [])
    Prev => ("prev", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn PaginationReceive::send(
  self : PaginationReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let pagination_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let pagination_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum PaginationInput {
  Next
  Prev
} derive(Eq, @debug.Debug)

///|
pub fn PaginationInput::name(self : PaginationInput) -> String {
  match self {
    Next => "next"
    Prev => "prev"
  }
}

///|
pub fn PaginationInput::parse(s : String) -> PaginationInput? {
  match s {
    "next" => Some(Next)
    "prev" => Some(Prev)
    _ => None
  }
}

///|
/// The VIEW-named half of @component.Dispatch's `Receive` arm, decoded.
/// Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to rendering.html breaks every `match` over this type
/// until it is handled.
///
/// Absent, because the script block answers them: prev, next.
/// `update` is consulted only for a dispatch the block did not claim, so
/// an arm for one of these could never run.
pub(all) enum PaginationMsg {
  /// A dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for an `Intent` — a routed name is raised by component code and
/// never written in a view, so the views cannot type it. A `Receive` the
/// views do not name decodes to `Unknown`, which is what the sibling
/// `<T>Receive` (the schema's half of the same bucket) is for.
pub fn PaginationMsg::from_dispatch(d : @component.Dispatch) -> PaginationMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
///
/// Absent, because the script block answers them: pageExists.
/// The block's own handlers are merged into this bucket, so an arm for one of
/// these could never run.
pub(all) enum PaginationMethod {
  IsFirstPage
  IsLastPage
  PageLabel
} derive(Eq, @debug.Debug)

///|
pub fn PaginationMethod::name(self : PaginationMethod) -> String {
  match self {
    IsFirstPage => "isFirstPage"
    IsLastPage => "isLastPage"
    PageLabel => "pageLabel"
  }
}

///|
/// Every `@loop-with` handler the views name.
pub(all) enum PaginationLoopWith {
  Paginate
} derive(Eq, @debug.Debug)

///|
pub fn PaginationLoopWith::name(self : PaginationLoopWith) -> String {
  match self {
    Paginate => "paginate"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for PaginationState with fn schema() {
  @component.SchemaInfo::new(
    name="Pagination",
    fingerprint="6c86d0e01aab6ee3",
    fields=[
      @component.FieldInfo::new("items", TyList(TyText)),
      @component.FieldInfo::new("page", TyInt(width=32, signed=true)),
      @component.FieldInfo::new("pageSize", TyInt(width=32, signed=true)),
    ],
    view_handlers=["next", "prev"],
    methods=["isFirstPage", "isLastPage", "pageExists", "pageLabel"],
    invariants=["pageExists"],
    domains=[
      { field: "pageSize", domain: DBetween(lo=DLit(1), hi=DLit(100)), },
      { field: "page", domain: DCompare(op=DGe, rhs=DLit(0)), },
    ],
    view_names=["main"],
  )
}
