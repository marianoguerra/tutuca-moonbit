// GENERATED by `tutuca gen-views` from rendering.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views rendering.html --name Rendering --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The views declared in rendering.html.
pub(all) enum PushViewView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn PushViewView::name(self : PushViewView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let push_view_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let push_view_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum PushViewMethod {
  GetToggleLabel
  ResetQuery
  SetQuery
  ToggleView
} derive(Eq, @debug.Debug)

///|
pub fn PushViewMethod::name(self : PushViewMethod) -> String {
  match self {
    GetToggleLabel => "getToggleLabel"
    ResetQuery => "resetQuery"
    SetQuery => "setQuery"
    ToggleView => "toggleView"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] push_view_mutate(
  f : (PushViewMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for
    m in (
      [GetToggleLabel, ResetQuery, SetQuery, ToggleView] : Array[PushViewMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] push_view_compute(
  f : (PushViewMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for
    m in (
      [GetToggleLabel, ResetQuery, SetQuery, ToggleView] : Array[PushViewMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] push_view_swap(
  f : (PushViewMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for
    m in (
      [GetToggleLabel, ResetQuery, SetQuery, ToggleView] : Array[PushViewMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let push_view_fields : Array[String] = ["query", "view"]

///|
/// The fields in `push_view_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] push_view_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  push_view_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in rendering.html.
pub(all) enum SeqItemAccessView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SeqItemAccessView::name(self : SeqItemAccessView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let seq_item_access_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let seq_item_access_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum SeqItemAccessMethod {
  GetMaxIndex
  SetCurrentIndex
  SetCurrentKey
} derive(Eq, @debug.Debug)

///|
pub fn SeqItemAccessMethod::name(self : SeqItemAccessMethod) -> String {
  match self {
    GetMaxIndex => "getMaxIndex"
    SetCurrentIndex => "setCurrentIndex"
    SetCurrentKey => "setCurrentKey"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] seq_item_access_mutate(
  f : (SeqItemAccessMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for
    m in (
      [GetMaxIndex, SetCurrentIndex, SetCurrentKey] : Array[SeqItemAccessMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] seq_item_access_compute(
  f : (SeqItemAccessMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for
    m in (
      [GetMaxIndex, SetCurrentIndex, SetCurrentKey] : Array[SeqItemAccessMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] seq_item_access_swap(
  f : (SeqItemAccessMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for
    m in (
      [GetMaxIndex, SetCurrentIndex, SetCurrentKey] : Array[SeqItemAccessMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let seq_item_access_fields : Array[String] = [
  "byIndex", "byKey", "currentIndex", "currentKey",
]

///|
/// The fields in `seq_item_access_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] seq_item_access_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  seq_item_access_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in rendering.html.
pub(all) enum PaginationView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn PaginationView::name(self : PaginationView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let pagination_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let pagination_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum PaginationInput {
  Next
  Prev
} derive(Eq, @debug.Debug)

///|
pub fn PaginationInput::name(self : PaginationInput) -> String {
  match self {
    Next => "next"
    Prev => "prev"
  }
}

///|
pub fn PaginationInput::parse(s : String) -> PaginationInput? {
  match s {
    "next" => Some(Next)
    "prev" => Some(Prev)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to rendering.html breaks every `match` over this type
/// until it is handled.
pub(all) enum PaginationMsg {
  /// `@on.click="next …"`
  Next
  /// `@on.click="prev …"`
  Prev
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn PaginationMsg::from_dispatch(d : @component.Dispatch) -> PaginationMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("next", []) => Next
      ("prev", []) => Prev
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum PaginationMethod {
  IsFirstPage
  IsLastPage
  PageLabel
} derive(Eq, @debug.Debug)

///|
pub fn PaginationMethod::name(self : PaginationMethod) -> String {
  match self {
    IsFirstPage => "isFirstPage"
    IsLastPage => "isLastPage"
    PageLabel => "pageLabel"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] pagination_mutate(
  f : (PaginationMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([IsFirstPage, IsLastPage, PageLabel] : Array[PaginationMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] pagination_compute(
  f : (PaginationMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([IsFirstPage, IsLastPage, PageLabel] : Array[PaginationMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] pagination_swap(
  f : (PaginationMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([IsFirstPage, IsLastPage, PageLabel] : Array[PaginationMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let pagination_fields : Array[String] = ["items"]

///|
/// The fields in `pagination_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] pagination_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  pagination_fields.filter(f => !present.contains(f))
}
