// GENERATED by `tutuca gen-views` from dynamic_selected_edit.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views dynamic_selected_edit.html --name DynamicSelectedEdit --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the DseRoot component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct DseRootState {
  items : Map[String, @tutuca.Value]
  selectedKey : String
  editor : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..DseRootState::zero(), label: "Counter" }
pub fn DseRootState::zero() -> DseRootState {
  { items: Map([]), selectedKey: "", editor: Null, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for DseRootState with fn encode(s) {
  {
    "items": {
      let m : Map[String, @tutuca.Value] = Map([])
      for k, x in s.items {
        m[k] = x
      }
      (Map(m) : @tutuca.Value)
    },
    "selectedKey": Str(s.selectedKey),
    "editor": s.editor,
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for DseRootState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is Map(u1_items) else { return None }
  let v_items : Map[String, @tutuca.Value] = Map([])
  for k, e1_items in u1_items {
    let x1_items = e1_items
    v_items[k] = x1_items
  }
  guard f.get("selectedKey") is Some(Str(v_selectedKey)) else { return None }
  let v_editor = f.get("editor").unwrap_or(Null)
  Some({ items: v_items, selectedKey: v_selectedKey, editor: v_editor, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum DseRootReceive {
  SelectItem(@tutuca.Value)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn DseRootReceive::from_dispatch(
  d : @component.Dispatch,
) -> DseRootReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("selectItem", [a0]) => SelectItem(a0)
      _ => {
        match name {
          "selectItem" => @tutuca.bad_payload(name, "Any", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn DseRootReceive::to_dispatch(
  self : DseRootReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    SelectItem(a0) => ("selectItem", [a0])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn DseRootReceive::send(self : DseRootReceive, ctx : &@tutuca.Ctx) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let dse_root_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let dse_root_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum DseRootInput {
  SelectItem
} derive(Eq, @debug.Debug)

///|
pub fn DseRootInput::name(self : DseRootInput) -> String {
  match self {
    SelectItem => "selectItem"
  }
}

///|
pub fn DseRootInput::parse(s : String) -> DseRootInput? {
  match s {
    "selectItem" => Some(SelectItem)
    _ => None
  }
}

///|
/// The VIEW-named half of @component.Dispatch's `Receive` arm, decoded.
/// Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to dynamic_selected_edit.html breaks every `match` over this type
/// until it is handled.
pub(all) enum DseRootMsg {
  /// `@on.click="selectItem …"`
  SelectItem(@tutuca.Value)
  /// A dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for an `Intent` — a routed name is raised by component code and
/// never written in a view, so the views cannot type it. A `Receive` the
/// views do not name decodes to `Unknown`, which is what the sibling
/// `<T>Receive` (the schema's half of the same bucket) is for.
pub fn DseRootMsg::from_dispatch(d : @component.Dispatch) -> DseRootMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("selectItem", [a0]) => SelectItem(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for DseRootState with fn schema() {
  @component.SchemaInfo::new(
    name="DseRoot",
    fingerprint="03406d62f8349aca",
    fields=[
      @component.FieldInfo::new("items", TyOMap(TyComp(Some("DseEntry")))),
      @component.FieldInfo::new("selectedKey", TyText),
      @component.FieldInfo::new("editor", TyComp(Some("Editor"))),
    ],
    view_handlers=["selectItem"],
    domains=[{ field: "selectedKey", domain: DKeyOf(field="items"), }],
    view_names=["main"],
  )
}

///|
/// The state of the DseEditor component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct DseEditorState {
  missing : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..DseEditorState::zero(), label: "Counter" }
pub fn DseEditorState::zero() -> DseEditorState {
  { missing: Null, }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for DseEditorState with fn encode(s) {
  { "missing": s.missing }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for DseEditorState with fn decode(f) {
  let v_missing = f.get("missing").unwrap_or(Null)
  Some({ missing: v_missing, })
}

///|
/// The file-level style — pass as `common_style~`.
pub let dse_editor_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let dse_editor_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for DseEditorState with fn schema() {
  @component.SchemaInfo::new(
    name="DseEditor",
    fingerprint="294c897cc8f5e7a9",
    fields=[@component.FieldInfo::new("missing", TyAny)],
    view_names=["main"],
  )
}

///|
/// The state of the DseEntry component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct DseEntryState {
  name : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..DseEntryState::zero(), label: "Counter" }
pub fn DseEntryState::zero() -> DseEntryState {
  { name: "", }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for DseEntryState with fn encode(s) {
  { "name": Str(s.name) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for DseEntryState with fn decode(f) {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  Some({ name: v_name, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum DseEntryReceive {
  SetName(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn DseEntryReceive::from_dispatch(
  d : @component.Dispatch,
) -> DseEntryReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("setName", [Str(a0)]) => SetName(a0)
      _ => {
        match name {
          "setName" => @tutuca.bad_payload(name, "String", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn DseEntryReceive::to_dispatch(
  self : DseEntryReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    SetName(a0) => ("setName", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn DseEntryReceive::send(
  self : DseEntryReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let dse_entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let dse_entry_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum DseEntryInput {
  SetName
} derive(Eq, @debug.Debug)

///|
pub fn DseEntryInput::name(self : DseEntryInput) -> String {
  match self {
    SetName => "setName"
  }
}

///|
pub fn DseEntryInput::parse(s : String) -> DseEntryInput? {
  match s {
    "setName" => Some(SetName)
    _ => None
  }
}

///|
/// The VIEW-named half of @component.Dispatch's `Receive` arm, decoded.
/// Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to dynamic_selected_edit.html breaks every `match` over this type
/// until it is handled.
pub(all) enum DseEntryMsg {
  /// `@on.input="setName …"`
  SetName(String)
  /// A dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for an `Intent` — a routed name is raised by component code and
/// never written in a view, so the views cannot type it. A `Receive` the
/// views do not name decodes to `Unknown`, which is what the sibling
/// `<T>Receive` (the schema's half of the same bucket) is for.
pub fn DseEntryMsg::from_dispatch(d : @component.Dispatch) -> DseEntryMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("setName", [Str(a0)]) => SetName(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for DseEntryState with fn schema() {
  @component.SchemaInfo::new(
    name="DseEntry",
    fingerprint="b9c226f2b409fd38",
    fields=[@component.FieldInfo::new("name", TyText)],
    view_handlers=["setName"],
    view_names=["main", "edit"],
  )
}
