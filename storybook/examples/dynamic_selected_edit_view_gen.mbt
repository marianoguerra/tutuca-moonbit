// GENERATED by `tutuca gen-views` from dynamic_selected_edit.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views dynamic_selected_edit.html --name DynamicSelectedEdit --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The views declared in dynamic_selected_edit.html.
pub(all) enum DseRootView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn DseRootView::name(self : DseRootView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let dse_root_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let dse_root_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum DseRootInput {
  SelectItem
} derive(Eq, @debug.Debug)

///|
pub fn DseRootInput::name(self : DseRootInput) -> String {
  match self {
    SelectItem => "selectItem"
  }
}

///|
pub fn DseRootInput::parse(s : String) -> DseRootInput? {
  match s {
    "selectItem" => Some(SelectItem)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to dynamic_selected_edit.html breaks every `match` over this type
/// until it is handled.
pub(all) enum DseRootMsg {
  /// `@on.click="selectItem …"`
  SelectItem(@tutuca.Value)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn DseRootMsg::of_dispatch(d : @component.Dispatch) -> DseRootMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("selectItem", [a0]) => SelectItem(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let dse_root_fields : Array[String] = ["editor", "selectedKey"]

///|
/// The fields in `dse_root_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] dse_root_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  dse_root_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in dynamic_selected_edit.html.
pub(all) enum DseEditorView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn DseEditorView::name(self : DseEditorView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let dse_editor_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let dse_editor_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let dse_editor_fields : Array[String] = []

///|
/// The fields in `dse_editor_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] dse_editor_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  dse_editor_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in dynamic_selected_edit.html.
pub(all) enum DseEntryView {
  Main
  Edit
} derive(Eq, @debug.Debug)

///|
pub fn DseEntryView::name(self : DseEntryView) -> String {
  match self {
    Main => "main"
    Edit => "edit"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let dse_entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let dse_entry_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum DseEntryMethod {
  SetName
} derive(Eq, @debug.Debug)

///|
pub fn DseEntryMethod::name(self : DseEntryMethod) -> String {
  match self {
    SetName => "setName"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] dse_entry_mutate(
  f : (DseEntryMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetName] : Array[DseEntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] dse_entry_compute(
  f : (DseEntryMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetName] : Array[DseEntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] dse_entry_swap(
  f : (DseEntryMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetName] : Array[DseEntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let dse_entry_fields : Array[String] = ["name"]

///|
/// The fields in `dse_entry_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] dse_entry_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  dse_entry_fields.filter(f => !present.contains(f))
}
