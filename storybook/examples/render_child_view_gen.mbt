// GENERATED by `tutuca gen-views` from render_child.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views render_child.html --name RenderChild --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the Greeting component.
///
/// Declared as `interface greeting` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct GreetingState {
  name : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..GreetingState::zero(), label: "Counter" }
fn GreetingState::zero() -> GreetingState {
  GreetingState::{ name: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn greeting_zero_args() -> Map[String, @tutuca.Value] {
  greeting_encode(GreetingState::zero())
}

///|
/// Every field the Greeting state declares — typed field names for tests and inspectors.
pub(all) enum GreetingField {
  Name
} derive(Eq, @debug.Debug)

///|
pub fn GreetingField::name(self : GreetingField) -> String {
  match self {
    Name => "name"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn GreetingField::kind(self : GreetingField) -> @component.FieldKind {
  match self {
    Name => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let greeting_field_names : Array[String] = ["name"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let greeting_schema_fingerprint : String = "291131f40d1ed859"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn greeting_encode(s : GreetingState) -> Map[String, @tutuca.Value] {
  { "name": Str(s.name) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn greeting_decode(f : Map[String, @tutuca.Value]) -> GreetingState? {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  Some(GreetingState::{ name: v_name })
}

///|
/// The views declared in render_child.html.
pub(all) enum GreetingView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn GreetingView::name(self : GreetingView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let greeting_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let greeting_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn greeting_schema() -> @component.SchemaInfo {
  {
    fingerprint: "291131f40d1ed859",
    fields: [
      {
        name: "name",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the Page component.
///
/// Declared as `interface page` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct PageState {}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..PageState::zero(), label: "Counter" }
fn PageState::zero() -> PageState {
  PageState::{  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn page_zero_args() -> Map[String, @tutuca.Value] {
  page_encode(PageState::zero())
}

///|
/// Every field the Page state declares — typed field names for tests and inspectors.
pub(all) enum PageField {
  Greeting
} derive(Eq, @debug.Debug)

///|
pub fn PageField::name(self : PageField) -> String {
  match self {
    Greeting => "greeting"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn PageField::kind(self : PageField) -> @component.FieldKind {
  match self {
    Greeting => @component.FComp(comp="", args=Map([]))
  }
}

///|
/// Every field, in declaration order.
pub let page_field_names : Array[String] = ["greeting"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let page_schema_fingerprint : String = "db02b6e5e92c5199"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn page_encode(_s : PageState) -> Map[String, @tutuca.Value] {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn page_decode(_f : Map[String, @tutuca.Value]) -> PageState? {
  Some(PageState::{  })
}

///|
/// The views declared in render_child.html.
pub(all) enum PageView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn PageView::name(self : PageView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let page_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let page_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn page_schema() -> @component.SchemaInfo {
  {
    fingerprint: "db02b6e5e92c5199",
    fields: [
      {
        name: "greeting",
        ty: "component",
        kind: @component.FComp(comp="", args=Map([])),
        elem: Some("any"),
        slot: Some(""),
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the Entry component.
///
/// Declared as `interface entry` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct EntryState {
  title : String
  description : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..EntryState::zero(), label: "Counter" }
fn EntryState::zero() -> EntryState {
  EntryState::{ title: "", description: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn entry_zero_args() -> Map[String, @tutuca.Value] {
  entry_encode(EntryState::zero())
}

///|
/// Every field the Entry state declares — typed field names for tests and inspectors.
pub(all) enum EntryField {
  Title
  Description
} derive(Eq, @debug.Debug)

///|
pub fn EntryField::name(self : EntryField) -> String {
  match self {
    Title => "title"
    Description => "description"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn EntryField::kind(self : EntryField) -> @component.FieldKind {
  match self {
    Title => @component.FText
    Description => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let entry_field_names : Array[String] = ["title", "description"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let entry_schema_fingerprint : String = "e63d2a9edab89eef"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn entry_encode(s : EntryState) -> Map[String, @tutuca.Value] {
  { "title": Str(s.title), "description": Str(s.description) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn entry_decode(f : Map[String, @tutuca.Value]) -> EntryState? {
  guard f.get("title") is Some(Str(v_title)) else { return None }
  guard f.get("description") is Some(Str(v_description)) else { return None }
  Some(EntryState::{ title: v_title, description: v_description })
}

///|
/// The views declared in render_child.html.
pub(all) enum EntryView {
  Main
  Edit
} derive(Eq, @debug.Debug)

///|
pub fn EntryView::name(self : EntryView) -> String {
  match self {
    Main => "main"
    Edit => "edit"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let entry_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum EntryInput {
  SetDescription
  SetTitle
} derive(Eq, @debug.Debug)

///|
pub fn EntryInput::name(self : EntryInput) -> String {
  match self {
    SetDescription => "setDescription"
    SetTitle => "setTitle"
  }
}

///|
pub fn EntryInput::parse(s : String) -> EntryInput? {
  match s {
    "setDescription" => Some(SetDescription)
    "setTitle" => Some(SetTitle)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to render_child.html breaks every `match` over this type
/// until it is handled.
pub(all) enum EntryMsg {
  /// `@on.input="setDescription …"`
  SetDescription(String)
  /// `@on.input="setTitle …"`
  SetTitle(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn EntryMsg::from_dispatch(d : @component.Dispatch) -> EntryMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setDescription", [Str(a0)]) => SetDescription(a0)
      ("setTitle", [Str(a0)]) => SetTitle(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] entry_swap(
  f : (EntryInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetDescription, SetTitle] : Array[EntryInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `$name` the views call.
pub(all) enum EntryMethod {
  ContainsText
} derive(Eq, @debug.Debug)

///|
pub fn EntryMethod::name(self : EntryMethod) -> String {
  match self {
    ContainsText => "containsText"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] entry_compute(
  f : (EntryMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([ContainsText] : Array[EntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn entry_schema() -> @component.SchemaInfo {
  {
    fingerprint: "e63d2a9edab89eef",
    fields: [
      {
        name: "title",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "description",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: ["setDescription", "setTitle"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: ["containsText"],
    ids: [],
    view_names: ["main", "edit"],
    init_names: [],
  }
}

///|
/// The state of the MultipleViews component.
///
/// Declared as `interface multiple-views` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct MultipleViewsState {}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..MultipleViewsState::zero(), label: "Counter" }
fn MultipleViewsState::zero() -> MultipleViewsState {
  MultipleViewsState::{  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn multiple_views_zero_args() -> Map[String, @tutuca.Value] {
  multiple_views_encode(MultipleViewsState::zero())
}

///|
/// Every field the MultipleViews state declares — typed field names for tests and inspectors.
pub(all) enum MultipleViewsField {
  Value
} derive(Eq, @debug.Debug)

///|
pub fn MultipleViewsField::name(self : MultipleViewsField) -> String {
  match self {
    Value => "value"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn MultipleViewsField::kind(
  self : MultipleViewsField,
) -> @component.FieldKind {
  match self {
    Value => @component.FComp(comp="", args=Map([]))
  }
}

///|
/// Every field, in declaration order.
pub let multiple_views_field_names : Array[String] = ["value"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let multiple_views_schema_fingerprint : String = "74788bd77ca3a59f"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn multiple_views_encode(_s : MultipleViewsState) -> Map[String, @tutuca.Value] {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn multiple_views_decode(
  _f : Map[String, @tutuca.Value],
) -> MultipleViewsState? {
  Some(MultipleViewsState::{  })
}

///|
/// The views declared in render_child.html.
pub(all) enum MultipleViewsView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn MultipleViewsView::name(self : MultipleViewsView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let multiple_views_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let multiple_views_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn multiple_views_schema() -> @component.SchemaInfo {
  {
    fingerprint: "74788bd77ca3a59f",
    fields: [
      {
        name: "value",
        ty: "component",
        kind: @component.FComp(comp="", args=Map([])),
        elem: Some("any"),
        slot: Some(""),
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}
