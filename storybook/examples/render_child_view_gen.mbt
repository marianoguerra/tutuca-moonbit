// GENERATED by `tutuca gen-views` from render_child.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views render_child.html --name RenderChild --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The views declared in render_child.html.
pub(all) enum GreetingView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn GreetingView::name(self : GreetingView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let greeting_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let greeting_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let greeting_fields : Array[String] = ["name"]

///|
/// The fields in `greeting_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] greeting_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  greeting_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in render_child.html.
pub(all) enum PageView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn PageView::name(self : PageView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let page_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let page_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let page_fields : Array[String] = ["greeting"]

///|
/// The fields in `page_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] page_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  page_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in render_child.html.
pub(all) enum EntryView {
  Main
  Edit
} derive(Eq, @debug.Debug)

///|
pub fn EntryView::name(self : EntryView) -> String {
  match self {
    Main => "main"
    Edit => "edit"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let entry_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum EntryMethod {
  SetDescription
  SetTitle
} derive(Eq, @debug.Debug)

///|
pub fn EntryMethod::name(self : EntryMethod) -> String {
  match self {
    SetDescription => "setDescription"
    SetTitle => "setTitle"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] entry_mutate(
  f : (EntryMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetDescription, SetTitle] : Array[EntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] entry_compute(
  f : (EntryMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetDescription, SetTitle] : Array[EntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] entry_swap(
  f : (EntryMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetDescription, SetTitle] : Array[EntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let entry_fields : Array[String] = ["description", "title"]

///|
/// The fields in `entry_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] entry_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  entry_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in render_child.html.
pub(all) enum MultipleViewsView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn MultipleViewsView::name(self : MultipleViewsView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let multiple_views_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let multiple_views_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let multiple_views_fields : Array[String] = ["value"]

///|
/// The fields in `multiple_views_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] multiple_views_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  multiple_views_fields.filter(f => !present.contains(f))
}
