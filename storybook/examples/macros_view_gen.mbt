// GENERATED by `tutuca gen-views` from macros.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views macros.html --name Macros --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state for the components in macros.mbt, which build
/// their views in MoonBit rather than here — a macro body is
/// registered at runtime, so it cannot be compiled ahead of time.
/// No <template>: this file declares state and nothing else.
///
/// Declared as `interface macros-no` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct MacrosNoState {} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..MacrosNoState::zero(), label: "Counter" }
fn MacrosNoState::zero() -> MacrosNoState {
  MacrosNoState::{  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn macros_no_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(MacrosNoState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn macros_no_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let macros_no_schema_fingerprint : String = "d913a4b9e7d1c76a"

///|
/// The state of the MacroParams component.
///
/// Declared as `interface macro-params` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct MacroParamsState {
  status : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..MacroParamsState::zero(), label: "Counter" }
fn MacroParamsState::zero() -> MacroParamsState {
  MacroParamsState::{ status: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn macro_params_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(MacroParamsState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the MacroParams state declares — typed field names for tests and inspectors.
pub(all) enum MacroParamsField {
  Status
} derive(Eq, @debug.Debug)

///|
pub fn MacroParamsField::name(self : MacroParamsField) -> String {
  match self {
    Status => "status"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn MacroParamsField::kind(self : MacroParamsField) -> @component.FieldKind {
  match self {
    Status => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let macro_params_field_names : Array[String] = ["status"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn macro_params_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let macro_params_schema_fingerprint : String = "e24255b8b42d4192"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn macro_params_encode(s : MacroParamsState) -> Map[String, @tutuca.Value] {
  { "status": Str(s.status) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn macro_params_decode(f : Map[String, @tutuca.Value]) -> MacroParamsState? {
  guard f.get("status") is Some(Str(v_status)) else { return None }
  Some(MacroParamsState::{ status: v_status })
}

///|
/// The state of the MacroSlots component.
///
/// Declared as `interface macro-slots` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct MacroSlotsState {
  count : Int
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..MacroSlotsState::zero(), label: "Counter" }
fn MacroSlotsState::zero() -> MacroSlotsState {
  MacroSlotsState::{ count: 0 }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn macro_slots_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(MacroSlotsState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the MacroSlots state declares — typed field names for tests and inspectors.
pub(all) enum MacroSlotsField {
  Count
} derive(Eq, @debug.Debug)

///|
pub fn MacroSlotsField::name(self : MacroSlotsField) -> String {
  match self {
    Count => "count"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn MacroSlotsField::kind(self : MacroSlotsField) -> @component.FieldKind {
  match self {
    Count => @component.FInt
  }
}

///|
/// Every field, in declaration order.
pub let macro_slots_field_names : Array[String] = ["count"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn macro_slots_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let macro_slots_schema_fingerprint : String = "29f5d9126343b9a9"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn macro_slots_encode(s : MacroSlotsState) -> Map[String, @tutuca.Value] {
  { "count": Num(s.count.to_double()) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn macro_slots_decode(f : Map[String, @tutuca.Value]) -> MacroSlotsState? {
  guard f.get("count") is Some(Num(n_count)) else { return None }
  let v_count = n_count.to_int()
  Some(MacroSlotsState::{ count: v_count })
}

///|
/// The state of the MacroNamedSlots component.
///
/// Declared as `interface macro-named-slots` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct MacroNamedSlotsState {
  count : Int
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..MacroNamedSlotsState::zero(), label: "Counter" }
fn MacroNamedSlotsState::zero() -> MacroNamedSlotsState {
  MacroNamedSlotsState::{ count: 0 }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn macro_named_slots_zero_args() -> Map[String, @tutuca.Value] {
  match
    @tutuca.Value::from_json(ToJson::to_json(MacroNamedSlotsState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the MacroNamedSlots state declares — typed field names for tests and inspectors.
pub(all) enum MacroNamedSlotsField {
  Count
} derive(Eq, @debug.Debug)

///|
pub fn MacroNamedSlotsField::name(self : MacroNamedSlotsField) -> String {
  match self {
    Count => "count"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn MacroNamedSlotsField::kind(
  self : MacroNamedSlotsField,
) -> @component.FieldKind {
  match self {
    Count => @component.FInt
  }
}

///|
/// Every field, in declaration order.
pub let macro_named_slots_field_names : Array[String] = ["count"]

///|
/// The `specs~` map for this component, derived from the schema.
///
/// Sets, ordered maps and child slots all encode to a plain Map or Null,
/// so the runtime cannot tell them apart from the init value alone. Every
/// other field's kind it infers correctly and is left to it.
pub fn macro_named_slots_specs() -> Map[String, @component.FieldSpec] {
  Map([])
}

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let macro_named_slots_schema_fingerprint : String = "ef1c08d80888b3fd"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn macro_named_slots_encode(
  s : MacroNamedSlotsState,
) -> Map[String, @tutuca.Value] {
  { "count": Num(s.count.to_double()) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn macro_named_slots_decode(
  f : Map[String, @tutuca.Value],
) -> MacroNamedSlotsState? {
  guard f.get("count") is Some(Num(n_count)) else { return None }
  let v_count = n_count.to_int()
  Some(MacroNamedSlotsState::{ count: v_count })
}
