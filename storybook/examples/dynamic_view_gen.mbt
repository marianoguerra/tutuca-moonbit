// GENERATED by `tutuca gen-views` from dynamic.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views dynamic.html --name Dynamic --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The views declared in dynamic.html.
pub(all) enum SelectorEntryView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SelectorEntryView::name(self : SelectorEntryView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let selector_entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let selector_entry_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum SelectorEntryMethod {
  SetLabel
  SetValue
} derive(Eq, @debug.Debug)

///|
pub fn SelectorEntryMethod::name(self : SelectorEntryMethod) -> String {
  match self {
    SetLabel => "setLabel"
    SetValue => "setValue"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] selector_entry_mutate(
  f : (SelectorEntryMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetLabel, SetValue] : Array[SelectorEntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] selector_entry_compute(
  f : (SelectorEntryMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetLabel, SetValue] : Array[SelectorEntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] selector_entry_swap(
  f : (SelectorEntryMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetLabel, SetValue] : Array[SelectorEntryMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let selector_entry_fields : Array[String] = ["label", "value"]

///|
/// The fields in `selector_entry_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] selector_entry_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  selector_entry_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in dynamic.html.
pub(all) enum SelectorView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SelectorView::name(self : SelectorView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let selector_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let selector_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum SelectorMethod {
  SetSelectedValue
} derive(Eq, @debug.Debug)

///|
pub fn SelectorMethod::name(self : SelectorMethod) -> String {
  match self {
    SetSelectedValue => "setSelectedValue"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] selector_mutate(
  f : (SelectorMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetSelectedValue] : Array[SelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] selector_compute(
  f : (SelectorMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetSelectedValue] : Array[SelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] selector_swap(
  f : (SelectorMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetSelectedValue] : Array[SelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let selector_fields : Array[String] = ["selectedValue"]

///|
/// The fields in `selector_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] selector_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  selector_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in dynamic.html.
pub(all) enum EntryEditorAndSelectorView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn EntryEditorAndSelectorView::name(
  self : EntryEditorAndSelectorView,
) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let entry_editor_and_selector_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let entry_editor_and_selector_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum EntryEditorAndSelectorInput {
  OnAddItem
} derive(Eq, @debug.Debug)

///|
pub fn EntryEditorAndSelectorInput::name(
  self : EntryEditorAndSelectorInput,
) -> String {
  match self {
    OnAddItem => "onAddItem"
  }
}

///|
pub fn EntryEditorAndSelectorInput::parse(
  s : String,
) -> EntryEditorAndSelectorInput? {
  match s {
    "onAddItem" => Some(OnAddItem)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to dynamic.html breaks every `match` over this type
/// until it is handled.
pub(all) enum EntryEditorAndSelectorMsg {
  /// `@on.click="onAddItem …"`
  OnAddItem
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn EntryEditorAndSelectorMsg::of_dispatch(
  d : @component.Dispatch,
) -> EntryEditorAndSelectorMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onAddItem", []) => OnAddItem
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum EntryEditorAndSelectorMethod {
  RemoveInItemsAt
} derive(Eq, @debug.Debug)

///|
pub fn EntryEditorAndSelectorMethod::name(
  self : EntryEditorAndSelectorMethod,
) -> String {
  match self {
    RemoveInItemsAt => "removeInItemsAt"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] entry_editor_and_selector_mutate(
  f : (EntryEditorAndSelectorMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([RemoveInItemsAt] : Array[EntryEditorAndSelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] entry_editor_and_selector_compute(
  f : (EntryEditorAndSelectorMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([RemoveInItemsAt] : Array[EntryEditorAndSelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] entry_editor_and_selector_swap(
  f : (EntryEditorAndSelectorMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([RemoveInItemsAt] : Array[EntryEditorAndSelectorMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let entry_editor_and_selector_fields : Array[String] = ["items", "selector"]

///|
/// The fields in `entry_editor_and_selector_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] entry_editor_and_selector_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  entry_editor_and_selector_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in dynamic.html.
pub(all) enum SheetView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SheetView::name(self : SheetView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let sheet_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let sheet_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum SheetMethod {
  SetTitle
} derive(Eq, @debug.Debug)

///|
pub fn SheetMethod::name(self : SheetMethod) -> String {
  match self {
    SetTitle => "setTitle"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] sheet_mutate(
  f : (SheetMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetTitle] : Array[SheetMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] sheet_compute(
  f : (SheetMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetTitle] : Array[SheetMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] sheet_swap(
  f : (SheetMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetTitle] : Array[SheetMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let sheet_fields : Array[String] = ["title"]

///|
/// The fields in `sheet_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] sheet_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  sheet_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in dynamic.html.
pub(all) enum ToolbarView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ToolbarView::name(self : ToolbarView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let toolbar_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let toolbar_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let toolbar_fields : Array[String] = []

///|
/// The fields in `toolbar_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] toolbar_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  toolbar_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in dynamic.html.
pub(all) enum PanelView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn PanelView::name(self : PanelView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let panel_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let panel_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let panel_fields : Array[String] = ["toolbar"]

///|
/// The fields in `panel_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] panel_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  panel_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in dynamic.html.
pub(all) enum WorkspaceView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn WorkspaceView::name(self : WorkspaceView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let workspace_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let workspace_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum WorkspaceMethod {
  SheetTitle
} derive(Eq, @debug.Debug)

///|
pub fn WorkspaceMethod::name(self : WorkspaceMethod) -> String {
  match self {
    SheetTitle => "sheetTitle"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] workspace_mutate(
  f : (WorkspaceMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SheetTitle] : Array[WorkspaceMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] workspace_compute(
  f : (WorkspaceMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SheetTitle] : Array[WorkspaceMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] workspace_swap(
  f : (WorkspaceMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SheetTitle] : Array[WorkspaceMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let workspace_fields : Array[String] = ["panel"]

///|
/// The fields in `workspace_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] workspace_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  workspace_fields.filter(f => !present.contains(f))
}
