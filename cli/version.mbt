///|
/// Own-version constant, reported by `agent-context` and stamped into
/// feedback records. Compile-time, since a native binary has no package
/// directory to read at runtime. Keep in sync with `version` in moon.mod.
pub const VERSION : String = "0.12.2"
