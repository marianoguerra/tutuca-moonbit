import {
  "marianoguerra/tutuca/anode",
  // The event-path profile the rule above advises about. The `e.<path>`
  // arguments themselves arrive as core Val variants, matched by inference —
  // no core import needed.
  "marianoguerra/tutuca/eventpath",
  "moonbit-community/html/src" @html5,
}

import {
  "moonbitlang/quickcheck" @qc,
  "moonbitlang/quickcheck/gen",
  "moonbit-community/html/src" @html5,
  "moonbitlang/core/debug",
} for "test"
