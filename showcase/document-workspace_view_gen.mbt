// GENERATED by `tutuca gen` from document-workspace.tutu — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen document-workspace.tutu --name DocumentWorkspace --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the DocumentWorkspace component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct DocumentWorkspaceState {
  documents : Map[String, @tutuca.Value]
  selected : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..DocumentWorkspaceState::zero(), label: "Counter" }
pub fn DocumentWorkspaceState::zero() -> DocumentWorkspaceState {
  { documents: Map([]), selected: "", }
}

///|
/// The `fresh` initial state, from the init block. Checked against
/// the schema at generation time, so a field the schema dropped fails
/// the build instead of being coerced away at runtime.
fn DocumentWorkspaceState::fresh() -> DocumentWorkspaceState {
  DocumentWorkspaceState::zero()
}

///|
/// The `unsaved draft` initial state, from the init block. Checked against
/// the schema at generation time, so a field the schema dropped fails
/// the build instead of being coerced away at runtime.
fn DocumentWorkspaceState::unsavedDraft() -> DocumentWorkspaceState {
  DocumentWorkspaceState::zero()
}

///|
/// A named fixture, or None when no fixture has that name.
///
/// Keyed by the name as WRITTEN, for a caller holding one at run time — a
/// storybook, a host, a test driven by a table. The functions above are
/// one per fixture and are the spelling to reach for inside this package,
/// where the name is known and a typo should not compile; their names are
/// mangled to be identifiers, so `with-history` answers to
/// `with_history()`, which is why this one exists for everybody else.
pub fn DocumentWorkspaceState::fixture(
  name : String,
) -> DocumentWorkspaceState? {
  match name {
    "fresh" => Some(DocumentWorkspaceState::fresh())
    "unsaved draft" => Some(DocumentWorkspaceState::unsavedDraft())
    _ => None
  }
}

///|
/// A named initial state as component arguments — what a ModuleDef
/// example takes. None when no fixture has that name.
///
/// This is the public face of the fixtures: they return the private state
/// type, so they cannot be public themselves, and a storybook needs the
/// encoded form anyway.
pub fn document_workspace_init_args(
  name : String,
) -> Map[String, @tutuca.Value]? {
  match name {
    "fresh" => Some(@component.Fields::encode(DocumentWorkspaceState::fresh()))
    "unsaved draft" =>
      Some(@component.Fields::encode(DocumentWorkspaceState::unsavedDraft()))
    _ => None
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for DocumentWorkspaceState with fn encode(s) {
  {
    "documents": {
      let m : Map[String, @tutuca.Value] = Map([])
      for k, x in s.documents {
        m[k] = x
      }
      (Map(m) : @tutuca.Value)
    },
    "selected": Str(s.selected),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for DocumentWorkspaceState with fn decode(f) {
  guard f.get("documents") is Some(t1_documents) else { return None }
  guard t1_documents is Map(u1_documents) else { return None }
  let v_documents : Map[String, @tutuca.Value] = Map([])
  for k, e1_documents in u1_documents {
    let x1_documents = e1_documents
    v_documents[k] = x1_documents
  }
  guard f.get("selected") is Some(Str(v_selected)) else { return None }
  Some({ documents: v_documents, selected: v_selected, })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Every addressed name: the `message` the `spec:` section declares,
/// plus every bare `~on_<event>` handler the views write. One bucket holds
/// them
/// however they were sent, so one type covers it — where the two overlap
/// the schema's declared payload wins, and a view whose call sites
/// disagree with it is a build error.
pub(all) enum DocumentWorkspaceMsg {
  Init
  /// A message the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn DocumentWorkspaceMsg::from_dispatch(
  d : @component.Dispatch,
) -> DocumentWorkspaceMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("init", []) => Init
      _ => {
        match name {
          "init" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn DocumentWorkspaceMsg::to_dispatch(
  self : DocumentWorkspaceMsg,
) -> (String, Array[@tutuca.Value]) {
  match self {
    Init => ("init", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn DocumentWorkspaceMsg::send(
  self : DocumentWorkspaceMsg,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let document_workspace_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let document_workspace_global_style : String = ""

///|
/// Every callable the views name, plus the file's own `pred` / `compute` names — the bucket `compute~` keys.
pub(all) enum DocumentWorkspaceCompute {
  SelectedDocument
} derive(Eq, @debug.Debug)

///|
pub fn DocumentWorkspaceCompute::name(
  self : DocumentWorkspaceCompute,
) -> String {
  match self {
    SelectedDocument => "selected_document"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for DocumentWorkspaceState with fn schema() {
  @component.SchemaInfo::new(
    name="DocumentWorkspace",
    fingerprint="a5881658a81650e5",
    fields=[
      @component.FieldInfo::new(
        "documents",
        TyOMap(TyComp(Some("DocumentEditor"))),
      ),
      @component.FieldInfo::new("selected", TyText),
    ],
    receives=["init"],
    methods=["selected_document"],
    view_names=["main"],
    init_names=["fresh", "unsaved draft"],
  )
}

///|
/// The state of the DocumentEditor component.
///
/// Declared with `field` in the `spec:` section. Adding one there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct DocumentEditorState {
  id : String
  title : String
  source : String
  saved_source : String
  revision : Int
  load_revision : Int
  busy : Bool
  fail_next : Bool
  feedback : String
  view : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The `spec:` section states no defaults here — a default is a value,
/// not a type — so this is type-directed, and `init` spreads it to name
/// only what differs:
///
///   initial={ ..DocumentEditorState::zero(), label: "Counter" }
pub fn DocumentEditorState::zero() -> DocumentEditorState {
  {
    id: "",
    title: "",
    source: "",
    saved_source: "",
    revision: 0,
    load_revision: 0,
    busy: false,
    fail_next: false,
    feedback: "",
    view: "",
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for DocumentEditorState with fn encode(s) {
  {
    "id": Str(s.id),
    "title": Str(s.title),
    "source": Str(s.source),
    "saved_source": Str(s.saved_source),
    "revision": Num(s.revision.to_double()),
    "load_revision": Num(s.load_revision.to_double()),
    "busy": Bool(s.busy),
    "fail_next": Bool(s.fail_next),
    "feedback": Str(s.feedback),
    "view": Str(s.view),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for DocumentEditorState with fn decode(f) {
  guard f.get("id") is Some(Str(v_id)) else { return None }
  guard f.get("title") is Some(Str(v_title)) else { return None }
  guard f.get("source") is Some(Str(v_source)) else { return None }
  guard f.get("saved_source") is Some(Str(v_saved_source)) else { return None }
  guard f.get("revision") is Some(Num(t1_revision)) else { return None }
  let v_revision = t1_revision.to_int()
  guard f.get("load_revision") is Some(Num(t1_load_revision)) else {
    return None
  }
  let v_load_revision = t1_load_revision.to_int()
  guard f.get("busy") is Some(Bool(v_busy)) else { return None }
  guard f.get("fail_next") is Some(Bool(v_fail_next)) else { return None }
  guard f.get("feedback") is Some(Str(v_feedback)) else { return None }
  guard f.get("view") is Some(Str(v_view)) else { return None }
  Some({
    id: v_id,
    title: v_title,
    source: v_source,
    saved_source: v_saved_source,
    revision: v_revision,
    load_revision: v_load_revision,
    busy: v_busy,
    fail_next: v_fail_next,
    feedback: v_feedback,
    view: v_view,
  })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Every addressed name: the `message` the `spec:` section declares,
/// plus every bare `~on_<event>` handler the views write. One bucket holds
/// them
/// however they were sent, so one type covers it — where the two overlap
/// the schema's declared payload wins, and a view whose call sites
/// disagree with it is a build error.
///
/// The `logic:` section answers edit, load, save, so an `update` arm for
/// one of those can never run — the wrapper consults `update` only for a
/// dispatch the section did not claim. They are still here because raising
/// one is what a parent does, and `to_dispatch` is how.
pub(all) enum DocumentEditorMsg {
  LoadDocumentOk(@tutuca.Value)
  LoadDocumentFailed(@tutuca.Value)
  LoadDocumentUnhandled
  SaveDocumentOk(@tutuca.Value)
  SaveDocumentFailed(@tutuca.Value)
  SaveDocumentUnhandled
  Edit(String)
  Load
  Save
  /// A message the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
///
/// It is also REPORTED. `Unknown` answers two different questions —
/// nobody declares this name, and this name is declared here and was
/// handed the wrong shape — and the second one is a bug in the sender
/// that used to be indistinguishable from silence. Every arm below that
/// can fail on a DECLARED name says so through `@tutuca.bad_payload`,
/// which costs nothing unless a host is listening.
pub fn DocumentEditorMsg::from_dispatch(
  d : @component.Dispatch,
) -> DocumentEditorMsg? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("load_document_ok", [a0]) => LoadDocumentOk(a0)
      ("load_document_failed", [a0]) => LoadDocumentFailed(a0)
      ("load_document_unhandled", []) => LoadDocumentUnhandled
      ("save_document_ok", [a0]) => SaveDocumentOk(a0)
      ("save_document_failed", [a0]) => SaveDocumentFailed(a0)
      ("save_document_unhandled", []) => SaveDocumentUnhandled
      ("edit", [Str(a0)]) => Edit(a0)
      ("load", []) => Load
      ("save", []) => Save
      _ => {
        match name {
          "load_document_ok" => @tutuca.bad_payload(name, "Any", args)
          "load_document_failed" => @tutuca.bad_payload(name, "Any", args)
          "load_document_unhandled" => @tutuca.bad_payload(name, "", args)
          "save_document_ok" => @tutuca.bad_payload(name, "Any", args)
          "save_document_failed" => @tutuca.bad_payload(name, "Any", args)
          "save_document_unhandled" => @tutuca.bad_payload(name, "", args)
          "edit" => @tutuca.bad_payload(name, "String", args)
          "load" => @tutuca.bad_payload(name, "", args)
          "save" => @tutuca.bad_payload(name, "", args)
          _ => ()
        }
        Unknown(name, args)
      }
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn DocumentEditorMsg::to_dispatch(
  self : DocumentEditorMsg,
) -> (String, Array[@tutuca.Value]) {
  match self {
    LoadDocumentOk(a0) => ("load_document_ok", [a0])
    LoadDocumentFailed(a0) => ("load_document_failed", [a0])
    LoadDocumentUnhandled => ("load_document_unhandled", [])
    SaveDocumentOk(a0) => ("save_document_ok", [a0])
    SaveDocumentFailed(a0) => ("save_document_failed", [a0])
    SaveDocumentUnhandled => ("save_document_unhandled", [])
    Edit(a0) => ("edit", [Str(a0)])
    Load => ("load", [])
    Save => ("save", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn DocumentEditorMsg::send(
  self : DocumentEditorMsg,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let document_editor_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let document_editor_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for DocumentEditorState with fn schema() {
  @component.SchemaInfo::new(
    name="DocumentEditor",
    fingerprint="b71013ca5879a93b",
    fields=[
      @component.FieldInfo::new("id", TyText),
      @component.FieldInfo::new("title", TyText),
      @component.FieldInfo::new("source", TyText),
      @component.FieldInfo::new("saved_source", TyText),
      @component.FieldInfo::new("revision", TyInt(width=32, signed=true)),
      @component.FieldInfo::new("load_revision", TyInt(width=32, signed=true)),
      @component.FieldInfo::new("busy", TyBool),
      @component.FieldInfo::new("fail_next", TyBool),
      @component.FieldInfo::new("feedback", TyText),
      @component.FieldInfo::new("view", TyText),
    ],
    view_handlers=["edit", "load", "save"],
    receives=[
      "load_document_ok", "load_document_failed", "load_document_unhandled", "save_document_ok",
      "save_document_failed", "save_document_unhandled",
    ],
    methods=["dirty"],
    view_names=["main"],
  )
}
