// GENERATED by `tutuca gen-views` from schema.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views schema.html --name Schema --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the SchemaProperty component.
///
/// Declared as `interface schema-property` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct SchemaPropertyState {
  key : String
  child : @tutuca.Value
  required : Bool
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SchemaPropertyState::zero(), label: "Counter" }
fn SchemaPropertyState::zero() -> SchemaPropertyState {
  SchemaPropertyState::{ key: "", child: Null, required: false }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn schema_property_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(SchemaPropertyState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the SchemaProperty state declares — typed field names for tests and inspectors.
pub(all) enum SchemaPropertyField {
  Key
  Child
  Required
} derive(Eq, @debug.Debug)

///|
pub fn SchemaPropertyField::name(self : SchemaPropertyField) -> String {
  match self {
    Key => "key"
    Child => "child"
    Required => "required"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn SchemaPropertyField::kind(
  self : SchemaPropertyField,
) -> @component.FieldKind {
  match self {
    Key => @component.FText
    Child => @component.FAny
    Required => @component.FBool
  }
}

///|
/// Every field, in declaration order.
pub let schema_property_field_names : Array[String] = [
  "key", "child", "required",
]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let schema_property_schema_fingerprint : String = "738c4fe1907764e4"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn schema_property_encode(
  s : SchemaPropertyState,
) -> Map[String, @tutuca.Value] {
  { "key": Str(s.key), "child": s.child, "required": Bool(s.required) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn schema_property_decode(
  f : Map[String, @tutuca.Value],
) -> SchemaPropertyState? {
  guard f.get("key") is Some(Str(v_key)) else { return None }
  let v_child = f.get("child").unwrap_or(Null)
  guard f.get("required") is Some(Bool(v_required)) else { return None }
  Some(SchemaPropertyState::{ key: v_key, child: v_child, required: v_required })
}

///|
/// The views declared in schema.html.
pub(all) enum SchemaPropertyView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SchemaPropertyView::name(self : SchemaPropertyView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let schema_property_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let schema_property_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn schema_property_schema() -> @component.SchemaInfo {
  {
    fingerprint: "738c4fe1907764e4",
    fields: [
      {
        name: "key",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "child",
        ty: "any",
        kind: @component.FAny,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "required",
        ty: "bool",
        kind: @component.FBool,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the SchemaBranch component.
///
/// Declared as `interface schema-branch` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct SchemaBranchState {
  label : String
  child : @tutuca.Value
  labelClass : String
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SchemaBranchState::zero(), label: "Counter" }
fn SchemaBranchState::zero() -> SchemaBranchState {
  SchemaBranchState::{ label: "", child: Null, labelClass: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn schema_branch_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(SchemaBranchState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the SchemaBranch state declares — typed field names for tests and inspectors.
pub(all) enum SchemaBranchField {
  Label
  Child
  LabelClass
} derive(Eq, @debug.Debug)

///|
pub fn SchemaBranchField::name(self : SchemaBranchField) -> String {
  match self {
    Label => "label"
    Child => "child"
    LabelClass => "labelClass"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn SchemaBranchField::kind(
  self : SchemaBranchField,
) -> @component.FieldKind {
  match self {
    Label => @component.FText
    Child => @component.FAny
    LabelClass => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let schema_branch_field_names : Array[String] = [
  "label", "child", "labelClass",
]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let schema_branch_schema_fingerprint : String = "b48cf0ecfc472225"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn schema_branch_encode(s : SchemaBranchState) -> Map[String, @tutuca.Value] {
  { "label": Str(s.label), "child": s.child, "labelClass": Str(s.labelClass) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn schema_branch_decode(f : Map[String, @tutuca.Value]) -> SchemaBranchState? {
  guard f.get("label") is Some(Str(v_label)) else { return None }
  let v_child = f.get("child").unwrap_or(Null)
  guard f.get("labelClass") is Some(Str(v_labelClass)) else { return None }
  Some(SchemaBranchState::{
    label: v_label,
    child: v_child,
    labelClass: v_labelClass,
  })
}

///|
/// The views declared in schema.html.
pub(all) enum SchemaBranchView {
  Main
  MargauiClasses
} derive(Eq, @debug.Debug)

///|
pub fn SchemaBranchView::name(self : SchemaBranchView) -> String {
  match self {
    Main => "main"
    MargauiClasses => "_margauiClasses"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let schema_branch_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let schema_branch_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum SchemaBranchMethod {
  LabelCssClass
} derive(Eq, @debug.Debug)

///|
pub fn SchemaBranchMethod::name(self : SchemaBranchMethod) -> String {
  match self {
    LabelCssClass => "labelCssClass"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] schema_branch_compute(
  f : (SchemaBranchMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([LabelCssClass] : Array[SchemaBranchMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn schema_branch_schema() -> @component.SchemaInfo {
  {
    fingerprint: "b48cf0ecfc472225",
    fields: [
      {
        name: "label",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "child",
        ty: "any",
        kind: @component.FAny,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "labelClass",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: ["labelCssClass"],
    ids: [],
    view_names: ["main", "_margauiClasses"],
    init_names: [],
  }
}
