// GENERATED by `tutuca gen-views` from activity.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views activity.html --name Activity --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the ActivityEntry component.
///
/// Declared as `interface activity-entry` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ActivityEntryState {
  kind : String
  name : String
  handlerName : String
  matched : String
  pathLabel : String
  time : String
  before : @tutuca.Value
  after : @tutuca.Value
  hasAfter : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ActivityEntryState::zero(), label: "Counter" }
fn ActivityEntryState::zero() -> ActivityEntryState {
  ActivityEntryState::{
    kind: "",
    name: "",
    handlerName: "",
    matched: "",
    pathLabel: "",
    time: "",
    before: Null,
    after: Null,
    hasAfter: false,
  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn activity_entry_zero_args() -> Map[String, @tutuca.Value] {
  activity_entry_encode(ActivityEntryState::zero())
}

///|
/// Every field the ActivityEntry state declares — typed field names for tests and inspectors.
pub(all) enum ActivityEntryField {
  Kind
  Name
  HandlerName
  Matched
  PathLabel
  Time
  Before
  After
  HasAfter
} derive(Eq, @debug.Debug)

///|
pub fn ActivityEntryField::name(self : ActivityEntryField) -> String {
  match self {
    Kind => "kind"
    Name => "name"
    HandlerName => "handlerName"
    Matched => "matched"
    PathLabel => "pathLabel"
    Time => "time"
    Before => "before"
    After => "after"
    HasAfter => "hasAfter"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn ActivityEntryField::kind(
  self : ActivityEntryField,
) -> @component.FieldKind {
  match self {
    Kind => @component.FText
    Name => @component.FText
    HandlerName => @component.FText
    Matched => @component.FText
    PathLabel => @component.FText
    Time => @component.FText
    Before => @component.FAny
    After => @component.FAny
    HasAfter => @component.FBool
  }
}

///|
/// Every field, in declaration order.
pub let activity_entry_field_names : Array[String] = [
  "kind", "name", "handlerName", "matched", "pathLabel", "time", "before", "after",
  "hasAfter",
]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let activity_entry_schema_fingerprint : String = "d3dd8d6a5a69b8b2"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn activity_entry_encode(s : ActivityEntryState) -> Map[String, @tutuca.Value] {
  {
    "kind": Str(s.kind),
    "name": Str(s.name),
    "handlerName": Str(s.handlerName),
    "matched": Str(s.matched),
    "pathLabel": Str(s.pathLabel),
    "time": Str(s.time),
    "before": s.before,
    "after": s.after,
    "hasAfter": Bool(s.hasAfter),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn activity_entry_decode(f : Map[String, @tutuca.Value]) -> ActivityEntryState? {
  guard f.get("kind") is Some(Str(v_kind)) else { return None }
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("handlerName") is Some(Str(v_handlerName)) else { return None }
  guard f.get("matched") is Some(Str(v_matched)) else { return None }
  guard f.get("pathLabel") is Some(Str(v_pathLabel)) else { return None }
  guard f.get("time") is Some(Str(v_time)) else { return None }
  let v_before = f.get("before").unwrap_or(Null)
  let v_after = f.get("after").unwrap_or(Null)
  guard f.get("hasAfter") is Some(Bool(v_hasAfter)) else { return None }
  Some(ActivityEntryState::{
    kind: v_kind,
    name: v_name,
    handlerName: v_handlerName,
    matched: v_matched,
    pathLabel: v_pathLabel,
    time: v_time,
    before: v_before,
    after: v_after,
    hasAfter: v_hasAfter,
  })
}

///|
/// The views declared in activity.html.
pub(all) enum ActivityEntryView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ActivityEntryView::name(self : ActivityEntryView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let activity_entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let activity_entry_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn activity_entry_schema() -> @component.SchemaInfo {
  {
    fingerprint: "d3dd8d6a5a69b8b2",
    fields: [
      {
        name: "kind",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "name",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "handlerName",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "matched",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "pathLabel",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "time",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "before",
        ty: "any",
        kind: @component.FAny,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "after",
        ty: "any",
        kind: @component.FAny,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "hasAfter",
        ty: "bool",
        kind: @component.FBool,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the ActivityLog component.
///
/// Declared as `interface activity-log` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ActivityLogState {
  events : Array[@tutuca.Value]
  hasEvents : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ActivityLogState::zero(), label: "Counter" }
fn ActivityLogState::zero() -> ActivityLogState {
  ActivityLogState::{ events: [], hasEvents: false }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn activity_log_zero_args() -> Map[String, @tutuca.Value] {
  activity_log_encode(ActivityLogState::zero())
}

///|
/// Every field the ActivityLog state declares — typed field names for tests and inspectors.
pub(all) enum ActivityLogField {
  Events
  HasEvents
} derive(Eq, @debug.Debug)

///|
pub fn ActivityLogField::name(self : ActivityLogField) -> String {
  match self {
    Events => "events"
    HasEvents => "hasEvents"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn ActivityLogField::kind(self : ActivityLogField) -> @component.FieldKind {
  match self {
    Events => @component.FList
    HasEvents => @component.FBool
  }
}

///|
/// Every field, in declaration order.
pub let activity_log_field_names : Array[String] = ["events", "hasEvents"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let activity_log_schema_fingerprint : String = "f3de3a72eae2fe61"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn activity_log_encode(s : ActivityLogState) -> Map[String, @tutuca.Value] {
  {
    "events": (List(s.events.map(x => x)) : @tutuca.Value),
    "hasEvents": Bool(s.hasEvents),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn activity_log_decode(f : Map[String, @tutuca.Value]) -> ActivityLogState? {
  guard f.get("events") is Some(t1_events) else { return None }
  guard t1_events is List(u1_events) else { return None }
  let v_events : Array[@tutuca.Value] = []
  for e1_events in u1_events {
    let x1_events = e1_events
    v_events.push(x1_events)
  }
  guard f.get("hasEvents") is Some(Bool(v_hasEvents)) else { return None }
  Some(ActivityLogState::{ events: v_events, hasEvents: v_hasEvents })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum ActivityLogReceive {
  AppendEntry(@tutuca.Value)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn ActivityLogReceive::from_dispatch(
  d : @component.Dispatch,
) -> ActivityLogReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("appendEntry", [a0]) => AppendEntry(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn ActivityLogReceive::to_dispatch(
  self : ActivityLogReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    AppendEntry(a0) => ("appendEntry", [a0])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn ActivityLogReceive::send(
  self : ActivityLogReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The views declared in activity.html.
pub(all) enum ActivityLogView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ActivityLogView::name(self : ActivityLogView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let activity_log_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let activity_log_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn activity_log_schema() -> @component.SchemaInfo {
  {
    fingerprint: "f3de3a72eae2fe61",
    fields: [
      {
        name: "events",
        ty: "list<any>",
        kind: @component.FList,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "hasEvents",
        ty: "bool",
        kind: @component.FBool,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: ["appendEntry"],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}
