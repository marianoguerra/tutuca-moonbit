// GENERATED by `tutuca gen-views` from state_editor.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views state_editor.html --name StateEditor --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the StateEditorRow component.
///
/// Declared as `interface state-editor-row` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct StateEditorRowState {
  name : String
  type_ : String
  kind : String
  text : String
  num : Double
  flag : Bool
  readonly_ : String
  editable : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..StateEditorRowState::zero(), label: "Counter" }
pub fn StateEditorRowState::zero() -> StateEditorRowState {
  StateEditorRowState::{
    name: "",
    type_: "",
    kind: "",
    text: "",
    num: 0.0,
    flag: false,
    readonly_: "",
    editable: false,
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for StateEditorRowState with fn encode(s) {
  {
    "name": Str(s.name),
    "type": Str(s.type_),
    "kind": Str(s.kind),
    "text": Str(s.text),
    "num": Num(s.num),
    "flag": Bool(s.flag),
    "readonly": Str(s.readonly_),
    "editable": Bool(s.editable),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for StateEditorRowState with fn decode(f) {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("type") is Some(Str(v_type_)) else { return None }
  guard f.get("kind") is Some(Str(v_kind)) else { return None }
  guard f.get("text") is Some(Str(v_text)) else { return None }
  guard f.get("num") is Some(Num(v_num)) else { return None }
  guard f.get("flag") is Some(Bool(v_flag)) else { return None }
  guard f.get("readonly") is Some(Str(v_readonly_)) else { return None }
  guard f.get("editable") is Some(Bool(v_editable)) else { return None }
  Some(StateEditorRowState::{
    name: v_name,
    type_: v_type_,
    kind: v_kind,
    text: v_text,
    num: v_num,
    flag: v_flag,
    readonly_: v_readonly_,
    editable: v_editable,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let state_editor_row_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let state_editor_row_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum StateEditorRowInput {
  Edit
} derive(Eq, @debug.Debug)

///|
pub fn StateEditorRowInput::name(self : StateEditorRowInput) -> String {
  match self {
    Edit => "edit"
  }
}

///|
pub fn StateEditorRowInput::parse(s : String) -> StateEditorRowInput? {
  match s {
    "edit" => Some(Edit)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to state_editor.html breaks every `match` over this type
/// until it is handled.
pub(all) enum StateEditorRowMsg {
  /// `@on.input="edit …"`
  Edit(@tutuca.Value)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn StateEditorRowMsg::from_dispatch(
  d : @component.Dispatch,
) -> StateEditorRowMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("edit", [a0]) => Edit(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for StateEditorRowState with fn schema() {
  @component.SchemaInfo::new(
    name="StateEditorRow",
    fingerprint="b3b1eecbb80cba4e",
    fields=[
      @component.FieldInfo::new("name", "string", FText),
      @component.FieldInfo::new("type", "string", FText),
      @component.FieldInfo::new("kind", "string", FText),
      @component.FieldInfo::new("text", "string", FText),
      @component.FieldInfo::new("num", "f64", FFloat),
      @component.FieldInfo::new("flag", "bool", FBool),
      @component.FieldInfo::new("readonly", "string", FText),
      @component.FieldInfo::new("editable", "bool", FBool),
    ],
    inputs=["edit"],
    view_names=["main"],
  )
}

///|
/// The state of the StateEditor component.
///
/// Declared as `interface state-editor` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct StateEditorState {
  target : @tutuca.Value
  rows : Array[@tutuca.Value]
  name : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..StateEditorState::zero(), label: "Counter" }
pub fn StateEditorState::zero() -> StateEditorState {
  StateEditorState::{ target: Null, rows: [], name: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for StateEditorState with fn encode(s) {
  {
    "target": s.target,
    "rows": (List(s.rows.map(x => x)) : @tutuca.Value),
    "name": Str(s.name),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for StateEditorState with fn decode(f) {
  let v_target = f.get("target").unwrap_or(Null)
  guard f.get("rows") is Some(t1_rows) else { return None }
  guard t1_rows is List(u1_rows) else { return None }
  let v_rows : Array[@tutuca.Value] = []
  for e1_rows in u1_rows {
    let x1_rows = e1_rows
    v_rows.push(x1_rows)
  }
  guard f.get("name") is Some(Str(v_name)) else { return None }
  Some(StateEditorState::{ target: v_target, rows: v_rows, name: v_name })
}

///|
/// The file-level style — pass as `common_style~`.
pub let state_editor_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let state_editor_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for StateEditorState with fn schema() {
  @component.SchemaInfo::new(
    name="StateEditor",
    fingerprint="1bfad85c93f3a53d",
    fields=[
      @component.FieldInfo::new("target", "any", FAny, elem="any"),
      @component.FieldInfo::new("rows", "list<any>", FList, elem="any"),
      @component.FieldInfo::new("name", "string", FText),
    ],
    view_names=["main"],
  )
}
