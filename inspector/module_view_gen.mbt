// GENERATED by `tutuca gen-views` from module.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views module.html --name Module --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// A sample descriptor exercising every summary channel (RichSample in
/// component-inspector.dev.js). Only introspected, never rendered — its
/// point is that every panel of the component inspector has something to
/// show, which is why it declares one of everything.
///
/// Declared as `interface rich-sample` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct RichSampleState {
  title : String
  count : Int
  tags : Array[@tutuca.Value]
  open : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..RichSampleState::zero(), label: "Counter" }
fn RichSampleState::zero() -> RichSampleState {
  RichSampleState::{ title: "", count: 0, tags: [], open: false }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn rich_sample_zero_args() -> Map[String, @tutuca.Value] {
  rich_sample_encode(RichSampleState::zero())
}

///|
/// Every field the RichSample state declares — typed field names for tests and inspectors.
pub(all) enum RichSampleField {
  Title
  Count
  Tags
  Open
} derive(Eq, @debug.Debug)

///|
pub fn RichSampleField::name(self : RichSampleField) -> String {
  match self {
    Title => "title"
    Count => "count"
    Tags => "tags"
    Open => "open"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn RichSampleField::kind(self : RichSampleField) -> @component.FieldKind {
  match self {
    Title => @component.FText
    Count => @component.FInt
    Tags => @component.FList
    Open => @component.FBool
  }
}

///|
/// Every field, in declaration order.
pub let rich_sample_field_names : Array[String] = [
  "title", "count", "tags", "open",
]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let rich_sample_schema_fingerprint : String = "98c5770c79268b31"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn rich_sample_encode(s : RichSampleState) -> Map[String, @tutuca.Value] {
  {
    "title": Str(s.title),
    "count": Num(s.count.to_double()),
    "tags": (List(s.tags.map(x => x)) : @tutuca.Value),
    "open": Bool(s.open),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn rich_sample_decode(f : Map[String, @tutuca.Value]) -> RichSampleState? {
  guard f.get("title") is Some(Str(v_title)) else { return None }
  guard f.get("count") is Some(Num(t1_count)) else { return None }
  let v_count = t1_count.to_int()
  guard f.get("tags") is Some(t1_tags) else { return None }
  guard t1_tags is List(u1_tags) else { return None }
  let v_tags : Array[@tutuca.Value] = []
  for e1_tags in u1_tags {
    let x1_tags = e1_tags
    v_tags.push(x1_tags)
  }
  guard f.get("open") is Some(Bool(v_open)) else { return None }
  Some(RichSampleState::{
    title: v_title,
    count: v_count,
    tags: v_tags,
    open: v_open,
  })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum RichSampleReceive {
  Ping
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn RichSampleReceive::from_dispatch(
  d : @component.Dispatch,
) -> RichSampleReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("ping", []) => Ping
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn RichSampleReceive::to_dispatch(
  self : RichSampleReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    Ping => ("ping", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn RichSampleReceive::send(
  self : RichSampleReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The Bubble arm of @component.Dispatch, decoded.
///
/// Declared as `variant bubble` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum RichSampleBubble {
  OnChild
  /// A bubble the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Bubble dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn RichSampleBubble::from_dispatch(
  d : @component.Dispatch,
) -> RichSampleBubble? {
  guard d is Bubble(name, args) else { return None }
  Some(
    match (name, args) {
      ("onChild", []) => OnChild
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn RichSampleBubble::to_dispatch(
  self : RichSampleBubble,
) -> (String, Array[@tutuca.Value]) {
  match self {
    OnChild => ("onChild", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Bubble this message to the parent component.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn RichSampleBubble::bubble(
  self : RichSampleBubble,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.bubble(name, args)
}

///|
/// The Response arm of @component.Dispatch, decoded.
///
/// Declared as `variant response` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum RichSampleResponse {
  OnData
  /// A response the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Response dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn RichSampleResponse::from_dispatch(
  d : @component.Dispatch,
) -> RichSampleResponse? {
  guard d is Response(name, args) else { return None }
  Some(
    match (name, args) {
      ("onData", []) => OnData
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn RichSampleResponse::to_dispatch(
  self : RichSampleResponse,
) -> (String, Array[@tutuca.Value]) {
  match self {
    OnData => ("onData", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// The views declared in module.html.
pub(all) enum RichSampleView {
  Main
  Compact
} derive(Eq, @debug.Debug)

///|
pub fn RichSampleView::name(self : RichSampleView) -> String {
  match self {
    Main => "main"
    Compact => "compact"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let rich_sample_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let rich_sample_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum RichSampleInput {
  Inc
  Reset
} derive(Eq, @debug.Debug)

///|
pub fn RichSampleInput::name(self : RichSampleInput) -> String {
  match self {
    Inc => "inc"
    Reset => "reset"
  }
}

///|
pub fn RichSampleInput::parse(s : String) -> RichSampleInput? {
  match s {
    "inc" => Some(Inc)
    "reset" => Some(Reset)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to module.html breaks every `match` over this type
/// until it is handled.
pub(all) enum RichSampleMsg {
  /// `@on.click="inc …"`
  Inc
  /// `@on.click="reset …"`
  Reset
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn RichSampleMsg::from_dispatch(d : @component.Dispatch) -> RichSampleMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("inc", []) => Inc
      ("reset", []) => Reset
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] rich_sample_swap(
  f : (RichSampleInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([Inc, Reset] : Array[RichSampleInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `$name` the views call.
pub(all) enum RichSampleMethod {
  Double
  Label
} derive(Eq, @debug.Debug)

///|
pub fn RichSampleMethod::name(self : RichSampleMethod) -> String {
  match self {
    Double => "double"
    Label => "label"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] rich_sample_compute(
  f : (RichSampleMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([Double, Label] : Array[RichSampleMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `@loop-with` handler the views name.
pub(all) enum RichSampleLoopWith {
  Rows
} derive(Eq, @debug.Debug)

///|
pub fn RichSampleLoopWith::name(self : RichSampleLoopWith) -> String {
  match self {
    Rows => "rows"
  }
}

///|
/// Build `loop_with~` from an exhaustive match over the names the views use.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] rich_sample_loop_with(
  f : (RichSampleLoopWith) -> ((S, @tutuca.Value, @component.LoopCtx) -> @component.LoopWith)?,
) -> Map[String, (S, @tutuca.Value, @component.LoopCtx) -> @component.LoopWith] {
  let out : Map[
    String,
    (S, @tutuca.Value, @component.LoopCtx) -> @component.LoopWith,
  ] = Map([])
  for m in ([Rows] : Array[RichSampleLoopWith]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn rich_sample_schema() -> @component.SchemaInfo {
  {
    fingerprint: "98c5770c79268b31",
    fields: [
      {
        name: "title",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "count",
        ty: "s32",
        kind: @component.FInt,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "tags",
        ty: "list<any>",
        kind: @component.FList,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "open",
        ty: "bool",
        kind: @component.FBool,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: ["inc", "reset"],
    receives: ["ping"],
    bubbles: ["onChild"],
    responses: ["onData"],
    methods: ["double", "label"],
    ids: [],
    view_names: ["main", "compact"],
    init_names: [],
  }
}

///|
/// The state of the SampleCard component.
///
/// Declared as `interface sample-card` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct SampleCardState {
  title : String
  count : Int
  tags : Array[@tutuca.Value]
  open : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SampleCardState::zero(), label: "Counter" }
fn SampleCardState::zero() -> SampleCardState {
  SampleCardState::{ title: "", count: 0, tags: [], open: false }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn sample_card_zero_args() -> Map[String, @tutuca.Value] {
  sample_card_encode(SampleCardState::zero())
}

///|
/// Every field the SampleCard state declares — typed field names for tests and inspectors.
pub(all) enum SampleCardField {
  Title
  Count
  Tags
  Open
} derive(Eq, @debug.Debug)

///|
pub fn SampleCardField::name(self : SampleCardField) -> String {
  match self {
    Title => "title"
    Count => "count"
    Tags => "tags"
    Open => "open"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn SampleCardField::kind(self : SampleCardField) -> @component.FieldKind {
  match self {
    Title => @component.FText
    Count => @component.FInt
    Tags => @component.FList
    Open => @component.FBool
  }
}

///|
/// Every field, in declaration order.
pub let sample_card_field_names : Array[String] = [
  "title", "count", "tags", "open",
]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let sample_card_schema_fingerprint : String = "33fd8877c57069f1"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn sample_card_encode(s : SampleCardState) -> Map[String, @tutuca.Value] {
  {
    "title": Str(s.title),
    "count": Num(s.count.to_double()),
    "tags": (List(s.tags.map(x => x)) : @tutuca.Value),
    "open": Bool(s.open),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn sample_card_decode(f : Map[String, @tutuca.Value]) -> SampleCardState? {
  guard f.get("title") is Some(Str(v_title)) else { return None }
  guard f.get("count") is Some(Num(t1_count)) else { return None }
  let v_count = t1_count.to_int()
  guard f.get("tags") is Some(t1_tags) else { return None }
  guard t1_tags is List(u1_tags) else { return None }
  let v_tags : Array[@tutuca.Value] = []
  for e1_tags in u1_tags {
    let x1_tags = e1_tags
    v_tags.push(x1_tags)
  }
  guard f.get("open") is Some(Bool(v_open)) else { return None }
  Some(SampleCardState::{
    title: v_title,
    count: v_count,
    tags: v_tags,
    open: v_open,
  })
}

///|
/// The views declared in module.html.
pub(all) enum SampleCardView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SampleCardView::name(self : SampleCardView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let sample_card_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let sample_card_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum SampleCardInput {
  Bump
} derive(Eq, @debug.Debug)

///|
pub fn SampleCardInput::name(self : SampleCardInput) -> String {
  match self {
    Bump => "bump"
  }
}

///|
pub fn SampleCardInput::parse(s : String) -> SampleCardInput? {
  match s {
    "bump" => Some(Bump)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to module.html breaks every `match` over this type
/// until it is handled.
pub(all) enum SampleCardMsg {
  /// `@on.click="bump …"`
  Bump
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn SampleCardMsg::from_dispatch(d : @component.Dispatch) -> SampleCardMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("bump", []) => Bump
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] sample_card_swap(
  f : (SampleCardInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([Bump] : Array[SampleCardInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `$name` the views call.
pub(all) enum SampleCardMethod {
  Label
} derive(Eq, @debug.Debug)

///|
pub fn SampleCardMethod::name(self : SampleCardMethod) -> String {
  match self {
    Label => "label"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] sample_card_compute(
  f : (SampleCardMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([Label] : Array[SampleCardMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn sample_card_schema() -> @component.SchemaInfo {
  {
    fingerprint: "33fd8877c57069f1",
    fields: [
      {
        name: "title",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "count",
        ty: "s32",
        kind: @component.FInt,
        elem: None,
        slot: None,
        members: [],
      },
      {
        name: "tags",
        ty: "list<any>",
        kind: @component.FList,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "open",
        ty: "bool",
        kind: @component.FBool,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: ["bump"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: ["label"],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}
