// GENERATED by `tutuca gen-views` from component_inspector.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views component_inspector.html --name ComponentInspector --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the CompName component.
///
/// Declared as `interface comp-name` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct CompNameState {
  name : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..CompNameState::zero(), label: "Counter" }
pub fn CompNameState::zero() -> CompNameState {
  CompNameState::{ name: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for CompNameState with fn encode(s) {
  { "name": Str(s.name) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for CompNameState with fn decode(f) {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  Some(CompNameState::{ name: v_name })
}

///|
/// The file-level style — pass as `common_style~`.
pub let comp_name_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let comp_name_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for CompNameState with fn schema() {
  @component.SchemaInfo::new(
    name="CompName",
    fingerprint="57b45308f862e883",
    fields=[@component.FieldInfo::new("name", "string", FText)],
    view_names=["main"],
  )
}

///|
/// The state of the CompField component.
///
/// Declared as `interface comp-field` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct CompFieldState {
  name : String
  typeName : String
  child : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..CompFieldState::zero(), label: "Counter" }
pub fn CompFieldState::zero() -> CompFieldState {
  CompFieldState::{ name: "", typeName: "", child: Null }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for CompFieldState with fn encode(s) {
  { "name": Str(s.name), "typeName": Str(s.typeName), "child": s.child }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for CompFieldState with fn decode(f) {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("typeName") is Some(Str(v_typeName)) else { return None }
  let v_child = f.get("child").unwrap_or(Null)
  Some(CompFieldState::{ name: v_name, typeName: v_typeName, child: v_child })
}

///|
/// The file-level style — pass as `common_style~`.
pub let comp_field_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let comp_field_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for CompFieldState with fn schema() {
  @component.SchemaInfo::new(
    name="CompField",
    fingerprint="b1ab3e9e7437de19",
    fields=[
      @component.FieldInfo::new("name", "string", FText),
      @component.FieldInfo::new("typeName", "string", FText),
      @component.FieldInfo::new("child", "any", FAny, elem="any"),
    ],
    view_names=["main"],
  )
}

///|
/// The state of the CompView component.
///
/// Declared as `interface comp-view` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct CompViewState {
  name : String
  rawView : String
  isExpanded : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..CompViewState::zero(), label: "Counter" }
pub fn CompViewState::zero() -> CompViewState {
  CompViewState::{ name: "", rawView: "", isExpanded: false }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for CompViewState with fn encode(s) {
  {
    "name": Str(s.name),
    "rawView": Str(s.rawView),
    "isExpanded": Bool(s.isExpanded),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for CompViewState with fn decode(f) {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("rawView") is Some(Str(v_rawView)) else { return None }
  guard f.get("isExpanded") is Some(Bool(v_isExpanded)) else { return None }
  Some(CompViewState::{
    name: v_name,
    rawView: v_rawView,
    isExpanded: v_isExpanded,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let comp_view_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let comp_view_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum CompViewInput {
  Toggle
} derive(Eq, @debug.Debug)

///|
pub fn CompViewInput::name(self : CompViewInput) -> String {
  match self {
    Toggle => "toggle"
  }
}

///|
pub fn CompViewInput::parse(s : String) -> CompViewInput? {
  match s {
    "toggle" => Some(Toggle)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to component_inspector.html breaks every `match` over this type
/// until it is handled.
pub(all) enum CompViewMsg {
  /// `@on.click="toggle …"`
  Toggle(Bool)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn CompViewMsg::from_dispatch(d : @component.Dispatch) -> CompViewMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("toggle", [Bool(a0)]) => Toggle(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum CompViewMethod {
  ArrowText
} derive(Eq, @debug.Debug)

///|
pub fn CompViewMethod::name(self : CompViewMethod) -> String {
  match self {
    ArrowText => "arrowText"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for CompViewState with fn schema() {
  @component.SchemaInfo::new(
    name="CompView",
    fingerprint="a2bb4d5d672ad06a",
    fields=[
      @component.FieldInfo::new("name", "string", FText),
      @component.FieldInfo::new("rawView", "string", FText),
      @component.FieldInfo::new("isExpanded", "bool", FBool),
    ],
    inputs=["toggle"],
    methods=["arrowText"],
    view_names=["main"],
  )
}

///|
/// The state of the CompSection component.
///
/// Declared as `interface comp-section` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct CompSectionState {
  items : Array[@tutuca.Value]
  isExpanded : Bool
  itemsPerPage : Int
  currentPage : Int
  label : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..CompSectionState::zero(), label: "Counter" }
pub fn CompSectionState::zero() -> CompSectionState {
  CompSectionState::{
    items: [],
    isExpanded: false,
    itemsPerPage: 0,
    currentPage: 0,
    label: "",
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for CompSectionState with fn encode(s) {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "isExpanded": Bool(s.isExpanded),
    "itemsPerPage": Num(s.itemsPerPage.to_double()),
    "currentPage": Num(s.currentPage.to_double()),
    "label": Str(s.label),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for CompSectionState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("isExpanded") is Some(Bool(v_isExpanded)) else { return None }
  guard f.get("itemsPerPage") is Some(Num(t1_itemsPerPage)) else { return None }
  let v_itemsPerPage = t1_itemsPerPage.to_int()
  guard f.get("currentPage") is Some(Num(t1_currentPage)) else { return None }
  let v_currentPage = t1_currentPage.to_int()
  guard f.get("label") is Some(Str(v_label)) else { return None }
  Some(CompSectionState::{
    items: v_items,
    isExpanded: v_isExpanded,
    itemsPerPage: v_itemsPerPage,
    currentPage: v_currentPage,
    label: v_label,
  })
}

///|
/// The file-level style — pass as `common_style~`.
pub let comp_section_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let comp_section_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum CompSectionInput {
  NextPage
  PrevPage
  Toggle
} derive(Eq, @debug.Debug)

///|
pub fn CompSectionInput::name(self : CompSectionInput) -> String {
  match self {
    NextPage => "nextPage"
    PrevPage => "prevPage"
    Toggle => "toggle"
  }
}

///|
pub fn CompSectionInput::parse(s : String) -> CompSectionInput? {
  match s {
    "nextPage" => Some(NextPage)
    "prevPage" => Some(PrevPage)
    "toggle" => Some(Toggle)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to component_inspector.html breaks every `match` over this type
/// until it is handled.
pub(all) enum CompSectionMsg {
  /// `@on.click="nextPage …"`
  NextPage
  /// `@on.click="prevPage …"`
  PrevPage
  /// `@on.click="toggle …"`
  Toggle(Bool)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn CompSectionMsg::from_dispatch(
  d : @component.Dispatch,
) -> CompSectionMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("nextPage", []) => NextPage
      ("prevPage", []) => PrevPage
      ("toggle", [Bool(a0)]) => Toggle(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum CompSectionMethod {
  ArrowText
  CannotNextPage
  CannotPrevPage
  CountText
  IsItemsEmpty
  PageIndicatorText
  ShowPagination
  TypeText
} derive(Eq, @debug.Debug)

///|
pub fn CompSectionMethod::name(self : CompSectionMethod) -> String {
  match self {
    ArrowText => "arrowText"
    CannotNextPage => "cannotNextPage"
    CannotPrevPage => "cannotPrevPage"
    CountText => "countText"
    IsItemsEmpty => "isItemsEmpty"
    PageIndicatorText => "pageIndicatorText"
    ShowPagination => "showPagination"
    TypeText => "typeText"
  }
}

///|
/// Every `@loop-with` handler the views name.
pub(all) enum CompSectionLoopWith {
  GetPageRange
} derive(Eq, @debug.Debug)

///|
pub fn CompSectionLoopWith::name(self : CompSectionLoopWith) -> String {
  match self {
    GetPageRange => "getPageRange"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for CompSectionState with fn schema() {
  @component.SchemaInfo::new(
    name="CompSection",
    fingerprint="fac650bb4a858759",
    fields=[
      @component.FieldInfo::new("items", "list<any>", FList, elem="any"),
      @component.FieldInfo::new("isExpanded", "bool", FBool),
      @component.FieldInfo::new("itemsPerPage", "s32", FInt),
      @component.FieldInfo::new("currentPage", "s32", FInt),
      @component.FieldInfo::new("label", "string", FText),
    ],
    inputs=["nextPage", "prevPage", "toggle"],
    methods=[
      "arrowText", "cannotNextPage", "cannotPrevPage", "countText", "isItemsEmpty",
      "pageIndicatorText", "showPagination", "typeText",
    ],
    view_names=["main"],
  )
}

///|
/// The state of the ComponentInspector component.
///
/// Declared as `interface component-inspector` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ComponentInspectorState {
  compName : String
  compId : Int
  sections : Array[@tutuca.Value]
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ComponentInspectorState::zero(), label: "Counter" }
pub fn ComponentInspectorState::zero() -> ComponentInspectorState {
  ComponentInspectorState::{ compName: "", compId: 0, sections: [] }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ComponentInspectorState with fn encode(s) {
  {
    "compName": Str(s.compName),
    "compId": Num(s.compId.to_double()),
    "sections": (List(s.sections.map(x => x)) : @tutuca.Value),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ComponentInspectorState with fn decode(f) {
  guard f.get("compName") is Some(Str(v_compName)) else { return None }
  guard f.get("compId") is Some(Num(t1_compId)) else { return None }
  let v_compId = t1_compId.to_int()
  guard f.get("sections") is Some(t1_sections) else { return None }
  guard t1_sections is List(u1_sections) else { return None }
  let v_sections : Array[@tutuca.Value] = []
  for e1_sections in u1_sections {
    let x1_sections = e1_sections
    v_sections.push(x1_sections)
  }
  Some(ComponentInspectorState::{
    compName: v_compName,
    compId: v_compId,
    sections: v_sections,
  })
}

///|
/// The Bubble arm of @component.Dispatch, decoded.
///
/// Declared as `variant bubble` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum ComponentInspectorBubble {
  ToggleAllSections(Bool)
  ToggleAllViews(Bool)
  /// A bubble the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Bubble dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn ComponentInspectorBubble::from_dispatch(
  d : @component.Dispatch,
) -> ComponentInspectorBubble? {
  guard d is Bubble(name, args) else { return None }
  Some(
    match (name, args) {
      ("toggleAllSections", [Bool(a0)]) => ToggleAllSections(a0)
      ("toggleAllViews", [Bool(a0)]) => ToggleAllViews(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn ComponentInspectorBubble::to_dispatch(
  self : ComponentInspectorBubble,
) -> (String, Array[@tutuca.Value]) {
  match self {
    ToggleAllSections(a0) => ("toggleAllSections", [Bool(a0)])
    ToggleAllViews(a0) => ("toggleAllViews", [Bool(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Bubble this message to the parent component.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn ComponentInspectorBubble::bubble(
  self : ComponentInspectorBubble,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.bubble(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let component_inspector_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let component_inspector_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum ComponentInspectorMethod {
  IdText
} derive(Eq, @debug.Debug)

///|
pub fn ComponentInspectorMethod::name(
  self : ComponentInspectorMethod,
) -> String {
  match self {
    IdText => "idText"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ComponentInspectorState with fn schema() {
  @component.SchemaInfo::new(
    name="ComponentInspector",
    fingerprint="177c6b469255fb1e",
    fields=[
      @component.FieldInfo::new("compName", "string", FText),
      @component.FieldInfo::new("compId", "s32", FInt),
      @component.FieldInfo::new("sections", "list<any>", FList, elem="any"),
    ],
    bubbles=["toggleAllSections", "toggleAllViews"],
    methods=["idText"],
    view_names=["main"],
  )
}
