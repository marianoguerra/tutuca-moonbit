// GENERATED by `tutuca gen-views` from schema_viewer.html — DO NOT EDIT.
//
// The views of SchemaViewer, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing schema_viewer_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn schema_viewer_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "div",
      [
        @anode.attr(
          "class", "inline-flex flex-col items-start gap-1 schema-viewer",
        ),
      ],
      [
        @anode.h(
          "button",
          [
            @anode.attr("type", "button"),
            @anode.attr("class", "btn btn-xs btn-ghost self-end raw-toggle"),
            @anode.eid(0),
          ],
          [@anode.dyn_text(Method("toggleLabel"))],
        ),
        @anode.text(" "),
        @anode.hide(Field("showRaw"), @anode.render(0, Field("value")), id=1),
        @anode.text(" "),
        @anode.show(Field("showRaw"), @anode.render(2, Field("raw")), id=3),
      ],
    ),
    [[@anode.on("click", HandlerName(name="toggleShowRaw", ns=Input))]],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn schema_viewer_views() -> Map[String, @anode.View] {
  { "main": schema_viewer_main_view_ir() }
}

///|
/// The SchemaViewer component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `SchemaViewerState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn schema_viewer_component(
  name? : String = "SchemaViewer",
  views? : Map[String, @anode.View] = schema_viewer_views(),
  init? : SchemaViewerState = SchemaViewerState::zero(),
  common_style? : String = schema_viewer_common_style,
  global_style? : String = schema_viewer_global_style,
  update? : (SchemaViewerState, @component.Dispatch, &@tutuca.Ctx) -> SchemaViewerState?,
  compute? : (SchemaViewerMethod) -> ((SchemaViewerState, Array[@tutuca.Value]) -> @tutuca.Value)?,
  swap? : (SchemaViewerInput) -> ((
    SchemaViewerState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    compute=match compute {
      Some(f) => schema_viewer_compute(f)
      None => Map([])
    },
    swap=match swap {
      Some(f) => schema_viewer_swap(f)
      None => Map([])
    },
    specs~,
    provide~,
    lookup~,
  )
}
