// GENERATED by `tutuca gen-views` from instance_inspector.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views instance_inspector.html --name InstanceInspector --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the InstanceFields component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct InstanceFieldsState {
  items : Array[@tutuca.Value]
  isExpanded : Bool
  itemsPerPage : Int
  currentPage : Int
  typeName : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..InstanceFieldsState::zero(), label: "Counter" }
pub fn InstanceFieldsState::zero() -> InstanceFieldsState {
  {
    items: [],
    isExpanded: false,
    itemsPerPage: 0,
    currentPage: 0,
    typeName: "",
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for InstanceFieldsState with fn encode(s) {
  {
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "isExpanded": Bool(s.isExpanded),
    "itemsPerPage": Num(s.itemsPerPage.to_double()),
    "currentPage": Num(s.currentPage.to_double()),
    "typeName": Str(s.typeName),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for InstanceFieldsState with fn decode(f) {
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  guard f.get("isExpanded") is Some(Bool(v_isExpanded)) else { return None }
  guard f.get("itemsPerPage") is Some(Num(t1_itemsPerPage)) else { return None }
  let v_itemsPerPage = t1_itemsPerPage.to_int()
  guard f.get("currentPage") is Some(Num(t1_currentPage)) else { return None }
  let v_currentPage = t1_currentPage.to_int()
  guard f.get("typeName") is Some(Str(v_typeName)) else { return None }
  Some({
    items: v_items,
    isExpanded: v_isExpanded,
    itemsPerPage: v_itemsPerPage,
    currentPage: v_currentPage,
    typeName: v_typeName,
  })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum InstanceFieldsReceive {
  NextPage
  PrevPage
  ToggleIsExpanded
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn InstanceFieldsReceive::from_dispatch(
  d : @component.Dispatch,
) -> InstanceFieldsReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("nextPage", []) => NextPage
      ("prevPage", []) => PrevPage
      ("toggleIsExpanded", []) => ToggleIsExpanded
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn InstanceFieldsReceive::to_dispatch(
  self : InstanceFieldsReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    NextPage => ("nextPage", [])
    PrevPage => ("prevPage", [])
    ToggleIsExpanded => ("toggleIsExpanded", [])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn InstanceFieldsReceive::send(
  self : InstanceFieldsReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let instance_fields_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let instance_fields_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum InstanceFieldsInput {
  NextPage
  PrevPage
  ToggleIsExpanded
} derive(Eq, @debug.Debug)

///|
pub fn InstanceFieldsInput::name(self : InstanceFieldsInput) -> String {
  match self {
    NextPage => "nextPage"
    PrevPage => "prevPage"
    ToggleIsExpanded => "toggleIsExpanded"
  }
}

///|
pub fn InstanceFieldsInput::parse(s : String) -> InstanceFieldsInput? {
  match s {
    "nextPage" => Some(NextPage)
    "prevPage" => Some(PrevPage)
    "toggleIsExpanded" => Some(ToggleIsExpanded)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to instance_inspector.html breaks every `match` over this type
/// until it is handled.
pub(all) enum InstanceFieldsMsg {
  /// `@on.click="nextPage …"`
  NextPage
  /// `@on.click="prevPage …"`
  PrevPage
  /// `@on.click="toggleIsExpanded …"`
  ToggleIsExpanded
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn InstanceFieldsMsg::from_dispatch(
  d : @component.Dispatch,
) -> InstanceFieldsMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("nextPage", []) => NextPage
      ("prevPage", []) => PrevPage
      ("toggleIsExpanded", []) => ToggleIsExpanded
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum InstanceFieldsMethod {
  ArrowText
  CannotNextPage
  CannotPrevPage
  CountText
  IsItemsEmpty
  PageIndicatorText
  ShowPagination
  TypeText
} derive(Eq, @debug.Debug)

///|
pub fn InstanceFieldsMethod::name(self : InstanceFieldsMethod) -> String {
  match self {
    ArrowText => "arrowText"
    CannotNextPage => "cannotNextPage"
    CannotPrevPage => "cannotPrevPage"
    CountText => "countText"
    IsItemsEmpty => "isItemsEmpty"
    PageIndicatorText => "pageIndicatorText"
    ShowPagination => "showPagination"
    TypeText => "typeText"
  }
}

///|
/// Every `@loop-with` handler the views name.
pub(all) enum InstanceFieldsLoopWith {
  GetPageRange
} derive(Eq, @debug.Debug)

///|
pub fn InstanceFieldsLoopWith::name(self : InstanceFieldsLoopWith) -> String {
  match self {
    GetPageRange => "getPageRange"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for InstanceFieldsState with fn schema() {
  @component.SchemaInfo::new(
    name="InstanceFields",
    fingerprint="250e84a7d398d710",
    fields=[
      @component.FieldInfo::new("items", TyList(TyAny)),
      @component.FieldInfo::new("isExpanded", TyBool),
      @component.FieldInfo::new("itemsPerPage", TyInt),
      @component.FieldInfo::new("currentPage", TyInt),
      @component.FieldInfo::new("typeName", TyText),
    ],
    inputs=["nextPage", "prevPage", "toggleIsExpanded"],
    methods=[
      "arrowText", "cannotNextPage", "cannotPrevPage", "countText", "isItemsEmpty",
      "pageIndicatorText", "showPagination", "typeText",
    ],
    view_names=["main"],
  )
}

///|
/// The state of the InstanceInspector component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct InstanceInspectorState {
  value : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..InstanceInspectorState::zero(), label: "Counter" }
pub fn InstanceInspectorState::zero() -> InstanceInspectorState {
  { value: Null }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for InstanceInspectorState with fn encode(s) {
  { "value": s.value }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for InstanceInspectorState with fn decode(f) {
  let v_value = f.get("value").unwrap_or(Null)
  Some({ value: v_value })
}

///|
/// The file-level style — pass as `common_style~`.
pub let instance_inspector_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let instance_inspector_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for InstanceInspectorState with fn schema() {
  @component.SchemaInfo::new(
    name="InstanceInspector",
    fingerprint="7671eca5ec353551",
    fields=[@component.FieldInfo::new("value", TyAny)],
    view_names=["main"],
  )
}

///|
/// The state of the InstanceExplorer component.
///
/// Declared as `state` in the state block. Adding a field there adds it
/// here, so a view that reads it type-checks and one that reads a name
/// this struct lacks fails generation.
pub(all) struct InstanceExplorerState {
  activeTab : String
  instanceView : @tutuca.Value
  componentView : @tutuca.Value
  hasComponent : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// The state block declares no defaults — a default is a value, not a
/// type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..InstanceExplorerState::zero(), label: "Counter" }
pub fn InstanceExplorerState::zero() -> InstanceExplorerState {
  {
    activeTab: "",
    instanceView: Null,
    componentView: Null,
    hasComponent: false,
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for InstanceExplorerState with fn encode(s) {
  {
    "activeTab": Str(s.activeTab),
    "instanceView": s.instanceView,
    "componentView": s.componentView,
    "hasComponent": Bool(s.hasComponent),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for InstanceExplorerState with fn decode(f) {
  guard f.get("activeTab") is Some(Str(v_activeTab)) else { return None }
  let v_instanceView = f.get("instanceView").unwrap_or(Null)
  let v_componentView = f.get("componentView").unwrap_or(Null)
  guard f.get("hasComponent") is Some(Bool(v_hasComponent)) else { return None }
  Some({
    activeTab: v_activeTab,
    instanceView: v_instanceView,
    componentView: v_componentView,
    hasComponent: v_hasComponent,
  })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum InstanceExplorerReceive {
  SetActiveTab(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn InstanceExplorerReceive::from_dispatch(
  d : @component.Dispatch,
) -> InstanceExplorerReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("setActiveTab", [Str(a0)]) => SetActiveTab(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn InstanceExplorerReceive::to_dispatch(
  self : InstanceExplorerReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    SetActiveTab(a0) => ("setActiveTab", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn InstanceExplorerReceive::send(
  self : InstanceExplorerReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let instance_explorer_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let instance_explorer_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum InstanceExplorerInput {
  SetActiveTab
} derive(Eq, @debug.Debug)

///|
pub fn InstanceExplorerInput::name(self : InstanceExplorerInput) -> String {
  match self {
    SetActiveTab => "setActiveTab"
  }
}

///|
pub fn InstanceExplorerInput::parse(s : String) -> InstanceExplorerInput? {
  match s {
    "setActiveTab" => Some(SetActiveTab)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to instance_inspector.html breaks every `match` over this type
/// until it is handled.
pub(all) enum InstanceExplorerMsg {
  /// `@on.click="setActiveTab …"`
  SetActiveTab(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn InstanceExplorerMsg::from_dispatch(
  d : @component.Dispatch,
) -> InstanceExplorerMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setActiveTab", [Str(a0)]) => SetActiveTab(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for InstanceExplorerState with fn schema() {
  @component.SchemaInfo::new(
    name="InstanceExplorer",
    fingerprint="eef9b127adea0340",
    fields=[
      @component.FieldInfo::new("activeTab", TyText),
      @component.FieldInfo::new("instanceView", TyAny),
      @component.FieldInfo::new("componentView", TyAny),
      @component.FieldInfo::new("hasComponent", TyBool),
    ],
    inputs=["setActiveTab"],
    view_names=["main"],
  )
}
