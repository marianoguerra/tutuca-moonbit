// GENERATED by `tutuca gen-views` from schema_viewer.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views schema_viewer.html --name SchemaViewer --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the SchemaViewer component.
///
/// Declared as `interface schema-viewer` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
priv struct SchemaViewerState {
  value : @tutuca.Value
  raw : @tutuca.Value
  rawSchema : @tutuca.Value
  showRaw : Bool
} derive(ToJson, FromJson)

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..SchemaViewerState::zero(), label: "Counter" }
fn SchemaViewerState::zero() -> SchemaViewerState {
  SchemaViewerState::{ value: Null, raw: Null, rawSchema: Null, showRaw: false }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn schema_viewer_zero_args() -> Map[String, @tutuca.Value] {
  match @tutuca.Value::from_json(ToJson::to_json(SchemaViewerState::zero())) {
    Map(m) => m
    _ => Map([])
  }
}

///|
/// Every field the SchemaViewer state declares — typed field names for tests and inspectors.
pub(all) enum SchemaViewerField {
  Value
  Raw
  RawSchema
  ShowRaw
} derive(Eq, @debug.Debug)

///|
pub fn SchemaViewerField::name(self : SchemaViewerField) -> String {
  match self {
    Value => "value"
    Raw => "raw"
    RawSchema => "rawSchema"
    ShowRaw => "showRaw"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn SchemaViewerField::kind(
  self : SchemaViewerField,
) -> @component.FieldKind {
  match self {
    Value => @component.FAny
    Raw => @component.FAny
    RawSchema => @component.FAny
    ShowRaw => @component.FBool
  }
}

///|
/// Every field, in declaration order.
pub let schema_viewer_field_names : Array[String] = [
  "value", "raw", "rawSchema", "showRaw",
]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let schema_viewer_schema_fingerprint : String = "ec51fa7271aa7a98"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through: it is exactly what `with_value_stash` exists to rescue from
/// the JSON path, and this way it never enters one.
fn schema_viewer_encode(s : SchemaViewerState) -> Map[String, @tutuca.Value] {
  {
    "value": s.value,
    "raw": s.raw,
    "rawSchema": s.rawSchema,
    "showRaw": Bool(s.showRaw),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn schema_viewer_decode(f : Map[String, @tutuca.Value]) -> SchemaViewerState? {
  let v_value = f.get("value").unwrap_or(Null)
  let v_raw = f.get("raw").unwrap_or(Null)
  let v_rawSchema = f.get("rawSchema").unwrap_or(Null)
  guard f.get("showRaw") is Some(Bool(v_showRaw)) else { return None }
  Some(SchemaViewerState::{
    value: v_value,
    raw: v_raw,
    rawSchema: v_rawSchema,
    showRaw: v_showRaw,
  })
}

///|
/// The views declared in schema_viewer.html.
pub(all) enum SchemaViewerView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn SchemaViewerView::name(self : SchemaViewerView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let schema_viewer_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let schema_viewer_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum SchemaViewerInput {
  ToggleShowRaw
} derive(Eq, @debug.Debug)

///|
pub fn SchemaViewerInput::name(self : SchemaViewerInput) -> String {
  match self {
    ToggleShowRaw => "toggleShowRaw"
  }
}

///|
pub fn SchemaViewerInput::parse(s : String) -> SchemaViewerInput? {
  match s {
    "toggleShowRaw" => Some(ToggleShowRaw)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to schema_viewer.html breaks every `match` over this type
/// until it is handled.
pub(all) enum SchemaViewerMsg {
  /// `@on.click="toggleShowRaw …"`
  ToggleShowRaw
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn SchemaViewerMsg::from_dispatch(
  d : @component.Dispatch,
) -> SchemaViewerMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("toggleShowRaw", []) => ToggleShowRaw
      _ => Unknown(name, args)
    },
  )
}

///|
/// Build `swap~` from an exhaustive match over the `@on` names the views raise.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] schema_viewer_swap(
  f : (SchemaViewerInput) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([ToggleShowRaw] : Array[SchemaViewerInput]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Every `$name` the views call.
pub(all) enum SchemaViewerMethod {
  ToggleLabel
} derive(Eq, @debug.Debug)

///|
pub fn SchemaViewerMethod::name(self : SchemaViewerMethod) -> String {
  match self {
    ToggleLabel => "toggleLabel"
  }
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] schema_viewer_compute(
  f : (SchemaViewerMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([ToggleLabel] : Array[SchemaViewerMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn schema_viewer_schema() -> @component.SchemaInfo {
  {
    fingerprint: "ec51fa7271aa7a98",
    fields: [
      {
        name: "value",
        ty: "any",
        kind: @component.FAny,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "raw",
        ty: "any",
        kind: @component.FAny,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "rawSchema",
        ty: "any",
        kind: @component.FAny,
        elem: Some("any"),
        slot: None,
        members: [],
      },
      {
        name: "showRaw",
        ty: "bool",
        kind: @component.FBool,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: ["toggleShowRaw"],
    receives: [],
    bubbles: [],
    responses: [],
    methods: ["toggleLabel"],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}
