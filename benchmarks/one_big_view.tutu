// GENERATED by benchmarks/build.mjs — do not edit.
// Every view body in the repo, concatenated into one view.
view:
  GiantView:
    @style{font-family: sans-serif}
    @div(~class: "counter"){@h1{@(it.label)@": "@span(~id: "count"){@(it.count)}} @button(~id: "inc", ~on_click: add(1)){+1} @button(~id: "dec", ~on_click: add(-1)){-1} @button(~id: "reset", ~on_click: it.count := default){reset} @input(~id: "label", ~value: it.label, ~on_input: it.label := e.value, ~on_keydown: it.label := "sent!" ~send) @ul(~id: "history"){@each(value, key in it.history){@li{@(value)}}}}
    @p{Hello, world!}
    @div(~class: "grid grid-cols-[auto_auto] gap-x-4 gap-y-2 items-center"){@span{String:} @span(~class: "v-str"){@(it.str)} @span{Number:} @span(~class: "v-num"){@(it.num)@" <- text directive is prepended"} @span{Boolean:} @(it.bool) @span{notSet:} @span(~class: "v-notset"){@(it.not_set)} @span{Method Call:} @span(~class: "v-method"){@(get_str_upper())}}
    @section{@div(~class: "raw", ~dangerously_inner_html: it.content)}
    @section{@div(~class: "md", ~inner_md: it.source)}
    @section(~class: "flex flex-col gap-3"){@input(~class: "input", ~value: it.text, ~on_input: it.text := e.value) @div(~enrich_with: enrich_scope){@p{@"Text: "@span(~class: "v-text"){@(it.text)}}@p{@"Len: "@span(~class: "v-len"){@(len)}}@p{@"Upper: "@span(~class: "v-upper"){@(upper)}}}}
    @section(~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input", ~value: it.query, ~on_input: it.query := e.value, ~on_keydown: it.query := default ~cancel, ~placeholder: "Filter entries") @ul{@each(value, key in it.items, ~when: filter_item, ~enrich_with: enrich_item){@li{@span{@(key)}@": "@(value)@" ("@(count)@" characters) "}}}}
    @section(~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input", ~value: it.query, ~on_input: it.query := e.value, ~on_keydown: it.query := default ~cancel, ~placeholder: "Filter entries") @ul{@each(value, key in it.items, ~when: filter_item, ~enrich_with: enrich_item, ~loop_with: get_iter_data){@li{@span{@(key)}@": "@(value)@" ("@(count)@" / "@(total)@") "}}}}
    @show(it.message.is_truthy()){
      @div(~class: "alert alert-info"){@span(~class: "msg"){@(it.message)} @small{@"(flashed "@(it.count)@" times)"}}
    }
    @section(~class: "flex flex-col gap-2"){@render(it.status) @input(~class: "input", ~value: it.draft, ~on_input: it.draft := e.value, ~on_keydown: submit ~send, ~placeholder: "Type a message and press Enter") @div(~class: "flex gap-2"){@button(~class: "btn btn-primary send", ~on_click: submit){Send} @button(~class: "btn btn-ghost clear", ~on_click: clear_status){Clear status}}}
    @style{user-select: none; .head { cursor: pointer; } .head:before { content: "⏺️"; margin-right: 0.25rem; } .head.type-file:before { content: "📄️"; } .head.open.type-dir:before { content: "📂️"; } .head.closed.type-dir:before { content: "📁️"; }}
    @section{@p(~class: "head", ~class: if it.is_open | @str{head open type-@(it.type)} | @str{head closed type-@(it.type)}, ~on_click: on_item_click){@(it.label)} @show(are_childs_visible()){@div(~class: "pl-3 pt-1 flex flex-col gap-2 kids"){@each(value, key in it.items){@render(value)}}}}
    @div(~class: "flex flex-col gap-3"){@render(it.root) @ul(~class: "flex flex-col gap-2 max-h-[40vh] overflow-y-auto log"){@each(value, key in it.log){@li{@(value)}}}}
    @section(~class: "flex flex-col gap-4"){@div(~role: "tablist", ~class: "tabs tabs-border"){@button(~role: "tab", ~id: "tab-todo", ~class: if (it.active_section == "todo") | "tab tab-active" | "tab", ~on_click: it.active_section := "todo"){To-Do} @button(~role: "tab", ~id: "tab-json", ~class: if (it.active_section == "json") | "tab tab-active" | "tab", ~on_click: it.active_section := "json"){JSON Editor} @button(~role: "tab", ~id: "tab-tree", ~class: if (it.active_section == "tree") | "tab tab-active" | "tab", ~on_click: it.active_section := "tree"){Tree} @button(~role: "tab", ~id: "tab-personal-site", ~class: if (it.active_section == "personalSite") | "tab tab-active" | "tab", ~on_click: it.active_section := "personalSite"){Personal Site} @button(~role: "tab", ~id: "tab-dnd", ~class: if (it.active_section == "dnd") | "tab tab-active" | "tab", ~on_click: it.active_section := "dnd"){Drag and Drop} @button(~role: "tab", ~id: "tab-visual-wasm", ~class: if (it.active_section == "visualWasm") | "tab tab-active" | "tab", ~on_click: it.active_section := "visualWasm"){Visual Wasm}} @show((it.active_section == "todo")){@div(~class: "pane-todo"){@render(it.todo)}} @show((it.active_section == "json")){@div(~class: "pane-json"){@render(it.json)}} @show((it.active_section == "tree")){@div(~class: "pane-tree"){@render(it.tree)}} @show((it.active_section == "personalSite")){@div(~class: "pane-personal-site"){@render(it.personal_site)}} @show((it.active_section == "dnd")){@div(~class: "pane-dnd"){@render(it.dnd)}} @show((it.active_section == "visualWasm")){@div(~class: "pane-visual-wasm"){@render(it.visual_wasm)}}}
    @section(~class: "flex flex-col gap-2"){@button(~class: "btn btn-primary", ~on_click: next_light){Next light} @p{@"Light is: "@code{@(light())}}@p(~class: "advice"){@" You must "@show((light() == "red")){@span{STOP}} @show((light() == "orange")){@span{SLOW DOWN}} @show((light() == "green")){@span{GO}}}}
    @section(~class: "flex flex-col gap-3"){@div(~role: "tablist", ~class: "tabs tabs-border"){@button(~role: "tab", ~id: "t-overview", ~class: if (it.tab == "overview") | "tab tab-active" | "tab", ~on_click: it.tab := "overview"){Overview} @button(~role: "tab", ~id: "t-features", ~class: if (it.tab == "features") | "tab tab-active" | "tab", ~on_click: it.tab := "features"){Features} @button(~role: "tab", ~id: "t-pricing", ~class: if (it.tab == "pricing") | "tab tab-active" | "tab", ~on_click: it.tab := "pricing"){Pricing}} @show((it.tab == "overview")){@div(~class: "card p-3 border panel"){@h4{Overview}@p{A short summary of what this product does.}}} @show((it.tab == "features")){@div(~class: "card p-3 border panel"){@h4{Features}@p{The list of features lives on this tab.}}} @show((it.tab == "pricing")){@div(~class: "card p-3 border panel"){@h4{Pricing}@p{What it costs.}}}}
    @section(~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input", ~value: it.query, ~on_input: it.query := e.value, ~on_keydown: it.query := default ~cancel, ~placeholder: "Filter entries") @ul{@each(value, key in it.items, ~when: filter_item){@li{@span{@(key)}@": "@(value)}}}}
    @div(~class: "join join-vertical"){@button(~class: "btn btn-error", ~on_click: dec){-} @div(~class: "stats"){@div(~class: "stat text-center"){@div(~class: "stat-title"){Count}@div(~class: "stat-value"){@(it.count)}@div(~class: "stat-desc"){Current Count}}} @button(~class: "btn btn-success", ~on_click: inc){+}}
    @ul(~class: "flex flex-col gap-1"){
      @each(value, key in it.songs){
        @li(~class: "flex gap-2 items-center"){@render(value) @button(~class: "btn btn-xs rm", ~on_click: remove_song(key)){remove}}
      }
    }
    @span(~class: "song"){@(it.title)}
    @style{[data-dragging="1"] { opacity: 0.5; } [data-draggingover="my-list-item"] { border-top: 2rem solid transparent; outline: 1px solid #77777777; transition: 0.15s border-top ease; }}
    @section(~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input", ~value: it.query, ~on_input: it.query := e.value, ~on_keydown: it.query := default ~cancel, ~placeholder: "Filter entries") @div{@each(value, key in it.items, ~when: filter_item){@div(~class: "cursor-grab row", ~draggable: "true", ~data_dragtype: "my-list-item", ~data_droptarget: "my-list-item", ~on_drop: on_drop_on_item(key, e.dragInfo)){@span(~class: "k"){@(key)}@": "@(value)}}}}
    @div(~class: "flex gap-3"){@input(~class: "input val", ~value: it.value, ~on_input: it.value := e.value) @input(~class: "input lbl", ~value: it.label, ~on_input: it.label := e.value)}
    @select(~class: "select", ~value: it.selected_value, ~on_input: it.selected_value := e.value){
      @each(value, key in dyn.entries){
        @option(~value: value.value){@(value.label)}
      }
    }
    @div(~class: "flex flex-col gap-3"){@div(~class: "flex gap-3 justify-center"){@render(it.selector)} @button(~class: "btn btn-soft btn-success add", ~on_click: on_add_item){Add Entry} @div(~class: "flex flex-col gap-3 w-full"){@each(value, key in it.items){@div(~class: "flex gap-3 justify-center items-center w-full row"){@render(value) @button(~class: "btn btn-soft btn-sm btn-error btn-circle font-mono rm", ~on_click: it.items.delete_at(key)){x}}}}}
    @div(~class: "card bg-base-200 p-3 gap-2 sheet"){@span(~class: "text-xs opacity-60 tag"){SbDynamicSheet (rendered via *active)} @input(~class: "input input-bordered title", ~value: it.title, ~on_input: it.title := e.value)}
    @div(~class: "border border-dashed border-warning rounded p-3 gap-2 toolbar"){@span(~class: "text-xs opacity-60 tag"){SbDynamicToolbar (consumer)} @render(dyn.active)}
    @div(~class: "border border-dashed border-info rounded p-3 gap-2 panel"){@span(~class: "text-xs opacity-60 tag"){SbDynamicPanel (intermediate)} @render(it.toolbar)}
    @div(~class: "flex flex-col gap-3 p-3"){@div(~class: "alert"){@span{@"SbDynamicWorkspace.sheet.title = "@b(~class: "echo"){@(sheet_title())}}} @render(it.panel)}
    @div(~class: "flex flex-col gap-3 p-3"){@div(~class: "alert"){@span{@"Selected key = "@b(~class: "selected-key"){@(it.selected_key)}}}@div(~class: "flex flex-col gap-2"){@span(~class: "text-xs opacity-60"){Entries — rendered from the *items dynamic (owner):} @each(value, key in dyn.items){@div(~class: "flex gap-3 items-center owner-row"){@render(value) @button(~class: "btn btn-xs btn-soft select", ~on_click: select_item(key)){select}}}} @render(it.editor)}
    @div(~class: "card bg-base-200 p-3 gap-3 editor"){@span(~class: "text-xs opacity-60"){Editor (child component)} @div(~class: "flex flex-col gap-1"){@span(~class: "text-xs opacity-60"){All entries — rendered from the *items lookup:} @each(value, key in dyn.items){@div(~class: "flex gap-3 flex-wrap child-items"){@render(value)}}}@div(~class: "flex flex-col gap-1"){@span(~class: "text-xs opacity-60"){Editing the *selected entry:} @render(dyn.selected, ~as: "edit")}}
    @span(~class: "font-mono entry"){@(it.name)}
    @input(~class: "input input-bordered w-full edit-name", ~value: it.name, ~on_input: it.name := e.value, ~placeholder: "Entry name")
    @section(~class: "flex flex-col gap-3"){@label(~class: "flex flex-col gap-1"){@span{Pick a file} @input(~type: "file", ~class: "file-input", ~on_change: on_pick_file(e.value))} @show(it.has_file){@table(~class: "table"){@tbody{@tr{@th{Name}@td(~class: "fname"){@(it.name)}}@tr{@th{Size}@td(~class: "fsize"){@(size_label())}}@tr{@th{Type}@td(~class: "ftype"){@(type_label())}}@tr{@th{Last modified}@td(~class: "fmodified"){@(last_modified_label())}}}}} @hide(it.has_file){@p(~class: "empty"){No file selected yet — pick one above.}}}
    @div(~class: "person flex gap-2 items-center grow"){@input(~class: "input input-sm grow name", ~value: it.name, ~on_input: it.name := e.value, ~placeholder: "name") @input(~class: "input input-sm grow email", ~value: it.email, ~on_input: it.email := e.value, ~placeholder: "email") @label(~class: "label cursor-pointer gap-1 chk-label"){@input(~type: "checkbox", ~class: "checkbox checkbox-sm active", ~checked: it.active, ~on_input: it.active := e.value)@" active "}}
    @div(~class: "flex flex-col gap-6"){
      @div(~class: "flex flex-col gap-1"){
        @h3(~class: "font-bold"){1. Naive — two independent scans}
        @render(it.naive)
      }
      @div(~class: "flex flex-col gap-1"){
        @h3(~class: "font-bold"){2. Shared — one count + one partial collect}
        @render(it.shared)
      }
      @div(~class: "flex flex-col gap-1"){
        @h3(~class: "font-bold"){3. Coupled — one scan; loop-with forwards the enrich's keys}
        @render(it.coupled)
      }
    }
    @section(~enrich_with: pager_info, ~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input q", ~value: it.query, ~on_input: search(e.value), ~placeholder: "Filter by name or email") @ul(~class: "flex flex-col gap-2"){@each(value, key in it.items, ~when: only_matches, ~loop_with: table_page){@li(~class: "flex gap-2 items-center row"){@span(~class: "badge badge-neutral"){@(key)} @render(value) @button(~class: "btn btn-sm btn-error btn-outline rm", ~on_click: it.items.delete_at(key)){x}}}}@div(~class: "join"){@button(~class: "btn join-item prev", ~on_click: prev, ~disabled: is_first){Prev} @button(~class: "btn join-item btn-disabled pglabel"){@(page_label)} @button(~class: "btn join-item next", ~on_click: next, ~disabled: is_last){Next}}}
    @section(~enrich_with: pager_info, ~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input q", ~value: it.query, ~on_input: search(e.value), ~placeholder: "Filter by name or email") @ul(~class: "flex flex-col gap-2"){@each(value, key in it.items, ~when: only_matches, ~loop_with: page){@li(~class: "flex gap-2 items-center row"){@span(~class: "badge badge-neutral"){@(key)} @render(value) @button(~class: "btn btn-sm btn-error btn-outline rm", ~on_click: it.items.delete_at(key)){x}}}}@div(~class: "join"){@button(~class: "btn join-item prev", ~on_click: prev, ~disabled: is_first){Prev} @button(~class: "btn join-item btn-disabled pglabel"){@(page_label)} @button(~class: "btn join-item next", ~on_click: next, ~disabled: is_last){Next}}}
    @section(~enrich_with: pager_info, ~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input q", ~value: it.query, ~on_input: search(e.value), ~placeholder: "Filter by name or email") @ul(~class: "flex flex-col gap-2"){@each(value, key in it.items, ~loop_with: forward_keys){@li(~class: "flex gap-2 items-center row"){@span(~class: "badge badge-neutral"){@(key)} @render(value) @button(~class: "btn btn-sm btn-error btn-outline rm", ~on_click: it.items.delete_at(key)){x}}}}@div(~class: "join"){@button(~class: "btn join-item prev", ~on_click: prev, ~disabled: is_first){Prev} @button(~class: "btn join-item btn-disabled pglabel"){@(page_label)} @button(~class: "btn join-item next", ~on_click: next, ~disabled: is_last){Next}}}
    @div(~class: "flex flex-col gap-2"){@svg(~viewBox: "0 0 380 130", ~role: "img"){@rect(~class: "preview", ~x: "20", ~y: "12", ~width: "340", ~height: "52", ~rx: "8", ~fill: it.color) @each(value, key in it.palette, ~enrich_with: swatch){@circle(~cx: cx, ~cy: "98", ~r: "18", ~fill: value, ~stroke_width: "3", ~stroke: ring, ~on_click: it.color := value)}} @p(~class: "text-sm label"){@"Selected: "@(it.color)}}
    @div(~class: "flex flex-col gap-3"){@div(~class: "flex gap-3 text-sm"){@label(~class: "flex items-center gap-1"){@"a "@input(~type: "number", ~class: "input input-sm w-16 in-a", ~value: it.a, ~on_input: it.a := e.valueAsInt)} @label(~class: "flex items-center gap-1"){@"b "@input(~type: "number", ~class: "input input-sm w-16 in-b", ~value: it.b, ~on_input: it.b := e.valueAsInt)} @label(~class: "flex items-center gap-1"){@"c "@input(~type: "number", ~class: "input input-sm w-16 in-c", ~value: it.c, ~on_input: it.c := e.valueAsInt)}} @math(~display: "block"){@mn(~class: "m-a"){@(it.a)} @mo{⁢} @msup{@mi{x}@mn{2}} @mo{+} @mn(~class: "m-b"){@(it.b)} @mo{⁢} @mi{x} @mo{+} @mn(~class: "m-c"){@(it.c)} @mo{=} @mn{0}} @p(~class: "text-sm"){@"Discriminant: "@span(~class: "disc"){@(discriminant())}}@p(~class: "text-sm"){@"There are "@span(~class: "classify"){@(classify())}.}}
    @div(~class: "join gap-3 font-mono json-selector"){@button(~class: "btn btn-sm btn-soft btn-warning to-null", ~on_click: set_to_null){null} @button(~class: "btn btn-sm btn-soft btn-success to-bool", ~on_click: set_to_bool(e.isCtrl)){bool} @button(~class: "btn btn-sm btn-soft btn-primary to-number", ~on_click: set_to_number){0} @button(~class: "btn btn-sm btn-soft btn-accent to-string", ~on_click: set_to_string){""} @button(~class: "btn btn-sm btn-soft btn-info to-array", ~on_click: set_to_array){[]} @button(~class: "btn btn-sm btn-soft btn-info to-object", ~on_click: set_to_object){@"{}"}}
    @span(~class: "text-warning text-xs font-mono p-3 json-null"){null}
    @button(~data_bool: "", ~class: if it.value | "btn btn-sm btn-soft btn-success font-mono true" | "btn btn-sm btn-soft btn-error font-mono false", ~on_click: it.value := !it.value){@show(it.value){@span{true}} @hide(it.value){@span{false}}}
    @input(~class: "input input-sm input-ghost text-green-500 font-mono json-string", ~value: it.value, ~on_input: it.value := e.value)
    @input(~type: "number", ~class: "input input-sm input-ghost text-primary font-mono json-number", ~title: it.value, ~value: it.str_value, ~on_input: set_raw_value(e.value))
    @style{[data-draggingover="json-list-item"] { border-top: 4rem solid transparent; transition: 0.15s border-top ease; }}
    @section(~class: "join join-vertical gap-3 pl-3 border-l-1 border-l-neutral-500 json-array"){@button(~class: "btn btn-sm btn-soft btn-primary add", ~on_click: add_item){+} @div(~class: "join join-vertical gap-3"){@each(value, key in it.items){@div(~class: "join gap-3 justify-between items-center group entry", ~draggable: "true", ~data_dragtype: "json-list-item", ~data_droptarget: "json-list-item", ~on_drop: on_drop_on_item(key, e.dragInfo)){@render(value) @button(~class: "btn btn-sm btn-soft btn-circle btn-error font-mono opacity-20 group-hover:opacity-100 rm", ~on_click: it.items.delete_at(key)){x}}}}}
    @div(~class: "join gap-3 items-center justify-center json-kv"){@input(~class: "input input-sm input-ghost text-green-500 font-mono json-key", ~value: it.key, ~on_input: it.key := e.value) @span(~class: "text-xs font-mono text-gray-500 colon"){:} @render(it.value)}
    @style{[data-draggingover="json-kv-item"] { border-top: 4rem solid transparent; transition: 0.15s border-top ease; }}
    @section(~class: "join join-vertical gap-3 pl-3 border-l-1 border-l-neutral-500 json-object"){@button(~class: "btn btn-sm btn-soft font-mono btn-primary add", ~on_click: add_item){+} @div(~class: "join join-vertical gap-3"){@each(value, key in it.items){@div(~class: "join gap-3 justify-between items-center group entry", ~draggable: "true", ~data_dragtype: "json-kv-item", ~data_droptarget: "json-kv-item", ~on_drop: on_drop_on_item(key, e.dragInfo)){@render(value) @button(~class: "btn btn-sm btn-soft btn-circle btn-error font-mono opacity-20 group-hover:opacity-100 rm", ~on_click: it.items.delete_at(key)){x}}}}}
    @section{
      @ul{
        @each(value, key in it.items){
          @li{@span(~class: "k"){@(key)}@": "@(value)}
        }
      }
    }
    @div(~class: "card bg-base-200 p-4"){@h3(~class: "card-title"){@(it.title)}@ul(~class: "menu"){@each(value, key in it.labels){@li{@button(~class: "btn btn-xs", ~on_click: toggle_label(key)){@span{@(value.text)}}}}} @button(~class: "btn btn-sm btn-primary", ~on_click: bump_priority){@" bump priority "}}
    @a(~class: "badge badge-ghost badge-sm cursor-pointer gap-1 no-underline hover:bg-base-200 alt-url", ~href: it.url, ~target: "_blank"){@"🔗 "@span{@(it.label)}}
    @div(~class: "card bg-base-200 shadow-sm p-2 sm:p-3 flex flex-col gap-2 ps-entry"){@div(~class: "flex gap-1 sm:gap-2 flex-wrap items-center"){@show(it.featured){@span(~class: "star"){⭐}} @show(is_spanish()){@span(~class: "flag"){@(lang_flag())}} @show(it.start_year.is_truthy()){@span(~class: "badge badge-ghost badge-sm border-0 font-bold years"){@(year_display())}} @show(it.role){@span(~class: "badge badge-neutral badge-sm border-0 font-bold role"){@(it.role)}}} @a(~href: it.url, ~target: "_blank", ~class: "link link-hover"){@h2(~class: "font-bold title"){@(it.title)}} @p(~class: "text-sm opacity-70 desc"){@(it.description)}@div(~class: "flex gap-1 sm:gap-2 flex-wrap cats"){@each(value, key in it.categories, ~enrich_with: enrich_category_badge){@span(~class: badge_class){@(value)}}} @hide(it.alt_urls.is_empty()){@div(~class: "alts"){@ul{@each(value, key in it.alt_urls){@li{@render(value)}}}}}}
    @section(~class: "ps-root flex flex-col gap-4"){
      @div(~class: "card bg-base-200 p-4 controls"){
        @p(~class: "text-sm mb-4"){@"💡 Click tags to toggle. Solo / unsolo toggle with "@kbd(~class: "text-mono"){alt/option + click}.}
        @div(~class: "grid grid-cols-1 sm:grid-cols-[auto_1fr] gap-y-1 sm:gap-x-3 sm:gap-y-3 items-start"){@span(~class: "font-semibold text-sm pt-1"){Categories} @div(~class: "cat-facets flex flex-wrap gap-2"){@each(value, key in it.all_categories, ~enrich_with: enrich_category_bindings){@button(~class: btn_class, ~on_click: on_category_toggle(key, e.isAlt)){@(value)}}} @span(~class: "font-semibold text-sm pt-1"){Roles} @div(~class: "role-facets flex flex-wrap gap-2"){@each(value, key in it.all_roles, ~enrich_with: enrich_role_bindings){@button(~class: btn_class, ~on_click: on_role_toggle(key, e.isAlt)){@(value)}}} @span(~class: "font-semibold text-sm pt-1"){Sort By} @div(~class: "flex flex-wrap items-center gap-2"){@label(~class: "swap btn btn-xs btn-outline btn-primary"){@input(~type: "checkbox", ~class: "sort-end", ~checked: it.sort_by_end, ~on_change: on_toggle_sort_by_end) @span(~class: "swap-on"){End Year} @span(~class: "swap-off"){Start Year}} @label(~class: "swap btn btn-xs btn-outline btn-primary"){@input(~type: "checkbox", ~class: "sort-asc", ~checked: it.sort_asc, ~on_change: on_toggle_sort_asc) @span(~class: "swap-on"){↑ Oldest first} @span(~class: "swap-off"){↓ Newest first}} @label(~class: "flex items-center gap-2 cursor-pointer"){@input(~type: "checkbox", ~class: "toggle toggle-sm feat-first", ~checked: it.featured_first, ~on_change: on_toggle_featured_first) @span(~class: "text-sm"){Featured first}}}}
        @div(~class: "flex flex-wrap items-center gap-4 mt-4"){
          @show(has_active_filters()){
            @button(~class: "btn btn-outline btn-sm clear-filters", ~on_click: clear_filters){Clear Filters}
          }
        }
      }
      @div(~class: "entries flex flex-col gap-3"){
        @each(value, key in it.all_entries, ~when: filter_entry){
          @div(~class: "entry-row"){@render(value)}
        }
      }
    }
    @tr{
      @td(~class: "name"){@(it.name)}
      @td(~class: "qty"){@(it.qty)}
      @td(~class: "price"){@(it.price)}
    }
    @table(~class: "table"){
      @thead{
        @tr{
          @th{Name}
          @th{Qty}
          @th{Price}
        }
      }
      @tbody{
        @each(value, key in it.rows){
          @render(value)
        }
      }
    }
    @option(~value: it.value){@(it.label)}
    @select(~class: "select", ~value: it.current, ~on_input: it.current := e.value){
      @each(value, key in it.options){
        @render(value)
      }
    }
    @div{@render(it.table) @render(it.select)}
    @p(~class: "p-2 border rounded"){@" Hello, "@strong{@(it.name)}@"! "}
    @section(~class: "flex flex-col gap-2"){@h2(~class: "text-lg"){SbRenderChildPage header} @render(it.greeting) @p(~class: "opacity-60"){— end of page —}}
    @div(~class: "card bg-base-100 shadow-sm"){
      @div(~class: "card-body"){
        @h2(~class: "card-title"){@(it.title)}
        @p{@(it.description)}
      }
    }
    @div(~class: "card bg-base-100 shadow-sm gap-3"){@div(~class: "card-body"){@input(~class: "input title", ~value: it.title, ~on_input: it.title := e.value) @input(~class: "input desc", ~value: it.description, ~on_input: it.description := e.value)}}
    @section(~class: "flex flex-col gap-3"){@render(it.value) @render(it.value, ~as: "edit")}
    @section(~class: "flex flex-col gap-3"){@div(~class: "flex justify-between"){@input(~type: "search", ~class: "input", ~value: it.query, ~on_input: it.query := e.value, ~on_keydown: it.query := default ~cancel, ~placeholder: "Filter entries") @button(~class: "btn bnt-sm btn-primary toggle-btn", ~on_click: toggle_view){@(get_toggle_label())}} @div(~push_view: it.view, ~class: "flex flex-col gap-3 max-h-[40vh] overflow-y-auto pr-3 rows"){@each(value, key in it.items, ~when: filter_item){@render(value)}}}
    @section(~class: "flex flex-col gap-3"){@input(~type: "range", ~min: "0", ~max: get_max_index(), ~value: it.current_index, ~on_input: it.current_index := e.valueAsInt) @render(it.by_index[it.current_index]) @select(~class: "select", ~value: it.current_key, ~on_input: it.current_key := e.value){@each(value, key in it.by_key){@option(~value: key){@(value.title)}}} @render(it.by_key[it.current_key])}
    @section(~class: "flex flex-col gap-3"){
      @div(~class: "join"){@button(~class: "btn join-item prev", ~on_click: prev, ~disabled: is_first_page()){‹ Prev} @button(~class: "btn join-item btn-disabled label"){@(page_label())} @button(~class: "btn join-item next", ~on_click: next, ~disabled: is_last_page()){Next ›}}
      @ul(~class: "flex flex-col gap-1"){
        @each(value, key in it.items, ~loop_with: paginate){
          @li(~class: "flex gap-2 items-center"){@span(~class: "badge badge-neutral"){@(key)} @(value)}
        }
      }
    }
    @section(~class: "flex flex-col gap-3"){@show(it.is_loading){@div(~class: "alert alert-info alert-outline loading"){Loading}} @hide(it.is_loading){@div(~class: "flex justify-between"){@input(~type: "search", ~class: "input", ~value: it.query, ~on_input: it.query := e.value, ~on_keydown: it.query := default ~cancel, ~placeholder: "Filter entries") @button(~class: "btn bnt-sm btn-primary btn-outline another", ~on_click: load_another_way){Load Another Way} @button(~class: "btn bnt-sm btn-primary toggle-btn", ~on_click: toggle_view){@(it.view)}}} @hide(it.is_loading){@div(~push_view: it.view, ~class: "flex flex-col gap-3 max-h-[40vh] overflow-y-auto pr-3 rows"){@each(value, key in it.items, ~when: filter_item){@render(value)}}} @show(it.error.is_truthy()){@div(~class: "error"){@(it.error)}}}
    @section(~class: "flex flex-col gap-3"){@input(~class: "input s", ~value: it.str, ~on_input: it.str := e.value, ~title: @str{Content is @(it.str)}) @input(~class: "input n", ~type: "number", ~value: it.num, ~on_input: set_raw_number(e.value)) @input(~class: "checkbox b", ~type: "checkbox", ~checked: it.bool, ~on_input: it.bool := e.value) @p{@"String: "@span(~class: "o-str"){@(it.str)}}@p{@"Number: "@span(~class: "o-num"){@(it.num)}}@p{@"Boolean: "@span(~class: "o-bool"){@(it.bool)}}}
    @section{@button(~class: if it.is_active | "btn btn-success" | "btn btn-ghost", ~title: if it.is_active | "Click to disable" | "Click to enable", ~on_click: it.is_active := !it.is_active){@show(it.is_active){@span(~class: "on"){Enabled}} @hide(it.is_active){@span(~class: "off"){Disabled}}}}
    @section(~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input", ~value: it.query, ~on_input: on_input(e.value), ~on_keydown: it.last_sent_search := e.value ~send, ~on_keydown: it.query := default ~cancel, ~placeholder: "Search Query (Enter to send, Esc to clear)") @show(it.last_sent_search.is_truthy()){@p(~class: "sent"){@" Search: \""@span(~class: "sent-q"){@(it.last_sent_search)}@"\" "}}}
    @section(~class: "flex flex-col gap-2"){@button(~class: "btn btn-primary toggle-btn", ~on_click: it.is_open := !it.is_open){@(label())} @show(it.is_open){@div(~class: "card p-3 border details"){@p{@"Details panel — only visible when "@code{isOpen}@" is true."} @button(~class: "btn btn-sm bump", ~on_click: inc_count){Click me} @p{@"Clicked "@(it.count)@" times."}}} @hide(it.is_open){@p(~class: "text-sm opacity-60 hint"){(details are hidden)}} @p(~class: "peek"){@"Count, only when open: "@show(it.is_open){@(it.count)}}}
    @style{.mine { color: red; }}
    @section{
      @p{View: main}
      @p(~class: "mine"){My Style}
      @p(~class: "common"){Common Style}
      @p(~class: "styles-example-global-class"){Global Style}
    }
    @section{
      @p{View: one}
      @p(~class: "mine"){My Style}
      @p(~class: "common"){Common Style}
      @p(~class: "styles-example-global-class"){Global Style}
    }
    @style{.mine { color: orange; text-decoration: underline; }}
    @section{
      @p{View: two}
      @p(~class: "mine"){My Style}
      @p(~class: "common"){Common Style}
      @p(~class: "styles-example-global-class"){Global Style}
    }
    @section(~class: "flex flex-col gap-3"){@render(it.value) @render(it.value, ~as: "one") @render(it.value, ~as: "two")}
    @div(~class: "flex gap-3 items-center"){@input(~type: "checkbox", ~class: "checkbox", ~checked: it.completed, ~on_input: it.completed := e.value) @input(~class: "input", ~value: it.text, ~on_input: it.text := e.value, ~disabled: it.completed)}
    @div(~class: "flex flex-col gap-3"){@button(~class: "btn btn-soft btn-success add", ~on_click: on_add_item){Add Task} @div(~class: "flex flex-col gap-3 w-full"){@each(value, key in it.items){@div(~class: "flex gap-3 justify-center items-center w-full row"){@render(value) @button(~class: "btn btn-soft btn-sm btn-error btn-circle font-mono rm", ~on_click: it.items.delete_at(key)){x}}}}}
    @section(~on_emoji_click: on_emoji_click(e.value), ~class: "join join-vertical gap-3 items-center"){@hide(it.current.is_truthy()){@p(~class: "hint"){@" Select the "@em{Toggle Picker}@" button and select an emoji, it will be displayed here "}} @show(it.current.is_truthy()){@p(~class: "label"){Selected emoji:}} @show(it.current.is_truthy()){@span(~class: "current text-9xl"){@(it.current)}} @button(~class: "btn btn-sm toggle-btn", ~on_click: it.is_picker_visible := !it.is_picker_visible){@" Toggle Picker "} @show(it.is_picker_visible){@emoji_picker}}
    @div(~style: "display:flex;gap:.5rem;align-items:center;font-size:1.5rem"){@button(~on_click: dec){-} @b{@(it.count)} @button(~on_click: inc){+}}
    @div(~class: "flex gap-3 items-center"){@input(~type: "checkbox", ~class: "checkbox", ~checked: it.completed, ~on_input: it.completed := e.value) @input(~class: "input", ~value: it.text, ~on_input: it.text := e.value, ~disabled: it.completed)}
    @div(~class: "flex flex-col gap-3"){@button(~class: "btn btn-soft btn-success add", ~on_click: on_add_item){Add Task} @div(~class: "flex flex-col gap-3 w-full"){@each(value, key in it.items){@div(~class: "flex gap-3 justify-center items-center w-full row"){@render(value) @button(~class: "btn btn-soft btn-sm btn-error btn-circle font-mono rm", ~on_click: it.items.delete_at(key)){x}}}}}
    @style{user-select: none; .head { cursor: pointer; } .head:before { content: "⏺️"; margin-right: 0.25rem; } .head.type-file:before { content: "📄️"; } .head.open.type-dir:before { content: "📂️"; } .head.closed.type-dir:before { content: "📁️"; }}
    @section{@p(~class: "head", ~class: if it.is_open | @str{head open type-@(it.type)} | @str{head closed type-@(it.type)}, ~on_click: on_item_click){@(it.label)} @show(are_childs_visible()){@div(~class: "pl-3 pt-1 flex flex-col gap-2 kids"){@each(value, key in it.items){@render(value)}}}}
    @div(~class: "flex flex-col gap-3"){@render(it.root) @ul(~class: "flex flex-col gap-2 max-h-[40vh] overflow-y-auto log"){@each(value, key in it.log){@li{@(value)}}}}
    @section(~class: "flex flex-col gap-4"){@div(~role: "tablist", ~class: "tabs tabs-border"){@button(~role: "tab", ~id: "tab-counter", ~class: if (it.active_section == "counter") | "tab tab-active" | "tab", ~on_click: it.active_section := "counter"){SiteComposabilityCounter} @button(~role: "tab", ~id: "tab-todo", ~class: if (it.active_section == "todo") | "tab tab-active" | "tab", ~on_click: it.active_section := "todo"){To-Do} @button(~role: "tab", ~id: "tab-tree", ~class: if (it.active_section == "tree") | "tab tab-active" | "tab", ~on_click: it.active_section := "tree"){Tree}} @show((it.active_section == "counter")){@div(~class: "pane-counter"){@render(it.counter)}} @show((it.active_section == "todo")){@div(~class: "pane-todo"){@render(it.todo)}} @show((it.active_section == "tree")){@div(~class: "pane-tree"){@render(it.tree)}}}
    @div(~style: "display:flex;gap:.5rem;align-items:center;font-size:1.5rem"){@button(~on_click: dec){-} @b{@(it.count)} @button(~on_click: inc){+}}
    @style{[data-dragging="1"] { opacity: 0.5; } [data-draggingover="my-list-item"] { border-top: 2rem solid transparent; outline: 1px solid #77777777; transition: 0.15s border-top ease; }}
    @section(~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input", ~value: it.query, ~on_input: it.query := e.value, ~on_keydown: it.query := "" ~cancel, ~placeholder: "Filter entries") @div{@each(value, key in it.items, ~when: filter_item){@div(~class: "cursor-grab row", ~draggable: "true", ~data_dragtype: "my-list-item", ~data_droptarget: "my-list-item", ~on_drop: on_drop_on_item(key, e.dragInfo)){@span(~class: "k"){@(key)}@": "@(value)}}}}
    @div(~class: "flex flex-col gap-6"){
      @div(~class: "flex flex-col gap-1"){
        @h3(~class: "font-bold"){1. Naive — two independent scans}
        @render(it.naive)
      }
      @div(~class: "flex flex-col gap-1"){
        @h3(~class: "font-bold"){2. Shared — one count + one partial collect}
        @render(it.shared)
      }
      @div(~class: "flex flex-col gap-1"){
        @h3(~class: "font-bold"){3. Coupled — one scan; loop-with forwards the enrich's keys}
        @render(it.coupled)
      }
    }
    @div(~class: "person flex gap-2 items-center grow"){@input(~class: "input input-sm grow name", ~value: it.name, ~on_input: it.name := e.value, ~placeholder: "name") @input(~class: "input input-sm grow email", ~value: it.email, ~on_input: it.email := e.value, ~placeholder: "email") @label(~class: "label cursor-pointer gap-1 chk-label"){@input(~type: "checkbox", ~class: "checkbox checkbox-sm active", ~checked: it.active, ~on_input: it.active := e.value)@" active "}}
    @section(~enrich_with: pager_info, ~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input q", ~value: it.query, ~on_input: search(e.value), ~placeholder: "Filter by name or email") @ul(~class: "flex flex-col gap-2"){@each(value, key in it.items, ~when: only_matches, ~loop_with: table_page){@li(~class: "flex gap-2 items-center row"){@span(~class: "badge badge-neutral"){@(key)} @render(value) @button(~class: "btn btn-sm btn-error btn-outline rm", ~on_click: it.items.delete_at(key)){x}}}}@div(~class: "join"){@button(~class: "btn join-item prev", ~on_click: prev, ~disabled: is_first){Prev} @button(~class: "btn join-item btn-disabled pglabel"){@(page_label)} @button(~class: "btn join-item next", ~on_click: next, ~disabled: is_last){Next}}}
    @section(~enrich_with: pager_info, ~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input q", ~value: it.query, ~on_input: search(e.value), ~placeholder: "Filter by name or email") @ul(~class: "flex flex-col gap-2"){@each(value, key in it.items, ~when: only_matches, ~loop_with: page){@li(~class: "flex gap-2 items-center row"){@span(~class: "badge badge-neutral"){@(key)} @render(value) @button(~class: "btn btn-sm btn-error btn-outline rm", ~on_click: it.items.delete_at(key)){x}}}}@div(~class: "join"){@button(~class: "btn join-item prev", ~on_click: prev, ~disabled: is_first){Prev} @button(~class: "btn join-item btn-disabled pglabel"){@(page_label)} @button(~class: "btn join-item next", ~on_click: next, ~disabled: is_last){Next}}}
    @section(~enrich_with: pager_info, ~class: "flex flex-col gap-3"){@input(~type: "search", ~class: "input q", ~value: it.query, ~on_input: search(e.value), ~placeholder: "Filter by name or email") @ul(~class: "flex flex-col gap-2"){@each(value, key in it.items, ~loop_with: forward_keys){@li(~class: "flex gap-2 items-center row"){@span(~class: "badge badge-neutral"){@(key)} @render(value) @button(~class: "btn btn-sm btn-error btn-outline rm", ~on_click: it.items.delete_at(key)){x}}}}@div(~class: "join"){@button(~class: "btn join-item prev", ~on_click: prev, ~disabled: is_first){Prev} @button(~class: "btn join-item btn-disabled pglabel"){@(page_label)} @button(~class: "btn join-item next", ~on_click: next, ~disabled: is_last){Next}}}
    @span(~class: "text-warning text-xs font-mono p-3 json-null"){null}
    @button(~data_bool: "", ~class: if it.value | "btn btn-sm btn-soft btn-success font-mono true" | "btn btn-sm btn-soft btn-error font-mono false", ~on_click: it.value := !it.value){@show(it.value){@span{true}} @hide(it.value){@span{false}}}
    @input(~class: "input input-sm input-ghost text-green-500 font-mono json-string", ~value: it.value, ~on_input: it.value := e.value)
    @input(~type: "number", ~class: "input input-sm input-ghost text-primary font-mono json-number", ~title: it.value, ~value: it.str_value, ~on_input: set_raw_value(e.value))
    @div(~class: "join gap-3 font-mono json-selector"){@button(~class: "btn btn-sm btn-soft btn-warning to-null", ~on_click: set_to_null){null} @button(~class: "btn btn-sm btn-soft btn-success to-bool", ~on_click: set_to_bool(e.isCtrl)){bool} @button(~class: "btn btn-sm btn-soft btn-primary to-number", ~on_click: set_to_number){0} @button(~class: "btn btn-sm btn-soft btn-accent to-string", ~on_click: set_to_string){""} @button(~class: "btn btn-sm btn-soft btn-info to-array", ~on_click: set_to_array){[]} @button(~class: "btn btn-sm btn-soft btn-info to-object", ~on_click: set_to_object){@"{}"}}
    @style{[data-draggingover="json-list-item"] { border-top: 4rem solid transparent; transition: 0.15s border-top ease; }}
    @section(~class: "join join-vertical gap-3 pl-3 border-l-1 border-l-neutral-500 json-array"){@button(~class: "btn btn-sm btn-soft btn-primary add", ~on_click: add_item){+} @div(~class: "join join-vertical gap-3"){@each(value, key in it.items){@div(~class: "join gap-3 justify-between items-center group entry", ~draggable: "true", ~data_dragtype: "json-list-item", ~data_droptarget: "json-list-item", ~on_drop: on_drop_on_item(key, e.dragInfo)){@render(value) @button(~class: "btn btn-sm btn-soft btn-circle btn-error font-mono opacity-20 group-hover:opacity-100 rm", ~on_click: it.items.delete_at(key)){x}}}}}
    @div(~class: "join gap-3 items-center justify-center json-kv"){@input(~class: "input input-sm input-ghost text-green-500 font-mono json-key", ~value: it.key, ~on_input: it.key := e.value) @span(~class: "text-xs font-mono text-gray-500 colon"){:} @render(it.value)}
    @style{[data-draggingover="json-kv-item"] { border-top: 4rem solid transparent; transition: 0.15s border-top ease; }}
    @section(~class: "join join-vertical gap-3 pl-3 border-l-1 border-l-neutral-500 json-object"){@button(~class: "btn btn-sm btn-soft font-mono btn-primary add", ~on_click: add_item){+} @div(~class: "join join-vertical gap-3"){@each(value, key in it.items){@div(~class: "join gap-3 justify-between items-center group entry", ~draggable: "true", ~data_dragtype: "json-kv-item", ~data_droptarget: "json-kv-item", ~on_drop: on_drop_on_item(key, e.dragInfo)){@render(value) @button(~class: "btn btn-sm btn-soft btn-circle btn-error font-mono opacity-20 group-hover:opacity-100 rm", ~on_click: it.items.delete_at(key)){x}}}}}
    @a(~class: "badge badge-ghost badge-sm cursor-pointer gap-1 no-underline hover:bg-base-200 alt-url", ~href: it.url, ~target: "_blank"){@"🔗 "@span{@(it.label)}}
    @div(~class: "card bg-base-200 shadow-sm p-2 sm:p-3 flex flex-col gap-2 ps-entry"){@div(~class: "flex gap-1 sm:gap-2 flex-wrap items-center"){@show(it.featured){@span(~class: "star"){⭐}} @show(is_spanish()){@span(~class: "flag"){@(lang_flag())}} @show(it.start_year.is_truthy()){@span(~class: "badge badge-ghost badge-sm border-0 font-bold years"){@(year_display())}} @show(it.role){@span(~class: "badge badge-neutral badge-sm border-0 font-bold role"){@(it.role)}}} @a(~href: it.url, ~target: "_blank", ~class: "link link-hover"){@h2(~class: "font-bold title"){@(it.title)}} @p(~class: "text-sm opacity-70 desc"){@(it.description)}@div(~class: "flex gap-1 sm:gap-2 flex-wrap cats"){@each(value, key in it.categories, ~enrich_with: enrich_category_badge){@span(~class: badge_class){@(value)}}} @hide(it.alt_urls.is_empty()){@div(~class: "alts"){@ul{@each(value, key in it.alt_urls){@li{@render(value)}}}}}}
    @section(~class: "ps-root flex flex-col gap-4"){
      @div(~class: "card bg-base-200 p-4 controls"){
        @p(~class: "text-sm mb-4"){@"💡 Click tags to toggle. Solo / unsolo toggle with "@kbd(~class: "text-mono"){alt/option + click}.}
        @div(~class: "grid grid-cols-1 sm:grid-cols-[auto_1fr] gap-y-1 sm:gap-x-3 sm:gap-y-3 items-start"){@span(~class: "font-semibold text-sm pt-1"){Categories} @div(~class: "cat-facets flex flex-wrap gap-2"){@each(value, key in it.all_categories, ~enrich_with: enrich_category_bindings){@button(~class: btn_class, ~on_click: on_category_toggle(key, e.isAlt)){@(value)}}} @span(~class: "font-semibold text-sm pt-1"){Roles} @div(~class: "role-facets flex flex-wrap gap-2"){@each(value, key in it.all_roles, ~enrich_with: enrich_role_bindings){@button(~class: btn_class, ~on_click: on_role_toggle(key, e.isAlt)){@(value)}}} @span(~class: "font-semibold text-sm pt-1"){Sort By} @div(~class: "flex flex-wrap items-center gap-2"){@label(~class: "swap btn btn-xs btn-outline btn-primary"){@input(~type: "checkbox", ~class: "sort-end", ~checked: it.sort_by_end, ~on_change: on_toggle_sort_by_end) @span(~class: "swap-on"){End Year} @span(~class: "swap-off"){Start Year}} @label(~class: "swap btn btn-xs btn-outline btn-primary"){@input(~type: "checkbox", ~class: "sort-asc", ~checked: it.sort_asc, ~on_change: on_toggle_sort_asc) @span(~class: "swap-on"){↑ Oldest first} @span(~class: "swap-off"){↓ Newest first}} @label(~class: "flex items-center gap-2 cursor-pointer"){@input(~type: "checkbox", ~class: "toggle toggle-sm feat-first", ~checked: it.featured_first, ~on_change: on_toggle_featured_first) @span(~class: "text-sm"){Featured first}}}}
        @div(~class: "flex flex-wrap items-center gap-4 mt-4"){
          @show(has_active_filters()){
            @button(~class: "btn btn-outline btn-sm clear-filters", ~on_click: clear_filters){Clear Filters}
          }
        }
      }
      @div(~class: "entries flex flex-col gap-3"){
        @each(value, key in it.all_entries, ~when: filter_entry){
          @div(~class: "entry-row"){@render(value)}
        }
      }
    }
    @div(~style: "font-family:system-ui;display:flex;flex-direction:column;gap:.5rem"){@input(~value: it.name, ~on_input: it.name := e.value, ~placeholder: "your name") @p{@"Hello, "@b{@(it.name)}!}}
    @div(~class: "flex gap-3 items-center"){@input(~type: "checkbox", ~class: "checkbox", ~checked: it.completed, ~on_input: it.completed := e.value) @input(~class: "input", ~value: it.text, ~on_input: it.text := e.value, ~disabled: it.completed)}
    @div(~class: "flex flex-col gap-3"){@button(~class: "btn btn-soft btn-success add", ~on_click: on_add_item){Add Task} @div(~class: "flex flex-col gap-3 w-full"){@each(value, key in it.items){@div(~class: "flex gap-3 justify-center items-center w-full row"){@render(value) @button(~class: "btn btn-soft btn-sm btn-error btn-circle font-mono rm", ~on_click: it.items.delete_at(key)){x}}}}}
    @section(~style: "font-family:system-ui"){@button(~on_click: it.open := !it.open){@(label())} @show(it.open){@p(~style: "padding:.5rem;border:1px solid #ccc;margin-top:.5rem"){@" Now you see me. Toggle again to hide. "}}}
    @style{user-select: none; .head { cursor: pointer; } .head:before { content: "⏺️"; margin-right: 0.25rem; } .head.type-file:before { content: "📄️"; } .head.open.type-dir:before { content: "📂️"; } .head.closed.type-dir:before { content: "📁️"; }}
    @section{@p(~class: "head", ~class: if it.is_open | @str{head open type-@(it.type)} | @str{head closed type-@(it.type)}, ~on_click: on_item_click){@(it.label)} @show(are_childs_visible()){@div(~class: "pl-3 pt-1 flex flex-col gap-2 kids"){@each(value, key in it.items){@render(value)}}}}
    @div(~class: "flex flex-col gap-3"){@render(it.root) @ul(~class: "flex flex-col gap-2 max-h-[40vh] overflow-y-auto log"){@each(value, key in it.log){@li{@(value)}}}}
