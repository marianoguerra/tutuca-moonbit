// GENERATED by `tutuca gen-views` from first_principles.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views first_principles.html --name FirstPrinciples --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the FpGreeting component.
///
/// Declared as `interface fp-greeting` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct FpGreetingState {
  name : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..FpGreetingState::zero(), label: "Counter" }
fn FpGreetingState::zero() -> FpGreetingState {
  FpGreetingState::{ name: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn fp_greeting_zero_args() -> Map[String, @tutuca.Value] {
  fp_greeting_encode(FpGreetingState::zero())
}

///|
/// Every field the FpGreeting state declares — typed field names for tests and inspectors.
pub(all) enum FpGreetingField {
  Name
} derive(Eq, @debug.Debug)

///|
pub fn FpGreetingField::name(self : FpGreetingField) -> String {
  match self {
    Name => "name"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn FpGreetingField::kind(self : FpGreetingField) -> @component.FieldKind {
  match self {
    Name => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let fp_greeting_field_names : Array[String] = ["name"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let fp_greeting_schema_fingerprint : String = "7fde5bb7d59361e6"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn fp_greeting_encode(s : FpGreetingState) -> Map[String, @tutuca.Value] {
  { "name": Str(s.name) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn fp_greeting_decode(f : Map[String, @tutuca.Value]) -> FpGreetingState? {
  guard f.get("name") is Some(Str(v_name)) else { return None }
  Some(FpGreetingState::{ name: v_name })
}

///|
/// The views declared in first_principles.html.
pub(all) enum FpGreetingView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn FpGreetingView::name(self : FpGreetingView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let fp_greeting_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let fp_greeting_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn fp_greeting_schema() -> @component.SchemaInfo {
  {
    fingerprint: "7fde5bb7d59361e6",
    fields: [
      {
        name: "name",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the Note component.
///
/// Declared as `interface note` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct NoteState {
  text : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..NoteState::zero(), label: "Counter" }
fn NoteState::zero() -> NoteState {
  NoteState::{ text: "" }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn note_zero_args() -> Map[String, @tutuca.Value] {
  note_encode(NoteState::zero())
}

///|
/// Every field the Note state declares — typed field names for tests and inspectors.
pub(all) enum NoteField {
  Text
} derive(Eq, @debug.Debug)

///|
pub fn NoteField::name(self : NoteField) -> String {
  match self {
    Text => "text"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn NoteField::kind(self : NoteField) -> @component.FieldKind {
  match self {
    Text => @component.FText
  }
}

///|
/// Every field, in declaration order.
pub let note_field_names : Array[String] = ["text"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let note_schema_fingerprint : String = "416c7af2c341fb4c"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn note_encode(s : NoteState) -> Map[String, @tutuca.Value] {
  { "text": Str(s.text) }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn note_decode(f : Map[String, @tutuca.Value]) -> NoteState? {
  guard f.get("text") is Some(Str(v_text)) else { return None }
  Some(NoteState::{ text: v_text })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum NoteReceive {
  Write(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn NoteReceive::from_dispatch(d : @component.Dispatch) -> NoteReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("write", [Str(a0)]) => Write(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn NoteReceive::to_dispatch(
  self : NoteReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    Write(a0) => ("write", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn NoteReceive::send(self : NoteReceive, ctx : &@tutuca.Ctx) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The views declared in first_principles.html.
pub(all) enum NoteView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn NoteView::name(self : NoteView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let note_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let note_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn note_schema() -> @component.SchemaInfo {
  {
    fingerprint: "416c7af2c341fb4c",
    fields: [
      {
        name: "text",
        ty: "string",
        kind: @component.FText,
        elem: None,
        slot: None,
        members: [],
      },
    ],
    inputs: [],
    receives: ["write"],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}

///|
/// The state of the Mailbox component.
///
/// Declared as `interface mailbox` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct MailboxState {}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..MailboxState::zero(), label: "Counter" }
fn MailboxState::zero() -> MailboxState {
  MailboxState::{  }
}

///|
/// The zero state as component arguments — the default a ModuleDef
/// example takes when it sets nothing of its own.
pub fn mailbox_zero_args() -> Map[String, @tutuca.Value] {
  mailbox_encode(MailboxState::zero())
}

///|
/// Every field the Mailbox state declares — typed field names for tests and inspectors.
pub(all) enum MailboxField {
  Note
} derive(Eq, @debug.Debug)

///|
pub fn MailboxField::name(self : MailboxField) -> String {
  match self {
    Note => "note"
  }
}

///|
/// The runtime kind this field carries. Declared, not inferred: the old
/// `FieldSpec::of_default` had to guess FInt from whether the seed value
/// happened to be integral.
pub fn MailboxField::kind(self : MailboxField) -> @component.FieldKind {
  match self {
    Note => @component.FComp(comp="", args=Map([]))
  }
}

///|
/// Every field, in declaration order.
pub let mailbox_field_names : Array[String] = ["note"]

///|
/// The structural fingerprint of this component's schema. Changes when
/// the SHAPE changes — a renamed or retyped field, a new message case —
/// and not when the block is merely reformatted.
pub let mailbox_schema_fingerprint : String = "44b8998831569bcf"

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
fn mailbox_encode(_s : MailboxState) -> Map[String, @tutuca.Value] {
  Map([])
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
fn mailbox_decode(_f : Map[String, @tutuca.Value]) -> MailboxState? {
  Some(MailboxState::{  })
}

///|
/// The views declared in first_principles.html.
pub(all) enum MailboxView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn MailboxView::name(self : MailboxView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let mailbox_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let mailbox_global_style : String = ""

///|
/// What this component's schema declares, for `schema~`.
///
/// Passing it to `component` also stops the runtime inferring field
/// kinds from the init value — the schema states them.
pub fn mailbox_schema() -> @component.SchemaInfo {
  {
    fingerprint: "44b8998831569bcf",
    fields: [
      {
        name: "note",
        ty: "component",
        kind: @component.FComp(comp="", args=Map([])),
        elem: Some("any"),
        slot: Some(""),
        members: [],
      },
    ],
    inputs: [],
    receives: [],
    bubbles: [],
    responses: [],
    methods: [],
    ids: [],
    view_names: ["main"],
    init_names: [],
  }
}
