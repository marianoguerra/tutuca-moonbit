// GENERATED by `tutuca gen-views` from first_principles.html — DO NOT EDIT.
//
// The views of FpGreeting, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing fp_greeting_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn fp_greeting_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("p", [], [@anode.dyn_text(Field("name"))]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn fp_greeting_views() -> Map[String, @anode.View] {
  { "main": fp_greeting_main_view_ir() }
}

///|
/// The FpGreeting component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `FpGreetingState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn fp_greeting_component(
  name? : String = "FpGreeting",
  views? : Map[String, @anode.View] = fp_greeting_views(),
  init? : FpGreetingState = FpGreetingState::zero(),
  common_style? : String = fp_greeting_common_style,
  global_style? : String = fp_greeting_global_style,
  update? : (FpGreetingState, @component.Dispatch, &@tutuca.Ctx) -> FpGreetingState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn note_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("p", [@anode.attr("class", "note")], [
      @anode.dyn_text(Field("text")),
    ]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn note_views() -> Map[String, @anode.View] {
  { "main": note_main_view_ir() }
}

///|
/// The Note component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `NoteState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn note_component(
  name? : String = "Note",
  views? : Map[String, @anode.View] = note_views(),
  init? : NoteState = NoteState::zero(),
  common_style? : String = note_common_style,
  global_style? : String = note_global_style,
  update? : (NoteState, @component.Dispatch, &@tutuca.Ctx) -> NoteState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}

///|
/// The compiled `main` view.
fn mailbox_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("section", [], [@anode.render(0, Field("note"))]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn mailbox_views() -> Map[String, @anode.View] {
  { "main": mailbox_main_view_ir() }
}

///|
/// The Mailbox component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `MailboxState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn mailbox_component(
  name? : String = "Mailbox",
  views? : Map[String, @anode.View] = mailbox_views(),
  init? : MailboxState = MailboxState::zero(),
  common_style? : String = mailbox_common_style,
  global_style? : String = mailbox_global_style,
  update? : (MailboxState, @component.Dispatch, &@tutuca.Ctx) -> MailboxState?,
  specs? : Map[String, @component.FieldSpec] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    specs~,
    provide~,
    lookup~,
  )
}
