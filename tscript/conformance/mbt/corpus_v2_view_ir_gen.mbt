// GENERATED by `tutuca gen-views` from corpus_v2.html — DO NOT EDIT.
//
// The views of CorpusV2, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing corpus_v2_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn corpus_v2_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [], [@anode.dyn_text(Field("name"), id=0)]),
    [],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn corpus_v2_views() -> Map[String, @anode.View] {
  { "main": corpus_v2_main_view_ir() }
}

///|
/// The dispatches the `<script type="tutuca/script">` block answers.
///
/// Composed AHEAD of the author's `update~`, which stays optional and is
/// consulted only for a name this does not answer. GENERATED — edit the
/// block, not this.
fn corpus_v2_inline_update(
  s : CorpusV2State,
  msg : @component.Dispatch,
  ctx : &@tutuca.Ctx,
) -> @component.Update[CorpusV2State] {
  match msg {
    Receive("case0", _args) => {
      let s = { ..s, count: (s.count.to_double() + 1.0).to_int() }
      Next(s)
    }
    Receive("case1", _args) => {
      let s = { ..s, count: (s.count.to_double() + 1.0).to_int() }
      Next(s)
    }
    Receive("case2", args) => {
      guard args.get(0) is Some(Num(n)) else { return Unchanged }
      let s = { ..s, count: n.to_int() }
      Next(s)
    }
    Receive("case3", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => ctx.forward(None, None))
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case4", args) => {
      guard args.get(0) is Some(Num(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.forward(
          None,
          Some(@tutuca.IntentOpts::new(route=[@tutuca.Leg::Dyn])),
        )
      })
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case5", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.forward(
          None,
          Some(@tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex])),
        )
      })
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case6", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.forward(
          None,
          Some(
            @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex, @tutuca.Leg::Dyn]),
          ),
        )
      })
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case7", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.forward(Some([(Str(s.name) : @tutuca.Value)]), None)
      })
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case8", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      let s = { ..s, count: (s.count.to_double() + 1.0).to_int() }
      effects.push(() => ctx.forward(None, None))
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case9", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "saveDraft",
          [(Str(s.name) : @tutuca.Value)],
          @tutuca.IntentOpts::new(),
        )
      })
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case10", args) => {
      guard args.get(0) is Some(Num(k)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "picked",
          [(Num(k) : @tutuca.Value)],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Dyn]),
        )
      })
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case11", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "loadRows",
          [(Str(s.name) : @tutuca.Value)],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case12", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "saveDraft",
          [(Str(s.name) : @tutuca.Value)],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex, @tutuca.Leg::Dyn]),
        )
      })
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case13", _args) => {
      let effects : Array[() -> Unit] = []
      effects.push(() => {
        ctx.intent(
          "loadRows",
          [],
          @tutuca.IntentOpts::new(route=[@tutuca.Leg::Lex]),
        )
      })
      let s = { ..s, ok: true }
      effects.push(() => ctx.send("flash", [(Str(s.name) : @tutuca.Value)]))
      effects.push(() => ctx.stop_propagation())
      for f in effects {
        f()
      }
      Next(s)
    }
    Intent("case14", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      let s = { ..s, count: (s.count.to_double() + 1.0).to_int() }
      effects.push(() => ctx.reply((Num(s.count.to_double()) : @tutuca.Value)))
      for f in effects {
        f()
      }
      Next(s)
    }
    Intent("case15", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => ctx.fail((Str("disk full") : @tutuca.Value)))
      for f in effects {
        f()
      }
      Next(s)
    }
    Intent("case16", args) => {
      guard args.get(0) is Some(Num(k)) else { return Unchanged }
      let s = { ..s, page: k.to_int() }
      Next(s)
    }
    Intent("case17", args) => {
      guard args.get(0) is Some(Num(k)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      let s = { ..s, page: k.to_int() }
      effects.push(() => ctx.forward(None, None))
      for f in effects {
        f()
      }
      Next(s)
    }
    Intent("case18", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      effects.push(() => ctx.reply((Num(1.0) : @tutuca.Value)))
      effects.push(() => ctx.reply((Num(2.0) : @tutuca.Value)))
      for f in effects {
        f()
      }
      Next(s)
    }
    Intent("case19", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      let s = { ..s, count: (s.count.to_double() + 1.0).to_int() }
      effects.push(() => ctx.stop_propagation())
      for f in effects {
        f()
      }
      Next(s)
    }
    Intent("case20", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      guard s.count.to_double() > 0.0 else {
        @tutuca.precondition_failed(
          handler="case20",
          pred="hasSome",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      let effects : Array[() -> Unit] = []
      let s = { ..s, count: (s.count.to_double() + 1.0).to_int() }
      effects.push(() => ctx.reply((Num(s.count.to_double()) : @tutuca.Value)))
      for f in effects {
        f()
      }
      Next(s)
    }
    Intent("case21", args) => {
      guard args.get(0) is Some(Str(_)) else { return Unchanged }
      let effects : Array[() -> Unit] = []
      let s = { ..s, count: (s.count.to_double() + 1.0).to_int() }
      effects.push(() => ctx.reply((Num(s.count.to_double()) : @tutuca.Value)))
      guard s.count.to_double() > 5.0 else {
        @tutuca.postcondition_failed(
          handler="case21",
          pred="small",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      for f in effects {
        f()
      }
      Next(s)
    }
    Receive("case22", args) => {
      guard args.get(0) is Some(Num(n)) else { return Unchanged }
      let s = { ..s, count: n.to_int() }
      Next(s)
    }
    Receive("case23", args) => {
      guard args.get(0) is Some(Str(why)) else { return Unchanged }
      let s = { ..s, name: why }
      Next(s)
    }
    _ => Unhandled
  }
}

///|
/// `pred hasSome` from the `<script type="tutuca/script">` block. GENERATED — edit
/// the block, not this.
fn corpus_v2_compute_hasSome(
  s : CorpusV2State,
  _args : Array[@tutuca.Value],
) -> @tutuca.Value {
  (Bool(s.count.to_double() > 0.0) : @tutuca.Value)
}

///|
/// `pred small` from the `<script type="tutuca/script">` block. GENERATED — edit
/// the block, not this.
fn corpus_v2_compute_small(
  s : CorpusV2State,
  _args : Array[@tutuca.Value],
) -> @tutuca.Value {
  (Bool(s.count.to_double() > 5.0) : @tutuca.Value)
}

///|
/// The `compute` names the `<script type="tutuca/script">` block answers.
/// GENERATED — edit the block, not this.
fn corpus_v2_inline_compute() -> Map[
  String,
  (CorpusV2State, Array[@tutuca.Value]) -> @tutuca.Value,
] {
  { "hasSome": corpus_v2_compute_hasSome, "small": corpus_v2_compute_small }
}

///|
/// The CorpusV2 component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `CorpusV2State` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn corpus_v2_component(
  name? : String = "CorpusV2",
  views? : Map[String, @anode.View] = corpus_v2_views(),
  init? : CorpusV2State = CorpusV2State::zero(),
  common_style? : String = corpus_v2_common_style,
  global_style? : String = corpus_v2_global_style,
  update? : (CorpusV2State, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    CorpusV2State,
  ],
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update=(s, msg, ctx) => {
      match corpus_v2_inline_update(s, msg, ctx) {
        Unhandled =>
          match update {
            Some(u) => u(s, msg, ctx)
            None => Unhandled
          }
        answered => answered
      }
    },
    compute=corpus_v2_inline_compute(),
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
