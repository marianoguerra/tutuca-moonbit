// GENERATED by `tutuca gen-views` from counter.html — DO NOT EDIT.
//
// The views of Counter, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing counter_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// This file's `<script type="tutuca/spec">` block, as source, with its
/// doc comments removed.
///
/// The schema each component carries is this block's PROJECTION, and the
/// projection is lossy exactly where a generator needs it: a rule reaches
/// `SchemaInfo` as a name with no body, and a message as a name with no
/// payload. Re-parsing this recovers both — see `testing/fuzz`.
pub let counter_spec_source : String =
  #|  state {
  #|    label : String
  #|    count : Int
  #|    history : Array[Int]
  #|
  #|    pred recorded { (len .history) > 0 }
  #|
  #|    invariant historyFollowsCount
  #|      format 'the counter moved and the history did not' {
  #|        recorded or (.count is 0)
  #|      }
  #|  }
  #|
  #|  handle {
  #|    message {
  #|    resetTo(Int)
  #|
  #|    }
  #|  }

///|
/// The compiled `main` view.
fn counter_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h("div", [@anode.attr("class", "counter")], [
      @anode.h("h1", [], [
        @anode.dyn_text(@tutuca.Expr::field("label"), id=0),
        @anode.text(": "),
        @anode.h("span", [@anode.attr("id", "count")], [
          @anode.dyn_text(@tutuca.Expr::field("count"), id=1),
        ]),
      ]),
      @anode.text(" "),
      @anode.h("button", [@anode.attr("id", "inc"), @anode.eid(0)], [
        @anode.text("+1"),
      ]),
      @anode.text(" "),
      @anode.h("button", [@anode.attr("id", "dec"), @anode.eid(1)], [
        @anode.text("-1"),
      ]),
      @anode.text(" "),
      @anode.h("button", [@anode.attr("id", "reset"), @anode.eid(2)], [
        @anode.text("reset"),
      ]),
      @anode.text(" "),
      @anode.h(
        "input",
        [
          @anode.attr("id", "label"),
          @anode.dyn_attr("value", @tutuca.Expr::field("label")),
          @anode.eid(3),
        ],
        [],
      ),
      @anode.text(" "),
      @anode.h("ul", [@anode.attr("id", "history")], [
        @anode.each(
          3,
          @tutuca.Expr::field("history"),
          @anode.h("li", [], [
            @anode.dyn_text(@tutuca.Expr::bind("value"), id=2),
          ]),
        ),
      ]),
    ]),
    [
      [
        @anode.on("click", @tutuca.Expr::name("add"), args=[
          @anode.const_num(1.0),
        ]),
      ],
      [
        @anode.on("click", @tutuca.Expr::name("add"), args=[
          @anode.const_num(-1.0),
        ]),
      ],
      [@anode.on("click", @tutuca.Expr::name("resetCount"))],
      [
        @anode.on("input", @tutuca.Expr::name("setLabel"), args=[
          @tutuca.Expr::event_path(["value"]),
        ]),
        @anode.on(
          "keydown",
          @tutuca.Expr::name("setLabel"),
          args=[@anode.const_str("sent!")],
          modifiers=["send"],
        ),
      ],
    ],
    style="font-family: sans-serif",
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn counter_views() -> Map[String, @anode.View] {
  { "main": counter_main_view_ir() }
}

///|
/// The dispatches the `<script type="tutuca/script">` block answers.
///
/// Composed AHEAD of the author's `update~`, which stays optional and is
/// consulted only for a name this does not answer. GENERATED — edit the
/// block, not this.
fn counter_inline_update(
  s : CounterState,
  msg : @component.Dispatch,
  ctx : &@tutuca.Ctx,
) -> @component.Update[CounterState] {
  match msg {
    Receive("add", args) => {
      guard args.get(0) is Some(Num(d_)) else { return Unchanged }
      let s = { ..s, count: (s.count.to_double() + d_).to_int(), }
      let s = { ..s, history: s.history + [s.count], }
      guard counter_compute_recorded(s, [], @tutuca.CtxStack(ctx)).is_truthy() ||
        (Num(s.count.to_double()) : @tutuca.Value) == (Num(0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="add",
          pred="historyFollowsCount",
          sentence="the counter moved and the history did not",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      Next(s)
    }
    Receive("resetTo", args) => {
      guard args.get(0) is Some(Num(n_)) else { return Unchanged }
      let s = { ..s, count: n_.to_int(), }
      let s = { ..s, history: s.history + [s.count], }
      guard counter_compute_recorded(s, [], @tutuca.CtxStack(ctx)).is_truthy() ||
        (Num(s.count.to_double()) : @tutuca.Value) == (Num(0.0) : @tutuca.Value) else {
        @tutuca.invariant_failed(
          handler="resetTo",
          pred="historyFollowsCount",
          sentence="the counter moved and the history did not",
          state=(Map(@component.Fields::encode(s)) : @tutuca.Value),
        )
        return Unchanged
      }
      Next(s)
    }
    _ => Unhandled
  }
}

///|
/// `pred recorded` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn counter_compute_recorded(
  s : CounterState,
  _args : Array[@tutuca.Value],
  _stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (Bool(s.history.length().to_double() > 0.0) : @tutuca.Value)
}

///|
/// `invariant historyFollowsCount` from the `<script type="tutuca/spec">` block. GENERATED — edit
/// the block, not this.
fn counter_compute_historyFollowsCount(
  s : CounterState,
  _args : Array[@tutuca.Value],
  stack : &@tutuca.Stack,
) -> @tutuca.Value {
  (
    Bool(
      counter_compute_recorded(s, [], stack).is_truthy() ||
      (Num(s.count.to_double()) : @tutuca.Value) == (Num(0.0) : @tutuca.Value),
    ) : @tutuca.Value)
}

///|
/// The `compute` names the `<script type="tutuca/script">` block answers.
/// GENERATED — edit the block, not this.
fn counter_inline_compute() -> Map[
  String,
  (CounterState, Array[@tutuca.Value], &@tutuca.Stack) -> @tutuca.Value,
] {
  {
    "recorded": counter_compute_recorded,
    "historyFollowsCount": counter_compute_historyFollowsCount,
  }
}

///|
/// The Counter component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `CounterState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn counter_component(
  name? : String = "Counter",
  views? : Map[String, @anode.View] = counter_views(),
  init? : CounterState = CounterState::zero(),
  common_style? : String = counter_common_style,
  global_style? : String = counter_global_style,
  update? : (CounterState, @component.Dispatch, &@tutuca.Ctx) -> @component.Update[
    CounterState,
  ],
  swap? : (CounterInput) -> ((CounterState, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Array[@component.LookupSpec] = [],
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update=(s, msg, ctx) => {
      match counter_inline_update(s, msg, ctx) {
        Unhandled =>
          match update {
            Some(u) => u(s, msg, ctx)
            None => Unhandled
          }
        answered => answered
      }
    },
    compute=counter_inline_compute(),
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([Add, ResetCount, SetLabel] : Array[CounterInput]),
          CounterInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
    spec=counter_spec_source,
  )
}

///|
/// Every declared initial state keeps every declared invariant.
///
/// A rule that does not hold in the state a component STARTS in is broken
/// before anything happens, and every transition after it is refused — so
/// this is the one check worth making before the component ever runs.
/// GENERATED — edit the spec block, not this.
test "counter: the declared initial states keep the invariants" {
  let s = CounterState::fresh()
  assert_false(
    counter_compute_historyFollowsCount(s, [], @tutuca.NullStack::{ })
    is Bool(false),
    msg="the `fresh` initial state breaks `historyFollowsCount`",
  )
  let s = CounterState::withHistory()
  assert_false(
    counter_compute_historyFollowsCount(s, [], @tutuca.NullStack::{ })
    is Bool(false),
    msg="the `with-history` initial state breaks `historyFollowsCount`",
  )
}
