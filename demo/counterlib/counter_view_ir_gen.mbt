// GENERATED by `tutuca gen-views` from counter.html — DO NOT EDIT.
//
// The views of Counter, already compiled: the @anode.ANode tree each one
// parses into, plus its event table. Passing counter_compiled_views() as
// `compiled_views~` means the template parser never runs at startup.
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn counter_main_view_ir() -> @anode.View {
  let root : @anode.ANode = Dom({
    tag: "div",
    attrs: ConstAttrs({ "class": LStr("counter"), "data-vid": LStr("main") }),
    childs: [
      Dom({
        tag: "h1",
        attrs: ConstAttrs(Map([])),
        childs: [
          RenderText({ node_id: Some(0), val: Field("label") }),
          Text({ val: ": " }),
          Dom({
            tag: "span",
            attrs: ConstAttrs({ "id": LStr("count") }),
            childs: [
              RenderText({ node_id: Some(1), val: Field("count") }),
            ],
            ns: None,
          }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({ "id": LStr("inc"), "data-eid": LNum(0.0) }),
        childs: [
          Text({ val: "+1" }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({ "id": LStr("dec"), "data-eid": LNum(1.0) }),
        childs: [
          Text({ val: "-1" }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "button",
        attrs: ConstAttrs({ "id": LStr("reset"), "data-eid": LNum(2.0) }),
        childs: [
          Text({ val: "reset" }),
        ],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "input",
        attrs: DynAttrs([ConstA(name="id", val=Const(lit=LStr("label"), from_macro=false)), Plain(name="value", val=Field("label")), ConstA(name="data-eid", val=Const(lit=LNum(3.0), from_macro=false))]),
        childs: [],
        ns: None,
      }),
      Text({ val: " " }),
      Dom({
        tag: "ul",
        attrs: ConstAttrs({ "id": LStr("history") }),
        childs: [
          Each({
            node_id: 3,
            iter_info: { val: Field("history"), when_val: None, loop_with_val: None, enrich_with_val: None },
            node: Dom({
              tag: "li",
              attrs: ConstAttrs(Map([])),
              childs: [
                RenderText({ node_id: Some(2), val: Bind("value") }),
              ],
              ns: None,
            }),
            from_render_each: false,
          }),
        ],
        ns: None,
      }),
    ],
    ns: None,
  })
  let events : Array[@anode.NodeEvents] = [
    {
      id: 0,
      handlers: [
        { name: "click", handler: { handler: HandlerName(name="add", ns=Input), args: [Const(lit=LNum(1.0), from_macro=false)] }, modifiers: [] },
      ],
    },
    {
      id: 1,
      handlers: [
        { name: "click", handler: { handler: HandlerName(name="add", ns=Input), args: [Const(lit=LNum(-1.0), from_macro=false)] }, modifiers: [] },
      ],
    },
    {
      id: 2,
      handlers: [
        { name: "click", handler: { handler: HandlerName(name="resetCount", ns=Input), args: [] }, modifiers: [] },
      ],
    },
    {
      id: 3,
      handlers: [
        { name: "input", handler: { handler: HandlerName(name="setLabel", ns=Input), args: [Name("value")] }, modifiers: [] },
        { name: "keydown", handler: { handler: HandlerName(name="setLabel", ns=Input), args: [Const(lit=LStr("sent!"), from_macro=false)] }, modifiers: ["send"] },
      ],
    },
  ]
  @anode.View::from_ir(
    "main",
    root,
    @anode.ParseContext::from_ir(root, events),
    style="font-family: sans-serif",
  )
}

///|
/// The ahead-of-time compiled views — pass as `compiled_views~` to
/// @component.component, in place of `view~` / `views~`.
pub fn counter_compiled_views() -> Map[String, @anode.View] {
  {
    "main": counter_main_view_ir(),
  }
}

