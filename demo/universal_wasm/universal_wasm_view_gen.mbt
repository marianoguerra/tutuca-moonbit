// GENERATED by `tutuca gen-views` from universal_wasm.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views universal_wasm.html --name UniversalWasm --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The state of the UniversalWasm component.
///
/// Declared as `interface universal-wasm` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct UniversalWasmState {
  status : String
  list : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..UniversalWasmState::zero(), label: "Counter" }
pub fn UniversalWasmState::zero() -> UniversalWasmState {
  { status: "", list: Null }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for UniversalWasmState with fn encode(s) {
  { "status": Str(s.status), "list": s.list }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for UniversalWasmState with fn decode(f) {
  guard f.get("status") is Some(Str(v_status)) else { return None }
  let v_list = f.get("list").unwrap_or(Null)
  Some({ status: v_status, list: v_list })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum UniversalWasmReceive {
  DyncompLoaded(String)
  DyncompError(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn UniversalWasmReceive::from_dispatch(
  d : @component.Dispatch,
) -> UniversalWasmReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("dyncompLoaded", [Str(a0)]) => DyncompLoaded(a0)
      ("dyncompError", [Str(a0)]) => DyncompError(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn UniversalWasmReceive::to_dispatch(
  self : UniversalWasmReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    DyncompLoaded(a0) => ("dyncompLoaded", [Str(a0)])
    DyncompError(a0) => ("dyncompError", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn UniversalWasmReceive::send(
  self : UniversalWasmReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The file-level style — pass as `common_style~`.
pub let universal_wasm_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let universal_wasm_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum UniversalWasmInput {
  LoadDropped
  LoadSample
} derive(Eq, @debug.Debug)

///|
pub fn UniversalWasmInput::name(self : UniversalWasmInput) -> String {
  match self {
    LoadDropped => "loadDropped"
    LoadSample => "loadSample"
  }
}

///|
pub fn UniversalWasmInput::parse(s : String) -> UniversalWasmInput? {
  match s {
    "loadDropped" => Some(LoadDropped)
    "loadSample" => Some(LoadSample)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to universal_wasm.html breaks every `match` over this type
/// until it is handled.
pub(all) enum UniversalWasmMsg {
  /// `@on.drop="loadDropped …"`
  LoadDropped(String)
  /// `@on.click="loadSample …"`
  LoadSample(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn UniversalWasmMsg::from_dispatch(
  d : @component.Dispatch,
) -> UniversalWasmMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("loadDropped", [Str(a0)]) => LoadDropped(a0)
      ("loadSample", [Str(a0)]) => LoadSample(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for UniversalWasmState with fn schema() {
  @component.SchemaInfo::new(
    name="UniversalWasm",
    fingerprint="1973ee9f5c583550",
    fields=[
      @component.FieldInfo::new("status", TyText),
      @component.FieldInfo::new("list", TyComp(Some("ComponentList"))),
    ],
    inputs=["loadDropped", "loadSample"],
    receives=["dyncompLoaded", "dyncompError"],
    view_names=["main"],
  )
}

///|
/// The state of the ComponentList component.
///
/// Declared as `interface component-list` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ComponentListState {
  entries : Array[@tutuca.Value]
  showSettings : Bool
  dir : String
  justify : String
  align : String
  gap : Int
  wrap : Bool
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ComponentListState::zero(), label: "Counter" }
pub fn ComponentListState::zero() -> ComponentListState {
  {
    entries: [],
    showSettings: false,
    dir: "",
    justify: "",
    align: "",
    gap: 0,
    wrap: false,
  }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ComponentListState with fn encode(s) {
  {
    "entries": (List(s.entries.map(x => x)) : @tutuca.Value),
    "showSettings": Bool(s.showSettings),
    "dir": Str(s.dir),
    "justify": Str(s.justify),
    "align": Str(s.align),
    "gap": Num(s.gap.to_double()),
    "wrap": Bool(s.wrap),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ComponentListState with fn decode(f) {
  guard f.get("entries") is Some(t1_entries) else { return None }
  guard t1_entries is List(u1_entries) else { return None }
  let v_entries : Array[@tutuca.Value] = []
  for e1_entries in u1_entries {
    let x1_entries = e1_entries
    v_entries.push(x1_entries)
  }
  guard f.get("showSettings") is Some(Bool(v_showSettings)) else { return None }
  guard f.get("dir") is Some(Str(v_dir)) else { return None }
  guard f.get("justify") is Some(Str(v_justify)) else { return None }
  guard f.get("align") is Some(Str(v_align)) else { return None }
  guard f.get("gap") is Some(Num(t1_gap)) else { return None }
  let v_gap = t1_gap.to_int()
  guard f.get("wrap") is Some(Bool(v_wrap)) else { return None }
  Some({
    entries: v_entries,
    showSettings: v_showSettings,
    dir: v_dir,
    justify: v_justify,
    align: v_align,
    gap: v_gap,
    wrap: v_wrap,
  })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum ComponentListReceive {
  Refresh
  Migrate(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn ComponentListReceive::from_dispatch(
  d : @component.Dispatch,
) -> ComponentListReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("refresh", []) => Refresh
      ("migrate", [Str(a0)]) => Migrate(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn ComponentListReceive::to_dispatch(
  self : ComponentListReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    Refresh => ("refresh", [])
    Migrate(a0) => ("migrate", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn ComponentListReceive::send(
  self : ComponentListReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The Response arm of @component.Dispatch, decoded.
///
/// Declared as `variant response` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum ComponentListResponse {
  ComponentPicker(@tutuca.Value)
  /// A response the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Response dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn ComponentListResponse::from_dispatch(
  d : @component.Dispatch,
) -> ComponentListResponse? {
  guard d is Response(name, args) else { return None }
  Some(
    match (name, args) {
      ("componentPicker", [a0]) => ComponentPicker(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn ComponentListResponse::to_dispatch(
  self : ComponentListResponse,
) -> (String, Array[@tutuca.Value]) {
  match self {
    ComponentPicker(a0) => ("componentPicker", [a0])
    Unknown(n, args) => (n, args)
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let component_list_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let component_list_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ComponentListInput {
  OnAdd
  SetAlign
  SetDir
  SetGap
  SetJustify
  ToggleShowSettings
  ToggleWrap
} derive(Eq, @debug.Debug)

///|
pub fn ComponentListInput::name(self : ComponentListInput) -> String {
  match self {
    OnAdd => "onAdd"
    SetAlign => "setAlign"
    SetDir => "setDir"
    SetGap => "setGap"
    SetJustify => "setJustify"
    ToggleShowSettings => "toggleShowSettings"
    ToggleWrap => "toggleWrap"
  }
}

///|
pub fn ComponentListInput::parse(s : String) -> ComponentListInput? {
  match s {
    "onAdd" => Some(OnAdd)
    "setAlign" => Some(SetAlign)
    "setDir" => Some(SetDir)
    "setGap" => Some(SetGap)
    "setJustify" => Some(SetJustify)
    "toggleShowSettings" => Some(ToggleShowSettings)
    "toggleWrap" => Some(ToggleWrap)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to universal_wasm.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ComponentListMsg {
  /// `@on.click="onAdd …"`
  OnAdd
  /// `@on.input="setAlign …"`
  SetAlign(String)
  /// `@on.input="setDir …"`
  SetDir(String)
  /// `@on.input="setGap …"`
  SetGap(Double)
  /// `@on.input="setJustify …"`
  SetJustify(String)
  /// `@on.click="toggleShowSettings …"`
  ToggleShowSettings
  /// `@on.input="toggleWrap …"`
  ToggleWrap
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ComponentListMsg::from_dispatch(
  d : @component.Dispatch,
) -> ComponentListMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onAdd", []) => OnAdd
      ("setAlign", [Str(a0)]) => SetAlign(a0)
      ("setDir", [Str(a0)]) => SetDir(a0)
      ("setGap", [Num(a0)]) => SetGap(a0)
      ("setJustify", [Str(a0)]) => SetJustify(a0)
      ("toggleShowSettings", []) => ToggleShowSettings
      ("toggleWrap", []) => ToggleWrap
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum ComponentListMethod {
  FlexStyle
} derive(Eq, @debug.Debug)

///|
pub fn ComponentListMethod::name(self : ComponentListMethod) -> String {
  match self {
    FlexStyle => "flexStyle"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ComponentListState with fn schema() {
  @component.SchemaInfo::new(
    name="ComponentList",
    fingerprint="522f1f7a3d8e97a1",
    fields=[
      @component.FieldInfo::new("entries", TyList(TyAny)),
      @component.FieldInfo::new("showSettings", TyBool),
      @component.FieldInfo::new("dir", TyText),
      @component.FieldInfo::new("justify", TyText),
      @component.FieldInfo::new("align", TyText),
      @component.FieldInfo::new("gap", TyInt),
      @component.FieldInfo::new("wrap", TyBool),
    ],
    inputs=[
      "onAdd", "setAlign", "setDir", "setGap", "setJustify", "toggleShowSettings",
      "toggleWrap",
    ],
    receives=["refresh", "migrate"],
    responses=["componentPicker"],
    methods=["flexStyle"],
    view_names=["main"],
  )
}

///|
/// The state of the ComponentPicker component.
///
/// Declared as `interface component-picker` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ComponentPickerState {
  filterText : String
  items : Array[@tutuca.Value]
  selected : @tutuca.Value
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ComponentPickerState::zero(), label: "Counter" }
pub fn ComponentPickerState::zero() -> ComponentPickerState {
  { filterText: "", items: [], selected: Null }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ComponentPickerState with fn encode(s) {
  {
    "filterText": Str(s.filterText),
    "items": (List(s.items.map(x => x)) : @tutuca.Value),
    "selected": s.selected,
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ComponentPickerState with fn decode(f) {
  guard f.get("filterText") is Some(Str(v_filterText)) else { return None }
  guard f.get("items") is Some(t1_items) else { return None }
  guard t1_items is List(u1_items) else { return None }
  let v_items : Array[@tutuca.Value] = []
  for e1_items in u1_items {
    let x1_items = e1_items
    v_items.push(x1_items)
  }
  let v_selected = f.get("selected").unwrap_or(Null)
  Some({ filterText: v_filterText, items: v_items, selected: v_selected })
}

///|
/// The Receive arm of @component.Dispatch, decoded.
///
/// Declared as `variant receive` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum ComponentPickerReceive {
  Migrate(String)
  /// A receive the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Receive dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn ComponentPickerReceive::from_dispatch(
  d : @component.Dispatch,
) -> ComponentPickerReceive? {
  guard d is Receive(name, args) else { return None }
  Some(
    match (name, args) {
      ("migrate", [Str(a0)]) => Migrate(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn ComponentPickerReceive::to_dispatch(
  self : ComponentPickerReceive,
) -> (String, Array[@tutuca.Value]) {
  match self {
    Migrate(a0) => ("migrate", [Str(a0)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Send this message to self; it arrives as a later Receive dispatch.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn ComponentPickerReceive::send(
  self : ComponentPickerReceive,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.send(name, args)
}

///|
/// The Bubble arm of @component.Dispatch, decoded.
///
/// Declared as `variant bubble` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum ComponentPickerBubble {
  ComponentSelected(String, String, String)
  /// A bubble the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Bubble dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn ComponentPickerBubble::from_dispatch(
  d : @component.Dispatch,
) -> ComponentPickerBubble? {
  guard d is Bubble(name, args) else { return None }
  Some(
    match (name, args) {
      ("componentSelected", [Str(a0), Str(a1), Str(a2)]) =>
        ComponentSelected(a0, a1, a2)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn ComponentPickerBubble::to_dispatch(
  self : ComponentPickerBubble,
) -> (String, Array[@tutuca.Value]) {
  match self {
    ComponentSelected(a0, a1, a2) =>
      ("componentSelected", [Str(a0), Str(a1), Str(a2)])
    Unknown(n, args) => (n, args)
  }
}

///|
/// Bubble this message to the parent component.
///
/// The name comes from the schema, so it cannot drift from the one
/// `from_dispatch` decodes.
pub fn ComponentPickerBubble::bubble(
  self : ComponentPickerBubble,
  ctx : &@tutuca.Ctx,
) -> Unit {
  let (name, args) = self.to_dispatch()
  ctx.bubble(name, args)
}

///|
/// The Response arm of @component.Dispatch, decoded.
///
/// Declared as `variant response` in the state block. These names are
/// raised by component code rather than written in a view, so the views
/// cannot type them — the schema is the only place they can be declared.
pub(all) enum ComponentPickerResponse {
  ListComponents(@tutuca.Value)
  MakeComponent(@tutuca.Value)
  /// A response the schema does not declare, or a declared name whose
  /// runtime arguments did not match the declared types.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// Decode a Response dispatch. None for any other bucket.
///
/// A payload that does not match falls into `Unknown` rather than being
/// coerced: an `s32` handler handed 2.5 has been sent the wrong thing,
/// and silently truncating it would hide that.
pub fn ComponentPickerResponse::from_dispatch(
  d : @component.Dispatch,
) -> ComponentPickerResponse? {
  guard d is Response(name, args) else { return None }
  Some(
    match (name, args) {
      ("listComponents", [a0]) => ListComponents(a0)
      ("makeComponent", [a0]) => MakeComponent(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// The dispatch name and arguments this message raises as.
pub fn ComponentPickerResponse::to_dispatch(
  self : ComponentPickerResponse,
) -> (String, Array[@tutuca.Value]) {
  match self {
    ListComponents(a0) => ("listComponents", [a0])
    MakeComponent(a0) => ("makeComponent", [a0])
    Unknown(n, args) => (n, args)
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let component_picker_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let component_picker_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ComponentPickerInput {
  SetFilterText
} derive(Eq, @debug.Debug)

///|
pub fn ComponentPickerInput::name(self : ComponentPickerInput) -> String {
  match self {
    SetFilterText => "setFilterText"
  }
}

///|
pub fn ComponentPickerInput::parse(s : String) -> ComponentPickerInput? {
  match s {
    "setFilterText" => Some(SetFilterText)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to universal_wasm.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ComponentPickerMsg {
  /// `@on.input="setFilterText …"`
  SetFilterText(String)
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ComponentPickerMsg::from_dispatch(
  d : @component.Dispatch,
) -> ComponentPickerMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("setFilterText", [Str(a0)]) => SetFilterText(a0)
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `@when` handler the views name.
pub(all) enum ComponentPickerWhen {
  MatchesFilter
} derive(Eq, @debug.Debug)

///|
pub fn ComponentPickerWhen::name(self : ComponentPickerWhen) -> String {
  match self {
    MatchesFilter => "matchesFilter"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ComponentPickerState with fn schema() {
  @component.SchemaInfo::new(
    name="ComponentPicker",
    fingerprint="88ff98b6e75014ce",
    fields=[
      @component.FieldInfo::new("filterText", TyText),
      @component.FieldInfo::new("items", TyList(TyAny)),
      @component.FieldInfo::new("selected", TyComp(None)),
    ],
    inputs=["setFilterText"],
    receives=["migrate"],
    bubbles=["componentSelected"],
    responses=["listComponents", "makeComponent"],
    view_names=["main"],
  )
}

///|
/// The state of the ComponentEntry component.
///
/// Declared as `interface component-entry` in the state block. Adding a
/// field there adds it here, so a view that reads it type-checks and one
/// that reads a name this struct lacks fails generation.
pub(all) struct ComponentEntryState {
  module_ : String
  name : String
  init_ : String
  label : String
}

///|
/// The zero state: every field at its type's empty value.
///
/// WIT declares no defaults — a default is a value, not a type — so this
/// is type-directed, and `init` spreads it to name only what differs:
///
///   init={ ..ComponentEntryState::zero(), label: "Counter" }
pub fn ComponentEntryState::zero() -> ComponentEntryState {
  { module_: "", name: "", init_: "", label: "" }
}

///|
/// The state as the fields map the render and path layers read, built
/// field by field rather than through a `Json` round trip.
///
/// A field that already holds a `@tutuca.Value` is passed straight
/// through. That is the case a JSON round trip could not do at all: an
/// `Obj` or an `Fn` does not survive one.
impl @component.Fields for ComponentEntryState with fn encode(s) {
  {
    "module": Str(s.module_),
    "name": Str(s.name),
    "init": Str(s.init_),
    "label": Str(s.label),
  }
}

///|
/// The fields map back into the state. None on a shape mismatch, which
/// callers treat as a no-op write — the same contract as the JSON path.
impl @component.Fields for ComponentEntryState with fn decode(f) {
  guard f.get("module") is Some(Str(v_module_)) else { return None }
  guard f.get("name") is Some(Str(v_name)) else { return None }
  guard f.get("init") is Some(Str(v_init_)) else { return None }
  guard f.get("label") is Some(Str(v_label)) else { return None }
  Some({ module_: v_module_, name: v_name, init_: v_init_, label: v_label })
}

///|
/// The file-level style — pass as `common_style~`.
pub let component_entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let component_entry_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ComponentEntryInput {
  OnSelect
} derive(Eq, @debug.Debug)

///|
pub fn ComponentEntryInput::name(self : ComponentEntryInput) -> String {
  match self {
    OnSelect => "onSelect"
  }
}

///|
pub fn ComponentEntryInput::parse(s : String) -> ComponentEntryInput? {
  match s {
    "onSelect" => Some(OnSelect)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to universal_wasm.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ComponentEntryMsg {
  /// `@on.click="onSelect …"`
  OnSelect
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ComponentEntryMsg::from_dispatch(
  d : @component.Dispatch,
) -> ComponentEntryMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onSelect", []) => OnSelect
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum ComponentEntryMethod {
  ContainsText
} derive(Eq, @debug.Debug)

///|
pub fn ComponentEntryMethod::name(self : ComponentEntryMethod) -> String {
  match self {
    ContainsText => "containsText"
  }
}

///|
/// What this component's schema declares, for `schema~`.
///
/// This is the only thing consulted for a field's KIND — there is no
/// inference off the init value behind it — and it is what an instance
/// answers `obj_schema()` with, so a holder of a bare `@tutuca.Value`
/// can name these fields without a component registry.
impl @component.Fields for ComponentEntryState with fn schema() {
  @component.SchemaInfo::new(
    name="ComponentEntry",
    fingerprint="871d24e104e2912e",
    fields=[
      @component.FieldInfo::new("module", TyText),
      @component.FieldInfo::new("name", TyText),
      @component.FieldInfo::new("init", TyText),
      @component.FieldInfo::new("label", TyText),
    ],
    inputs=["onSelect"],
    methods=["containsText"],
    view_names=["main"],
  )
}
