// GENERATED by `tutuca gen-views` from universal_wasm.html — DO NOT EDIT.
// Regenerate with:
//
//   tutuca gen-views universal_wasm.html --name UniversalWasm --out <dir>
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component",
//   "moonbitlang/core/debug"

///|
/// The views declared in universal_wasm.html.
pub(all) enum UniversalWasmView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn UniversalWasmView::name(self : UniversalWasmView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let universal_wasm_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let universal_wasm_global_style : String = ""

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let universal_wasm_fields : Array[String] = ["list", "status"]

///|
/// The fields in `universal_wasm_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] universal_wasm_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  universal_wasm_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in universal_wasm.html.
pub(all) enum ComponentListView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ComponentListView::name(self : ComponentListView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let component_list_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let component_list_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ComponentListInput {
  OnAdd
} derive(Eq, @debug.Debug)

///|
pub fn ComponentListInput::name(self : ComponentListInput) -> String {
  match self {
    OnAdd => "onAdd"
  }
}

///|
pub fn ComponentListInput::parse(s : String) -> ComponentListInput? {
  match s {
    "onAdd" => Some(OnAdd)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to universal_wasm.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ComponentListMsg {
  /// `@on.click="onAdd …"`
  OnAdd
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ComponentListMsg::of_dispatch(
  d : @component.Dispatch,
) -> ComponentListMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onAdd", []) => OnAdd
      _ => Unknown(name, args)
    },
  )
}

///|
/// Every `$name` the views call.
pub(all) enum ComponentListMethod {
  FlexStyle
  SetAlign
  SetDir
  SetGap
  SetJustify
  ToggleShowSettings
  ToggleWrap
} derive(Eq, @debug.Debug)

///|
pub fn ComponentListMethod::name(self : ComponentListMethod) -> String {
  match self {
    FlexStyle => "flexStyle"
    SetAlign => "setAlign"
    SetDir => "setDir"
    SetGap => "setGap"
    SetJustify => "setJustify"
    ToggleShowSettings => "toggleShowSettings"
    ToggleWrap => "toggleWrap"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] component_list_mutate(
  f : (ComponentListMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for
    m in (
      [
        FlexStyle,
        SetAlign,
        SetDir,
        SetGap,
        SetJustify,
        ToggleShowSettings,
        ToggleWrap,
      ] : Array[ComponentListMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] component_list_compute(
  f : (ComponentListMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for
    m in (
      [
        FlexStyle,
        SetAlign,
        SetDir,
        SetGap,
        SetJustify,
        ToggleShowSettings,
        ToggleWrap,
      ] : Array[ComponentListMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] component_list_swap(
  f : (ComponentListMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for
    m in (
      [
        FlexStyle,
        SetAlign,
        SetDir,
        SetGap,
        SetJustify,
        ToggleShowSettings,
        ToggleWrap,
      ] : Array[ComponentListMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let component_list_fields : Array[String] = [
  "align", "dir", "entries", "gap", "justify", "showSettings", "wrap",
]

///|
/// The fields in `component_list_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] component_list_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  component_list_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in universal_wasm.html.
pub(all) enum ComponentPickerView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ComponentPickerView::name(self : ComponentPickerView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let component_picker_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let component_picker_global_style : String = ""

///|
/// Every `$name` the views call.
pub(all) enum ComponentPickerMethod {
  SetFilterText
} derive(Eq, @debug.Debug)

///|
pub fn ComponentPickerMethod::name(self : ComponentPickerMethod) -> String {
  match self {
    SetFilterText => "setFilterText"
  }
}

///|
/// Build `mutate~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] component_picker_mutate(
  f : (ComponentPickerMethod) -> ((S, Array[@tutuca.Value]) -> S)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> S] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> S] = Map([])
  for m in ([SetFilterText] : Array[ComponentPickerMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `compute~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] component_picker_compute(
  f : (ComponentPickerMethod) -> ((S, Array[@tutuca.Value]) -> @tutuca.Value)?,
) -> Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] {
  let out : Map[String, (S, Array[@tutuca.Value]) -> @tutuca.Value] = Map([])
  for m in ([SetFilterText] : Array[ComponentPickerMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// Build `swap~` from an exhaustive match over the names the views call.
/// Return None for a name another bucket serves, or one of the mutators
/// generated from the state's fields.
pub fn[S] component_picker_swap(
  f : (ComponentPickerMethod) -> ((S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?)?,
) -> Map[String, (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?] {
  let out : Map[
    String,
    (S, Array[@tutuca.Value], &@tutuca.Ctx) -> @tutuca.Value?,
  ] = Map([])
  for m in ([SetFilterText] : Array[ComponentPickerMethod]) {
    match f(m) {
      Some(h) => out[m.name()] = h
      None => ()
    }
  }
  out
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let component_picker_fields : Array[String] = [
  "filterText", "items", "selected",
]

///|
/// The fields in `component_picker_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] component_picker_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  component_picker_fields.filter(f => !present.contains(f))
}

///|
/// The views declared in universal_wasm.html.
pub(all) enum ComponentEntryView {
  Main
} derive(Eq, @debug.Debug)

///|
pub fn ComponentEntryView::name(self : ComponentEntryView) -> String {
  match self {
    Main => "main"
  }
}

///|
/// The file-level style — pass as `common_style~`.
pub let component_entry_common_style : String = ""

///|
/// The `data-global` style — pass as `global_style~`.
pub let component_entry_global_style : String = ""

///|
/// Every bare `@on` handler name the views reference.
pub(all) enum ComponentEntryInput {
  OnSelect
} derive(Eq, @debug.Debug)

///|
pub fn ComponentEntryInput::name(self : ComponentEntryInput) -> String {
  match self {
    OnSelect => "onSelect"
  }
}

///|
pub fn ComponentEntryInput::parse(s : String) -> ComponentEntryInput? {
  match s {
    "onSelect" => Some(OnSelect)
    _ => None
  }
}

///|
/// The Input arm of @component.Dispatch, decoded. Payload types are
/// inferred from the argument shapes written at the call sites, so a
/// handler added to universal_wasm.html breaks every `match` over this type
/// until it is handled.
pub(all) enum ComponentEntryMsg {
  /// `@on.click="onSelect …"`
  OnSelect
  /// An Input dispatch the views do not declare, or a declared name
  /// whose runtime arguments did not match the inferred shape.
  Unknown(String, Array[@tutuca.Value])
}

///|
/// None for Receive / Bubble / Response — those names are raised by
/// component code, never written in a view, so the views cannot type them.
pub fn ComponentEntryMsg::of_dispatch(
  d : @component.Dispatch,
) -> ComponentEntryMsg? {
  guard d is Input(name, args) else { return None }
  Some(
    match (name, args) {
      ("onSelect", []) => OnSelect
      _ => Unknown(name, args)
    },
  )
}

///|
/// State fields the views read at a view's ROOT scope. Reads under an
/// @each, an @enrich-with, a push-view or an <x render> address the loop
/// item or the child component, not this state, so they are not listed.
pub let component_entry_fields : Array[String] = ["label"]

///|
/// The fields in `component_entry_fields` that `s` does not carry — assert this is
/// empty in a test. Fields the component declares through `specs~` rather
/// than through its state struct go in `extra~`.
pub fn[S : ToJson] component_entry_missing_fields(
  s : S,
  extra? : Array[String] = [],
) -> Array[String] {
  let present : Map[String, Bool] = Map([])
  if s.to_json() is Object(o) {
    for k, _ in o {
      present[k] = true
    }
  }
  for k in extra {
    present[k] = true
  }
  component_entry_fields.filter(f => !present.contains(f))
}
