// GENERATED by `tutuca gen-views` from universal_wasm.html — DO NOT EDIT.
//
// The views of HostTools, already compiled: the @anode.ANode tree each one
// parses into, plus its event table, written with anode's builders.
// Passing host_tools_views() as `views~` means the template parser
// never runs at startup.
//
// A component that also declares a schema gets a `_component` wrapper
// beside its views: component() with the name, the views, the styles,
// the codec and the schema already filled in — pass it the handlers.
//
// The package this file lives in must import:
//   "marianoguerra/tutuca/anode", and for the wrapper
//   "marianoguerra/tutuca/core" @tutuca, "marianoguerra/tutuca/component"
//
// Every builder returns a FRESH tree: View::from_ir stamps and optimizes
// it in place, and each component that adopts a view stamps its own
// data-cid onto it.

///|
/// The compiled `main` view.
fn host_tools_main_view_ir() -> @anode.View {
  @anode.View::from_ir(
    "main",
    @anode.h(
      "span",
      [@anode.attr("class", "host-tools flex items-center gap-2")],
      [
        @anode.show(
          Field("samples"),
          @anode.h("div", [@anode.attr("class", "join")], [
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-xs join-item load-counter"),
                @anode.eid(0),
              ],
              [@anode.text("counter")],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-xs join-item load-todo"),
                @anode.eid(1),
              ],
              [@anode.text("todo")],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-xs join-item load-todomvc"),
                @anode.eid(2),
              ],
              [@anode.text("todomvc")],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-xs join-item load-calc"),
                @anode.eid(3),
              ],
              [@anode.text("calculator")],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-xs join-item load-ttt"),
                @anode.eid(4),
              ],
              [@anode.text("tic-tac-toe")],
            ),
            @anode.text(" "),
            @anode.h(
              "button",
              [
                @anode.attr("class", "btn btn-xs join-item load-rust"),
                @anode.eid(5),
              ],
              [@anode.text("rust temp")],
            ),
          ]),
        ),
        @anode.text(" "),
        @anode.h(
          "span",
          [@anode.attr("class", "text-xs opacity-60 host-status")],
          [@anode.dyn_text(Field("status"))],
        ),
      ],
    ),
    [
      [
        @anode.on("click", HandlerName(name="loadSample", ns=Input), args=[
          @anode.const_str("./examples/counter.tutuca.tar.gz"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="loadSample", ns=Input), args=[
          @anode.const_str("./examples/todo.tutuca.tar.gz"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="loadSample", ns=Input), args=[
          @anode.const_str("./examples/todomvc.tutuca.tar.gz"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="loadSample", ns=Input), args=[
          @anode.const_str("./examples/calculator.tutuca.tar.gz"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="loadSample", ns=Input), args=[
          @anode.const_str("./examples/tictactoe.tutuca.tar.gz"),
        ]),
      ],
      [
        @anode.on("click", HandlerName(name="loadSample", ns=Input), args=[
          @anode.const_str("./examples/rust-tempconv.tutuca.tar.gz"),
        ]),
      ],
    ],
  )
}

///|
/// The component's views, already compiled — pass as `views~` to
/// @component.component.
pub fn host_tools_views() -> Map[String, @anode.View] {
  { "main": host_tools_main_view_ir() }
}

///|
/// The HostTools component: `component()` with everything the view file already
/// states filled in — its name, its compiled views and its styles. The
/// schema and the codec are not passed at all: `HostToolsState` states them
/// through `@component.Fields`, and `component()` reads them off the
/// bound. What is left is the code no generator can write.
///
/// `init` defaults to the zero state and `name` to the name the template
/// id declares — pass either when yours differs. The signature below is
/// the whole story: anything not a parameter here, the view file states.
pub fn host_tools_component(
  name? : String = "HostTools",
  views? : Map[String, @anode.View] = host_tools_views(),
  init? : HostToolsState = HostToolsState::zero(),
  common_style? : String = host_tools_common_style,
  global_style? : String = host_tools_global_style,
  update? : (HostToolsState, @component.Dispatch, &@tutuca.Ctx) -> HostToolsState?,
  swap? : (HostToolsInput) -> ((
    HostToolsState,
    Array[@tutuca.Value],
    &@tutuca.Ctx,
  ) -> @tutuca.Value?)?,
  slots? : Map[String, String] = Map([]),
  slot_args? : Map[String, Map[String, @tutuca.Value]] = Map([]),
  provide? : Map[String, String] = Map([]),
  lookup? : Map[String, @component.LookupSpec] = Map([]),
) -> @component.Component {
  @component.component(
    name~,
    views~,
    init~,
    common_style~,
    global_style~,
    update?,
    swap=match swap {
      Some(f) =>
        @component.build_bucket(
          ([LoadSample] : Array[HostToolsInput]),
          HostToolsInput::name,
          f,
        )
      None => Map([])
    },
    slots~,
    slot_args~,
    provide~,
    lookup~,
  )
}
