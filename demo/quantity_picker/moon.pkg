// Browser demo entry: the shared quantity picker on the real document.
// Build with `moon build --target js`, then serve the repo root and open
// demo/quantity_picker/index.html (see that file for the script path).
import {
  "marianoguerra/tutuca/css",
  "marianoguerra/tutuca/showcase",
  "marianoguerra/tutuca/app",
  "marianoguerra/tutuca/app/browser" @glue,
  "marianoguerra/tutuca/vdom/browser" @bdom,
  "mizchi/js_browser/dom",
}

supported_targets = "js"

pkgtype(kind: "executable")
